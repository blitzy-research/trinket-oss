<!-- provenance-json: {"analysedTree":{"head":"e5e7ad170be58faf55ff4d9b7db8439bbc3bd977","headShort":"e5e7ad1","isBaselineCommit":false,"subject":"QA o002: re-stamp the authorized-difference register at a committed generator","worktreeState":"clean"},"artifact":"conversion-inventory.md","baselineCommit":"2f8712a112db46f923918c4507c75abc732d83d0","bodyDigest":{"algorithm":"sha256","canonicalization":"the document with its provenance-json line removed and trailing whitespace stripped per line, hashed as utf8","value":"3b5787fa7f3cbdcff0f2d0afe777d83faeba06565d81cab0214d78dfce4a2e73"},"delivered":{"head":"e5e7ad170be58faf55ff4d9b7db8439bbc3bd977","headShort":"e5e7ad1","isBaselineCommit":false,"subject":"QA o002: re-stamp the authorized-difference register at a committed generator","worktreeState":"clean"},"detail":{"analysedFiles":13,"analysedSourceDigest":"sha256:3ff94f8cbda442102f9386c20ae3e0abe95ce4b94fc0408fcce1d2ce0dc0c0fe","analysedTreeLabel":"the repository containing the generator (no --app given)","artifactLabel":"tool:docs/conversion-inventory.md","atBaseline":false,"evidenceAvailable":true,"evidenceCaptured":false,"invocation":"node test/parity/convert-inventory.js --replay \"$REPLAY_RESULT\" --out docs/conversion-inventory.md"},"generator":{"blob":"1d39ab4c14d91af3bde9784e8133490850dacbf6","commit":"e5e7ad170be58faf55ff4d9b7db8439bbc3bd977","commitState":"contains-this-exact-source","deliveredHead":"e5e7ad170be58faf55ff4d9b7db8439bbc3bd977","deliveredHeadShort":"e5e7ad1","matchesDeliveredHead":true,"path":"test/parity/convert-inventory.js","verified":true},"provenanceSchema":2,"role":"analysis","runtime":{"node":"v22.23.2","platform":"linux"}} -->
<!--
  GENERATED FILE -- do not hand-edit it. Every line below this block is written
  by the generator named here from the analysed tree named here. An edit made by
  hand is lost on the next run and, while it survives, is indistinguishable from a
  measurement. To change what this document says, change the generator or the
  tree and re-run the exact command.

  generator            : test/parity/convert-inventory.js
  generator sha256     : sha256:6ec1da793d0cb6c53a4204d46dfed6aae8c1d8f2c5a1807374e49545bd951ecf
  generator blob       : 1d39ab4c14d91af3bde9784e8133490850dacbf6
                         `git cat-file blob 1d39ab4c14d9` retrieves the exact source that ran
  generator commit     : e5e7ad170be58faf55ff4d9b7db8439bbc3bd977  <-- verified: this commit's tree holds the blob above at that path
  exact command        : node test/parity/convert-inventory.js --replay "$REPLAY_RESULT" --out docs/conversion-inventory.md
  analysed tree        : the repository containing the generator (no --app given)
  analysed tree HEAD   : e5e7ad170be58faf55ff4d9b7db8439bbc3bd977
  analysed source      : 13 files, sha256:3ff94f8cbda442102f9386c20ae3e0abe95ce4b94fc0408fcce1d2ce0dc0c0fe
  analysed source state: clean -- every analysed file matches that revision
  parity evidence      : test/parity/corpus.json, sha256:bc80401fb4de8811d8c6d185e14e61bc6b1eead7066c70851d57f0c9566ca798, captured: no
  base commit          : 2f8712a  <-- the analysed tree is NOT the base commit, so the
                         baseline-calibrated self-checks are reported as deltas
  node                 : v22.23.2

  WHAT IDENTIFIES THIS DOCUMENT'S SUBJECT, and why there are four lines above
  where a single commit hash would seem to do:

    * "analysed tree HEAD" is the revision the analysed tree was checked out at.
    * "analysed source state" says whether that revision still describes the
      analysed BYTES, over the analysed files only -- this document is not one of
      them, so writing it cannot make its own provenance stale.
    * "analysed source" is a digest over exactly the files that were read, which
      is checkable from the files themselves with no repository at all. It is the
      claim that survives a rebase, a squash, or a clone that never received the
      commit.
    * "generator sha256" identifies the code that produced this, unconditionally.
      "generator commit" is recorded only when the generator file matches its
      last commit, and says so plainly when it does not, because a revision that
      is not an object in this repository cannot be retrieved by a reader.

  Re-run the exact command above to check this document against a tree; or run
  it with --check, which regenerates in memory, compares, and exits 3 if the
  committed document no longer describes the tree. --check normalizes the
  "analysed tree HEAD" and "generator commit" lines above -- and the
  `provenance-json` line, which carries both -- because each of them moves for
  reasons that have nothing to do with what was analysed: a revision that
  commit including the one that stores this document, and a commit identity whose
  content is already compared through "generator sha256". Everything it does
  compare is content-derived: every row, and the three digests.

  No absolute path appears anywhere in this document. A worktree's location on
  disk is specific to the machine it was generated on, so recording it would make
  two correct runs differ for a reason that says nothing about the tree.

  Everything below this block is deterministic: two runs over the same tree
  produce byte-identical output. Nothing in this document records a wall clock,
  a process id, a port or a path, so there is nothing left to differ.
-->

# Conversion inventory

One row per site an implementing agent must close to move this application from
the 2013 callback idiom to the hapi lifecycle contract. **There is no target row
count.** Rows are derived from the tree; nothing is padded to reach a number.

## Which tree this describes, and what that makes it

Every row below was computed from the tree named in the header block, and the
document is only ever a statement about that tree. The distinction is not
pedantic: the same generator over the base commit and over the delivered tree
produces two documents that look alike and mean opposite things.

**This run analysed the delivered tree, not the base commit, so this is the
COMPLETION view: what is actually closed and what is not.** A row is open here
for one of exactly two reasons, and the row states which: the site in the
delivered code has not reached its target, or -- for the row kinds whose
correctness is behavioural rather than textual -- the driven comparison that
settles it has not been completed. Those two are not interchangeable, and the
accounting under "Row totals" keeps them apart rather than adding them up:
reading a missing measurement as unfinished conversion work overstates what is
left to do, and reading it as finished overstates what has been proved.
Re-running the exact command in the header against a `git worktree` at `2f8712a` reproduces the
planning view, which is how the two are compared.

Both views are the same generator over different inputs, and `--check` proves the
committed document still matches the tree it claims. Nothing here is transcribed
by hand, and a hand edit would be lost on the next run.

## Why a signature count is not enough

In hapi 17 and later every lifecycle method must return a value, return a promise,
or throw. `undefined` is converted into a server error by the toolkit. So the
requirement that makes a conversion *correct* is that **each handler returns exactly
once on every path** -- and neither a returned-but-unawaited chain nor an
awaited-but-unreturned one is visible in a signature count. Both are
`async (request, h)`. Both satisfy any grep for the new signature. Only one works.

The same reasoning rules out the two counts that look like adequate substitutes:

- **A count of unreturned signalling calls.** An EMPTY `async (request, h)` has
  none -- no legacy signature, no stray `reply(`, nothing unreturned -- and returns
  `undefined` to a toolkit that converts it into `Boom.badImplementation`. So every
  handler and pre-handler row also carries a **proof of exit**: whether every path
  through the body ends in `return` or `throw`. A body that cannot be shown to
  leave stays open, and the row says which half is missing.
- **A count of legacy calls that disappeared.** For a reply chain or a stream site,
  the legacy syntax vanishing proves only that the syntax vanished. What a client
  received from a reply chain depended on which builder method ran last, and
  whether a stream still errors after the response began is a timing question. Both
  are settled by a driven request, so those rows name the corpus scenario that
  drives them and close on a driven COMPARISON -- a captured baseline and a
  confirming replay verdict against this tree -- rather than on absence. A
  baseline on its own records what the pre-migration code did, which is one side
  of that comparison.

That is the entire reason this document exists.

## Why the work is safely orderable

The mechanism being removed is the response emulation in the route wrapper:

```js
// lib/util/routeParser.js:567-570  (baseline coordinates)
if (result === undefined) {
    result = await responsePromise;
}
```

It intercepts **only** an `undefined` result and passes any defined result straight
through, so a handler converted to return its response **already works under the
shim**. Converted and unconverted handlers coexist, which is what lets these rows be
closed one at a time and re-verified as they go.

Immediately **below** that block, `lib/util/routeParser.js:574-576` is a *different*
mechanism -- the `else` branch returning `request.success(request.params)` when a
route names a controller method that does not exist. Three registered routes depend
on it. **The emulation goes; the fallback stays.** It is easy to delete by
association, which is why those three routes have their own section below.

## How to read a row

| Column | Meaning |
| --- | --- |
| Done | `[x]` when this run measured the site to have reached its target, `[ ]` otherwise, with the reason in the row. Recomputed on every run: re-generating against a tree is how progress is demonstrated, and no box is ever set by hand. |
| Site | The carrier SYMBOL the row is anchored on, plus its coordinate in the analysed tree. Where another document cites a baseline coordinate, that coordinate is shown too, because a line number stops being an address once a file is edited. |
| Kind | One of: routed handler, routed pre-handler, inline pre-handler, promise chain, callback boundary, reply chain, stream site. |
| Current shape | What the code does **now**, measured -- not what it looks like. |
| Target disposition | The exact converted shape. Under R-d this is always the *preserved* behaviour, with one approved exception among these rows, labelled as such -- the register holds eighteen numbered deviations, thirteen of them live in the delivered tree, and only one of them reaches a row here. |

### What closes a row, by kind

| Kind | Closes when |
| --- | --- |
| Routed handler, routed pre-handler, inline pre-handler | The signature is `(request, h)`; no `reply(` reference remains; **every path through the body DELIVERS a value or throws**; and the body does not RELY ON THE INTERCEPTION. Exiting is not enough: a path ending in a bare `return`, a statically `undefined` return, or a non-settling loop reaches hapi as `undefined`, which the toolkit converts into `Boom.badImplementation`. Reliance is about the mechanism being removed -- the interception substitutes a value only when the body returns `undefined` -- so a body that delivers on every path cannot reach it, and a signalling call whose value is discarded there is reported, instructs nothing, and does not hold the row open: it was discarded at baseline too. |
| Promise chain | Its value or settlement reaches a consumer AND its terminal link registers a reaction that produces a value -- or the value is deliberately discarded somewhere nothing could have depended on it. Five mechanisms deliver: `return`, `await`, `resolve(chain)`, an argument such as `promises.push(chain)`, and a capture whose name a later expression delivers. A reaction produces a value when it is a function literal in any argument slot, or a bare reference to `request.success` / `request.fail`, which now RETURN the toolkit response. A statement-position chain is deliberate when its carrier is proven to deliver on every path -- so the discarded value provably was not the response -- or when its carrier is not a lifecycle method at all. |
| Callback boundary | The callee is one the migration KEEPS in callback form, and its carrier is settled. Rules T-3 and T-5 divide the population: a Node-core callback API inside the hapi-facing surface must become its promise equivalent, warning or no warning, and stays OPEN until it does; anything else -- `lib/util/file.js`, `lib/util/store.js`, `lib/util/queues.js`, `lib/models/model.js`, a plugin API, a module-local helper -- has no promise form to await without pushing the boundary into the callee, which T-3 forbids, so it is finished in its current shape. The carrier half is the same one a discarded chain answers to. This population SHRINKS as work completes, because a converted site passes no callback literal and leaves it. |
| Reply chain, dead-301 branch site, unreturned bare `reply(` | The legacy construct is gone from its carrier, the declared target shape (where one can be declared) is present in it, **and** the corpus scenario that drives the site carries BOTH a captured baseline response AND a confirming replay verdict against this tree. A baseline alone records what the pre-migration code did, which is one half of a comparison. |
| Stream site | A scenario is pinned to **this branch** -- from the located reply chain that CONTAINS the site, or, for a site outside every located chain, from the single reply chain its carrier DECLARES, since a carrier with one declared branch has no other branch for the site to be on -- and that scenario carries a captured baseline plus a confirming replay. Whether completion and error timing survived is not visible in the text at all, and a sibling branch's result is silent about this one, which is why a carrier declaring two branches yields no pin. |

The `Current shape` column for handlers and pre-handlers reports two measurements,
and both are needed because each misses what the other catches.

The first is **delivery**: whether each signalling call's value reaches hapi. That
is not the same as a `return` in front of the call. A response is also delivered
when it settles the promise the method returns (`resolve(request.fail(err))` inside
a returned `new Promise`), when it is returned from a nested handler of a chain the
method returns, or when it is captured into a name a returning branch reads
(`var response = request.success(); ... return response;`). All three are counted as
delivery, and the row says which mechanism carried each call -- because counting
them as "unreturned" flagged 17 correctly converted handlers in an earlier run of
this generator, and a false flag sends a reader to fix code that is already right.

The second is the **proof of delivery** described above -- which is asked
separately from the proof of exit, because the two come apart. `while (true) {}`
exits on every path and delivers nothing; `return;` does the same. Both are
reported distinctly from "no exit", since they need different fixes. It is
deliberately
conservative: a construct the analysis cannot show to exit is reported as not
exiting. The direction is chosen on cost -- a false "open" costs one look, a false
"closed" ships a 500 -- and a site that deliberately reproduces a baseline
non-settlement is expected to be recorded as such in `docs/preserved-quirks.md`
rather than silently accepted here.

This remains an analysis of TEXT, and it says so. It does not execute the
application, and for the two behavioural row kinds it does not pretend to: those
rows carry a scenario identifier and defer to it.

## What this document is NOT

Saying so matters, because a checklist that looks complete is read as complete.

- It is **not the error-edge inventory**. Every target disposition here preserves the
  error mapping of the site it describes -- a swallowed error stays swallowed, a
  fire-and-forget stays fire-and-forget, a mapped error reaches the same funnel -- but
  the per-branch enumeration that R-e actually calls for lives in
  `docs/error-edge-inventory.md`, generated by `test/parity/error-edges.js`. Closing
  every row below does not discharge R-e.
- It is **not itself evidence of parity**, and it does not close a behavioural row
  on its own authority. Whether a converted site behaves identically is decided by
  the request corpus (`test/parity/capture.js` and `replay.js`), the storage cases
  and the joi matrix. What this document does is name, per row, the scenario that
  settles it and report that scenario's state as read from
  `test/parity/corpus.json`: recorded (both halves), captured-but-not-replayed,
  defined-but-not-yet-captured, route-level-only, or absent. A row
  whose evidence is pending stays open and says so; a row with no scenario at all
  says that too, because an unlinked site is a gap in the corpus rather than a
  finished piece of work.
- It is **not a route inventory**. The 233-route surface is gated by
  `test/parity/manifest.js`. This document counts hapi-invoked *functions*, of which
  there are 154 -- a different number for a different purpose.

## Where the rest of the story lives

A row here records the **return shape** of one site and stops. Two sibling
documents own the rest about the same sites, and rows point at them by section
number rather than repeating them -- two copies of one fact is how the two drift
apart.

| Document | What it owns about a site in this checklist |
| --- | --- |
| `docs/preserved-quirks.md` | The measured baseline **outcome** of a quirk, and the precedence argument in full for each of the migration's **eighteen** numbered approved deviations, **thirteen** of which are live in the delivered tree and five of which were withdrawn on measurement with their numbers retired rather than reused (§11, a register closed to tools and to preference but not to measurement, indexed with a per-entry state column at §11.0). They differ in kind and this table does not collapse them: **deviation 1** (§11.1), the never-settling image-download branch that the target serves, is a **response** deviation and the one a row in this checklist **cites**, because the site it changes is a row here; **deviation 2** (§11.2, reasoned in full in `docs/deferred-dependencies.md` §4.2), the retained `marked` fork and its one named high advisory, is an **audit** deviation that no conversion row touches, because retaining the fork is what keeps rendered output identical. Of the nine admitted later by measurement, two reach a route whose handler holds a row here **without changing any row's return shape** -- §11.10 answers where the baseline process died, and §11.18 lets the four `output:'file'` upload routes parse a multipart body the baseline refused with 415, so those handlers now run where they were previously never reached. What a row states is its return shape, and that is unaffected by both. A row whose target reproduces a defect says so and cites the section. |
| `docs/error-edge-inventory.md` | The **status, payload, side effects and timing** of every changed error edge. Rows that are themselves error edges -- a chain carrying a `.catch(` link, an error-first callback, an unreturned `reply(err)` -- cite the per-file section that owns them. |

Neither reference is decorative. R-d requires that a preserved defect be recorded
rather than fixed, and R-e requires that the error mapping survive unchanged; this
document would contradict both if it restated their content in its own words.

**The quirk reference is resolved BEFORE a row's target action is composed, not
appended after it.** `docs/preserved-quirks.md` Appendix A is the allow-list, and for a
site whose preserved outcome requires the body *not* to return -- to be left
unsettled, or to throw -- a generic mandate and a quirk pointer in one cell say
opposite things with the mandate leading. So a row on that allow-list carries a
**governing action** instead: it states what to do, names the generic mandate it
**replaces** or **qualifies**, says why that mandate is wrong there, and still cites
the section that owns the measured outcome. Rows where the mandate is compatible are
unchanged, and one allow-list entry is the inverse case -- at
`lib/controllers/trinket.js:375` the statement genuinely must change to preserve the
outcome, which is why this is an allow-list and not a blanket "change nothing".

## The four recurring shapes, and streams

Four shapes recur across the conversion set, each with its own distinct failure
mode, plus stream sites which cut across all of them.

### 1. Promise chains

Their value is **discarded by the wrapper today**. After conversion each must be
**returned or awaited exactly once per path**. Measured scale across the ten
controllers: **183** `.then(` and **85** `.catch(` links at `2f8712a`.

The canonical trap is `lib/controllers/pages.js:52` -- the AAP's own citation for
the chain in `pages.home`, which ends `.then(...).catch(request.fail)`. The terminal
link passes a **bare function reference**, so after conversion `request.fail`'s
return value must **propagate** out of the handler rather than being dropped on the
floor. A chain that is awaited but not returned, or returned but not awaited, looks
identical in a signature scan.

Rows are emitted **per chain**, not per link: "returned or awaited exactly once" is a
property of the chain, and closing one means fixing all of its links together.

### 2. Callback boundaries

The `await` is created **at the call site**, per rule T-3 -- never pushed down into
the utility. `lib/util/file.js`, `lib/util/store.js` and `lib/util/queues.js` keep
their callback interfaces even though controllers call them directly (three, three
and one controller respectively), because they are **not lifecycle methods**. Each
row records where the `await` goes.

A **promise constructor is not a boundary**, and the exclusion is tested on the
callee text rather than on the token before it: `return new Promise(function () {})`
at baseline `lib/controllers/trinket.js:876` was classified as an empty-parameter
completion callback because the walk that finds the callee absorbs the `new`
keyword into it, so a test on the preceding token never fired. `new Promise` is a
promise being constructed, whose settlement is the point; the boundary this section
tracks is a call that takes a completion callback.

### 3. Reply chains -- exactly eight, and they are not uniform

In the shim's response builder (`lib/util/routeParser.js:375-405`) `.type()` and
`.bytes()` return the builder **without** resolving the deferred, while `.code()`,
`.header()`, `.redirect()` and `.view()` resolve it. So **what a client receives
depends on which chain method ran last** -- and the 13 `.type()`/`.bytes()` calls
spread across 8 chains produce three different outcomes. This is the largest source
of preserved-quirk work in the migration.

### 4. Streams

**17** sites across four controllers, several of which
error **after the response has begun**. Every row records that completion and error
**timing** must be preserved.

These are **derived, not grepped**. A crude pattern returns 10. The rule is: a stream
site is a source line that creates a stream (`createReadStream` / `createWriteStream`),
pipes one (`.pipe(`), constructs an archive (`archiver(`), hands one to the response
(`reply(<stream>)` / `h.response(<stream>)`), or binds a call result to a
stream-named identifier -- **deduped by line**, because
`.pipe(fs.createWriteStream(p))` is two operations at one place a reader has to look.
Lifecycle listeners (`.on('close')`, `.on('error')`) are attached *at* a site already
counted, and a `.then(function (stream))` parameter merely names one. Excluding those
two is what lands the rule on 17 rather than a larger number.

### Target idiom, anchored on code already in the tree

The conversion has references to work from, not inventions. Coordinates are baseline
(`2f8712a`); a converted tree will have moved some of them.

- **`lib/util/helpers.js:397-403`** -- The target PRE-HANDLER shape, already present: `async function (request, h) { ...; return await internals.namedTrinketList(lang, 'featured'); }`
- **`app.js:116-137`** -- A server extension returning `h.continue` -- the shape for a lifecycle method that has nothing to contribute but must still return.
- **`app.js:152-201`** -- The onPreResponse error mapper. Branch ORDER is load-bearing: it returns immediately on 401/404/403/>=500 for browser HTML requests, BEFORE the cache and frame header assignments, so those headers reach API/JSON and non-Boom responses only.
- **`app.js:243-281`** -- The auth scheme, with all five outcomes -- absent userId, missing user, disabled user, valid user, lookup error. The modern `h.unauthenticated(...)` / `h.authenticated(...)` idiom.
- **`lib/controllers/*.js`** -- Controllers converge on `async function (request, h) { ...; return request.success(data); }`.

In the analysed tree the pre-handler exemplar `findFeaturedTrinkets` sits at `lib/util/helpers.js:800`, declared `async function (request, h)`.

## The conversion set

Derived from the **binding graph**, not from the export list. The two disagree in
both directions, which is exactly why the export list is the wrong source: three
routes name a controller method that does not exist, and two exported handlers are
never routed.

```text
  145  routed handlers        (147 controller exports minus the 2 that no route references)
    8  routed pre-handlers    (of 11 named pre-handlers in lib/util/helpers.js)
    1  inline pre-handler     (config/api_routes.js:1104, on POST /api/users/login)
  ---
  154  hapi-invoked functions to convert
```

`145 + 8 + 1 = 154`, and the
arithmetic is asserted by the self-check rather than asserted by this sentence.

Measured in the analysed tree: 178 route declarations produce 148 distinct controller bindings, of which 145 resolve to a defined export and 3 do not. 5 declarations build their route string by
concatenation and would be missed entirely by a scan over string literals alone.

Three groups are **excluded from the 154** and have their
own sections: the 2 defined-but-unrouted controller exports, the 3 unrouted named
pre-handlers, and the 3 routes whose named controller method does not exist.

## Self-check

This generator refuses to emit a quietly incomplete checklist. A checklist that
has silently dropped sites is worse than no checklist at all, because it reads as
completed work. Three tiers of check therefore run before anything is written, and
a failure in any of them exits non-zero with no document produced.

| Tier | What it asserts | Result |
| --- | --- | --- |
| 1 -- invariants | Handler census (147 exports and its per-file distribution), route declarations (178), binding graph (145 routed / 3 nonexistent / 2 unrouted), pre-handler census (11), tokenizer length and delimiter balance per file. Must hold on **any** tree, converted or not. | pass |
| 2 -- baseline-calibrated | Progress metrics: 202 `reply(` sites and their distribution, 183 `.then(`, 85 `.catch(`, 13 `.type()`/`.bytes()`, 17 stream sites, the 2 rejected non-reaction `.catch(` calls, and the eight-entry reply-chain roster -- each entry located by its **carrier symbol**, at the baseline line it records, in the category the analysis derives independently. Asserted **exactly** at `2f8712a`; reported as a delta elsewhere. | reported (tree is not at `2f8712a`) |
| 3 -- directional | Off baseline, `reply(` sites may fall but must never rise above 202, and the handler census may not move. | pass |

Why tier 2 is conditional rather than absolute: a converted tree has fewer `reply(`
sites **by design**. Asserting the baseline figure unconditionally would make this
tool fail on exactly the tree whose progress it exists to demonstrate. The check
that actually catches a mis-tokenized file is the tier-1 handler census, because a
desynchronized scrub loses exports and the census collapses immediately.

### Measured against baseline

| Metric | Baseline (`2f8712a`) | This tree | Kind |
| --- | --- | --- | --- |
| Controller handler exports | 147 | 147 | invariant |
| Route declarations | 178 | 178 | invariant |
| Distinct controller bindings | 148 | 148 | invariant |
| Routed handlers | 145 | 145 | invariant |
| `reply(` call sites | 202 | 6 | progress |
| `.then(` links | 183 | 192 | progress |
| `.catch(` links | 85 | 94 | progress |
| `.type()` / `.bytes()` calls | 13 | 10 | progress |
| Derived stream sites | 17 | 18 | derived |
| Pre-handlers still `(request, reply)` | 11 | 3 | progress |
| Non-reaction `.catch(` member calls rejected | 2 | 2 | invariant (a preserved quirk) |
| Function rows emitted (the conversion set) | 154 | 154 | invariant |
| Handlers and pre-handlers with a proven exit on every path | not measured at `2f8712a` | 145 of 145 routed handlers, 8 of 8 routed pre-handlers | progress |
| Parity evidence for the 29 behavioural rows | n/a | 24 recorded (baseline + confirming replay), 4 route-level only (no branch-exact scenario), 1 with no scenario | evidence |

### Deltas from baseline

The analysed tree is not at `2f8712a`, so the following differences are reported rather than asserted. Each one should be explicable as conversion progress; anything that is not is a finding.

- .catch( links across the ten controllers: found 94, baseline 85.
- .then( links across the ten controllers: found 192, baseline 183.
- .type() / .bytes() calls across the ten controllers: found 10, baseline 13.
- Callback boundaries derived across the ten controllers: found 46, baseline 57.
- Derived stream sites: found 18, baseline 17. This figure is DERIVED (see deriveStreamSites) rather than grepped -- a crude pattern returns 10 -- so a mismatch means the rule needs review, not that the rule is wrong.
- Legacy `reply(`-rooted chains carrying .type()/.bytes(): derived 0, roster has 8. At the base commit the two must agree; on a converted tree a lower figure is the expected direction, since each converted chain leaves the legacy population.
- Legacy `reply(`-rooted chains in category builder-returned: derived 0, roster has 3. Asserted at the base commit; on a converted tree the derived figure falls as chains convert.
- Legacy `reply(`-rooted chains in category header-resolved: derived 0, roster has 4. Asserted at the base commit; on a converted tree the derived figure falls as chains convert.
- Legacy `reply(`-rooted chains in category never-settles: derived 0, roster has 1. Asserted at the base commit; on a converted tree the derived figure falls as chains convert.
- Named pre-handlers still declared (request, reply): found 3, baseline 11.
- Open promise chains: found 0, baseline 21.
- Promise chains derived across the ten controllers: found 143, baseline 127.
- Reply chain lib/controllers/courses.js:269-272 (header-resolved) has no `reply(`-rooted chain with links .type().bytes().header() at ordinal 1 inside `returnZip`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/files.js:102-105 (header-resolved) has no `reply(`-rooted chain with links .type().bytes().header() at ordinal 1 inside `download`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/files.js:98-100 (never-settles) has no `reply(`-rooted chain with links .type().bytes() at ordinal 1 inside `download`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1204 (builder-returned) has no `reply(`-rooted chain with links .type() at ordinal 1 inside `downloadMain`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1246 (builder-returned) has no `reply(`-rooted chain with links .type() at ordinal 1 inside `downloadFile`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1259 (builder-returned) has no `reply(`-rooted chain with links .type() at ordinal 2 inside `downloadFile`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1383-1386 (header-resolved) has no `reply(`-rooted chain with links .type().bytes().header() at ordinal 1 inside `downloadPostedZip`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1548-1551 (header-resolved) has no `reply(`-rooted chain with links .type().bytes().header() at ordinal 1 inside `downloadZip`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Stream sites in lib/controllers/users.js: found 2, baseline 1.
- reply( call sites: found 6, baseline 202.
- reply( sites in config/api_routes.js: found 0, baseline 1.
- reply( sites in lib/controllers/admin.js: found 0, baseline 6.
- reply( sites in lib/controllers/classes.js: found 0, baseline 13.
- reply( sites in lib/controllers/course.js: found 0, baseline 63.
- reply( sites in lib/controllers/courses.js: found 0, baseline 4.
- reply( sites in lib/controllers/files.js: found 0, baseline 5.
- reply( sites in lib/controllers/folders.js: found 0, baseline 7.
- reply( sites in lib/controllers/pages.js: found 0, baseline 2.
- reply( sites in lib/controllers/trinket.js: found 0, baseline 35.
- reply( sites in lib/controllers/users.js: found 0, baseline 37.
- reply( sites in lib/util/helpers.js: found 6, baseline 29.

## Row totals

| Section | Rows | Closed | In the 154? |
| --- | --- | --- | --- |
| 1. Routed handlers | 145 | 145 | yes |
| 2a. Routed pre-handler functions | 8 | 8 | yes |
| 2b. Dead pre-handler 301 branch sites (inside 2a) | 2 | 2 | sites within |
| 3. Inline pre-handler | 1 | 1 | yes |
| 4. Promise chains | 143 | 143 | sites within |
| 5. Callback boundaries | 46 | 46 | sites within |
| 6. Reply chains (+1 distinct shape) | 9 | 8 | sites within |
| 7. Stream sites | 18 | 14 | sites within |
| 8. Unrouted controller exports | 2 | 2 | NO -- excluded |
| 9. Unrouted named pre-handlers | 3 | 3 | NO -- excluded |
| 10. Routes with no function | 3 | 3 | NO -- excluded |
| **Total** | **380** | **375** | |

### What that total does and does not say

The census above is correct as a census. **Its Closed column must not be read as
a single completion figure**, because the rows answer to four different
authorities, and only two of the four are statements about conversion work at
all. So the claim is made per population:

| Population | Rows | Closed | Decided by |
| --- | --- | --- | --- |
| Lifecycle functions -- the conversion set | 154 | 154 | the analysed tree: a lifecycle shape, no residual `reply(`, and a PROVEN delivery of a value on every path through the body |
| Sites inside those functions, decided by the tree | 189 | 189 | the analysed tree: a chain's value or settlement reaching a consumer, and a callback boundary falling on the side of rules T-3 and T-5 that keeps it |
| Sites whose target IS a measurement | 29 | 24 | a DRIVEN COMPARISON -- the corpus scenario that drives the site carrying both a captured baseline response and a confirming replay verdict against this tree |
| Rows outside the conversion set | 8 | 8 | a recorded per-function determination -- whether an independent gate (the zero-deprecation-warning bar, or the repaired suite) forces a defined-but-unrouted function into scope -- and, for a route with no function, whether the preserved fallback still serves it |
| **All rows** | **380** | **375** | |

- **Lifecycle functions -- the conversion set -- 154 of 154 closed, none open.** Every row in this population is settled by the analysed tree.
- **Sites inside those functions, decided by the tree -- 189 of 189 closed, none open.** Every row in this population is settled by the analysed tree.
- **Sites whose target IS a measurement -- 24 of 29 closed, 5 open.** An open row here means the comparison has not been performed, or has been performed on one side only. This is NOT a statement that the site is unconverted: what the site now serves is unmeasured, and no reading of the source can substitute.
- **Rows outside the conversion set -- 8 of 8 closed, none open.** Every row in this population is settled by the analysed tree.

**The one completion claim this document supports, stated as narrowly as the
evidence allows.** 343 of the 343 rows whose disposition the analysed tree decides are
closed -- every routed handler, every routed pre-handler, the inline pre-handler, every promise chain and every callback boundary.

**What is NOT claimed.** 5 of the 29 rows whose target is a measurement remain
open, and no amount of reading this tree closes one: a reply chain's outcome
depended on which builder method ran last, and whether a stream still errors
after the response has begun is a timing question. What closes them is
`test/parity/replay.js` driving `test/parity/corpus.json` against this tree and
recording a verdict per scenario. This tree carries 392 scenarios, 392 with a captured
baseline and **380 with a confirming replay**, so
the comparison exists on one side. Each open row below names the scenario whose
verdict would close it, or states that no scenario is pinned to its branch.

**The replay artifact this run was given.** `replay-result.json`, written by
`test/parity/replay.js`, digest `sha256:36364c9f24fb024b...`, verdict "PASS". It carries 392 scenario
verdicts, 380 of them confirming, of which 392 matched a scenario in the corpus.


The row count is **derived, not targeted**. It is not 154, and it should not be: the 154 counts hapi-invoked
*functions*, while a function typically contains several sites -- a chain, two
callback boundaries, a stream -- each of which is closed separately.

**The function rows are exactly the 154, and they are the rows marked `yes` above**: 145 (Routed handlers) + 8 (Routed pre-handler functions) + 1 (Inline pre-handler) = **154**, of which 154 are closed.
That arithmetic is asserted by the self-check rather than asserted in prose: 145 routed handlers + 8 routed
pre-handlers + 1 inline pre-handler = 154. Everything else is a **site within** one of those functions or
deliberately outside the set:

- **2b** holds two BRANCH SITES, not two functions. They live inside `findTrinket`
  and `courseBySlug`, which are already two of the 8 rows in 2a, so counting them
  as functions would say this document tracks 156 hapi-invoked functions when it
  tracks 154.
- **Sections 4 to 7** are sites inside the function rows: promise chains, callback
  boundaries, reply chains and stream sites.
- **Sections 8 to 10** are outside the set entirely -- the 2 unrouted exports, the 3
  unrouted pre-handlers and the 3 routes with no function to convert.

## 1. Routed handlers -- 145 rows (145 closed)

Every controller method that hapi invokes. **A declared signature settles nothing**,
which is why the `Current shape` column reports what the body does rather than what
the parameters are called. `lib/controllers/auth.js` contributes two rows that are
declared `(request, h)` already, and the analysis still has to read their bodies to
decide whether every signalling call sits in a `return` position -- while several
handlers still declared `(request, reply)` turn out to return on every path and
others fall off the end. Neither population is identifiable from the signature.

### `lib/controllers/course.js` -- 42 handlers (42 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/course.js:249` `createCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:301` `getCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 returned directly, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:413` `updateCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:480` `deleteCourse` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:500` `archiveCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:519` `copyCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:576` `addLesson` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:619` `getLesson` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:623` `updateLesson` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:662` `moveLesson` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:693` `deleteLesson` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:717` `addMaterial` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:777` `getMaterial` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:808` `updateMaterial` (4 routes) | routed handler | async toolkit `(request, h)`; delivers on every path, with 1 of 2 signalling calls discarded (0 unreturned in the body, 1 dropped inside a nested function) -- the interception cannot fire for this body, so the discarded value was already discarded at baseline; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every path delivering a lifecycle value. 1 signalling call produces a value that is discarded (0 unreturned in the body, 1 dropped inside a nested function). That is the BASELINE outcome, not an unfinished conversion: the wrapper's interception substitutes a value only when the body returns `undefined`, which this body never does, so those calls resolved a deferred nobody read before the migration and return a response nobody reads after it. Do NOT reroute them to deliver -- that replaces the value this body returns with a different response, which is a behaviour change (R-d). Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:906` `moveMaterial` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:979` `deleteMaterial` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1002` `listUsers` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1034` `listInvitations` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1053` `userLookup` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1095` `removeUser` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1124` `addUser` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1151` `updateRoles` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1179` `updateViews` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1201` `sendInvitations` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 returned directly, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1237` `removeInvitation` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1263` `updateInvitation` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 returned directly, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1360` `generateAccessCode` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1380` `getAccessCode` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1393` `join` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 1 returned directly, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1462` `startAssignment` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1501` `submitAssignment` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1575` `updateMySubmission` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1636` `autosaveComments` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1656` `getUserSubmissionsForMaterial` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1703` `dashboardOverview` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1824` `materialDashboard` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1881` `getMaterialSubmissionsForAllUsers` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1970` `getUserSubmissionsForCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:2038` `autosaveFeedbackComments` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:2063` `autosaveSubmissionOpt` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:2075` `sendFeedback` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:2173` `acceptSubmission` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/trinket.js` -- 34 handlers (34 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/trinket.js:332` `index` (11 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:404` `beta` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:419` `library` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:468` `list` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:497` `getById` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 returned directly, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:525` `grant` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:549` `update` (4 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:597` `create` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:663` `createFork` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:745` `updateMetrics` | routed handler | async toolkit `(request, h)`; delivers on every path, with 1 of 2 signalling calls discarded (0 unreturned in the body, 1 dropped inside a nested function) -- the interception cannot fire for this body, so the discarded value was already discarded at baseline; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every path delivering a lifecycle value. 1 signalling call produces a value that is discarded (0 unreturned in the body, 1 dropped inside a nested function). That is the BASELINE outcome, not an unfinished conversion: the wrapper's interception substitutes a value only when the body returns `undefined`, which this body never does, so those calls resolved a deferred nobody read before the migration and return a response nobody reads after it. Do NOT reroute them to deliver -- that replaces the value this body returns with a different response, which is a behaviour change (R-d). Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:777` `remove` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:803` `getByShortCode` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:872` `embed` (3 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1121` `assignment` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1131` `assignmentFeedback` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1153` `viewOnly` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1164` `email` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 2 returned directly, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1251` `snapshot` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1285` `interactions` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1296` `addToList` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1302` `namedList` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1305` `removeFromList` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1309` `logError` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1335` `logClientMetric` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1392` `draft` | routed handler | async toolkit `(request, h)`; returns its response -- all 7 signalling calls delivered: 4 settling the returned promise, 2 returned through a nested handler on a returned chain, 1 captured into a name a returning branch reads; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (4 settling the returned promise, 2 returned through a nested handler on a returned chain, 1 captured into a name a returning branch reads) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1488` `discardDraft` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1514` `autosave` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 settling the returned promise, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1596` `addToFolder` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1641` `removeFromFolder` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1685` `search` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1703` `downloadMain` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1744` `downloadFile` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1814` `updateSlug` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1836` `downloadPostedZip` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |

### `lib/controllers/users.js` -- 31 handlers (31 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/users.js:425` `create` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 6 signalling calls delivered: 6 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (6 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:501` `login` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 6 signalling calls delivered: 6 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (6 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:665` `remove` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:688` `deleted` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:692` `logout` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:702` `sendPassReset` | routed handler | async toolkit `(request, h)`; returns its response -- all 5 signalling calls delivered: 2 returned directly, 3 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 3 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:777` `resetPasswordForm` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 1 returned directly, 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:799` `savePassword` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:835` `account` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:891` `updateProfile` | routed handler | async toolkit `(request, h)`; returns its response -- all 5 signalling calls delivered: 3 settling the returned promise, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (3 settling the returned promise, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:998` `assetList` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1048` `assetUpload` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1064` `replaceAsset` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1091` `removeAsset` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1111` `restoreAsset` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1143` `assetUploadFromURL` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 1 returned directly, 2 settling the returned promise; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- on transport refusal, log and leave the request UNSETTLED -- one path deliberately returns nothing, and the other paths return as they do today. That REPLACES the generic handler conversion mandate, which is wrong here: "every path returns exactly once" is the direct negation of this quirk. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [x] | `lib/controllers/users.js:1365` `changePassword` | routed handler | async toolkit `(request, h)`; returns its response -- all 5 signalling calls delivered: 1 returned directly, 4 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 4 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1406` `getAvatar` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1422` `getInfo` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1437` `updateSettings` (3 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1492` `sendEmailChange` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 1 returned directly, 3 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 3 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1555` `resendEmailChange` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 3 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (3 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1582` `changeEmail` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 4 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (4 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1627` `sendEmailVerification` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 2 returned directly, 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1666` `verifyEmail` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 4 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (4 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1699` `activateAccountForm` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 4 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (4 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1727` `activateAccount` | routed handler | async toolkit `(request, h)`; returns its response -- all 5 signalling calls delivered: 3 returned directly, 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (3 returned directly, 2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1775` `requestExport` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 returned through a nested handler on a returned chain, 2 captured into a name a returning branch reads; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain, 2 captured into a name a returning branch reads) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1904` `listExports` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1951` `getExportStatus` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:2011` `downloadExport` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/admin.js` -- 8 handlers (8 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/admin.js:11` `index` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:88` `ohnoes` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 captured into a name a returning branch reads; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 captured into a name a returning branch reads) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:154` `uploadUsers` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 settling the returned promise, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:239` `updateUser` | routed handler | async toolkit `(request, h)`; returns its response -- all 6 signalling calls delivered: 6 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (6 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:319` `grantRole` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 settling the returned promise, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:378` `addFeaturedCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:418` `removeFeaturedCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:427` `moveFeaturedCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/courses.js` -- 7 handlers (7 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/courses.js:51` `creationForm` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:64` `create` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:126` `getCourses` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:135` `featuredCourses` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:159` `copy` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:260` `coursePage` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:295` `download` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- keep the residual `reply(Boom.forbidden())` in the unauthorized branch exactly as it stands, so it throws and the route answers 500. That REPLACES the generic handler conversion mandate, which is wrong here: converting every `reply(...)` into a returned toolkit response -- or taking the residual-reply row's "either return a toolkit response there" option -- would turn the 500 into the 403 the expression names. Measured outcome: the unauthorized branch evaluates an UNBOUND `Boom`, so it throws a `ReferenceError` before any response is constructed and the route answers 500 where the expression names 403. Owned by `docs/preserved-quirks.md` §9.7; reproduce it, do not fix it. |

### `lib/controllers/pages.js` -- 6 handlers (6 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/pages.js:10` `index` (3 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/pages.js:15` `login` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- keep `return reply.redirect('/home')` as the expression it is, so the authenticated branch throws its `TypeError` and reaches the preserved handler catch-all as a 500. That REPLACES the generic handler conversion mandate, which is wrong here: returning `request.success(...)` / `request.fail(...)` on every path would replace the throw with a working response, which is the quirk itself. Measured outcome: the authenticated-visitor 500 is REPRODUCED -- `reply.redirect` is a property access on a bare function and throws a TypeError that reaches the catch-all. Owned by `docs/preserved-quirks.md` §5; reproduce it, do not fix it. |
| [x] | `lib/controllers/pages.js:29` `signup` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- keep `return reply.redirect('/welcome')` as the expression it is, so the authenticated branch throws and answers 500, and leave `request.yar.set('next', ...)` in the `else` branch only. That REPLACES the generic handler conversion mandate, which is wrong here: returning `request.success(...)` / `request.fail(...)` on every path would replace the throw with a working response, which is the quirk itself. Measured outcome: the authenticated-visitor 500 is REPRODUCED, and `request.yar.set('next', ...)` stays in the `else` branch only -- it does not precede the throw. Owned by `docs/preserved-quirks.md` §5; reproduce it, do not fix it. |
| [x] | `lib/controllers/pages.js:44` `welcome` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/pages.js:50` `home` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/pages.js:84` `forgotPasswordForm` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/folders.js` -- 6 handlers (6 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/folders.js:125` `list` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/folders.js:158` `listView` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/folders.js:161` `trinkets` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- pass NO folder filter on the queryless path, and pass it only when a query is present -- the extracted core reproduces both cases. That REPLACES the generic handler conversion mandate, which is wrong here: "every path returns exactly once" is satisfiable while accidentally passing the folder through on the queryless path, which R-d forbids. Measured outcome: the queryless case passes NO folder filter because the injected URL is malformed; the extraction must reproduce both cases. Owned by `docs/preserved-quirks.md` §7; reproduce it, do not fix it. |
| [x] | `lib/controllers/folders.js:207` `create` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/folders.js:368` `update` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/folders.js:467` `deleteFolder` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/classes.js` -- 5 handlers (5 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/classes.js:25` `viewCourses` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/classes.js:59` `viewClass` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 returned directly, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/classes.js:97` `getClass` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/classes.js:143` `acceptInvitation` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/classes.js:259` `joinFromLink` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |

### `lib/controllers/files.js` -- 4 handlers (4 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/files.js:340` `uploadAvatar` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/files.js:386` `upload` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/files.js:496` `download` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/files.js:647` `setThumbnail` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/auth.js` -- 2 handlers (2 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/auth.js:323` `google` | routed handler | toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/auth.js:348` `googleCallback` | routed handler | toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 returned directly, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Signature is already `function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- persist the user, mutate the session, and THEN report the generic authentication failure -- preserve that ORDER and the absence of a login, so a first-time sign-in still creates the account and still reports failure. That REPLACES the generic handler conversion mandate, which is wrong here: a mandate to return a response on every path can silently drop the throw that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |

## 2. Routed pre-handlers -- 10 rows (10 closed): 8 pre-handler functions plus 2 dead 301 branch sites inside two of them

Named pre-handlers in `lib/util/helpers.js` that a route references. The census is
11 named pre-handlers in total; 8 are routed and 3 are not.

Two exports are deliberately **not** in that census, and saying why keeps the
arithmetic honest: `lowerUserFields` is an alias -- `module.exports.lowerUserFields =
internals.lowerUserFields;` -- with no function literal at the declaration, and the
function it points at is already `(request, h)`; and `findFeaturedTrinkets` is
already `(request, h)` too, which makes it the exemplar rather than the work.
`register` is a `server.method` registrar, not a pre-handler at all.

A pre-handler with nothing to contribute returns **`null`**, which is exactly what
the shim already produces for it.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/util/helpers.js:284` `findTrinket` (descriptor `method`) | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | PRESERVED QUIRK, GOVERNING ACTION -- at the language-mismatch branch, `return null` -- the value the shim produced. Its redirect construction is REMOVED, not converted. That QUALIFIES the generic pre-handler conversion mandate rather than displacing it. The mandate still governs every OTHER path in this body: Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. The carve-out exists because converting that chain into a returned toolkit response would emit a 301 the baseline never emitted. Measured outcome: the language-mismatch 301 at `lib/util/helpers.js:182` is MEASURED DEAD -- the deferred is settled with `null` at `lib/util/routeParser.js:147` before `.takeover()` reaches its own resolve at `:154`, so the pre value is already `null`. Owned by `docs/preserved-quirks.md` §2; reproduce it, do not fix it. |
| [x] | `lib/util/helpers.js:453` `validLang` (descriptor `method`) | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:472` `trinketTypeEnabled` (descriptor `method`) | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:503` `coursesEnabled` (descriptor `method`) | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:633` `verifyEmailToken` | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:730` `getDefaultTrinket` | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:747` `userByUsername` | routed pre-handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:765` `courseBySlug` | routed pre-handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | PRESERVED QUIRK, GOVERNING ACTION -- at the slug-alias branch, `return null` -- the value the shim produced. Its redirect construction is REMOVED, not converted. That QUALIFIES the generic pre-handler conversion mandate rather than displacing it. The mandate still governs every OTHER path in this body: Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. The carve-out exists because converting that chain into a returned toolkit response would emit a 301 the baseline never emitted. Measured outcome: the slug-alias 301 at `lib/util/helpers.js:385`, reached from 5 route declarations, is MEASURED DEAD by the same mechanism -- the deferred is already settled with `null`. Owned by `docs/preserved-quirks.md` §2; reproduce it, do not fix it. |

### The two dead pre-handler 301 redirects

Both are **measured dead**, and the mechanism matters because deleting it does not
preserve the outcome. In the shim, `fakeReply(undefined)` settles the deferred with
`null` at `lib/util/routeParser.js:147` **before** `.takeover()` reaches its own
resolve at `:154`. So the redirect is discarded and the pre value is already `null`.
The capability is dead end to end: `_isRedirect`, `_permanent` and `_takeover` appear
**only** on the six lines that define them -- `lib/util/routeParser.js:98`, `:100`,
`:101`, `:151`, `:153`, `:154`.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/util/helpers.js:182` `findTrinket` (baseline coordinate; the legacy construct is no longer in this tree) | routed pre-handler | findTrinket language-mismatch branch: return reply().redirect(location).permanent().takeover(). MEASURED DEAD: fakeReply(undefined) settles the deferred with null at lib/util/routeParser.js:147 BEFORE .takeover() reaches its own resolve at :154, so the redirect is discarded and the pre value is already null.; the legacy construct is gone from `findTrinket` | return null. The probe confirmed GET /prenull -> {"pre":null}. The capability is dead end to end: _isRedirect, _permanent and _takeover appear only on the six lines defining them (routeParser :98, :100, :101, :151, :153, :154). Baseline outcome owned by `docs/preserved-quirks.md` §2; reproduce it, do not fix it. EVIDENCE: `quirk.dead-301.find-trinket-language-mismatch` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the pre value this row claims is a recorded comparison rather than an expectation. |
| [x] | `lib/util/helpers.js:385` `courseBySlug` (baseline coordinate; the legacy construct is no longer in this tree) | routed pre-handler | courseBySlug slug-alias branch (reached from 5 route declarations): return reply().redirect(location).permanent().takeover(). MEASURED DEAD by the same mechanism -- the deferred is already settled with null.; the legacy construct is gone from `courseBySlug` | return null. Coverage is counted from the route manifest rather than from lexical references, because the per-language expansion multiplies them. Baseline outcome owned by `docs/preserved-quirks.md` §2; reproduce it, do not fix it. EVIDENCE: `quirk.dead-301.course-by-slug-alias` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the pre value this row claims is a recorded comparison rather than an expectation. |

## 3. Inline pre-handler -- 1 row (1 closed)

A function literal declared directly inside a `pre :` array in a route config.
There is exactly one, on `POST /api/users/login`, and it is the sole member of its
own category in the conversion set. Note its parameter is `req`, not `request`.

The other 116 declarations in `config/api_routes.js` are untouched by this
migration -- only this pre-handler changes.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `config/api_routes.js:1219` | inline pre-handler | `(request, h)`; body is `function(request, h) { return true; }` -- signals through the shim rather than returning to hapi | A function returning `true`: `function (request, h) { return true; }`. The assigned pre value stays `true`, so `request.pre.encryptRoles` is unchanged. |

## 4. Promise chains -- 143 rows (143 closed)

One row per chain. AAP §0.4.1 states the requirement this section measures:
after conversion each chain must be delivered **exactly once per path**, because
"neither a returned-but-unawaited chain nor an awaited-but-unreturned one is
visible in a signature count". A row closes on one of two settled dispositions,
and the `Current shape` and `Target disposition` columns name which one it is.

**DELIVERED -- 130 rows.** The chain's value or its
settlement reaches a consumer, AND its terminal link registers a reaction that
produces a value. Delivery is not one mechanism: this codebase composes work
through 6, and a row names the one it uses.

| Delivery mechanism | Rows | What it looks like |
| --- | --- | --- |
| `return` | 110 | `return chain.then(...)` |
| `await` | 4 | `await chain.then(...)` |
| settlement argument | 2 | `resolve(chain)` inside a `new Promise` executor -- the chain's value becomes the settled value |
| call argument | 2 | `promises.push(chain)` -- handed to a consumer that awaits it later |
| transitive capture | 6 | `promise = chain`, bound to a name a later expression delivers. Resolved to a fixpoint, so a name delivered by a delivered name counts |
| reaction settlement | 6 | the chain settles the carrier's promise from INSIDE one of its own reactions, so the expression itself has nothing left to return |

Those columns sum to the 130 chains whose value leaves the expression, which is
asserted rather than asked of the reader: a mechanism added to the classifier and
not to this table would make the section's own arithmetic fail before the
document is written. 130 of those 130 also have a terminal link that
produces a value, which is what closes the row; the other 0 are the
delivered-into-nothing rows below.
16 chains carry a settling reaction in total -- the 6 counted under
reaction settlement above, plus the ones that also leave by one of the earlier
mechanisms and are counted there instead.

**A terminal link passing a BARE REFERENCE to a response helper produces the
value.** `request.success` and `request.fail` RETURN a toolkit response, so a
chain ending `.catch(request.fail)` resolves to that response and a `return` in
front of it delivers it. That is the shape AAP §0.3.1 singles out as the
correct one, and it occurs here as 7 terminal reference(s) to `request.fail` and 3 terminal reference(s) to `request.success`.

An earlier revision of this section asserted the opposite -- that such a link
"reads as handled and produces nothing the handler returns" -- and the classifier
behind it reported every one of those rows as open. Both were wrong in the
safe-looking direction. The `Current shape` column now states per row that the
bare reference resolves to the toolkit response.

**DELIBERATELY DISCARDED -- 13 rows.** The chain is in statement position, so its
value is discarded, and nothing could have depended on it. A discard is
intentional for one of exactly two reasons, and the row states which:

- **7 rows** -- the carrier is a lifecycle
  method PROVEN to deliver on every path, so the discarded value provably was not
  the response. The discard is fire-and-forget timing, and R-d preserves it. Two
  are mandated by rule T-5 in as many words: the
  `fs.promises.unlink(...).catch(() => {})` sites.
- **6 rows** -- the carrier is not
  a lifecycle method at all. Rule T-3 places no delivery obligation there, so the
  discard is the helper's own timing, preserved under R-d.

**A row stays OPEN in exactly two states**, and the row prints the one it is in:

- **0 rows** -- discarded inside
  a lifecycle method whose own delivery is NOT proven. There the discarded value
  may be the response on the path that does not deliver, so section 1's row for
  the carrier has to close first.
- **0 rows** -- delivered into a
  terminal link that produces nothing (0 chains have such a
  terminal). What reaches hapi is then whatever the previous link resolved to.

What happens INSIDE the links -- a `reply(...)` becoming a returned toolkit
response -- is the enclosing function's row in section 1, not this one. Splitting
it that way keeps each site closable by one person looking at one thing.

A `.then(` / `.catch(` / `.finally(` call is admitted as a chain link only when its
STRUCTURE is a promise reaction: a receiver that is itself a call or an index, or a
plain binding receiving a function-shaped argument. The name alone is not enough,
and the difference is accounted for here rather than left to a reader who greps
`.catch(` and finds more hits than rows:

- `lib/controllers/users.js:2109` -- `sent.then(...)` takes an object literal on a plain receiver. It is **not** a
  promise reaction: the member does not exist on `sent`, so the
  call throws `TypeError` and the route catch-all answers 500. That is a
  preserved baseline outcome owned by `docs/preserved-quirks.md`, and an error edge
  rather than a chain -- reading it as one produced a row describing a chain
  that is not there and a target proposing to wrap an object literal in a
  function.
- `lib/controllers/folders.js:424` `update` -- `request.catch(...)` takes an object literal on a plain receiver. It is **not** a
  promise reaction: the member does not exist on `request`, so the
  call throws `TypeError` and the route catch-all answers 500. That is a
  preserved baseline outcome owned by `docs/preserved-quirks.md`, and an error edge
  rather than a chain -- reading it as one produced a row describing a chain
  that is not there and a target proposing to wrap an object literal in a
  function.

### `lib/controllers/course.js` -- 38 chains (38 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/course.js:379-409` `getCourse` | promise chain | 2-link chain `.then().then()`; currently RETURNED; head `course.populate(lessonsPopulate)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:484-492` `deleteCourse` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.deleteCourse()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:510` `archiveCourse` | promise chain | 1-link chain `.catch()`; currently AWAITED; head `course.save()` | Already delivered (awaited), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:558-567` `copyCourse` | promise chain | 1-link chain `.then()`; currently RETURNED; head `course.addUser(request.user, ["course-owner"])` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:590-604` `addLesson` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `lesson.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:702-710` `deleteLesson` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE to `request.fail`, which returns the toolkit response, so the chain resolves to it; head `lesson.remove()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:741-770` `addMaterial` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `trinketPromise` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:796-805` `getMaterial` | promise chain | 1-link chain `.then()`; currently RETURNED; head `promise` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:839-899` `updateMaterial` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `trinketPromise` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:957-972` `moveMaterial` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE to `request.fail`, which returns the toolkit response, so the chain resolves to it; head `lesson.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:985-995` `deleteMaterial` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE to `request.fail`, which returns the toolkit response, so the chain resolves to it; head `material.remove()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1038-1046` `listInvitations` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `CourseInvitation.findUnacceptedByCourse(course)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1057-1088` `userLookup` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `User.findByLogin(request.payload.user)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1101-1117` `removeUser` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `User.findById(userId)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1128-1144` `addUser` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `User.findById(request.payload.user)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1156-1172` `updateRoles` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `User.findById(request.payload.user)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1186-1194` `updateViews` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.updateView(userId, view, action)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1216-1231` `sendInvitations` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `CourseInvitation.addList(request.payload.emailList, course)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1218-1221` `sendInvitations` | promise chain | 1-link chain `.then()`; currently RETURNED; head `CourseInvitation.sendEmails(invitations, course, request.user)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:1247-1257` `removeInvitation` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `CourseInvitation.findById(request.params.invitationId)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1304-1347` `updateInvitation` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `invitation.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1306-1309` `updateInvitation` | promise chain | 1-link chain `.then()`; currently RETURNED; head `CourseInvitation.sendEmails([savedInvitation], course, request.user)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:1365-1374` `generateAccessCode` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1427-1450` `join` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.addUser(request.user, ["course-student"])` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1465-1499` `startAssignment` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Trinket.findById(request.payload.parent)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1516-1573` `submitAssignment` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `Material.findById(request.params.materialId)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1582-1630` `updateMySubmission` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Material.findById(submission.materialId)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1650` `autosaveComments` | promise chain | 1-link chain `.then()`; currently RETURNED; terminal `.then(request.success)` passes a BARE REFERENCE to `request.success`, which returns the toolkit response, so the chain resolves to it; head `submission.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:1672-1701` `getUserSubmissionsForMaterial` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findByUserAndMaterial(userId, request.params.materialId)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1740-1818` `dashboardOverview` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `course.populate({ path : 'lessons', select : 'materials' })` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1846-1875` `materialDashboard` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.courseDashboard(request.params.courseId)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1911-1964` `getMaterialSubmissionsForAllUsers` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findSubmissionsByMaterial(request.params.materialId)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1986-2032` `getUserSubmissionsForCourse` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Trinket.findSubmissionsByUserAndCourse(user._id, course._id)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:2057` `autosaveFeedbackComments` | promise chain | 1-link chain `.then()`; currently RETURNED; terminal `.then(request.success)` passes a BARE REFERENCE to `request.success`, which returns the toolkit response, so the chain resolves to it; head `submission.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:2069` `autosaveSubmissionOpt` | promise chain | 1-link chain `.then()`; currently RETURNED; terminal `.then(request.success)` passes a BARE REFERENCE to `request.success`, which returns the toolkit response, so the chain resolves to it; head `submission.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:2085-2167` `sendFeedback` | promise chain | 5-link chain `.then().then().then().then().catch()`; currently RETURNED; head `Trinket.findById(request.payload.trinketId)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:2185-2190` `acceptSubmission` | promise chain | 1-link chain `.then()`; currently RETURNED; head `trinket.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/course.js:2254-2257` (module scope) | promise chain | 1-link chain `.then()`; currently settled through `resolve(...)` / `reject(...)` inside one of its own reactions; head `savedCourse.addUser(actingUser, ["course-owner"])` | Already delivered (settled through `resolve(...)` / `reject(...)` inside one of its own reactions), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |

### `lib/controllers/trinket.js` -- 46 chains (46 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/trinket.js:431` `library` | promise chain | 1-link chain `.catch()`; currently RETURNED; head `Trinket.findById(id)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:510-520` `getById` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `User.findById( data._owner)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:527-529` `grant` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE to `request.fail`, which returns the toolkit response, so the chain resolves to it; head `trinket.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:568-594` `update` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE to `request.fail`, which returns the toolkit response, so the chain resolves to it; head `trinket.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:572-584` `update` | promise chain | 2-link chain `.then().catch()`; currently in statement position -- its value is discarded (preceded by `{`); head `Folder.findById(trinket.folder.folderId)` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position and `update` is proven to deliver a lifecycle value on every path, so the discarded value was not the response -- this is fire-and-forget timing, and preserving it is the target (R-d). Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:639-660` `create` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `trinket.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:699-718` `createFork` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Trinket.findByIdAndUpdateMetrics(parent.id, 'forks', meta)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:768-774` `updateMetrics` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findByIdAndUpdateMetrics(request.params.trinketId, metric, me…` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:783-788` `remove` | promise chain | 1-link chain `.then()`; currently passed to `promises.push`; head `Folder.findById(trinket.folder.folderId)` | Already delivered (passed to `promises.push`), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/trinket.js:792-800` `remove` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Promise.all(promises)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:794-797` `remove` | promise chain | 1-link chain `.then()`; currently RETURNED; head `trinket.softDelete()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/trinket.js:849-869` `getByShortCode` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `updateMetrics()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:918-928` `embed` | promise chain | 1-link chain `.then()`; currently captured into `promise`, which a later expression delivers; head `Trinket.findRemix(request.pre.trinket.id, request.user.id)` | Already delivered (captured into `promise`, which a later expression delivers), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/trinket.js:934-945` `embed` | promise chain | 1-link chain `.then()`; currently captured into `promise`, which a later expression delivers; head `promise` | Already delivered (captured into `promise`, which a later expression delivers), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/trinket.js:939-944` `embed` | promise chain | 1-link chain `.then()`; currently RETURNED; head `ownerPromise` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/trinket.js:943` `embed` | promise chain | 1-link chain `.then()`; currently in statement position -- its value is discarded (preceded by `:`); head `Trinket.findByIdAndUpdateMetrics(request.pre.trinket.id, 'embedViews'…` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position and `embed` is proven to deliver a lifecycle value on every path, so the discarded value was not the response -- this is fire-and-forget timing, and preserving it is the target (R-d). Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. |
| [x] | `lib/controllers/trinket.js:951-1119` `embed` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `promise` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1062-1115` `embed` | promise chain | 2-link chain `.then().then()`; currently RETURNED; head `Promise.resolve()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/trinket.js:1140-1151` `assignmentFeedback` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Draft.findOneMoreRecent(query)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/trinket.js:1226-1245` `email` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `mailer.send(request.payload.email, subject, options)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1268-1278` `snapshot` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE to `request.fail`, which returns the toolkit response, so the chain resolves to it; head `new Promise(function(resolve, reject) { FileUtil.uploadSnapshotFromBu…` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1288-1294` `interactions` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Interaction.findByTrinketId(trinket.id)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1331` `logError` | promise chain | 1-link chain `.catch()`; currently AWAITED; head `error.save()` | Already delivered (awaited), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1365-1371` `logClientMetric` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `ClientMetric.addMetric(values)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1437-1473` `draft` | promise chain | 3-link chain `.then().then().catch()`; currently settled through `resolve(...)` / `reject(...)` inside one of its own reactions; head `zip.loadAsync(request.payload.zipCode, { base64: true })` | Already delivered (settled through `resolve(...)` / `reject(...)` inside one of its own reactions), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1455-1463` `draft` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Draft.findOneAndUpdate(query, update)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1477-1485` `draft` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Draft.findOneAndUpdate(query, update)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1504-1512` `discardDraft` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Draft.discard(query)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1543-1578` `autosave` | promise chain | 3-link chain `.then().then().catch()`; currently settled through `resolve(...)` / `reject(...)` inside one of its own reactions; head `zip.loadAsync(request.payload.zipCode, { base64: true })` | Already delivered (settled through `resolve(...)` / `reject(...)` inside one of its own reactions), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1561-1568` `autosave` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `trinket.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1582-1589` `autosave` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `trinket.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1617-1635` `addToFolder` | promise chain | 5-link chain `.then().then().then().then().catch()`; currently RETURNED; head `checkCurrent` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1668-1679` `removeFromFolder` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `folder.removeTrinket(trinket.id)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1693-1701` `search` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.searchForOwner(request.user, request.query.q)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1707-1742` `downloadMain` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findById(request.params.shortCode)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1750-1812` `downloadFile` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findById(request.params.shortCode)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1800-1804` `downloadFile` | promise chain | 1-link chain `.then()`; currently RETURNED; head `FileUtil.downloadUserAsset(file)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/trinket.js:1824-1832` `updateSlug` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `trinket.updateSlug(request.payload.slug)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1865` `downloadPostedZip` | promise chain | 1-link chain `.catch()`; currently in statement position -- its value is discarded (preceded by `{`); head `fs.promises.rm(zipDir, { recursive : true, force : true })` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position inside `removeZipDir`, which is not a lifecycle method, so rule T-3 places no delivery obligation on it and the discard is the helper's own timing -- preserved under R-d. Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1921` `downloadPostedZip` | promise chain | 1-link chain `.catch()`; currently in statement position -- its value is discarded (preceded by `;`); head `outputPromise` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position and `downloadPostedZip` is proven to deliver a lifecycle value on every path, so the discarded value was not the response -- this is fire-and-forget timing, and preserving it is the target (R-d). Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1990-2000` `downloadPostedZip` | promise chain | 2-link chain `.then().catch()`; currently passed to `assetPromises.push`; head `FileUtil.downloadUserAsset(assetFile)` | Already delivered (passed to `assetPromises.push`), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:2010-2053` `downloadPostedZip` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Promise.all(assetPromises)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:2326-2344` (module scope) | promise chain | 2-link chain `.then().then()`; currently RETURNED; head `getUserId()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/trinket.js:2437` (module scope) | promise chain | 1-link chain `.catch()`; currently in statement position -- its value is discarded (preceded by `{`); head `fs.promises.rm(zipDir, { recursive : true, force : true })` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position inside `removeZipDir`, which is not a lifecycle method, so rule T-3 places no delivery obligation on it and the discard is the helper's own timing -- preserved under R-d. Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:2505` (module scope) | promise chain | 1-link chain `.catch()`; currently in statement position -- its value is discarded (preceded by `;`); head `outputPromise` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position inside `downloadZip`, which is not a lifecycle method, so rule T-3 places no delivery obligation on it and the discard is the helper's own timing -- preserved under R-d. Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:2570-2617` (module scope) | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Promise.allSettled(assetPromises)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |

### `lib/controllers/users.js` -- 18 chains (18 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/users.js:65-67` (module scope) | promise chain | 1-link chain `.then()`; currently in statement position -- its value is discarded (preceded by `{`); head `response.body.cancel()` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position inside `assetDiscardBody`, which is not a lifecycle method, so rule T-3 places no delivery obligation on it and the discard is the helper's own timing -- preserved under R-d. Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. |
| [x] | `lib/controllers/users.js:96-126` (module scope) | promise chain | 1-link chain `.then()`; currently RETURNED; head `globalThis.fetch(target, { method : 'GET', // Always manual: the hop…` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/users.js:668-677` `remove` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `request.user.remove()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:864-888` `account` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `promise` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:927-995` `updateProfile` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `usernameCheck` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:952-962` `updateProfile` | promise chain | 2-link chain `.then().catch()`; currently captured into `addFolderSlugJob`, which a later expression delivers; head `Folder.findByOwner(user)` | Already delivered (captured into `addFolderSlugJob`, which a later expression delivers), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:980-987` `updateProfile` | promise chain | 2-link chain `.then().then()`; currently settled through `resolve(...)` / `reject(...)` inside one of its own reactions; head `addFolderSlugJob` | Already delivered (settled through `resolve(...)` / `reject(...)` inside one of its own reactions), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/users.js:1015-1045` `assetList` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `getUserFiles` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1071-1082` `replaceAsset` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `new Promise(function(resolve, reject) { FileUtil.uploadUserAsset(requ…` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1096-1102` `removeAsset` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `file.hide()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1115-1121` `restoreAsset` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `file.show()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1291-1362` `assetUploadFromURL` | promise chain | 1-link chain `.then()`; currently in statement position -- its value is discarded (preceded by `;`); head `fetchAssetResource(request.payload.url)` | PRESERVED QUIRK, GOVERNING ACTION -- await the transport chain where baseline continues, and leave the refused-connection path UNSETTLED -- that path deliberately produces no response. That REPLACES the generic promise-chain mandate, which is wrong here: returning the chain's value exactly once per path settles the path this quirk requires to stay open. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [x] | `lib/controllers/users.js:1359` `assetUploadFromURL` | promise chain | 1-link chain `.catch()`; currently in statement position -- its value is discarded (preceded by `{`); head `fs.promises.unlink(tmpPath)` | PRESERVED QUIRK, GOVERNING ACTION -- await the transport chain where baseline continues, and leave the refused-connection path UNSETTLED -- that path deliberately produces no response. That REPLACES the generic promise-chain mandate, which is wrong here: returning the chain's value exactly once per path settles the path this quirk requires to stay open. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [x] | `lib/controllers/users.js:1438-1446` `updateSettings` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `request.user.updateSettings(request.payload)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1529-1550` `sendEmailChange` | promise chain | 1-link chain `.then()`; currently settled through `resolve(...)` / `reject(...)` inside one of its own reactions; head `Store.set(changeKey, JSON.stringify(changeVal))` | Already delivered (settled through `resolve(...)` / `reject(...)` inside one of its own reactions), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/users.js:1786-1901` `requestExport` | promise chain | 4-link chain `.then().then().then().catch()`; currently AWAITED; head `Export.findPendingOrProcessing(userId)` | Already delivered (awaited), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1854-1861` `requestExport` | promise chain | 1-link chain `.then()`; currently in statement position -- its value is discarded (preceded by `;`); head `exportsQueue.add({ action: 'bulk-export', exportId: exportRecord._id.…` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position and `requestExport` is proven to deliver a lifecycle value on every path, so the discarded value was not the response -- this is fire-and-forget timing, and preserving it is the target (R-d). Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. |
| [x] | `lib/controllers/users.js:1907-1926` `listExports` | promise chain | 2-link chain `.then().catch()`; currently AWAITED; head `Export.findByOwner(request.user)` | Already delivered (awaited), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |

### `lib/controllers/admin.js` -- 13 chains (13 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/admin.js:43-72` `index` | promise chain | 3-link chain `.then().then().catch()`; currently captured into `promise`, which a later expression delivers; head `featuredStore.getList()` | Already delivered (captured into `promise`, which a later expression delivers), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:46-52` `index` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Course.findById(member.id)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/admin.js:78-86` `index` | promise chain | 1-link chain `.then()`; currently RETURNED; head `promise` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/admin.js:210-229` `uploadUsers` | promise chain | 1-link chain `.then()`; currently handed to `resolve(...)` / `reject(...)`, settling the promise the carrier returns; head `Promise.allSettled(promises)` | Already delivered (handed to `resolve(...)` / `reject(...)`, settling the promise the carrier returns), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/admin.js:329-374` `grantRole` | promise chain | 3-link chain `.then().then().catch()`; currently handed to `resolve(...)` / `reject(...)`, settling the promise the carrier returns; head `user.grant(request.payload.role, "site")` | Already delivered (handed to `resolve(...)` / `reject(...)`, settling the promise the carrier returns), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:337-339` `grantRole` | promise chain | 1-link chain `.then()`; currently captured into `promise`, which a later expression delivers; head `promise` | Already delivered (captured into `promise`, which a later expression delivers), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/admin.js:342-344` `grantRole` | promise chain | 1-link chain `.then()`; currently captured into `promise`, which a later expression delivers; head `promise` | Already delivered (captured into `promise`, which a later expression delivers), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/admin.js:384-416` `addFeaturedCourse` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `User.findByLogin(request.payload.ownerSlug)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:395-396` `addFeaturedCourse` | promise chain | 1-link chain `.then()`; currently RETURNED; head `featuredStore.addMember(course.id, request.payload.page)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/admin.js:419-425` `removeFeaturedCourse` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `featuredStore.removeMember(request.params.courseId, request.query.pag…` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:428-434` `moveFeaturedCourse` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `featuredStore.moveMember(request.payload.courseId, request.payload.pa…` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:580-591` (module scope) | promise chain | 3-link chain `.then().then().catch()`; currently settled through `resolve(...)` / `reject(...)` inside one of its own reactions; head `Trinket.findForUser(user.id)` | Already delivered (settled through `resolve(...)` / `reject(...)` inside one of its own reactions), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:601-607` (module scope) | promise chain | 1-link chain `.then()`; currently RETURNED; head `User.findByRole(role)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |

### `lib/controllers/courses.js` -- 9 chains (9 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/courses.js:129-132` `getCourses` | promise chain | 1-link chain `.then()`; currently RETURNED; head `request.user.getCourses()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/courses.js:136-156` `featuredCourses` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Course.findFeaturedForUser(request.user)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. |
| [x] | `lib/controllers/courses.js:252-255` `copy` | promise chain | 1-link chain `.then()`; currently RETURNED; head `request.user.grant("course-owner", "course", { id : course.id })` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/courses.js:392-407` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Lesson.findById(lesson)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/courses.js:396-406` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `fs.promises.mkdir(lessonDir, { recursive : true })` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/courses.js:414-437` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Material.findById(info.material)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/courses.js:477-483` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `nunjucks.render('courses/download/view.html', context)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/courses.js:501-507` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Promise.resolve()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/courses.js:613-636` `download` | promise chain | 7-link chain `.then().then().then().then().then().then().catch()`; currently RETURNED; head `makeWorkTree()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. |

### `lib/controllers/pages.js` -- 1 chain (1 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/pages.js:62-68` `home` -- the AAP cites this chain as `lib/controllers/pages.js:52` (measured at the base commit as `:48-54`, here `:62-68`) | promise chain | 2-link chain `.then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE to `request.fail`, which returns the toolkit response, so the chain resolves to it; head `Trinket.findRecentByOwner(request.user._id)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.9. |

### `lib/controllers/folders.js` -- 5 chains (5 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/folders.js:142-156` `list` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `getUserId()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |
| [x] | `lib/controllers/folders.js:357-364` `create` | promise chain | 1-link chain `.then()`; currently RETURNED; head `request.user.grant("folder-owner", "folder", { id : savedFolder.id })` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/folders.js:377-432` `update` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `folder.save()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |
| [x] | `lib/controllers/folders.js:389-399` `update` | promise chain | 2-link chain `.then().catch()`; currently in statement position -- its value is discarded (preceded by `{`); head `Trinket.findById(folderTrinket.trinketId)` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position and `update` is proven to deliver a lifecycle value on every path, so the discarded value was not the response -- this is fire-and-forget timing, and preserving it is the target (R-d). Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |
| [x] | `lib/controllers/folders.js:471-479` `deleteFolder` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `folder.deleteFolder()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |

### `lib/controllers/classes.js` -- 6 chains (6 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/classes.js:29-56` `viewCourses` | promise chain | 1-link chain `.then()`; currently RETURNED; head `request.pre.user.getOwnedCourses()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/classes.js:79-93` `viewClass` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Course.findFeaturedForUser(request.user)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/classes.js:170-256` `acceptInvitation` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `CourseInvitation.findByToken(request.params.token)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.4. |
| [x] | `lib/controllers/classes.js:173-246` `acceptInvitation` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Course.findById(invitation.courseId)` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.4. |
| [x] | `lib/controllers/classes.js:223-231` `acceptInvitation` | promise chain | 2-link chain `.then().then()`; currently RETURNED; head `course.addUser(request.user, ['course-student'])` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/classes.js:295-303` `joinFromLink` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.addUser(request.user, ["course-student"])` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.4. |

### `lib/controllers/auth.js` -- 7 chains (7 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/auth.js:172-174` (module scope) | promise chain | 1-link chain `.then()`; currently in statement position -- its value is discarded (preceded by `{`); head `response.body.cancel()` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position inside `discard`, which is not a lifecycle method, so rule T-3 places no delivery obligation on it and the discard is the helper's own timing -- preserved under R-d. Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. |
| [x] | `lib/controllers/auth.js:209-213` (module scope) | promise chain | 1-link chain `.then()`; currently RETURNED; head `response.text()` | Already delivered (returned), and its terminal reaction produces the value. Nothing further is required of this site: what remains is that no branch inside the links returns nothing, which the enclosing function's own row proves. |
| [x] | `lib/controllers/auth.js:233-305` (module scope) | promise chain | 2-link chain `.then().catch()`; currently in statement position -- its value is discarded (preceded by `;`); head `fetch(current.href, init)` | PRESERVE AS IT STANDS -- fire-and-forget, and that timing is the target. The chain is in statement position inside `attempt`, which is not a lifecycle method, so rule T-3 places no delivery obligation on it and the discard is the helper's own timing -- preserved under R-d. Do not await it and do not attach a handler that reports an error the baseline swallowed: either would change observable behaviour. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. |
| [x] | `lib/controllers/auth.js:361-564` `googleCallback` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `new Promise(function(resolve, reject) { legacyJsonRequest('https://oa…` | PRESERVED QUIRK, GOVERNING ACTION -- keep the chain returned, and keep the link that throws on the undefined `opts` reference throwing, so the `.catch` still produces the generic authentication failure after the user has been persisted. That REPLACES the generic promise-chain mandate, which is wrong here: requiring that no branch inside the links return nothing is the same mandate one level down, and the branch that returns nothing is the one that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [x] | `lib/controllers/auth.js:450-532` `googleCallback` | promise chain | 1-link chain `.then()`; currently RETURNED; head `new Promise(function(resolve, reject) { User.findByMultiple({ email:…` | PRESERVED QUIRK, GOVERNING ACTION -- keep the chain returned, and keep the link that throws on the undefined `opts` reference throwing, so the `.catch` still produces the generic authentication failure after the user has been persisted. That REPLACES the generic promise-chain mandate, which is wrong here: requiring that no branch inside the links return nothing is the same mandate one level down, and the branch that returns nothing is the one that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [x] | `lib/controllers/auth.js:492-494` `googleCallback` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Promise.all(promises)` | PRESERVED QUIRK, GOVERNING ACTION -- keep the chain returned, and keep the link that throws on the undefined `opts` reference throwing, so the `.catch` still produces the generic authentication failure after the user has been persisted. That REPLACES the generic promise-chain mandate, which is wrong here: requiring that no branch inside the links return nothing is the same mandate one level down, and the branch that returns nothing is the one that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [x] | `lib/controllers/auth.js:521-530` `googleCallback` | promise chain | 1-link chain `.then()`; currently RETURNED; head `user.save()` | PRESERVED QUIRK, GOVERNING ACTION -- keep the chain returned, and keep the link that throws on the undefined `opts` reference throwing, so the `.catch` still produces the generic authentication failure after the user has been persisted. That REPLACES the generic promise-chain mandate, which is wrong here: requiring that no branch inside the links return nothing is the same mandate one level down, and the branch that returns nothing is the one that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |

## 5. Callback boundaries -- 46 rows (46 closed)

**A CONVERTED callback boundary ceases to exist**, so this population shrinks as
work completes: replacing `util.f(x, function (err, r) { ... })` with `await`
removes the callback literal, and the row with it. Baseline: **57**. This tree: **46**, so 11 boundaries have been converted away.

**A remaining boundary is not by itself unfinished work, and the `Done` box says
which it is.** Rules T-3 and T-5 divide this population, and the division is the
whole content of the box:

- **T-5 -- must convert, box OPEN.** A Node-core callback API inside the
  hapi-facing surface becomes its promise equivalent *whether or not it emits a
  deprecation warning*: AAP §0.6.4 measured `util.promisify(fs.writeFile)`,
  callback `fs.stat` and `fs.unlink` as silent on Node 22 and converts them
  anyway. `rimraf` and `mkdirp` count as the same thing, and converting them is
  what removes those packages.
- **T-3 -- keeps its callback interface, box CLOSED.** The promise boundary sits
  AT the lifecycle method and no deeper, so `lib/util/file.js`,
  `lib/util/store.js`, `lib/util/queues.js` and `lib/models/model.js` keep their
  callback interfaces even though controllers call them directly. There is no
  promise form to await without pushing the boundary into the callee, which T-3
  forbids -- so such a site is finished in its current shape.
- **Node-core but not a completion callback, box CLOSED.** `process.nextTick` and
  `setTimeout` schedule work rather than report an outcome. T-5 does not reach
  them and converting one would move when the work runs, which is a timing change
  (R-d).

The second half of a closed box is the CARRIER. A boundary hands its own value to
the callee, so the response has to come from somewhere else -- and "somewhere
else" is established only where the enclosing lifecycle method is PROVEN to
deliver a value on every path, which is section 1's and section 2a's measurement.
Inside a module-scope helper there is no lifecycle obligation to prove (T-3
again), so the interface question decides the row alone. Every row states which
of these applies to it.

An earlier revision computed this box as "the call site already carries an
`await`" -- a shape that is neither the legacy one nor the converted one, and
occurs nowhere in this tree. The column could not reach anything but zero, and
zero out of a shrinking population read, in the row totals, as 46 units of unfinished work.

Each row still records **where the `await` goes** for the boundaries that must
take one: at the call site, inside the converted lifecycle method.

A boundary here is a call receiving a function literal whose parameters are either
error-first or empty -- the two shapes a completion callback takes. Empty-parameter
callbacks are deliberately included, because two named conversions are exactly that
shape: `rimraf(dir, function () {})` and `fs.unlink(file, function () {})`. Promise
links, event registrations and synchronous iteration helpers are excluded -- they
have their own categories or are not boundaries at all.

Where baseline swallows a callback error or fires and forgets, **that timing is the
target**: an error made visible, or a response made to wait, is a behaviour change.

### `lib/controllers/course.js` -- 6 boundaries (6 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/course.js:426` `updateCourse` | callback boundary | `course.save(..., function (err, savedCourse) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateCourse` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateCourse` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:536` `copyCourse` | callback boundary | `request.pre.course.copy(..., function (err, course) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `copyCourse` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `copyCourse` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:640` `updateLesson` | callback boundary | `lesson.save(..., function (err, savedLesson) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateLesson` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateLesson` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:678` `moveLesson` | callback boundary | `course.save(..., function (err, savedCourse) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `moveLesson` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `moveLesson` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1406` `join` | callback boundary | `Course.findByAccessCode(..., function (err, course) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `join` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `join` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:2244` (module scope) | callback boundary | `course.save(..., function (err, savedCourse) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3, and the boundary sits in module scope, which is not a lifecycle method, so there is no delivery obligation to prove here | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3, and the boundary sits in module scope, which is not a lifecycle method, so there is no delivery obligation to prove here. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |

### `lib/controllers/trinket.js` -- 3 boundaries (3 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/trinket.js:759` `updateMetrics` | callback boundary | `Trinket.findById(..., function (err, trinket) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateMetrics` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateMetrics` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1269` `snapshot` | callback boundary | `FileUtil.uploadSnapshotFromBuffer(..., function (err) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `snapshot` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `snapshot` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:2255` (module scope) | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3, and the boundary sits in `getUserId`, which is not a lifecycle method, so there is no delivery obligation to prove here | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3, and the boundary sits in `getUserId`, which is not a lifecycle method, so there is no delivery obligation to prove here. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |

### `lib/controllers/users.js` -- 25 boundaries (25 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/users.js:464` `create` | callback boundary | `User.exists(..., function (err, result) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `create` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `create` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:482` `create` | callback boundary | `request.yar._logIn(..., function (err) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `create` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `create` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:521` `login` | callback boundary | `User.findByLogin(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `login` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `login` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:574` `login` | callback boundary | `user.comparePassword(..., function (err, isMatch) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `login` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `login` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:606` `login` | callback boundary | `request.yar._logIn(..., function () { ... })` -- completion callback with no parameters; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `login` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `login` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. |
| [x] | `lib/controllers/users.js:724` `sendPassReset` | callback boundary | `User.findByLogin(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `sendPassReset` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `sendPassReset` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:785` `resetPasswordForm` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `resetPasswordForm` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `resetPasswordForm` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:817` `savePassword` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `savePassword` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `savePassword` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:822` `savePassword` | callback boundary | `user.save(..., function (err) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `savePassword` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `savePassword` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:845` `account` | callback boundary | `Course.findForUser(..., function (err, courses) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `account` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `account` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:911` `updateProfile` | callback boundary | `User.exists(..., function (err, result) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateProfile` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateProfile` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:937` `updateProfile` | callback boundary | `user.save(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateProfile` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateProfile` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1005` `assetList` | callback boundary | `File.findForUser(..., function (err, files) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `assetList` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `assetList` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1057` `assetUpload` | callback boundary | `FileUtil.uploadUserAsset(..., function (err, file) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `assetUpload` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `assetUpload` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1072` `replaceAsset` | callback boundary | `FileUtil.uploadUserAsset(..., function (err, file) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `replaceAsset` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `replaceAsset` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1154` `assetUploadFromURL` | callback boundary | `tmp.tmpName(..., function (err, tmpPath) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `assetUploadFromURL` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `assetUploadFromURL` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- keep the refused-connection path UNSETTLED -- when `end` never arrives the upload never starts and that path deliberately produces no response, so nothing done to this boundary or to the code around it may make the route answer on it. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path removes the unsettled outcome. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [x] | `lib/controllers/users.js:1210` `assetUploadFromURL` | callback boundary | `FileUtil.uploadUserAsset(..., function (err, file) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3, and the boundary sits in `startUpload`, which is not a lifecycle method, so there is no delivery obligation to prove here | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3, and the boundary sits in `startUpload`, which is not a lifecycle method, so there is no delivery obligation to prove here. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- keep the refused-connection path UNSETTLED -- when `end` never arrives the upload never starts and that path deliberately produces no response, so nothing done to this boundary or to the code around it may make the route answer on it. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path removes the unsettled outcome. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [x] | `lib/controllers/users.js:1370` `changePassword` | callback boundary | `request.user.comparePassword(..., function (err, match) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `changePassword` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `changePassword` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1379` `changePassword` | callback boundary | `request.user.save(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `changePassword` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `changePassword` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1502` `sendEmailChange` | callback boundary | `User.findByLogin(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `sendEmailChange` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `sendEmailChange` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1747` `activateAccount` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `activateAccount` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `activateAccount` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1757` `activateAccount` | callback boundary | `user.save(..., function (err) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `activateAccount` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `activateAccount` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1759` `activateAccount` | callback boundary | `request.yar._logIn(..., function (err) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `activateAccount` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `activateAccount` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1957` `getExportStatus` | callback boundary | `Export.findById(..., function (err, exportRecord) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `getExportStatus` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `getExportStatus` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:2016` `downloadExport` | callback boundary | `Export.findById(..., function (err, exportRecord) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `downloadExport` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `downloadExport` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |

### `lib/controllers/admin.js` -- 5 boundaries (5 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/admin.js:186` `uploadUsers` | callback boundary | `parse(..., function (err, records) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `uploadUsers` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `uploadUsers` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:292` `updateUser` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateUser` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateUser` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:309` `updateUser` | callback boundary | `user.save(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateUser` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `updateUser` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:324` `grantRole` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `grantRole` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `grantRole` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:567` (module scope) | callback boundary | `User.findByLogin(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3, and the boundary sits in `userSearch`, which is not a lifecycle method, so there is no delivery obligation to prove here | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3, and the boundary sits in `userSearch`, which is not a lifecycle method, so there is no delivery obligation to prove here. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |

### `lib/controllers/courses.js` -- 2 boundaries (2 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/courses.js:218` `copy` | callback boundary | `request.pre.course.copy(..., function (err, course) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `copy` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `copy` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. |
| [x] | `lib/controllers/courses.js:594` `download` | callback boundary | `process.nextTick(..., function () { ... })` -- completion callback with no parameters; the callee schedules work rather than reporting an outcome, so it has no promise equivalent that preserves when the work runs; rule T-5 does not reach it and changing it would be a timing change (R-d) | LEAVE AS IT STANDS. The callee schedules work rather than reporting an outcome, so it has no promise equivalent that preserves when the work runs; rule T-5 does not reach it and changing it would be a timing change (R-d). The callback body's own contents are covered by the rows for whatever it contains. |

### `lib/controllers/folders.js` -- 1 boundary (1 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/folders.js:241` `create` | callback boundary | `folder.save(..., function (err, savedFolder) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `create` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `create` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |

### `lib/controllers/files.js` -- 1 boundary (1 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/files.js:246` (module scope) | callback boundary | `invoke(..., function (err, value) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3, and the boundary sits in `settle`, which is not a lifecycle method, so there is no delivery obligation to prove here | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3, and the boundary sits in `settle`, which is not a lifecycle method, so there is no delivery obligation to prove here. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.7. |

### `lib/controllers/auth.js` -- 3 boundaries (3 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/auth.js:362` `googleCallback` | callback boundary | `legacyJsonRequest(..., function (err, response, body) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `googleCallback` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `googleCallback` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- keep the surrounding ORDER intact through this callback -- persist the user, mutate the session, and only THEN report the generic failure -- and do not let the conversion of anything around it drop the throw that produces that failure. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path can drop the throw that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [x] | `lib/controllers/auth.js:426` `googleCallback` | callback boundary | `legacyJsonRequest(..., function (err, response, profile) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `googleCallback` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `googleCallback` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- keep the surrounding ORDER intact through this callback -- persist the user, mutate the session, and only THEN report the generic failure -- and do not let the conversion of anything around it drop the throw that produces that failure. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path can drop the throw that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [x] | `lib/controllers/auth.js:451` `googleCallback` | callback boundary | `User.findByMultiple(..., function (err, user) { ... })` -- Node error-first callback; the callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `googleCallback` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape | LEAVE THE INTERFACE AS IT STANDS -- this boundary is settled, not pending. The callee keeps its callback interface under rule T-3 -- awaiting it would push the promise boundary into the callee -- and `googleCallback` is proven to deliver a lifecycle value on every path, so this boundary is settled in its current shape. Do not promisify the callee to create an `await` here: rule T-3 puts the promise boundary at the lifecycle method and no deeper, and the baseline's error handling inside the callback is the target -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- keep the surrounding ORDER intact through this callback -- persist the user, mutate the session, and only THEN report the generic failure -- and do not let the conversion of anything around it drop the throw that produces that failure. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path can drop the throw that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |

## 6. Reply chains -- 9 rows (8 closed): the 8 chains of the roster plus 1 unreturned bare `reply(` site

The roster below is a **recorded baseline measurement**, not a transcription: the
three-way classification depends on which builder method ran last, and the
analysis derives the same classification independently from
`RESOLVING_BUILDER_METHODS` and `NON_RESOLVING_BUILDER_METHODS`. At `2f8712a` every entry
must be locatable by its carrier symbol and link sequence, sit at the line the
roster records, and derive the category the roster records -- and the self-check
fails if any of the three disagrees. Two independent routes to the same answer is
the point; the symbol is what makes the first of them survive an edit.

Removing the builder removes a **mechanism**, not a set of outcomes. Three of these
chains return a builder object to hapi, four resolve real responses, and one never
settles at all -- so the target disposition has to be stated per chain.

In the analysed tree the derived scan finds 0 `reply(`-rooted chains carrying `.type()`/`.bytes()` and 5 `h.response(`-rooted equivalents.

**Each row is anchored on its carrier SYMBOL, not on a line number**, and closes on
three conditions rather than one. Baseline coordinates stop being addresses the
moment a file is edited -- every one of these eight chains moved during the
conversion -- so a row is located by the exported handler or module-scope function
that contains it, by its link sequence, and, where one carrier holds two chains of
the same shape, by their order within it. The three conditions are: the legacy
`reply(`-rooted chain is gone from that carrier; the declared target shape is
present in it, where a target shape can be declared at all; and the corpus scenario
that drives the site carries a captured baseline response AND a confirming replay
verdict against this tree. The third is not ceremony, and it has two halves for a
reason. What a client received from one of these chains depended on which builder
method ran last, so "the legacy syntax is gone" says nothing about what the route
now serves; and a captured baseline alone says only what the OLD code did, which
is one side of a comparison. Both sides are required, because a row that closed on
the baseline half would be asserting a match nothing had checked.

Evidence state in this tree: 380 of 392 scenarios in `test/parity/corpus.json` carry both a
captured baseline and a confirming replay verdict. 8 of 8 rows below close on that
recorded comparison; the rest name the scenario whose verdict is still missing.

### Never settles -- 1 chain

Only non-resolving links, and the chain's value is not returned either, so nothing ever produces a response and the branch hangs.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/files.js:98-100` `download` (converted; target chain at `:632-636`) | reply chain (never-settles) | reply(stream).type(...).bytes(...) with no `return` and no resolving call. Neither .type() nor .bytes() settles the deferred, so the image-download branch never produces a response at all -- the request hangs.; the legacy chain is gone from `download` and the declared target `h.response(...).type().bytes().header().header()` is present | **APPROVED DEVIATION.** Return `h.response(stream).type(request.pre.file.mime).bytes(request.pre.file.size)` -- and NO Content-Disposition header, because the image branch deliberately omits it. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.1; the deviation and its precedence argument by §11.1. EVIDENCE: `quirk.reply-chain.never-settles.image-download` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the outcome this row claims is a recorded comparison rather than an expectation. |

> **Approved deviation, `lib/controllers/files.js:98-100`.** This is the
> only row in this document whose target changes observable behaviour -- the
> migration's other approved deviation, the retained `marked` advisory, reaches no
> conversion site -- and it is
> approved rather than assumed. The response to serve is not inferred: the sibling branch four lines below performs the identical chain ending in .header() and returns a working stream response, so the target is that same response minus the Content-Disposition header the image branch deliberately omits.
>
> The precedence argument -- why R-b ("every route serves") controls over
> R-d ("behaviour improvements prohibited") here and nowhere else -- is owned by
> `docs/preserved-quirks.md` §11.1 and is not restated here.
>
> **The corpus treatment of this deviation, as it actually stands.** the corpus DEFINES the comparison: scenario `quirk.reply-chain.never-settles.image-download` drives `GET /api/files/{fileId}/{fileName}` with `intent: timeout` and `expectedDeviation.replayDisposition: approved-change`, so a baseline timeout against a target 200 stream response is expected to read as an approved change rather than a failure. Both halves exist: the baseline response has been captured on that scenario AND a replay against this tree recorded a confirming verdict (`approved-deviation`), so the timeout-to-200 comparison is a measurement.

### Header-resolved and working -- 4 chains

The chain reaches `.header(...)`, which resolves the deferred, so a real hapi response is produced. These work today and must come through the migration unchanged -- they must not become collateral damage of the approved deviation above.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/files.js:102-105` `download` (converted; target chain at `:641-644`) | reply chain (header-resolved) | reply(stream).type(...).bytes(...).header('Content-Disposition', ...). Also has no `return`, but .header() resolves the deferred, so it works and returns a real hapi response.; the legacy chain is gone from `download` and the declared target `h.response(...).type().bytes().header()` is present | Identical response. This is the non-image branch and it must NOT become collateral damage of the approved deviation four lines above. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.2; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.file-download-attachment` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the outcome this row claims is a recorded comparison rather than an expectation. |
| [x] | `lib/controllers/courses.js:269-272` `returnZip` (converted; target chain at `:607-610`) | reply chain (header-resolved) | return reply(stream).type('application/zip').bytes(stats.size).header('Content-Disposition', ...), inside the rimraf callback. Works: .header() resolves the deferred.; the legacy chain is gone from `returnZip` and the declared target `h.response(...).type().bytes().header()` is present | Identical response. Baseline WAITS for the deletion callback before the final .header() resolves, so the conversion awaits fs.promises.rm, swallows its error exactly as the empty callback does, and only then returns the response. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.2; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.course-download-zip` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the outcome this row claims is a recorded comparison rather than an expectation. |
| [x] | `lib/controllers/trinket.js:1383-1386` `downloadPostedZip` (converted; target chain at `:2041-2044`) | reply chain (header-resolved) | return reply(outputReadStream).type('application/zip').bytes(bytes).header('Content-Disposition', ...). Works: .header() resolves the deferred.; the legacy chain is gone from `downloadPostedZip` and the declared target `h.response(...).type().bytes().header()` is present | Identical response, including the quoted filename form this chain uses. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.2; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.posted-zip-download` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the outcome this row claims is a recorded comparison rather than an expectation. |
| [x] | `lib/controllers/trinket.js:1548-1551` `downloadZip` (converted; target chain at `:2605-2608`) | reply chain (header-resolved) | return reply(outputReadStream).type('application/zip').bytes(bytes).header('Content-Disposition', ...). Works: .header() resolves the deferred.; the legacy chain is gone from `downloadZip` and the declared target `h.response(...).type().bytes().header()` is present | Identical response, including the unquoted filename form this chain uses. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.2; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.short-code-zip` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the outcome this row claims is a recorded comparison rather than an expectation. |

### Builder returned to hapi -- 3 chains

`return reply(...).type(type)` hands the wrapper the **builder object** rather than a hapi response. What is emitted depends on whether the deferred was already resolved earlier in the request, so the outcome has to be captured at the baseline BEFORE conversion rather than reasoned about.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/trinket.js:1204` `downloadMain` (legacy chain gone; no declared target shape to locate) | reply chain (builder-returned) | return reply(code[0].content).type(type) -- hands the WRAPPER the builder object rather than a hapi response. What is emitted depends on whether the deferred had already been resolved earlier in the request.; the legacy chain is gone from `downloadMain`, and this category declares no target shape -- what the builder emitted is not inferable from the text, so the outcome is settled by measurement alone | Reproduce the status, content-type and body this branch measures, as recorded for its scenario by test/parity/capture.js. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.3; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.builder-returned.download-main` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the outcome this row claims is a recorded comparison rather than an expectation. |
| [x] | `lib/controllers/trinket.js:1246` `downloadFile` (legacy chain gone; no declared target shape to locate) | reply chain (builder-returned) | return reply(file.content).type(type) -- builder object returned to hapi.; the legacy chain is gone from `downloadFile`, and this category declares no target shape -- what the builder emitted is not inferable from the text, so the outcome is settled by measurement alone | Reproduce the status, content-type and body this branch measures, as recorded for its scenario by test/parity/capture.js. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.3; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.builder-returned.download-code-file` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the outcome this row claims is a recorded comparison rather than an expectation. |
| [x] | `lib/controllers/trinket.js:1259` `downloadFile` (legacy chain gone; no declared target shape to locate) | reply chain (builder-returned) | return reply(stream).type(type) inside a .then() -- builder object returned to hapi, wrapping a stream.; the legacy chain is gone from `downloadFile`, and this category declares no target shape -- what the builder emitted is not inferable from the text, so the outcome is settled by measurement alone | Reproduce the status, content-type and body this branch measures, as recorded for its scenario by test/parity/capture.js. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.3; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.builder-returned.download-asset` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the outcome this row claims is a recorded comparison rather than an expectation. |

### A distinct shape, not one of the eight chains

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/trinket.js:375` `create` (baseline coordinate; the legacy construct is no longer in this tree) | reply call, no return | reply(err); with NO `return`, inside .catch(function(err) { ... }) on an error path. The shim resolved a Boom for an Error and the handler fell off the end returning undefined, so the response came from the deferred.; the unreturned bare `reply(` is gone from `create` | Return the mapped error so the same status and payload reach the same error funnel. This is a DISTINCT shape, not one of the eight reply chains. Baseline outcome owned by `docs/preserved-quirks.md` §4.4; reproduce it, do not fix it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **no linked scenario**. Nothing in `test/parity/corpus.json` drives this site, so the status and payload this row claims reach the same funnel rests on nothing measurable. The row cannot close until a scenario covers it. |

## 7. Stream sites -- 18 rows across 4 controllers (14 closed)

Derived, then reviewed -- see "Streams" above for the rule and for what it
deliberately excludes. Several of these error **after the response has begun**,
which is why every row carries the same non-negotiable target: completion and error
**timing** are preserved.

**Each row names the scenario that drives it, and a row with none cannot close.**
Whether timing survived is not a property of the text: no reading of a
`.pipe(...)` tells you whether the response still begins at the same point or
whether an error raised after it began is still swallowed. Only a driven request
does. The join is mechanical rather than asserted -- the site resolves to the
innermost symbol that contains it, that symbol resolves to the routes it answers
(through the dispatch table where the carrier is a module-scope function, with the
hops named in the row), and the routes resolve to the corpus scenarios whose
`covers` list them.

In this tree 18 of 18 stream rows resolve to at least one scenario.

### `lib/controllers/trinket.js` -- 11 sites

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/trinket.js:179` `readZipCodeWithinBounds` (module-scope function, reached via `draft` -> `autosave`) -- serves `POST /api/trinkets/{trinketId}/autosave`, `POST /api/trinkets/{trinketId}/draft` | stream site | binds a stream -- `stream = entry.internalStream('string');` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **route-level only**. `route.post.api-trinkets-trinketId-autosave.json`, `route.post.api-trinkets-trinketId-draft.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [x] | `lib/controllers/trinket.js:1837` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | constructs an archive -- `var archive = archiver('zip', {` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.posted-zip-download` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site is outside the located chain's span, but `downloadPostedZip` DECLARES exactly one reply chain, so there is no other branch in it for the site to be on (`downloadPostedZip 2041-2044`). |
| [x] | `lib/controllers/trinket.js:1854` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | binds a stream, creates a stream -- `, outputWriteStream = fs.createWriteStream(zipFile)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.posted-zip-download` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site is outside the located chain's span, but `downloadPostedZip` DECLARES exactly one reply chain, so there is no other branch in it for the site to be on (`downloadPostedZip 2041-2044`). |
| [x] | `lib/controllers/trinket.js:1944` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | pipes a stream -- `archive.pipe(outputWriteStream);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.posted-zip-download` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site is outside the located chain's span, but `downloadPostedZip` DECLARES exactly one reply chain, so there is no other branch in it for the site to be on (`downloadPostedZip 2041-2044`). |
| [x] | `lib/controllers/trinket.js:2025` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | binds a stream, creates a stream -- `outputReadStream = fs.createReadStream(zipFile);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.posted-zip-download` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site is outside the located chain's span, but `downloadPostedZip` DECLARES exactly one reply chain, so there is no other branch in it for the site to be on (`downloadPostedZip 2041-2044`). |
| [x] | `lib/controllers/trinket.js:2041` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | hands a stream to the response -- `return h.response(outputReadStream)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.posted-zip-download` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site sits inside the located chain (`downloadPostedZip 2041-2044`). |
| [x] | `lib/controllers/trinket.js:2415` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | constructs an archive -- `var archive = archiver('zip', {` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.short-code-zip` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site is outside the located chain's span, but `downloadZip` DECLARES exactly one reply chain, so there is no other branch in it for the site to be on (`downloadZip 2605-2608`). |
| [x] | `lib/controllers/trinket.js:2425` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | binds a stream, creates a stream -- `, outputWriteStream = fs.createWriteStream(zipFile)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.short-code-zip` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site is outside the located chain's span, but `downloadZip` DECLARES exactly one reply chain, so there is no other branch in it for the site to be on (`downloadZip 2605-2608`). |
| [x] | `lib/controllers/trinket.js:2521` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | pipes a stream -- `archive.pipe(outputWriteStream);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.short-code-zip` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site is outside the located chain's span, but `downloadZip` DECLARES exactly one reply chain, so there is no other branch in it for the site to be on (`downloadZip 2605-2608`). |
| [x] | `lib/controllers/trinket.js:2589` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | binds a stream, creates a stream -- `outputReadStream = fs.createReadStream(zipFile);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.short-code-zip` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site is outside the located chain's span, but `downloadZip` DECLARES exactly one reply chain, so there is no other branch in it for the site to be on (`downloadZip 2605-2608`). |
| [x] | `lib/controllers/trinket.js:2605` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | hands a stream to the response -- `return h.response(outputReadStream)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.short-code-zip` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site sits inside the located chain (`downloadZip 2605-2608`). |

### `lib/controllers/users.js` -- 2 sites

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/users.js:1180` `assetUploadFromURL` -- serves `POST /api/users/assetFromURL` | stream site | binds a stream, creates a stream -- `var writeStream = fs.createWriteStream(tmpPath);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. PRESERVED QUIRK -- a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. EVIDENCE: **route-level only**. `error-edge.asset-from-url.midstream-failure`, `error-edge.asset-from-url.query-bearing-url`, `error-edge.asset-from-url.transport-refused`, `route.post.api-users-assetFromURL.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [ ] | `lib/controllers/users.js:1315` `assetUploadFromURL` -- serves `POST /api/users/assetFromURL` | stream site | pipes a stream -- `body.pipe(writeStream);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. PRESERVED QUIRK -- a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. EVIDENCE: **route-level only**. `error-edge.asset-from-url.midstream-failure`, `error-edge.asset-from-url.query-bearing-url`, `error-edge.asset-from-url.transport-refused`, `route.post.api-users-assetFromURL.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |

### `lib/controllers/courses.js` -- 2 sites

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/courses.js:553` `returnZip` (module-scope function, reached via `download`) -- serves `GET /{userSlug}/courses/{courseSlug}/download.zip` | stream site | binds a stream, creates a stream -- `var stream = fs.createReadStream(zipFile);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. PRESERVED QUIRK -- the unauthorized branch evaluates an UNBOUND `Boom`, so it throws a `ReferenceError` before any response is constructed and the route answers 500 where the expression names 403. Owned by `docs/preserved-quirks.md` §9.7; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.course-download-zip` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site is outside the located chain's span, but `returnZip` DECLARES exactly one reply chain, so there is no other branch in it for the site to be on (`returnZip 607-610`). |
| [x] | `lib/controllers/courses.js:607` `returnZip` (module-scope function, reached via `download`) -- serves `GET /{userSlug}/courses/{courseSlug}/download.zip` | stream site | hands a stream to the response -- `return h.response(stream)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. PRESERVED QUIRK -- the unauthorized branch evaluates an UNBOUND `Boom`, so it throws a `ReferenceError` before any response is constructed and the route answers 500 where the expression names 403. Owned by `docs/preserved-quirks.md` §9.7; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.course-download-zip` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site sits inside the located chain (`returnZip 607-610`). |

### `lib/controllers/files.js` -- 3 sites

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/files.js:535` `download` -- serves `GET /api/files/{fileId}/{fileName}` | stream site | binds a stream -- `stream = FileUtil.downloadMaterialFile(remoteFile, resolve);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.7. EVIDENCE: **route-level only**. `quirk.reply-chain.header-resolved.file-download-attachment`, `quirk.reply-chain.never-settles.image-download`, `route.get.api-files-fileId-fileName.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [x] | `lib/controllers/files.js:632` `download` -- serves `GET /api/files/{fileId}/{fileName}` | stream site | hands a stream to the response -- `return h.response(stream)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.7. EVIDENCE: `quirk.reply-chain.never-settles.image-download` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site sits inside the located chain (`download 632-636`). |
| [x] | `lib/controllers/files.js:641` `download` -- serves `GET /api/files/{fileId}/{fileName}` | stream site | hands a stream to the response -- `return h.response(stream)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.7. EVIDENCE: `quirk.reply-chain.header-resolved.file-download-attachment` in `test/parity/corpus.json` carries a captured baseline response AND a confirming replay verdict against this tree, so the preserved timing this row claims is a recorded comparison rather than an expectation. PIN: the site sits inside the located chain (`download 641-644`). |

A stream row closes on measured behaviour of ITS OWN BRANCH or not at all. Two
things have to hold. First a scenario must be pinned to this branch, and the pin is
derived rather than tabulated -- from the reply chains this document already
anchors structurally, not from the routes the carrier is reached from. That
distinction is load-bearing: `files.download` reaches three stream sites across two
mutually exclusive branches, so a result for the inline-image branch is silent
about the attachment branch, and spreading one route-level scenario across both
would tick a row nothing exercised.

Two derivations produce a pin, and each row says which one it used. A site INSIDE a
located chain is on that chain's branch. A site outside every located chain is on
the branch of the one reply chain its carrier declares -- when its carrier declares
exactly one, because then there is no other branch in that carrier for the site to
be on. `downloadZip` and `downloadPostedZip` build the archive and the read stream
BEFORE the response expression, which is why the containment test alone left four
sites in each of them resting on nothing. The branch count is the number of chains
the carrier DECLARES rather than the number this run located, which is what
refuses the pin for `files.download`: only one of its two branches is locatable in
a converted tree, and counting located chains would have found "exactly one" and
pinned a site to a branch it may not be on.

Second, the pinned scenario must carry a captured baseline AND a confirming replay
verdict. Until both hold the row stays open with the reason stated in it, which is
the difference between an open checklist item and an unsupported claim. A site that
cannot be pinned at all reports its route-level scenarios as candidates, or says
that it has none.

## 8. EXCLUDED -- defined-but-unrouted controller exports (2)

**Not counted in the 154.** These are exported handlers
that no route references, so under blocking-only scope they are not hapi-facing
work. They convert only if another gate independently forces it, and that
determination is **recorded per function below rather than assumed**. Two gates can
force one: the zero-deprecation-warning bar, and the repaired test suite.

**The `Done` box means something different in this section and the next**: it is
ticked when the recorded determination is that no gate forces this function into
scope, and left open when one does. It is not a conversion claim -- an unrouted
function in its legacy shape is finished work under blocking-only scope.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/admin.js:151` `uploadForm` | unrouted export (excluded) | legacy `(request, reply)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws; no route declaration references `admin.uploadForm` | **DO NOT CONVERT -- no gate forces it under blocking-only scope.** deprecation bar: no deprecated Node-core API in the body; repaired suite: no reference from test/lib or test/helpers. Leave as it is; R-a confines the diff to conversion work that something requires. |
| [x] | `lib/controllers/pages.js:70` `features` | unrouted export (excluded) | legacy `(request, reply)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws; no route declaration references `pages.features` | **DO NOT CONVERT -- no gate forces it under blocking-only scope.** deprecation bar: no deprecated Node-core API in the body; repaired suite: no reference from test/lib or test/helpers. Leave as it is; R-a confines the diff to conversion work that something requires. |

## 9. EXCLUDED -- unrouted named pre-handlers (3)

**Not counted in the 154.** Same conditional basis as the
section above: no route references them, so nothing forces their conversion unless
a gate does. Recorded, not assumed.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/util/helpers.js:691` `toLowerCaseURI` | unrouted pre-handler (excluded) | legacy `(request, reply)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws; still consumes the shim `reply`; no route declaration references `helpers.toLowerCaseURI` | **DO NOT CONVERT -- no gate forces it under blocking-only scope.** deprecation bar: no deprecated Node-core API in the body; repaired suite: no reference from test/lib or test/helpers. Leave as it is. |
| [x] | `lib/util/helpers.js:714` `logUnauth` | unrouted pre-handler (excluded) | legacy `(request, reply)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws; still consumes the shim `reply`; no route declaration references `helpers.logUnauth` | **DO NOT CONVERT -- no gate forces it under blocking-only scope.** deprecation bar: no deprecated Node-core API in the body; repaired suite: no reference from test/lib or test/helpers. Leave as it is. |
| [x] | `lib/util/helpers.js:808` `trinketByOwnerAndSlug` | unrouted pre-handler (excluded) | legacy `(request, reply)`; RELIES ON THE INTERCEPTION -- 2 of 3 signalling calls produce a value that never leaves the function (0 unreturned in the body, 2 dropped inside a nested function), and at least one path delivers nothing, which is where the interception fires; every path through the body delivers a value or throws; still consumes the shim `reply`; no route declaration references `helpers.trinketByOwnerAndSlug` | **DO NOT CONVERT -- no gate forces it under blocking-only scope.** deprecation bar: no deprecated Node-core API in the body; repaired suite: no reference from test/lib or test/helpers. Leave as it is. |

One of these -- `trinketByOwnerAndSlug` -- carries a `reply().redirect(...)
.permanent().takeover()` chain of the same shape as the two dead 301s in section 2,
and `toLowerCaseURI` carries a fourth `.permanent()` chain of a different shape
(`reply('')` settles the deferred immediately and there is no `.takeover()`).
Neither is in the conversion set, because neither is routed. They are named here so
that a file-wide search for `.permanent()` in `lib/util/helpers.js` -- which finds
four hits, not two -- does not read as unfinished work.

## 10. EXCLUDED -- routes whose named controller method does not exist (3)

**Not counted in the 154, and there is nothing to convert:
these are routes with no function.** They are recorded so that nobody hunts for a
handler that was never written.

All three answer entirely through the wrapper's missing-controller fallback at
`lib/util/routeParser.js:574-576`, which returns `request.success(request.params)`.
**That fallback must be PRESERVED.** It sits immediately below the interception
block being removed and is easy to delete by association -- which would turn all
three of these routes into failures.

| Done | Route | Named binding | Declared at | Behaviour to preserve |
| --- | --- | --- | --- | --- |
| [x] | `POST /api/interest` | `pages.interest` -- **not defined** | `config/api_routes.js:1204` | Resolves through the preserved fallback at `lib/util/routeParser.js:574-576`, returning `request.success(request.params)`. Same status, same payload, same template resolution as today. Baseline outcome owned by `docs/preserved-quirks.md` §1. |
| [x] | `GET /api/trinkets/popular` | `trinket.mostActive` -- **not defined** | `config/api_routes.js:805` | Resolves through the preserved fallback at `lib/util/routeParser.js:574-576`, returning `request.success(request.params)`. Same status, same payload, same template resolution as today. Baseline outcome owned by `docs/preserved-quirks.md` §1. |
| [x] | `GET /api/trinkets/active` | `trinket.risingActive` -- **not defined** | `config/api_routes.js:820` | Resolves through the preserved fallback at `lib/util/routeParser.js:574-576`, returning `request.success(request.params)`. Same status, same payload, same template resolution as today. Baseline outcome owned by `docs/preserved-quirks.md` §1. |

The `Done` box here means "the route declaration is present and the fallback is
still what serves it" -- these rows are a **preservation** check, not conversion
work.

## Retained by design -- never a removal row

R-a confines the diff to four things: the runtime bump, the hapi API migration, the
async conversion, and blocking-only dependency swaps. The following are explicitly
**retained**, and if any of them ever shows up in this document as work to be done,
the generator is wrong -- not the code.

| Retained | Why it must not be touched |
| --- | --- |
| Per-request debug logging at `lib/util/routeParser.js:311`, `:543-552`, `:556` | Explicitly retained. Performance is not a goal of this migration, so there is no licence to remove logging along the way. Recorded in `docs/preserved-quirks.md` §9.3. |
| The missing-controller fallback at `lib/util/routeParser.js:574-576` | Three registered routes answer through it (section 10). It is adjacent to the interception block being removed, which is the whole risk. |
| `request.success` (`:412-479`), `request.fail` (`:482-514`) and the hand-rolled validation block (`:516-541`) | Reshaped to *return* their response, but their projection, flash-and-redirect behaviour and validation outcomes are invariant. |
| The handler catch-all at `lib/util/routeParser.js:578-589`, including its `if (err)` guard | A falsy throw produces no return and the toolkit converts `undefined` to the same status with a different message. That is observable, so the guard is copied verbatim. |
| The route DSL, the `route.config` -> `route.options` rename, the forced `cors = false` and `delete route.options.validate` | The route surface is an invariant of this migration. |
| The cross-request `fail.redirect` state leak | A measured baseline defect with a documented blast radius. R-d preserves it; two consecutive corpus requests exist so it cannot be silently fixed. Recorded in `docs/preserved-quirks.md` §3. |
| The two validation message maps in `config/routes.js` | Measured inert on both joi 17.13.3 and 18.2.5. Inert is the baseline behaviour, so inert is the target. Recorded in `docs/preserved-quirks.md` §9.1. |

No row in this document proposes a rename, a cleanup, or a "while we're here".
Every target disposition is the **preserved** behaviour, with exactly one approved
exception among these rows, labelled as such in section 6 and argued in
`docs/preserved-quirks.md` §11.1. The migration approves
**two** deviations; the second (`docs/preserved-quirks.md` §11.2)
is an audit result and reaches no row in this document.
