<!-- provenance-json: {"analysedTree":{"head":"9aa3d396954e0aa2c284410d9852e4940304f0ef","headShort":"9aa3d39","isBaselineCommit":false,"subject":"Regenerate the error-edge inventory from the delivered tree","worktreeState":"dirty"},"artifact":"conversion-inventory.md","baselineCommit":"2f8712a112db46f923918c4507c75abc732d83d0","bodyDigest":{"algorithm":"sha256","canonicalization":"the document with its provenance-json line removed and trailing whitespace stripped per line, hashed as utf8","value":"959519144745b43632489bcef0de238262bb3774b2ef63e55903f9602483040f"},"delivered":{"head":"9aa3d396954e0aa2c284410d9852e4940304f0ef","headShort":"9aa3d39","isBaselineCommit":false,"subject":"Regenerate the error-edge inventory from the delivered tree","worktreeState":"dirty"},"detail":{"analysedFiles":13,"analysedSourceDigest":"sha256:47e250a8d09e8c62e0607ae151a1377d9fa702294de7a2cf2cad1ec496f50ba5","analysedTreeLabel":"the repository containing the generator (no --app given)","artifactLabel":"tool:docs/conversion-inventory.md","atBaseline":false,"evidenceAvailable":true,"evidenceCaptured":true,"invocation":"node test/parity/convert-inventory.js --out docs/conversion-inventory.md"},"generator":{"blob":"b0fd907735c672ecd57c26d5a4d7a0ff6e769bde","commit":"9aa3d396954e0aa2c284410d9852e4940304f0ef","commitState":"contains-this-exact-source","deliveredHead":"9aa3d396954e0aa2c284410d9852e4940304f0ef","deliveredHeadShort":"9aa3d39","matchesDeliveredHead":true,"path":"test/parity/convert-inventory.js","verified":true},"provenanceSchema":2,"role":"analysis","runtime":{"node":"v22.23.2","platform":"linux"}} -->
<!--
  GENERATED FILE -- do not hand-edit it. Every line below this block is written
  by the generator named here from the analysed tree named here. An edit made by
  hand is lost on the next run and, while it survives, is indistinguishable from a
  measurement. To change what this document says, change the generator or the
  tree and re-run the exact command.

  generator            : test/parity/convert-inventory.js
  generator sha256     : sha256:5dbdd7987bc8df1faa71e2676c239342959f19f875f18b0d74271b105ed9bfc2
  generator blob       : b0fd907735c672ecd57c26d5a4d7a0ff6e769bde
                         `git cat-file blob b0fd907735c6` retrieves the exact source that ran
  generator commit     : 9aa3d396954e0aa2c284410d9852e4940304f0ef  <-- verified: this commit's tree holds the blob above at that path
  exact command        : node test/parity/convert-inventory.js --out docs/conversion-inventory.md
  analysed tree        : the repository containing the generator (no --app given)
  analysed tree HEAD   : 9aa3d396954e0aa2c284410d9852e4940304f0ef
  analysed source      : 13 files, sha256:47e250a8d09e8c62e0607ae151a1377d9fa702294de7a2cf2cad1ec496f50ba5
  analysed source state: clean -- every analysed file matches that revision
  parity evidence      : test/parity/corpus.json, sha256:b4af89aaa532ec69c545aaf7efbae66a4066c1c9d96c910965512d4f68632ca1, captured: yes
  base commit          : 2f8712a  <-- the analysed tree is NOT the base commit, so the
                         baseline-calibrated self-checks are reported as deltas
  node                 : v22.23.2

  WHAT IDENTIFIES THIS DOCUMENT'S SUBJECT, and why there are four lines above
  where a single commit hash would seem to do:

    * "analysed tree HEAD" is the revision the analysed tree was checked out at.
    * "analysed source state" says whether that revision still describes the
      analysed BYTES, over the analysed files only -- this document is not one of
      them, so writing it cannot make its own provenance stale.
    * "analysed source" is a digest over exactly the files that were read, which
      is checkable from the files themselves with no repository at all. It is the
      claim that survives a rebase, a squash, or a clone that never received the
      commit.
    * "generator sha256" identifies the code that produced this, unconditionally.
      "generator commit" is recorded only when the generator file matches its
      last commit, and says so plainly when it does not, because a revision that
      is not an object in this repository cannot be retrieved by a reader.

  Re-run the exact command above to check this document against a tree; or run
  it with --check, which regenerates in memory, compares, and exits 3 if the
  committed document no longer describes the tree. --check normalizes the
  "analysed tree HEAD" and "generator commit" lines above -- and the
  `provenance-json` line, which carries both -- because each of them moves for
  reasons that have nothing to do with what was analysed: a revision that
  commit including the one that stores this document, and a commit identity whose
  content is already compared through "generator sha256". Everything it does
  compare is content-derived: every row, and the three digests.

  No absolute path appears anywhere in this document. A worktree's location on
  disk is specific to the machine it was generated on, so recording it would make
  two correct runs differ for a reason that says nothing about the tree.

  Everything below this block is deterministic: two runs over the same tree
  produce byte-identical output. Nothing in this document records a wall clock,
  a process id, a port or a path, so there is nothing left to differ.
-->

# Conversion inventory

One row per site an implementing agent must close to move this application from
the 2013 callback idiom to the hapi lifecycle contract. **There is no target row
count.** Rows are derived from the tree; nothing is padded to reach a number.

## Which tree this describes, and what that makes it

Every row below was computed from the tree named in the header block, and the
document is only ever a statement about that tree. The distinction is not
pedantic: the same generator over the base commit and over the delivered tree
produces two documents that look alike and mean opposite things.

**This run analysed the delivered tree, not the base commit, so this is the
COMPLETION view: what is actually closed and what is not.** A row is open here
because the site in the delivered code has not reached its target -- or, for the
two row kinds whose correctness is behavioural rather than textual, because the
parity evidence that would settle it has not been captured. Re-running the exact
command in the header against a `git worktree` at `2f8712a` reproduces the
planning view, which is how the two are compared.

Both views are the same generator over different inputs, and `--check` proves the
committed document still matches the tree it claims. Nothing here is transcribed
by hand, and a hand edit would be lost on the next run.

## Why a signature count is not enough

In hapi 17 and later every lifecycle method must return a value, return a promise,
or throw. `undefined` is converted into a server error by the toolkit. So the
requirement that makes a conversion *correct* is that **each handler returns exactly
once on every path** -- and neither a returned-but-unawaited chain nor an
awaited-but-unreturned one is visible in a signature count. Both are
`async (request, h)`. Both satisfy any grep for the new signature. Only one works.

The same reasoning rules out the two counts that look like adequate substitutes:

- **A count of unreturned signalling calls.** An EMPTY `async (request, h)` has
  none -- no legacy signature, no stray `reply(`, nothing unreturned -- and returns
  `undefined` to a toolkit that converts it into `Boom.badImplementation`. So every
  handler and pre-handler row also carries a **proof of exit**: whether every path
  through the body ends in `return` or `throw`. A body that cannot be shown to
  leave stays open, and the row says which half is missing.
- **A count of legacy calls that disappeared.** For a reply chain or a stream site,
  the legacy syntax vanishing proves only that the syntax vanished. What a client
  received from a reply chain depended on which builder method ran last, and
  whether a stream still errors after the response began is a timing question. Both
  are settled by a driven request, so those rows name the corpus scenario that
  drives them and close on captured evidence rather than on absence.

That is the entire reason this document exists.

## Why the work is safely orderable

The mechanism being removed is the response emulation in the route wrapper:

```js
// lib/util/routeParser.js:567-570  (baseline coordinates)
if (result === undefined) {
    result = await responsePromise;
}
```

It intercepts **only** an `undefined` result and passes any defined result straight
through, so a handler converted to return its response **already works under the
shim**. Converted and unconverted handlers coexist, which is what lets these rows be
closed one at a time and re-verified as they go.

Immediately **below** that block, `lib/util/routeParser.js:574-576` is a *different*
mechanism -- the `else` branch returning `request.success(request.params)` when a
route names a controller method that does not exist. Three registered routes depend
on it. **The emulation goes; the fallback stays.** It is easy to delete by
association, which is why those three routes have their own section below.

## How to read a row

| Column | Meaning |
| --- | --- |
| Done | `[x]` when this run measured the site to have reached its target, `[ ]` otherwise, with the reason in the row. Recomputed on every run: re-generating against a tree is how progress is demonstrated, and no box is ever set by hand. |
| Site | The carrier SYMBOL the row is anchored on, plus its coordinate in the analysed tree. Where another document cites a baseline coordinate, that coordinate is shown too, because a line number stops being an address once a file is edited. |
| Kind | One of: routed handler, routed pre-handler, inline pre-handler, promise chain, callback boundary, reply chain, stream site. |
| Current shape | What the code does **now**, measured -- not what it looks like. |
| Target disposition | The exact converted shape. Under R-d this is always the *preserved* behaviour, with one approved exception among these rows, labelled as such -- the migration approves two deviations in total and only one of them reaches a row here. |

### What closes a row, by kind

| Kind | Closes when |
| --- | --- |
| Routed handler, routed pre-handler, inline pre-handler | The signature is `(request, h)`; **every** signalling call's value reaches hapi; no `reply(` reference remains; and **every path through the body DELIVERS a value or throws**. Exiting is not enough: a path ending in a bare `return`, a statically `undefined` return, or a non-settling loop reaches hapi as `undefined`, which the toolkit converts into `Boom.badImplementation`. |
| Promise chain | Its value leaves the enclosing function -- returned or awaited -- and its terminal link does not pass a bare function reference whose return value is dropped. |
| Callback boundary | It ceases to exist. Replacing the call with an `await` removes the callback literal and the row with it, so this section shrinks rather than ticks; the box is set only for a call site that already carries an `await`. |
| Reply chain, dead-301 branch site, unreturned bare `reply(` | The legacy construct is gone from its carrier, the declared target shape (where one can be declared) is present in it, **and** the corpus scenario that drives the site carries BOTH a captured baseline response AND a confirming replay verdict against this tree. A baseline alone records what the pre-migration code did, which is one half of a comparison. |
| Stream site | A scenario is pinned to **this branch** -- derived from the structurally located reply chain that contains the site, not from the routes its carrier is reached from -- and that scenario carries a captured baseline plus a confirming replay. Whether completion and error timing survived is not visible in the text at all, and a sibling branch's result is silent about this one. |

The `Current shape` column for handlers and pre-handlers reports two measurements,
and both are needed because each misses what the other catches.

The first is **delivery**: whether each signalling call's value reaches hapi. That
is not the same as a `return` in front of the call. A response is also delivered
when it settles the promise the method returns (`resolve(request.fail(err))` inside
a returned `new Promise`), when it is returned from a nested handler of a chain the
method returns, or when it is captured into a name a returning branch reads
(`var response = request.success(); ... return response;`). All three are counted as
delivery, and the row says which mechanism carried each call -- because counting
them as "unreturned" flagged 17 correctly converted handlers in an earlier run of
this generator, and a false flag sends a reader to fix code that is already right.

The second is the **proof of delivery** described above -- which is asked
separately from the proof of exit, because the two come apart. `while (true) {}`
exits on every path and delivers nothing; `return;` does the same. Both are
reported distinctly from "no exit", since they need different fixes. It is
deliberately
conservative: a construct the analysis cannot show to exit is reported as not
exiting. The direction is chosen on cost -- a false "open" costs one look, a false
"closed" ships a 500 -- and a site that deliberately reproduces a baseline
non-settlement is expected to be recorded as such in `docs/preserved-quirks.md`
rather than silently accepted here.

This remains an analysis of TEXT, and it says so. It does not execute the
application, and for the two behavioural row kinds it does not pretend to: those
rows carry a scenario identifier and defer to it.

## What this document is NOT

Saying so matters, because a checklist that looks complete is read as complete.

- It is **not the error-edge inventory**. Every target disposition here preserves the
  error mapping of the site it describes -- a swallowed error stays swallowed, a
  fire-and-forget stays fire-and-forget, a mapped error reaches the same funnel -- but
  the per-branch enumeration that R-e actually calls for lives in
  `docs/error-edge-inventory.md`, generated by `test/parity/error-edges.js`. Closing
  every row below does not discharge R-e.
- It is **not itself evidence of parity**, and it does not close a behavioural row
  on its own authority. Whether a converted site behaves identically is decided by
  the request corpus (`test/parity/capture.js` and `replay.js`), the storage cases
  and the joi matrix. What this document does is name, per row, the scenario that
  settles it and report that scenario's state as read from
  `test/parity/corpus.json`: recorded, defined-but-not-yet-captured, or absent. A row
  whose evidence is pending stays open and says so; a row with no scenario at all
  says that too, because an unlinked site is a gap in the corpus rather than a
  finished piece of work.
- It is **not a route inventory**. The 233-route surface is gated by
  `test/parity/manifest.js`. This document counts hapi-invoked *functions*, of which
  there are 154 -- a different number for a different purpose.

## Where the rest of the story lives

A row here records the **return shape** of one site and stops. Two sibling
documents own the rest about the same sites, and rows point at them by section
number rather than repeating them -- two copies of one fact is how the two drift
apart.

| Document | What it owns about a site in this checklist |
| --- | --- |
| `docs/preserved-quirks.md` | The measured baseline **outcome** of a quirk, and the precedence argument in full for each of the migration's **two** approved deviations (§11, a closed register). The two have different roles and this table does not collapse them: **deviation 1** (§11.1), the never-settling image-download branch that the target serves, is a **response** deviation and the only one a row in this checklist can be affected by -- it is the one row whose target changes observable behaviour; **deviation 2** (§11.2, reasoned in full in `docs/deferred-dependencies.md` §4.2), the retained `marked` fork and its one named high advisory, is an **audit** deviation that no conversion row touches, because retaining the fork is what keeps rendered output identical. A row whose target reproduces a defect says so and cites the section. |
| `docs/error-edge-inventory.md` | The **status, payload, side effects and timing** of every changed error edge. Rows that are themselves error edges -- a chain carrying a `.catch(` link, an error-first callback, an unreturned `reply(err)` -- cite the per-file section that owns them. |

Neither reference is decorative. R-d requires that a preserved defect be recorded
rather than fixed, and R-e requires that the error mapping survive unchanged; this
document would contradict both if it restated their content in its own words.

**The quirk reference is resolved BEFORE a row's target action is composed, not
appended after it.** `docs/preserved-quirks.md` Appendix A is the allow-list, and for a
site whose preserved outcome requires the body *not* to return -- to be left
unsettled, or to throw -- a generic mandate and a quirk pointer in one cell say
opposite things with the mandate leading. So a row on that allow-list carries a
**governing action** instead: it states what to do, names the generic mandate it
**replaces** or **qualifies**, says why that mandate is wrong there, and still cites
the section that owns the measured outcome. Rows where the mandate is compatible are
unchanged, and one allow-list entry is the inverse case -- at
`lib/controllers/trinket.js:375` the statement genuinely must change to preserve the
outcome, which is why this is an allow-list and not a blanket "change nothing".

## The four recurring shapes, and streams

Four shapes recur across the conversion set, each with its own distinct failure
mode, plus stream sites which cut across all of them.

### 1. Promise chains

Their value is **discarded by the wrapper today**. After conversion each must be
**returned or awaited exactly once per path**. Measured scale across the ten
controllers: **183** `.then(` and **85** `.catch(` links at `2f8712a`.

The canonical trap is `lib/controllers/pages.js:52` -- the AAP's own citation for
the chain in `pages.home`, which ends `.then(...).catch(request.fail)`. The terminal
link passes a **bare function reference**, so after conversion `request.fail`'s
return value must **propagate** out of the handler rather than being dropped on the
floor. A chain that is awaited but not returned, or returned but not awaited, looks
identical in a signature scan.

Rows are emitted **per chain**, not per link: "returned or awaited exactly once" is a
property of the chain, and closing one means fixing all of its links together.

### 2. Callback boundaries

The `await` is created **at the call site**, per rule T-3 -- never pushed down into
the utility. `lib/util/file.js`, `lib/util/store.js` and `lib/util/queues.js` keep
their callback interfaces even though controllers call them directly (three, three
and one controller respectively), because they are **not lifecycle methods**. Each
row records where the `await` goes.

A **promise constructor is not a boundary**, and the exclusion is tested on the
callee text rather than on the token before it: `return new Promise(function () {})`
at baseline `lib/controllers/trinket.js:876` was classified as an empty-parameter
completion callback because the walk that finds the callee absorbs the `new`
keyword into it, so a test on the preceding token never fired. `new Promise` is a
promise being constructed, whose settlement is the point; the boundary this section
tracks is a call that takes a completion callback.

### 3. Reply chains -- exactly eight, and they are not uniform

In the shim's response builder (`lib/util/routeParser.js:375-405`) `.type()` and
`.bytes()` return the builder **without** resolving the deferred, while `.code()`,
`.header()`, `.redirect()` and `.view()` resolve it. So **what a client receives
depends on which chain method ran last** -- and the 13 `.type()`/`.bytes()` calls
spread across 8 chains produce three different outcomes. This is the largest source
of preserved-quirk work in the migration.

### 4. Streams

**17** sites across four controllers, several of which
error **after the response has begun**. Every row records that completion and error
**timing** must be preserved.

These are **derived, not grepped**. A crude pattern returns 10. The rule is: a stream
site is a source line that creates a stream (`createReadStream` / `createWriteStream`),
pipes one (`.pipe(`), constructs an archive (`archiver(`), hands one to the response
(`reply(<stream>)` / `h.response(<stream>)`), or binds a call result to a
stream-named identifier -- **deduped by line**, because
`.pipe(fs.createWriteStream(p))` is two operations at one place a reader has to look.
Lifecycle listeners (`.on('close')`, `.on('error')`) are attached *at* a site already
counted, and a `.then(function (stream))` parameter merely names one. Excluding those
two is what lands the rule on 17 rather than a larger number.

### Target idiom, anchored on code already in the tree

The conversion has references to work from, not inventions. Coordinates are baseline
(`2f8712a`); a converted tree will have moved some of them.

- **`lib/util/helpers.js:397-403`** -- The target PRE-HANDLER shape, already present: `async function (request, h) { ...; return await internals.namedTrinketList(lang, 'featured'); }`
- **`app.js:116-137`** -- A server extension returning `h.continue` -- the shape for a lifecycle method that has nothing to contribute but must still return.
- **`app.js:152-201`** -- The onPreResponse error mapper. Branch ORDER is load-bearing: it returns immediately on 401/404/403/>=500 for browser HTML requests, BEFORE the cache and frame header assignments, so those headers reach API/JSON and non-Boom responses only.
- **`app.js:243-281`** -- The auth scheme, with all five outcomes -- absent userId, missing user, disabled user, valid user, lookup error. The modern `h.unauthenticated(...)` / `h.authenticated(...)` idiom.
- **`lib/controllers/*.js`** -- Controllers converge on `async function (request, h) { ...; return request.success(data); }`.

In the analysed tree the pre-handler exemplar `findFeaturedTrinkets` sits at `lib/util/helpers.js:143`.

## The conversion set

Derived from the **binding graph**, not from the export list. The two disagree in
both directions, which is exactly why the export list is the wrong source: three
routes name a controller method that does not exist, and two exported handlers are
never routed.

```text
  145  routed handlers        (147 controller exports minus the 2 that no route references)
    8  routed pre-handlers    (of 11 named pre-handlers in lib/util/helpers.js)
    1  inline pre-handler     (config/api_routes.js:1104, on POST /api/users/login)
  ---
  154  hapi-invoked functions to convert
```

`145 + 8 + 1 = 154`, and the
arithmetic is asserted by the self-check rather than asserted by this sentence.

Measured in the analysed tree: 178 route declarations produce 148 distinct controller bindings, of which 145 resolve to a defined export and 3 do not. 5 declarations build their route string by
concatenation and would be missed entirely by a scan over string literals alone.

Three groups are **excluded from the 154** and have their
own sections: the 2 defined-but-unrouted controller exports, the 3 unrouted named
pre-handlers, and the 3 routes whose named controller method does not exist.

## Self-check

This generator refuses to emit a quietly incomplete checklist. A checklist that
has silently dropped sites is worse than no checklist at all, because it reads as
completed work. Three tiers of check therefore run before anything is written, and
a failure in any of them exits non-zero with no document produced.

| Tier | What it asserts | Result |
| --- | --- | --- |
| 1 -- invariants | Handler census (147 exports and its per-file distribution), route declarations (178), binding graph (145 routed / 3 nonexistent / 2 unrouted), pre-handler census (11), tokenizer length and delimiter balance per file. Must hold on **any** tree, converted or not. | pass |
| 2 -- baseline-calibrated | Progress metrics: 202 `reply(` sites and their distribution, 183 `.then(`, 85 `.catch(`, 13 `.type()`/`.bytes()`, 17 stream sites, the 2 rejected non-reaction `.catch(` calls, and the eight-entry reply-chain roster -- each entry located by its **carrier symbol**, at the baseline line it records, in the category the analysis derives independently. Asserted **exactly** at `2f8712a`; reported as a delta elsewhere. | reported (tree is not at `2f8712a`) |
| 3 -- directional | Off baseline, `reply(` sites may fall but must never rise above 202, and the handler census may not move. | pass |

Why tier 2 is conditional rather than absolute: a converted tree has fewer `reply(`
sites **by design**. Asserting the baseline figure unconditionally would make this
tool fail on exactly the tree whose progress it exists to demonstrate. The check
that actually catches a mis-tokenized file is the tier-1 handler census, because a
desynchronized scrub loses exports and the census collapses immediately.

### Measured against baseline

| Metric | Baseline (`2f8712a`) | This tree | Kind |
| --- | --- | --- | --- |
| Controller handler exports | 147 | 147 | invariant |
| Route declarations | 178 | 178 | invariant |
| Distinct controller bindings | 148 | 148 | invariant |
| Routed handlers | 145 | 145 | invariant |
| `reply(` call sites | 202 | 6 | progress |
| `.then(` links | 183 | 191 | progress |
| `.catch(` links | 85 | 90 | progress |
| `.type()` / `.bytes()` calls | 13 | 12 | progress |
| Derived stream sites | 17 | 17 | derived |
| Pre-handlers still `(request, reply)` | 11 | 3 | progress |
| Non-reaction `.catch(` member calls rejected | 2 | 2 | invariant (a preserved quirk) |
| Function rows emitted (the conversion set) | 154 | 154 | invariant |
| Handlers and pre-handlers with a proven exit on every path | not measured at `2f8712a` | 145 of 145 routed handlers, 8 of 8 routed pre-handlers | progress |
| Parity evidence for the 28 behavioural rows | n/a | 0 recorded (baseline + confirming replay), 14 baseline captured but not replayed, 8 route-level only (no branch-exact scenario), 6 with no scenario | evidence |

### Deltas from baseline

The analysed tree is not at `2f8712a`, so the following differences are reported rather than asserted. Each one should be explicable as conversion progress; anything that is not is a finding.

- .catch( links across the ten controllers: found 90, baseline 85.
- .then( links across the ten controllers: found 191, baseline 183.
- .type() / .bytes() calls across the ten controllers: found 12, baseline 13.
- Callback boundaries derived across the ten controllers: found 52, baseline 57.
- Legacy `reply(`-rooted chains carrying .type()/.bytes(): derived 0, roster has 8. At the base commit the two must agree; on a converted tree a lower figure is the expected direction, since each converted chain leaves the legacy population.
- Legacy `reply(`-rooted chains in category builder-returned: derived 0, roster has 3. Asserted at the base commit; on a converted tree the derived figure falls as chains convert.
- Legacy `reply(`-rooted chains in category header-resolved: derived 0, roster has 4. Asserted at the base commit; on a converted tree the derived figure falls as chains convert.
- Legacy `reply(`-rooted chains in category never-settles: derived 0, roster has 1. Asserted at the base commit; on a converted tree the derived figure falls as chains convert.
- Named pre-handlers still declared (request, reply): found 3, baseline 11.
- Open promise chains: found 34, baseline 37.
- Promise chains derived across the ten controllers: found 140, baseline 127.
- Reply chain lib/controllers/courses.js:269-272 (header-resolved) has no `reply(`-rooted chain with links .type().bytes().header() at ordinal 1 inside `returnZip`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/files.js:102-105 (header-resolved) has no `reply(`-rooted chain with links .type().bytes().header() at ordinal 1 inside `download`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/files.js:98-100 (never-settles) has no `reply(`-rooted chain with links .type().bytes() at ordinal 1 inside `download`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1204 (builder-returned) has no `reply(`-rooted chain with links .type() at ordinal 1 inside `downloadMain`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1246 (builder-returned) has no `reply(`-rooted chain with links .type() at ordinal 1 inside `downloadFile`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1259 (builder-returned) has no `reply(`-rooted chain with links .type() at ordinal 2 inside `downloadFile`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1383-1386 (header-resolved) has no `reply(`-rooted chain with links .type().bytes().header() at ordinal 1 inside `downloadPostedZip`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Reply chain lib/controllers/trinket.js:1548-1551 (header-resolved) has no `reply(`-rooted chain with links .type().bytes().header() at ordinal 1 inside `downloadZip`. At the base commit that is a fault -- a mis-specified anchor or a tokenizer fault. On a converted tree it is the expected state, and section 6 closes the row on its target shape and its corpus scenario instead.
- Stream sites in lib/controllers/trinket.js: found 10, baseline 11.
- Stream sites in lib/controllers/users.js: found 2, baseline 1.
- reply( call sites: found 6, baseline 202.
- reply( sites in config/api_routes.js: found 0, baseline 1.
- reply( sites in lib/controllers/admin.js: found 0, baseline 6.
- reply( sites in lib/controllers/classes.js: found 0, baseline 13.
- reply( sites in lib/controllers/course.js: found 0, baseline 63.
- reply( sites in lib/controllers/courses.js: found 0, baseline 4.
- reply( sites in lib/controllers/files.js: found 0, baseline 5.
- reply( sites in lib/controllers/folders.js: found 0, baseline 7.
- reply( sites in lib/controllers/pages.js: found 0, baseline 2.
- reply( sites in lib/controllers/trinket.js: found 0, baseline 35.
- reply( sites in lib/controllers/users.js: found 0, baseline 37.
- reply( sites in lib/util/helpers.js: found 6, baseline 29.

## Row totals

| Section | Rows | Closed | In the 154? |
| --- | --- | --- | --- |
| 1. Routed handlers | 145 | 144 | yes |
| 2a. Routed pre-handler functions | 8 | 8 | yes |
| 2b. Dead pre-handler 301 branch sites (inside 2a) | 2 | 0 | sites within |
| 3. Inline pre-handler | 1 | 1 | yes |
| 4. Promise chains | 140 | 106 | sites within |
| 5. Callback boundaries | 52 | 0 | sites within |
| 6. Reply chains (+1 distinct shape) | 9 | 0 | sites within |
| 7. Stream sites | 17 | 0 | sites within |
| 8. Unrouted controller exports | 2 | 1 | NO -- excluded |
| 9. Unrouted named pre-handlers | 3 | 3 | NO -- excluded |
| 10. Routes with no function | 3 | 3 | NO -- excluded |
| **Total** | **382** | **266** | |

The row count is **derived, not targeted**. It is not 154, and it should not be: the 154 counts hapi-invoked
*functions*, while a function typically contains several sites -- a chain, two
callback boundaries, a stream -- each of which is closed separately.

**The function rows are exactly the 154, and they are the rows marked `yes` above**: 145 (Routed handlers) + 8 (Routed pre-handler functions) + 1 (Inline pre-handler) = **154**, of which 153 are closed.
That arithmetic is asserted by the self-check rather than asserted in prose: 145 routed handlers + 8 routed
pre-handlers + 1 inline pre-handler = 154. Everything else is a **site within** one of those functions or
deliberately outside the set:

- **2b** holds two BRANCH SITES, not two functions. They live inside `findTrinket`
  and `courseBySlug`, which are already two of the 8 rows in 2a, so counting them
  as functions would say this document tracks 156 hapi-invoked functions when it
  tracks 154.
- **Sections 4 to 7** are sites inside the function rows: promise chains, callback
  boundaries, reply chains and stream sites.
- **Sections 8 to 10** are outside the set entirely -- the 2 unrouted exports, the 3
  unrouted pre-handlers and the 3 routes with no function to convert.

## 1. Routed handlers -- 145 rows (144 closed)

Every controller method that hapi invokes. **A declared signature settles nothing**,
which is why the `Current shape` column reports what the body does rather than what
the parameters are called. `lib/controllers/auth.js` contributes two rows that are
declared `(request, h)` already, and the analysis still has to read their bodies to
decide whether every signalling call sits in a `return` position -- while several
handlers still declared `(request, reply)` turn out to return on every path and
others fall off the end. Neither population is identifiable from the signature.

### `lib/controllers/course.js` -- 42 handlers (42 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/course.js:10` `createCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:64` `getCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 returned directly, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:139` `updateCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:190` `deleteCourse` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:210` `archiveCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:229` `copyCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:292` `addLesson` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:324` `getLesson` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:328` `updateLesson` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:367` `moveLesson` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:398` `deleteLesson` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:422` `addMaterial` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:482` `getMaterial` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:504` `updateMaterial` (4 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:599` `moveMaterial` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:633` `deleteMaterial` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:654` `listUsers` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:686` `listInvitations` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:705` `userLookup` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:747` `removeUser` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:776` `addUser` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:803` `updateRoles` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:831` `updateViews` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:853` `sendInvitations` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 returned directly, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:884` `removeInvitation` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:904` `updateInvitation` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 returned directly, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:988` `generateAccessCode` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1008` `getAccessCode` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1021` `join` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 1 returned directly, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1069` `startAssignment` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1108` `submitAssignment` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1172` `updateMySubmission` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1233` `autosaveComments` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1253` `getUserSubmissionsForMaterial` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1292` `dashboardOverview` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1413` `materialDashboard` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1470` `getMaterialSubmissionsForAllUsers` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1553` `getUserSubmissionsForCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1621` `autosaveFeedbackComments` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1646` `autosaveSubmissionOpt` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1658` `sendFeedback` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/course.js:1751` `acceptSubmission` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/trinket.js` -- 34 handlers (33 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/trinket.js:84` `index` (11 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:110` `beta` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:121` `library` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:173` `list` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:199` `getById` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 returned directly, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:227` `grant` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:234` `update` (4 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:282` `create` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:328` `createFork` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [ ] | `lib/controllers/trinket.js:381` `updateMetrics` | routed handler | async toolkit `(request, h)`; RELIES ON THE INTERCEPTION -- 1 of 2 signalling calls produce a value that never leaves the function (0 unreturned in the body, 1 dropped inside a nested function); every path through the body delivers a value or throws | Signature is already `async function (request, h)`, but 1 signalling call produce a value that never leaves the function (0 unreturned in the body, 1 dropped inside a nested function). Deliver the response on every path: return it, settle the promise the method returns with it, or return it from the nested handler of a chain this method returns. |
| [x] | `lib/controllers/trinket.js:416` `remove` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:442` `getByShortCode` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:512` `embed` (3 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:751` `assignment` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:761` `assignmentFeedback` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:783` `viewOnly` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:794` `email` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 2 returned directly, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:891` `snapshot` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:931` `interactions` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:942` `addToList` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:948` `namedList` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:951` `removeFromList` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:955` `logError` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:966` `logClientMetric` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1004` `draft` | routed handler | async toolkit `(request, h)`; returns its response -- all 6 signalling calls delivered: 3 settling the returned promise, 2 returned through a nested handler on a returned chain, 1 captured into a name a returning branch reads; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (3 settling the returned promise, 2 returned through a nested handler on a returned chain, 1 captured into a name a returning branch reads) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1088` `discardDraft` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1103` `autosave` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 settling the returned promise, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1180` `addToFolder` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1213` `removeFromFolder` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1235` `search` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1246` `downloadMain` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1293` `downloadFile` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1365` `updateSlug` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/trinket.js:1387` `downloadPostedZip` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |

### `lib/controllers/users.js` -- 31 handlers (31 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/users.js:595` `create` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 6 signalling calls delivered: 6 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (6 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:676` `login` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 6 signalling calls delivered: 6 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (6 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:794` `remove` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:817` `deleted` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:821` `logout` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:831` `sendPassReset` | routed handler | async toolkit `(request, h)`; returns its response -- all 5 signalling calls delivered: 2 returned directly, 3 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 3 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:888` `resetPasswordForm` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 1 returned directly, 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:910` `savePassword` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:938` `account` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:994` `updateProfile` | routed handler | async toolkit `(request, h)`; returns its response -- all 5 signalling calls delivered: 3 settling the returned promise, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (3 settling the returned promise, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1101` `assetList` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1151` `assetUpload` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1166` `replaceAsset` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1192` `removeAsset` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1211` `restoreAsset` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1253` `assetUploadFromURL` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 1 returned directly, 2 settling the returned promise; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- on transport refusal, log and leave the request UNSETTLED -- one path deliberately returns nothing, and the other paths return as they do today. That REPLACES the generic handler conversion mandate, which is wrong here: "every path returns exactly once" is the direct negation of this quirk. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [x] | `lib/controllers/users.js:1564` `changePassword` | routed handler | async toolkit `(request, h)`; returns its response -- all 5 signalling calls delivered: 1 returned directly, 4 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 4 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1605` `getAvatar` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1620` `getInfo` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1634` `updateSettings` (3 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1645` `sendEmailChange` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 1 returned directly, 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1688` `resendEmailChange` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 3 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (3 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1714` `changeEmail` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 4 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (4 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1759` `sendEmailVerification` | routed handler | async toolkit `(request, h)`; returns its response -- all 3 signalling calls delivered: 2 returned directly, 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly, 1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1792` `verifyEmail` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 4 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (4 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1825` `activateAccountForm` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 4 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (4 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1853` `activateAccount` | routed handler | async toolkit `(request, h)`; returns its response -- all 5 signalling calls delivered: 3 returned directly, 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (3 returned directly, 2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1902` `requestExport` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 returned through a nested handler on a returned chain, 2 captured into a name a returning branch reads; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain, 2 captured into a name a returning branch reads) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:1968` `listExports` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:2008` `getExportStatus` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/users.js:2067` `downloadExport` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/admin.js` -- 8 handlers (8 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/admin.js:11` `index` (2 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:88` `ohnoes` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 captured into a name a returning branch reads; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 captured into a name a returning branch reads) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:118` `uploadUsers` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 settling the returned promise, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:176` `updateUser` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 4 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (4 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:207` `grantRole` | routed handler | async toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 settling the returned promise, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 settling the returned promise, 2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:250` `addFeaturedCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:291` `removeFeaturedCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/admin.js:300` `moveFeaturedCourse` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/courses.js` -- 7 handlers (7 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/courses.js:33` `creationForm` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:55` `create` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:134` `getCourses` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:143` `featuredCourses` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:168` `copy` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:218` `coursePage` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/courses.js:254` `download` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- keep the residual `reply(Boom.forbidden())` in the unauthorized branch exactly as it stands, so it throws and the route answers 500. That REPLACES the generic handler conversion mandate, which is wrong here: converting every `reply(...)` into a returned toolkit response -- or taking the residual-reply row's "either return a toolkit response there" option -- would turn the 500 into the 403 the expression names. Measured outcome: the unauthorized branch evaluates an UNBOUND `Boom`, so it throws a `ReferenceError` before any response is constructed and the route answers 500 where the expression names 403. Owned by `docs/preserved-quirks.md` §9.7; reproduce it, do not fix it. |

### `lib/controllers/pages.js` -- 6 handlers (6 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/pages.js:10` `index` (3 routes) | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/pages.js:15` `login` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- keep `return reply.redirect('/home')` as the expression it is, so the authenticated branch throws its `TypeError` and reaches the preserved handler catch-all as a 500. That REPLACES the generic handler conversion mandate, which is wrong here: returning `request.success(...)` / `request.fail(...)` on every path would replace the throw with a working response, which is the quirk itself. Measured outcome: the authenticated-visitor 500 is REPRODUCED -- `reply.redirect` is a property access on a bare function and throws a TypeError that reaches the catch-all. Owned by `docs/preserved-quirks.md` §5; reproduce it, do not fix it. |
| [x] | `lib/controllers/pages.js:33` `signup` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- keep `return reply.redirect('/welcome')` as the expression it is, so the authenticated branch throws and answers 500, and leave `request.yar.set('next', ...)` in the `else` branch only. That REPLACES the generic handler conversion mandate, which is wrong here: returning `request.success(...)` / `request.fail(...)` on every path would replace the throw with a working response, which is the quirk itself. Measured outcome: the authenticated-visitor 500 is REPRODUCED, and `request.yar.set('next', ...)` stays in the `else` branch only -- it does not precede the throw. Owned by `docs/preserved-quirks.md` §5; reproduce it, do not fix it. |
| [x] | `lib/controllers/pages.js:48` `welcome` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/pages.js:54` `home` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/pages.js:87` `forgotPasswordForm` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/folders.js` -- 6 handlers (6 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/folders.js:131` `list` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/folders.js:164` `listView` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/folders.js:167` `trinkets` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- pass NO folder filter on the queryless path, and pass it only when a query is present -- the extracted core reproduces both cases. That REPLACES the generic handler conversion mandate, which is wrong here: "every path returns exactly once" is satisfiable while accidentally passing the folder through on the queryless path, which R-d forbids. Measured outcome: the queryless case passes NO folder filter because the injected URL is malformed; the extraction must reproduce both cases. Owned by `docs/preserved-quirks.md` §7; reproduce it, do not fix it. |
| [x] | `lib/controllers/folders.js:231` `create` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 settling the returned promise; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 settling the returned promise) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/folders.js:317` `update` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/folders.js:386` `deleteFolder` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/classes.js` -- 5 handlers (5 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/classes.js:25` `viewCourses` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/classes.js:59` `viewClass` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 1 returned directly, 1 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly, 1 returned through a nested handler on a returned chain) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/classes.js:97` `getClass` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/classes.js:134` `acceptInvitation` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/classes.js:191` `joinFromLink` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |

### `lib/controllers/files.js` -- 4 handlers (4 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/files.js:580` `uploadAvatar` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/files.js:603` `upload` | routed handler | async toolkit `(request, h)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (1 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/files.js:678` `download` | routed handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/controllers/files.js:767` `setThumbnail` | routed handler | async toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `async function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |

### `lib/controllers/auth.js` -- 2 handlers (2 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/auth.js:446` `google` | routed handler | toolkit `(request, h)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws | Already converted: `function (request, h)`, every signalling call delivered (2 returned directly) and every path ending in `return` or `throw`. Nothing further is required of this site. |
| [x] | `lib/controllers/auth.js:471` `googleCallback` | routed handler | toolkit `(request, h)`; returns its response -- all 4 signalling calls delivered: 2 returned directly, 2 returned through a nested handler on a returned chain; every path through the body delivers a value or throws | Signature is already `async function (request, h)`, returning on every measured path. PRESERVED QUIRK, GOVERNING ACTION -- persist the user, mutate the session, and THEN report the generic authentication failure -- preserve that ORDER and the absence of a login, so a first-time sign-in still creates the account and still reports failure. That REPLACES the generic handler conversion mandate, which is wrong here: a mandate to return a response on every path can silently drop the throw that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |

## 2. Routed pre-handlers -- 8 rows (8 closed)

Named pre-handlers in `lib/util/helpers.js` that a route references. The census is
11 named pre-handlers in total; 8 are routed and 3 are not.

Two exports are deliberately **not** in that census, and saying why keeps the
arithmetic honest: `lowerUserFields` is an alias -- `module.exports.lowerUserFields =
internals.lowerUserFields;` -- with no function literal at the declaration, and the
function it points at is already `(request, h)`; and `findFeaturedTrinkets` is
already `(request, h)` too, which makes it the exemplar rather than the work.
`register` is a `server.method` registrar, not a pre-handler at all.

A pre-handler with nothing to contribute returns **`null`**, which is exactly what
the shim already produces for it.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/util/helpers.js:143` `findTrinket` (descriptor `method`) | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | PRESERVED QUIRK, GOVERNING ACTION -- at the language-mismatch branch, `return null` -- the value the shim produced. Its redirect construction is REMOVED, not converted. That QUALIFIES the generic pre-handler conversion mandate rather than displacing it. The mandate still governs every OTHER path in this body: Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. The carve-out exists because converting that chain into a returned toolkit response would emit a 301 the baseline never emitted. Measured outcome: the language-mismatch 301 at `lib/util/helpers.js:182` is MEASURED DEAD -- the deferred is settled with `null` at `lib/util/routeParser.js:147` before `.takeover()` reaches its own resolve at `:154`, so the pre value is already `null`. Owned by `docs/preserved-quirks.md` §2; reproduce it, do not fix it. |
| [x] | `lib/util/helpers.js:200` `validLang` (descriptor `method`) | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:219` `trinketTypeEnabled` (descriptor `method`) | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:250` `coursesEnabled` (descriptor `method`) | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:258` `verifyEmailToken` | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:345` `getDefaultTrinket` | routed pre-handler | toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:362` `userByUsername` | routed pre-handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. |
| [x] | `lib/util/helpers.js:380` `courseBySlug` | routed pre-handler | async toolkit `(request, h)`; no `request.success` / `request.fail` / `reply` call in the body; every path through the body delivers a value or throws | PRESERVED QUIRK, GOVERNING ACTION -- at the slug-alias branch, `return null` -- the value the shim produced. Its redirect construction is REMOVED, not converted. That QUALIFIES the generic pre-handler conversion mandate rather than displacing it. The mandate still governs every OTHER path in this body: Already converted: `async function (request, h)` with no `request.success` / `request.fail` / `reply` call in the body, and every path ending in `return` or `throw`, so its response is whatever it returns. Nothing further is required of this site. The carve-out exists because converting that chain into a returned toolkit response would emit a 301 the baseline never emitted. Measured outcome: the slug-alias 301 at `lib/util/helpers.js:385`, reached from 5 route declarations, is MEASURED DEAD by the same mechanism -- the deferred is already settled with `null`. Owned by `docs/preserved-quirks.md` §2; reproduce it, do not fix it. |

### The two dead pre-handler 301 redirects

Both are **measured dead**, and the mechanism matters because deleting it does not
preserve the outcome. In the shim, `fakeReply(undefined)` settles the deferred with
`null` at `lib/util/routeParser.js:147` **before** `.takeover()` reaches its own
resolve at `:154`. So the redirect is discarded and the pre value is already `null`.
The capability is dead end to end: `_isRedirect`, `_permanent` and `_takeover` appear
**only** on the six lines that define them -- `lib/util/routeParser.js:98`, `:100`,
`:101`, `:151`, `:153`, `:154`.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/util/helpers.js:182` `findTrinket` (baseline coordinate; the legacy construct is no longer in this tree) | routed pre-handler | findTrinket language-mismatch branch: return reply().redirect(location).permanent().takeover(). MEASURED DEAD: fakeReply(undefined) settles the deferred with null at lib/util/routeParser.js:147 BEFORE .takeover() reaches its own resolve at :154, so the redirect is discarded and the pre value is already null.; the legacy construct is gone from `findTrinket` | return null. The probe confirmed GET /prenull -> {"pre":null}. The capability is dead end to end: _isRedirect, _permanent and _takeover appear only on the six lines defining them (routeParser :98, :100, :101, :151, :153, :154). Baseline outcome owned by `docs/preserved-quirks.md` §2; reproduce it, do not fix it. EVIDENCE: `quirk.dead-301.find-trinket-language-mismatch` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The pre value this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |
| [ ] | `lib/util/helpers.js:385` `courseBySlug` (baseline coordinate; the legacy construct is no longer in this tree) | routed pre-handler | courseBySlug slug-alias branch (reached from 5 route declarations): return reply().redirect(location).permanent().takeover(). MEASURED DEAD by the same mechanism -- the deferred is already settled with null.; the legacy construct is gone from `courseBySlug` | return null. Coverage is counted from the route manifest rather than from lexical references, because the per-language expansion multiplies them. Baseline outcome owned by `docs/preserved-quirks.md` §2; reproduce it, do not fix it. EVIDENCE: `quirk.dead-301.course-by-slug-alias` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The pre value this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |

## 3. Inline pre-handler -- 1 row

A function literal declared directly inside a `pre :` array in a route config.
There is exactly one, on `POST /api/users/login`, and it is the sole member of its
own category in the conversion set. Note its parameter is `req`, not `request`.

The other 116 declarations in `config/api_routes.js` are untouched by this
migration -- only this pre-handler changes.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `config/api_routes.js:1105` | inline pre-handler | `(request, h)`; body is `function(request, h) { return true; }` -- signals through the shim rather than returning to hapi | A function returning `true`: `function (request, h) { return true; }`. The assigned pre value stays `true`, so `request.pre.encryptRoles` is unchanged. |

## 4. Promise chains -- 140 rows (106 closed)

One row per chain. A chain is closed when its value leaves the enclosing function --
`return`ed or `await`ed -- exactly once. Today the wrapper discards it.

Terminal links that pass a **bare function reference** rather than a function
literal are called out in the `Current shape` column, because that is the shape
whose return value is silently dropped: `.catch(request.fail)` reads as handled and
produces nothing the handler returns.

A `.then(` / `.catch(` / `.finally(` call is admitted as a chain link only when its
STRUCTURE is a promise reaction: a receiver that is itself a call or an index, or a
plain binding receiving a function-shaped argument. The name alone is not enough,
and the difference is accounted for here rather than left to a reader who greps
`.catch(` and finds more hits than rows:

- `lib/controllers/folders.js:279` `create` -- `request.catch(...)` takes an object literal on a plain receiver. It is **not** a
  promise reaction: the member does not exist on `request`, so the
  call throws `TypeError` and the route catch-all answers 500. That is a
  preserved baseline outcome owned by `docs/preserved-quirks.md`, and an error edge
  rather than a chain -- reading it as one produced a row describing a chain
  that is not there and a target proposing to wrap an object literal in a
  function.
- `lib/controllers/folders.js:364` `update` -- `request.catch(...)` takes an object literal on a plain receiver. It is **not** a
  promise reaction: the member does not exist on `request`, so the
  call throws `TypeError` and the route catch-all answers 500. That is a
  preserved baseline outcome owned by `docs/preserved-quirks.md`, and an error edge
  rather than a chain -- reading it as one produced a row describing a chain
  that is not there and a target proposing to wrap an object literal in a
  function.

### `lib/controllers/course.js` -- 38 chains (31 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/course.js:116-135` `getCourse` | promise chain | 2-link chain `.then().then()`; currently RETURNED; head `course.populate(lessonsPopulate)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/course.js:194-202` `deleteCourse` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.deleteCourse()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:220` `archiveCourse` | promise chain | 1-link chain `.catch()`; currently AWAITED; head `course.save()` | Already leaves the function (`await`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:274-283` `copyCourse` | promise chain | 1-link chain `.then()`; currently RETURNED; head `course.addUser(request.user, ["course-owner"])` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/course.js:306-317` `addLesson` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `lesson.save()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [ ] | `lib/controllers/course.js:407-415` `deleteLesson` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE, so its return value is dropped; head `lesson.remove()` | Await the chain and return its value, and make sure the bare `request.fail` reference's return value PROPAGATES -- `.catch(function (err) { return request.fail(err); })` or an equivalent that does not drop it. Exactly once per path. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:446-475` `addMaterial` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `trinketPromise` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:492-501` `getMaterial` | promise chain | 1-link chain `.then()`; currently RETURNED; head `promise` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/course.js:544-592` `updateMaterial` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `trinketPromise` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [ ] | `lib/controllers/course.js:611-626` `moveMaterial` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE, so its return value is dropped; head `lesson.save()` | Await the chain and return its value, and make sure the bare `request.fail` reference's return value PROPAGATES -- `.catch(function (err) { return request.fail(err); })` or an equivalent that does not drop it. Exactly once per path. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [ ] | `lib/controllers/course.js:639-647` `deleteMaterial` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE, so its return value is dropped; head `material.remove()` | Await the chain and return its value, and make sure the bare `request.fail` reference's return value PROPAGATES -- `.catch(function (err) { return request.fail(err); })` or an equivalent that does not drop it. Exactly once per path. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:690-698` `listInvitations` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `CourseInvitation.findUnacceptedByCourse(course)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:709-740` `userLookup` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `User.findByLogin(request.payload.user)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:753-769` `removeUser` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `User.findById(userId)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:780-796` `addUser` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `User.findById(request.payload.user)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:808-824` `updateRoles` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `User.findById(request.payload.user)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:838-846` `updateViews` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.updateView(userId, view, action)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:863-878` `sendInvitations` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `CourseInvitation.addList(request.payload.emailList, course)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:865-868` `sendInvitations` | promise chain | 1-link chain `.then()`; currently RETURNED; head `CourseInvitation.sendEmails(invitations, course, request.user)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/course.js:888-898` `removeInvitation` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `CourseInvitation.findById(request.params.invitationId)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:935-975` `updateInvitation` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `invitation.save()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:937-940` `updateInvitation` | promise chain | 1-link chain `.then()`; currently RETURNED; head `CourseInvitation.sendEmails([savedInvitation], course, request.user)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/course.js:993-1002` `generateAccessCode` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.save()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1051-1067` `join` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.addUser(request.user, ["course-student"])` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1072-1106` `startAssignment` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Trinket.findById(request.payload.parent)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1113-1170` `submitAssignment` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `Material.findById(request.params.materialId)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1179-1227` `updateMySubmission` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Material.findById(submission.materialId)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [ ] | `lib/controllers/course.js:1247` `autosaveComments` | promise chain | 1-link chain `.then()`; currently RETURNED; terminal `.then(request.success)` passes a BARE REFERENCE, so its return value is dropped; head `submission.save()` | Await the chain and return its value, and make sure the bare `request.success` reference's return value PROPAGATES -- `.then(function (err) { return request.success(err); })` or an equivalent that does not drop it. Exactly once per path. |
| [x] | `lib/controllers/course.js:1269-1290` `getUserSubmissionsForMaterial` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findByUserAndMaterial(userId, request.params.materialId)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1329-1407` `dashboardOverview` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `course.populate({ path : 'lessons', select : 'materials' })` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1435-1464` `materialDashboard` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.courseDashboard(request.params.courseId)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1494-1547` `getMaterialSubmissionsForAllUsers` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findSubmissionsByMaterial(request.params.materialId)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1569-1615` `getUserSubmissionsForCourse` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Trinket.findSubmissionsByUserAndCourse(user._id, course._id)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [ ] | `lib/controllers/course.js:1640` `autosaveFeedbackComments` | promise chain | 1-link chain `.then()`; currently RETURNED; terminal `.then(request.success)` passes a BARE REFERENCE, so its return value is dropped; head `submission.save()` | Await the chain and return its value, and make sure the bare `request.success` reference's return value PROPAGATES -- `.then(function (err) { return request.success(err); })` or an equivalent that does not drop it. Exactly once per path. |
| [ ] | `lib/controllers/course.js:1652` `autosaveSubmissionOpt` | promise chain | 1-link chain `.then()`; currently RETURNED; terminal `.then(request.success)` passes a BARE REFERENCE, so its return value is dropped; head `submission.save()` | Await the chain and return its value, and make sure the bare `request.success` reference's return value PROPAGATES -- `.then(function (err) { return request.success(err); })` or an equivalent that does not drop it. Exactly once per path. |
| [x] | `lib/controllers/course.js:1663-1745` `sendFeedback` | promise chain | 5-link chain `.then().then().then().then().catch()`; currently RETURNED; head `Trinket.findById(request.payload.trinketId)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [x] | `lib/controllers/course.js:1758-1763` `acceptSubmission` | promise chain | 1-link chain `.then()`; currently RETURNED; head `trinket.save()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [ ] | `lib/controllers/course.js:1840-1843` (module scope) | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `}`); head `savedCourse.addUser(actingUser, ["course-owner"])` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |

### `lib/controllers/trinket.js` -- 43 chains (31 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/trinket.js:135` `library` | promise chain | 1-link chain `.catch()`; currently RETURNED; head `Trinket.findById(id)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:212-222` `getById` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `User.findById( data._owner)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:229-231` `grant` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE, so its return value is dropped; head `trinket.save()` | Await the chain and return its value, and make sure the bare `request.fail` reference's return value PROPAGATES -- `.catch(function (err) { return request.fail(err); })` or an equivalent that does not drop it. Exactly once per path. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:253-279` `update` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE, so its return value is dropped; head `trinket.save()` | Await the chain and return its value, and make sure the bare `request.fail` reference's return value PROPAGATES -- `.catch(function (err) { return request.fail(err); })` or an equivalent that does not drop it. Exactly once per path. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:257-269` `update` | promise chain | 2-link chain `.then().catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `{`); head `Folder.findById(trinket.folder.folderId)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:311-325` `create` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `trinket.save()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:364-378` `createFork` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Trinket.findByIdAndUpdateMetrics(parent.id, 'forks', meta)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:407-413` `updateMetrics` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findByIdAndUpdateMetrics(request.params.trinketId, metric, me…` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:422-427` `remove` | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `(`); head `Folder.findById(trinket.folder.folderId)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [x] | `lib/controllers/trinket.js:431-439` `remove` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Promise.all(promises)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:433-436` `remove` | promise chain | 1-link chain `.then()`; currently RETURNED; head `trinket.softDelete()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/trinket.js:489-509` `getByShortCode` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `updateMetrics()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:558-568` `embed` | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `=`); head `Trinket.findRemix(request.pre.trinket.id, request.user.id)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [ ] | `lib/controllers/trinket.js:574-585` `embed` | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `=`); head `promise` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [x] | `lib/controllers/trinket.js:579-584` `embed` | promise chain | 1-link chain `.then()`; currently RETURNED; head `ownerPromise` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [ ] | `lib/controllers/trinket.js:583` `embed` | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `:`); head `Trinket.findByIdAndUpdateMetrics(request.pre.trinket.id, 'embedViews'…` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [x] | `lib/controllers/trinket.js:591-749` `embed` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `promise` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:692-745` `embed` | promise chain | 2-link chain `.then().then()`; currently RETURNED; head `Promise.resolve()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/trinket.js:770-781` `assignmentFeedback` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Draft.findOneMoreRecent(query)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/trinket.js:861-885` `email` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `mailer.send(request.payload.email, subject, options)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:909-919` `snapshot` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE, so its return value is dropped; head `new Promise(function(resolve, reject) { FileUtil.uploadSnapshotFromBu…` | Await the chain and return its value, and make sure the bare `request.fail` reference's return value PROPAGATES -- `.catch(function (err) { return request.fail(err); })` or an equivalent that does not drop it. Exactly once per path. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:934-940` `interactions` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Interaction.findByTrinketId(trinket.id)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:962` `logError` | promise chain | 1-link chain `.catch()`; currently AWAITED; head `error.save()` | Already leaves the function (`await`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:996-1002` `logClientMetric` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `ClientMetric.addMetric(values)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:1049-1073` `draft` | promise chain | 2-link chain `.then().then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `{`); head `zip.loadAsync(request.payload.zipCode, { base64: true })` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [x] | `lib/controllers/trinket.js:1062-1070` `draft` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Draft.findOneAndUpdate(query, update)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1077-1085` `draft` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Draft.findOneAndUpdate(query, update)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1093-1101` `discardDraft` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Draft.discard(query)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:1139-1162` `autosave` | promise chain | 2-link chain `.then().then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `{`); head `zip.loadAsync(request.payload.zipCode, { base64: true })` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [x] | `lib/controllers/trinket.js:1152-1159` `autosave` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `trinket.save()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1166-1173` `autosave` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `trinket.save()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1189-1207` `addToFolder` | promise chain | 5-link chain `.then().then().then().then().catch()`; currently RETURNED; head `checkCurrent` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1218-1229` `removeFromFolder` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `folder.removeTrinket(trinket.id)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1236-1244` `search` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.searchForOwner(request.user, request.query.q)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1250-1291` `downloadMain` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findById(request.params.shortCode)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1299-1363` `downloadFile` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Trinket.findById(request.params.shortCode)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1351-1355` `downloadFile` | promise chain | 1-link chain `.then()`; currently RETURNED; head `FileUtil.downloadUserAsset(file)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/trinket.js:1375-1383` `updateSlug` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `trinket.updateSlug(request.payload.slug)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:1452-1459` `downloadPostedZip` | promise chain | 2-link chain `.then().catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `(`); head `FileUtil.downloadUserAsset(assetFile)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1464-1500` `downloadPostedZip` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Promise.all(assetPromises)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:1498` `downloadPostedZip` | promise chain | 1-link chain `.catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `{`); head `fs.promises.unlink(zipFile)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [x] | `lib/controllers/trinket.js:1673-1691` (module scope) | promise chain | 2-link chain `.then().then()`; currently RETURNED; head `getUserId()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/trinket.js:1840-1873` (module scope) | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `Promise.allSettled(assetPromises)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |

### `lib/controllers/users.js` -- 18 chains (13 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/users.js:103-105` (module scope) | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `{`); terminal `.then(null, function(uncancellable) { return uncancellable; })` passes a BARE REFERENCE, so its return value is dropped; head `response.body.cancel()` | Await the chain and return its value, and make sure the bare `null, function(uncancellable) { return uncancellable; }` reference's return value PROPAGATES -- `.then(function (err) { return null, function(uncancellable) { return uncancellable; }(err); })` or an equivalent that does not drop it. Exactly once per path. |
| [x] | `lib/controllers/users.js:350-374` (module scope) | promise chain | 1-link chain `.then()`; currently RETURNED; head `dns.promises.lookup(host, { all: true })` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/users.js:418-490` (module scope) | promise chain | 1-link chain `.then()`; currently RETURNED; head `assetAddressDenial(target)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/users.js:443-489` (module scope) | promise chain | 1-link chain `.then()`; currently RETURNED; head `globalThis.fetch(target.href, { method : 'GET', // Always manual: the…` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/users.js:796-805` `remove` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `request.user.remove()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:967-991` `account` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `promise` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1029-1098` `updateProfile` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `usernameCheck` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1054-1064` `updateProfile` | promise chain | 2-link chain `.then().catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `=`); head `Folder.findByOwner(user)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1083-1090` `updateProfile` | promise chain | 2-link chain `.then().then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `}`); head `addFolderSlugJob` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [x] | `lib/controllers/users.js:1118-1148` `assetList` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `getUserFiles` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1173-1184` `replaceAsset` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `new Promise(function(resolve, reject) { FileUtil.uploadUserAsset(requ…` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1197-1203` `removeAsset` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `file.hide()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1215-1221` `restoreAsset` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `file.show()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1320` `assetUploadFromURL` | promise chain | 1-link chain `.catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `{`); head `fs.promises.unlink(tmpPath)` | PRESERVED QUIRK, GOVERNING ACTION -- await the transport chain where baseline continues, and leave the refused-connection path UNSETTLED -- that path deliberately produces no response. That REPLACES the generic promise-chain mandate, which is wrong here: returning the chain's value exactly once per path settles the path this quirk requires to stay open. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [ ] | `lib/controllers/users.js:1509-1561` `assetUploadFromURL` | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `;`); head `fetchAssetResource(target, controller.signal)` | PRESERVED QUIRK, GOVERNING ACTION -- await the transport chain where baseline continues, and leave the refused-connection path UNSETTLED -- that path deliberately produces no response. That REPLACES the generic promise-chain mandate, which is wrong here: returning the chain's value exactly once per path settles the path this quirk requires to stay open. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [x] | `lib/controllers/users.js:1635-1643` `updateSettings` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `request.user.updateSettings(request.payload)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1912-1965` `requestExport` | promise chain | 4-link chain `.then().then().then().catch()`; currently AWAITED; head `Export.findPendingOrProcessing(userId)` | Already leaves the function (`await`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [x] | `lib/controllers/users.js:1971-1990` `listExports` | promise chain | 2-link chain `.then().catch()`; currently AWAITED; head `Export.findByOwner(request.user)` | Already leaves the function (`await`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |

### `lib/controllers/admin.js` -- 13 chains (7 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/admin.js:43-72` `index` | promise chain | 3-link chain `.then().then().catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `=`); head `featuredStore.getList()` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:46-52` `index` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Course.findById(member.id)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/admin.js:78-86` `index` | promise chain | 1-link chain `.then()`; currently RETURNED; head `promise` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [ ] | `lib/controllers/admin.js:153-172` `uploadUsers` | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `(`); head `Promise.allSettled(promises)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [ ] | `lib/controllers/admin.js:217-246` `grantRole` | promise chain | 3-link chain `.then().then().catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `(`); head `user.grant(request.payload.role, "site")` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [ ] | `lib/controllers/admin.js:225-227` `grantRole` | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `=`); head `promise` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [ ] | `lib/controllers/admin.js:230-232` `grantRole` | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `=`); head `promise` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. |
| [x] | `lib/controllers/admin.js:257-289` `addFeaturedCourse` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `User.findByLogin(request.payload.ownerSlug)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:268-269` `addFeaturedCourse` | promise chain | 1-link chain `.then()`; currently RETURNED; head `featuredStore.addMember(course.id, request.payload.page)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/admin.js:292-298` `removeFeaturedCourse` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `featuredStore.removeMember(request.params.courseId, request.query.pag…` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:301-307` `moveFeaturedCourse` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `featuredStore.moveMember(request.payload.courseId, request.payload.pa…` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [ ] | `lib/controllers/admin.js:357-368` (module scope) | promise chain | 3-link chain `.then().then().catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `;`); head `Trinket.findForUser(user.id)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [x] | `lib/controllers/admin.js:378-384` (module scope) | promise chain | 1-link chain `.then()`; currently RETURNED; head `User.findByRole(role)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |

### `lib/controllers/courses.js` -- 9 chains (9 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/courses.js:137-140` `getCourses` | promise chain | 1-link chain `.then()`; currently RETURNED; head `request.user.getCourses()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/courses.js:144-165` `featuredCourses` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Course.findFeaturedForUser(request.user)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. |
| [x] | `lib/controllers/courses.js:210-213` `copy` | promise chain | 1-link chain `.then()`; currently RETURNED; head `request.user.grant("course-owner", "course", { id : course.id })` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/courses.js:283-298` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Lesson.findById(lesson)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/courses.js:287-297` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `fs.promises.mkdir(lessonDir, { recursive : true })` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/courses.js:305-317` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Material.findById(info.material)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/courses.js:357-363` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `nunjucks.render('courses/download/view.html', context)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/courses.js:381-387` `download` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Promise.resolve()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/courses.js:468-486` `download` | promise chain | 7-link chain `.then().then().then().then().then().then().catch()`; currently RETURNED; head `fs.promises.mkdir(courseDir, { recursive : true })` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. |

### `lib/controllers/pages.js` -- 1 chain (0 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/pages.js:65-71` `home` -- the AAP cites this chain as `lib/controllers/pages.js:52` (measured at the base commit as `:48-54`, here `:65-71`) | promise chain | 2-link chain `.then().catch()`; currently RETURNED; terminal `.catch(request.fail)` passes a BARE REFERENCE, so its return value is dropped; head `Trinket.findRecentByOwner(request.user._id)` | Await the chain and return its value, and make sure the bare `request.fail` reference's return value PROPAGATES -- `.catch(function (err) { return request.fail(err); })` or an equivalent that does not drop it. Exactly once per path. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.9. |

### `lib/controllers/folders.js` -- 5 chains (4 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/folders.js:148-162` `list` | promise chain | 3-link chain `.then().then().catch()`; currently RETURNED; head `getUserId()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |
| [x] | `lib/controllers/folders.js:306-313` `create` | promise chain | 1-link chain `.then()`; currently RETURNED; head `request.user.grant("folder-owner", "folder", { id : savedFolder.id })` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/folders.js:325-372` `update` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `folder.save()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |
| [ ] | `lib/controllers/folders.js:337-347` `update` | promise chain | 2-link chain `.then().catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `{`); head `Trinket.findById(folderTrinket.trinketId)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |
| [x] | `lib/controllers/folders.js:390-398` `deleteFolder` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `folder.deleteFolder()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |

### `lib/controllers/classes.js` -- 6 chains (6 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/classes.js:29-56` `viewCourses` | promise chain | 1-link chain `.then()`; currently RETURNED; head `request.pre.user.getOwnedCourses()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/classes.js:79-93` `viewClass` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Course.findFeaturedForUser(request.user)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/classes.js:137-188` `acceptInvitation` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `CourseInvitation.findByToken(request.params.token)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.4. |
| [x] | `lib/controllers/classes.js:140-178` `acceptInvitation` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `Course.findById(invitation.courseId)` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.4. |
| [x] | `lib/controllers/classes.js:155-163` `acceptInvitation` | promise chain | 2-link chain `.then().then()`; currently RETURNED; head `course.addUser(request.user, ['course-student'])` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [x] | `lib/controllers/classes.js:222-230` `joinFromLink` | promise chain | 2-link chain `.then().catch()`; currently RETURNED; head `course.addUser(request.user, ["course-student"])` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.4. |

### `lib/controllers/auth.js` -- 7 chains (5 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/auth.js:199-201` (module scope) | promise chain | 1-link chain `.then()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `{`); terminal `.then(null, function(uncancellable) { return uncancellable; })` passes a BARE REFERENCE, so its return value is dropped; head `response.body.cancel()` | Await the chain and return its value, and make sure the bare `null, function(uncancellable) { return uncancellable; }` reference's return value PROPAGATES -- `.then(function (err) { return null, function(uncancellable) { return uncancellable; }(err); })` or an equivalent that does not drop it. Exactly once per path. |
| [x] | `lib/controllers/auth.js:237-241` (module scope) | promise chain | 1-link chain `.then()`; currently RETURNED; head `response.text()` | Already leaves the function (`return`). Confirm the value reaching hapi is a response and not a builder, and that no branch inside the links returns nothing. |
| [ ] | `lib/controllers/auth.js:261-345` (module scope) | promise chain | 2-link chain `.then().catch()`; currently NEITHER returned nor awaited -- its value is discarded (preceded by `;`); head `fetch(current.href, init)` | Await the chain and return its value -- exactly once per path. Every branch inside the links must produce the response, not signal it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. |
| [x] | `lib/controllers/auth.js:484-661` `googleCallback` | promise chain | 4-link chain `.then().then().then().catch()`; currently RETURNED; head `new Promise(function(resolve, reject) { legacyJsonRequest('https://oa…` | PRESERVED QUIRK, GOVERNING ACTION -- keep the chain returned, and keep the link that throws on the undefined `opts` reference throwing, so the `.catch` still produces the generic authentication failure after the user has been persisted. That REPLACES the generic promise-chain mandate, which is wrong here: requiring that no branch inside the links return nothing is the same mandate one level down, and the branch that returns nothing is the one that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [x] | `lib/controllers/auth.js:553-626` `googleCallback` | promise chain | 1-link chain `.then()`; currently RETURNED; head `new Promise(function(resolve, reject) { User.findByMultiple({ email:…` | PRESERVED QUIRK, GOVERNING ACTION -- keep the chain returned, and keep the link that throws on the undefined `opts` reference throwing, so the `.catch` still produces the generic authentication failure after the user has been persisted. That REPLACES the generic promise-chain mandate, which is wrong here: requiring that no branch inside the links return nothing is the same mandate one level down, and the branch that returns nothing is the one that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [x] | `lib/controllers/auth.js:595-597` `googleCallback` | promise chain | 1-link chain `.then()`; currently RETURNED; head `Promise.all(promises)` | PRESERVED QUIRK, GOVERNING ACTION -- keep the chain returned, and keep the link that throws on the undefined `opts` reference throwing, so the `.catch` still produces the generic authentication failure after the user has been persisted. That REPLACES the generic promise-chain mandate, which is wrong here: requiring that no branch inside the links return nothing is the same mandate one level down, and the branch that returns nothing is the one that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [x] | `lib/controllers/auth.js:615-624` `googleCallback` | promise chain | 1-link chain `.then()`; currently RETURNED; head `user.save()` | PRESERVED QUIRK, GOVERNING ACTION -- keep the chain returned, and keep the link that throws on the undefined `opts` reference throwing, so the `.catch` still produces the generic authentication failure after the user has been persisted. That REPLACES the generic promise-chain mandate, which is wrong here: requiring that no branch inside the links return nothing is the same mandate one level down, and the branch that returns nothing is the one that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |

## 5. Callback boundaries -- 52 rows

**A closed callback boundary ceases to exist**, so this section shrinks rather
than ticks: replacing `util.f(x, function (err, r) { ... })` with `await` removes
the callback literal, and the row with it. The `Done` box is therefore ticked only
in the one case that is still detectable -- a call site that already carries an
`await` -- and progress is read from the count instead. Baseline: **57**. This tree: **52**, so 5 boundaries have been resolved.

Each row records **where the `await` goes**: at the call site, inside the converted
lifecycle method. Rule T-3 puts the promise boundary at the lifecycle method and
nowhere deeper, which is why `lib/util/file.js`, `lib/util/store.js` and
`lib/util/queues.js` keep their callback interfaces -- a handler `await`s them, they
are not lifecycle methods themselves.

A boundary here is a call receiving a function literal whose parameters are either
error-first or empty -- the two shapes a completion callback takes. Empty-parameter
callbacks are deliberately included, because two named conversions are exactly that
shape: `rimraf(dir, function () {})` and `fs.unlink(file, function () {})`. Promise
links, event registrations and synchronous iteration helpers are excluded -- they
have their own categories or are not boundaries at all.

Where baseline swallows a callback error or fires and forgets, **that timing is the
target**: an error made visible, or a response made to wait, is a behaviour change.

### `lib/controllers/course.js` -- 6 boundaries (0 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/course.js:153` `updateCourse` | callback boundary | `course.save(..., function (err, savedCourse) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [ ] | `lib/controllers/course.js:248` `copyCourse` | callback boundary | `request.pre.course.copy(..., function (err, course) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [ ] | `lib/controllers/course.js:351` `updateLesson` | callback boundary | `lesson.save(..., function (err, savedLesson) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [ ] | `lib/controllers/course.js:383` `moveLesson` | callback boundary | `course.save(..., function (err, savedCourse) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [ ] | `lib/controllers/course.js:1030` `join` | callback boundary | `Course.findByAccessCode(..., function (err, course) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |
| [ ] | `lib/controllers/course.js:1826` (module scope) | callback boundary | `course.save(..., function (err, savedCourse) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.5. |

### `lib/controllers/trinket.js` -- 3 boundaries (0 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/trinket.js:398` `updateMetrics` | callback boundary | `Trinket.findById(..., function (err, trinket) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:910` `snapshot` | callback boundary | `FileUtil.uploadSnapshotFromBuffer(..., function (err) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |
| [ ] | `lib/controllers/trinket.js:1613` (module scope) | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. |

### `lib/controllers/users.js` -- 30 boundaries (0 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/users.js:639` `create` | callback boundary | `User.exists(..., function (err, result) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:657` `create` | callback boundary | `request.yar._logIn(..., function (err) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:691` `login` | callback boundary | `User.findByLogin(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:715` `login` | callback boundary | `user.comparePassword(..., function (err, isMatch) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:738` `login` | callback boundary | `request.yar._logIn(..., function () { ... })` -- completion callback with no parameters; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. |
| [ ] | `lib/controllers/users.js:852` `sendPassReset` | callback boundary | `User.findByLogin(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:896` `resetPasswordForm` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:920` `savePassword` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:925` `savePassword` | callback boundary | `user.save(..., function (err) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:948` `account` | callback boundary | `Course.findForUser(..., function (err, courses) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1013` `updateProfile` | callback boundary | `User.exists(..., function (err, result) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1039` `updateProfile` | callback boundary | `user.save(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1108` `assetList` | callback boundary | `File.findForUser(..., function (err, files) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1159` `assetUpload` | callback boundary | `FileUtil.uploadUserAsset(..., function (err, file) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1174` `replaceAsset` | callback boundary | `FileUtil.uploadUserAsset(..., function (err, file) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1265` `assetUploadFromURL` | callback boundary | `tmp.tmpName(..., function (err, tmpPath) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler, not in the callee, and preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- take the `await` at this call site WITHOUT making the refused-connection path settle: the upload never starts when `end` never arrives, and the route is left unsettled. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path removes the unsettled outcome. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [ ] | `lib/controllers/users.js:1406` `assetUploadFromURL` | callback boundary | `process.nextTick(..., function () { ... })` -- completion callback with no parameters; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler, not in the callee, and preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- take the `await` at this call site WITHOUT making the refused-connection path settle: the upload never starts when `end` never arrives, and the route is left unsettled. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path removes the unsettled outcome. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [ ] | `lib/controllers/users.js:1424` `assetUploadFromURL` | callback boundary | `process.nextTick(..., function () { ... })` -- completion callback with no parameters; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler, not in the callee, and preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- take the `await` at this call site WITHOUT making the refused-connection path settle: the upload never starts when `end` never arrives, and the route is left unsettled. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path removes the unsettled outcome. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [ ] | `lib/controllers/users.js:1463` `assetUploadFromURL` | callback boundary | `process.nextTick(..., function () { ... })` -- completion callback with no parameters; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler, not in the callee, and preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- take the `await` at this call site WITHOUT making the refused-connection path settle: the upload never starts when `end` never arrives, and the route is left unsettled. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path removes the unsettled outcome. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [ ] | `lib/controllers/users.js:1471` `assetUploadFromURL` | callback boundary | `setTimeout(..., function () { ... })` -- completion callback with no parameters; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler, not in the callee, and preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- take the `await` at this call site WITHOUT making the refused-connection path settle: the upload never starts when `end` never arrives, and the route is left unsettled. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path removes the unsettled outcome. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [ ] | `lib/controllers/users.js:1503` `assetUploadFromURL` | callback boundary | `FileUtil.uploadUserAsset(..., function (err, file) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler, not in the callee, and preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- take the `await` at this call site WITHOUT making the refused-connection path settle: the upload never starts when `end` never arrives, and the route is left unsettled. That REPLACES the generic callback-boundary mandate, which is wrong here: a callback-boundary mandate that continues with a result on every path removes the unsettled outcome. Measured outcome: a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. |
| [ ] | `lib/controllers/users.js:1569` `changePassword` | callback boundary | `request.user.comparePassword(..., function (err, match) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1578` `changePassword` | callback boundary | `request.user.save(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1653` `sendEmailChange` | callback boundary | `User.findByLogin(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1677` `sendEmailChange` | callback boundary | `Store.set(..., function (err) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1873` `activateAccount` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1884` `activateAccount` | callback boundary | `user.save(..., function (err) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:1886` `activateAccount` | callback boundary | `request.yar._logIn(..., function (err) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:2014` `getExportStatus` | callback boundary | `Export.findById(..., function (err, exportRecord) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |
| [ ] | `lib/controllers/users.js:2072` `downloadExport` | callback boundary | `Export.findById(..., function (err, exportRecord) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. |

### `lib/controllers/admin.js` -- 5 boundaries (0 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/admin.js:131` `uploadUsers` | callback boundary | `parse(..., function (err, records) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [ ] | `lib/controllers/admin.js:189` `updateUser` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [ ] | `lib/controllers/admin.js:196` `updateUser` | callback boundary | `user.save(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [ ] | `lib/controllers/admin.js:212` `grantRole` | callback boundary | `User.findById(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |
| [ ] | `lib/controllers/admin.js:348` (module scope) | callback boundary | `User.findByLogin(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.2. |

### `lib/controllers/courses.js` -- 2 boundaries (0 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/courses.js:194` `copy` | callback boundary | `request.pre.course.copy(..., function (err, course) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. |
| [ ] | `lib/controllers/courses.js:444` `download` | callback boundary | `process.nextTick(..., function () { ... })` -- completion callback with no parameters; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. |

### `lib/controllers/folders.js` -- 1 boundary (0 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/folders.js:245` `create` | callback boundary | `folder.save(..., function (err, savedFolder) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.8. |

### `lib/controllers/files.js` -- 2 boundaries (0 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/files.js:540` (module scope) | callback boundary | `setTimeout(..., function () { ... })` -- completion callback with no parameters; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. |
| [ ] | `lib/controllers/files.js:573` (module scope) | callback boundary | `invoke(..., function (err, value) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler: await the promise form (or a promisified wrapper) and continue with its result. Do not push the boundary into the callee. Preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.7. |

### `lib/controllers/auth.js` -- 3 boundaries (0 closed)

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/auth.js:485` `googleCallback` | callback boundary | `legacyJsonRequest(..., function (err, response, body) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler, not in the callee, and preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- take the `await` at this call site, and keep the surrounding order intact -- persist, mutate the session, then report the generic failure. That REPLACES the generic callback-boundary mandate, which is wrong here: continuing with a result on every path can drop the throw that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [ ] | `lib/controllers/auth.js:533` `googleCallback` | callback boundary | `legacyJsonRequest(..., function (err, response, profile) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler, not in the callee, and preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- take the `await` at this call site, and keep the surrounding order intact -- persist, mutate the session, then report the generic failure. That REPLACES the generic callback-boundary mandate, which is wrong here: continuing with a result on every path can drop the throw that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |
| [ ] | `lib/controllers/auth.js:554` `googleCallback` | callback boundary | `User.findByMultiple(..., function (err, user) { ... })` -- Node error-first callback; call site is not awaited | Create the `await` AT THIS CALL SITE inside the converted handler, not in the callee, and preserve the baseline's error handling exactly -- swallowed stays swallowed, fire-and-forget stays fire-and-forget. PRESERVED QUIRK, GOVERNING ACTION -- take the `await` at this call site, and keep the surrounding order intact -- persist, mutate the session, then report the generic failure. That REPLACES the generic callback-boundary mandate, which is wrong here: continuing with a result on every path can drop the throw that produces the failure. Measured outcome: the new-user path persists the user, mutates session state and THEN reports the generic failure; that sequence is reproduced, not repaired. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.3. Owned by `docs/preserved-quirks.md` §6; reproduce it, do not fix it. |

## 6. Reply chains -- 8 rows

The roster below is a **recorded baseline measurement**, not a transcription: the
three-way classification depends on which builder method ran last, and the
analysis derives the same classification independently from
`RESOLVING_BUILDER_METHODS` and `NON_RESOLVING_BUILDER_METHODS`. At `2f8712a` every entry
must be locatable by its carrier symbol and link sequence, sit at the line the
roster records, and derive the category the roster records -- and the self-check
fails if any of the three disagrees. Two independent routes to the same answer is
the point; the symbol is what makes the first of them survive an edit.

Removing the builder removes a **mechanism**, not a set of outcomes. Three of these
chains return a builder object to hapi, four resolve real responses, and one never
settles at all -- so the target disposition has to be stated per chain.

In the analysed tree the derived scan finds 0 `reply(`-rooted chains carrying `.type()`/`.bytes()` and 6 `h.response(`-rooted equivalents.

**Each row is anchored on its carrier SYMBOL, not on a line number**, and closes on
three conditions rather than one. Baseline coordinates stop being addresses the
moment a file is edited -- every one of these eight chains moved during the
conversion -- so a row is located by the exported handler or module-scope function
that contains it, by its link sequence, and, where one carrier holds two chains of
the same shape, by their order within it. The three conditions are: the legacy
`reply(`-rooted chain is gone from that carrier; the declared target shape is
present in it, where a target shape can be declared at all; and the corpus scenario
that drives the site carries a captured baseline response AND a confirming replay
verdict against this tree. The third is not ceremony, and it has two halves for a
reason. What a client received from one of these chains depended on which builder
method ran last, so "the legacy syntax is gone" says nothing about what the route
now serves; and a captured baseline alone says only what the OLD code did, which
is one side of a comparison. Both sides are required, because a row that closed on
the baseline half would be asserting a match nothing had checked.

Evidence state in this tree: `test/parity/corpus.json` reports itself captured, so rows below close on the recorded comparison.

### Never settles -- 1 chain

Only non-resolving links, and the chain's value is not returned either, so nothing ever produces a response and the branch hangs.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/files.js:98-100` `download` (legacy chain gone; no declared target shape to locate) | reply chain (never-settles) | reply(stream).type(...).bytes(...) with no `return` and no resolving call. Neither .type() nor .bytes() settles the deferred, so the image-download branch never produces a response at all -- the request hangs.; the legacy chain is gone from `download` but the declared target `h.response(...).type().bytes()` was NOT found there | **APPROVED DEVIATION.** Return `h.response(stream).type(request.pre.file.mime).bytes(request.pre.file.size)` -- and NO Content-Disposition header, because the image branch deliberately omits it. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.1; the deviation and its precedence argument by §11.1. EVIDENCE: `quirk.reply-chain.never-settles.image-download` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The outcome this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |

> **Approved deviation, `lib/controllers/files.js:98-100`.** This is the
> only row in this document whose target changes observable behaviour -- the
> migration's other approved deviation, the retained `marked` advisory, reaches no
> conversion site -- and it is
> approved rather than assumed. The response to serve is not inferred: the sibling branch four lines below performs the identical chain ending in .header() and returns a working stream response, so the target is that same response minus the Content-Disposition header the image branch deliberately omits.
>
> The precedence argument -- why R-b ("every route serves") controls over
> R-d ("behaviour improvements prohibited") here and nowhere else -- is owned by
> `docs/preserved-quirks.md` §11.1 and is not restated here.
>
> **The corpus treatment of this deviation, as it actually stands.** the corpus DEFINES the comparison: scenario `quirk.reply-chain.never-settles.image-download` drives `GET /api/files/{fileId}/{fileName}` with `intent: timeout` and `expectedDeviation.replayDisposition: approved-change`, so a baseline timeout against a target 200 stream response is expected to read as an approved change rather than a failure. The baseline response HAS been captured -- but **no replay result for the target is recorded**, so only the pre-migration half of the comparison exists. A baseline says what the unmigrated code did; it cannot say that this tree now serves the 200 stream response. The timeout-to-200 claim stays PROSPECTIVE and the row stays open until `test/parity/replay.js` records a confirming verdict.

### Header-resolved and working -- 4 chains

The chain reaches `.header(...)`, which resolves the deferred, so a real hapi response is produced. These work today and must come through the migration unchanged -- they must not become collateral damage of the approved deviation above.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/files.js:102-105` `download` (converted; target chain at `:761-764`) | reply chain (header-resolved) | reply(stream).type(...).bytes(...).header('Content-Disposition', ...). Also has no `return`, but .header() resolves the deferred, so it works and returns a real hapi response.; the legacy chain is gone from `download` and the declared target `h.response(...).type().bytes().header()` is present | Identical response. This is the non-image branch and it must NOT become collateral damage of the approved deviation four lines above. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.2; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.file-download-attachment` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The outcome this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |
| [ ] | `lib/controllers/courses.js:269-272` `returnZip` (converted; target chain at `:462-465`) | reply chain (header-resolved) | return reply(stream).type('application/zip').bytes(stats.size).header('Content-Disposition', ...), inside the rimraf callback. Works: .header() resolves the deferred.; the legacy chain is gone from `returnZip` and the declared target `h.response(...).type().bytes().header()` is present | Identical response. Baseline WAITS for the deletion callback before the final .header() resolves, so the conversion awaits fs.promises.rm, swallows its error exactly as the empty callback does, and only then returns the response. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.2; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.course-download-zip` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The outcome this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |
| [ ] | `lib/controllers/trinket.js:1383-1386` `downloadPostedZip` (converted; target chain at `:1489-1492`) | reply chain (header-resolved) | return reply(outputReadStream).type('application/zip').bytes(bytes).header('Content-Disposition', ...). Works: .header() resolves the deferred.; the legacy chain is gone from `downloadPostedZip` and the declared target `h.response(...).type().bytes().header()` is present | Identical response, including the quoted filename form this chain uses. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.2; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.posted-zip-download` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The outcome this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |
| [ ] | `lib/controllers/trinket.js:1548-1551` `downloadZip` (converted; target chain at `:1866-1869`) | reply chain (header-resolved) | return reply(outputReadStream).type('application/zip').bytes(bytes).header('Content-Disposition', ...). Works: .header() resolves the deferred.; the legacy chain is gone from `downloadZip` and the declared target `h.response(...).type().bytes().header()` is present | Identical response, including the unquoted filename form this chain uses. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.2; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.short-code-zip` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The outcome this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |

### Builder returned to hapi -- 3 chains

`return reply(...).type(type)` hands the wrapper the **builder object** rather than a hapi response. What is emitted depends on whether the deferred was already resolved earlier in the request, so the outcome has to be captured at the baseline BEFORE conversion rather than reasoned about.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/trinket.js:1204` `downloadMain` (legacy chain gone; no declared target shape to locate) | reply chain (builder-returned) | return reply(code[0].content).type(type) -- hands the WRAPPER the builder object rather than a hapi response. What is emitted depends on whether the deferred had already been resolved earlier in the request.; the legacy chain is gone from `downloadMain`, and this category declares no target shape -- what the builder emitted is not inferable from the text, so the outcome is settled by measurement alone | Reproduce the measured status, content-type and body, captured before conversion by test/parity/capture.js. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.3; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.builder-returned.download-main` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The outcome this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |
| [ ] | `lib/controllers/trinket.js:1246` `downloadFile` (legacy chain gone; no declared target shape to locate) | reply chain (builder-returned) | return reply(file.content).type(type) -- builder object returned to hapi.; the legacy chain is gone from `downloadFile`, and this category declares no target shape -- what the builder emitted is not inferable from the text, so the outcome is settled by measurement alone | Reproduce the measured status, content-type and body, captured before conversion by test/parity/capture.js. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.3; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.builder-returned.download-code-file` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The outcome this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |
| [ ] | `lib/controllers/trinket.js:1259` `downloadFile` (legacy chain gone; no declared target shape to locate) | reply chain (builder-returned) | return reply(stream).type(type) inside a .then() -- builder object returned to hapi, wrapping a stream.; the legacy chain is gone from `downloadFile`, and this category declares no target shape -- what the builder emitted is not inferable from the text, so the outcome is settled by measurement alone | Reproduce the measured status, content-type and body, captured before conversion by test/parity/capture.js. Measured baseline outcome owned by `docs/preserved-quirks.md` §4.3; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.builder-returned.download-asset` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The outcome this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |

### A distinct shape, not one of the eight chains

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/trinket.js:375` `create` (baseline coordinate; the legacy construct is no longer in this tree) | reply call, no return | reply(err); with NO `return`, inside .catch(function(err) { ... }) on an error path. The shim resolved a Boom for an Error and the handler fell off the end returning undefined, so the response came from the deferred.; the unreturned bare `reply(` is gone from `create` | Return the mapped error so the same status and payload reach the same error funnel. This is a DISTINCT shape, not one of the eight reply chains. Baseline outcome owned by `docs/preserved-quirks.md` §4.4; reproduce it, do not fix it. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **no linked scenario**. Nothing in `test/parity/corpus.json` drives this site, so the status and payload this row claims reach the same funnel rests on nothing measurable. The row cannot close until a scenario covers it. |

## 7. Stream sites -- 17 rows across 4 controllers

Derived, then reviewed -- see "Streams" above for the rule and for what it
deliberately excludes. Several of these error **after the response has begun**,
which is why every row carries the same non-negotiable target: completion and error
**timing** are preserved.

**Each row names the scenario that drives it, and a row with none cannot close.**
Whether timing survived is not a property of the text: no reading of a
`.pipe(...)` tells you whether the response still begins at the same point or
whether an error raised after it began is still swallowed. Only a driven request
does. The join is mechanical rather than asserted -- the site resolves to the
innermost symbol that contains it, that symbol resolves to the routes it answers
(through the dispatch table where the carrier is a module-scope function, with the
hops named in the row), and the routes resolve to the corpus scenarios whose
`covers` list them.

In this tree 12 of 17 stream rows resolve to at least one scenario, and 5 resolve to none: `lib/controllers/trinket.js:1763`, `lib/controllers/trinket.js:1768`, `lib/controllers/trinket.js:1804`, `lib/controllers/trinket.js:1856`, `lib/controllers/files.js:527`. An unlinked row is a gap in the corpus, not a closed row.

### `lib/controllers/trinket.js` -- 10 sites

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/trinket.js:1388` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | constructs an archive -- `var archive = archiver('zip', {` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **route-level only**. `quirk.reply-chain.header-resolved.posted-zip-download`, `route.post.api-trinkets-download.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [ ] | `lib/controllers/trinket.js:1396` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | binds a stream, creates a stream -- `, outputWriteStream = fs.createWriteStream(zipFile)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **route-level only**. `quirk.reply-chain.header-resolved.posted-zip-download`, `route.post.api-trinkets-download.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [ ] | `lib/controllers/trinket.js:1424` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | pipes a stream -- `archive.pipe(outputWriteStream);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **route-level only**. `quirk.reply-chain.header-resolved.posted-zip-download`, `route.post.api-trinkets-download.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [ ] | `lib/controllers/trinket.js:1477` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | binds a stream, creates a stream -- `outputReadStream = fs.createReadStream(zipFile);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **route-level only**. `quirk.reply-chain.header-resolved.posted-zip-download`, `route.post.api-trinkets-download.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [ ] | `lib/controllers/trinket.js:1489` `downloadPostedZip` -- serves `POST /api/trinkets/download` | stream site | hands a stream to the response -- `return h.response(outputReadStream)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.posted-zip-download` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The preserved timing this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |
| [ ] | `lib/controllers/trinket.js:1763` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | constructs an archive -- `var archive = archiver('zip', {` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **no linked scenario**. Nothing in `test/parity/corpus.json` drives this site, so the preserved timing this row claims rests on nothing measurable. The row cannot close until a scenario covers it. |
| [ ] | `lib/controllers/trinket.js:1768` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | binds a stream, creates a stream -- `, outputWriteStream = fs.createWriteStream(zipFile)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **no linked scenario**. Nothing in `test/parity/corpus.json` drives this site, so the preserved timing this row claims rests on nothing measurable. The row cannot close until a scenario covers it. |
| [ ] | `lib/controllers/trinket.js:1804` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | pipes a stream -- `archive.pipe(outputWriteStream);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **no linked scenario**. Nothing in `test/parity/corpus.json` drives this site, so the preserved timing this row claims rests on nothing measurable. The row cannot close until a scenario covers it. |
| [ ] | `lib/controllers/trinket.js:1856` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | binds a stream, creates a stream -- `outputReadStream = fs.createReadStream(zipFile);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: **no linked scenario**. Nothing in `test/parity/corpus.json` drives this site, so the preserved timing this row claims rests on nothing measurable. The row cannot close until a scenario covers it. |
| [ ] | `lib/controllers/trinket.js:1866` `downloadZip` (module-scope function, reached via `supportedDownloadFormats` -> `getByShortCode`) -- serves `GET /{lang}/{shortCode}` | stream site | hands a stream to the response -- `return h.response(outputReadStream)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.10. EVIDENCE: `quirk.reply-chain.header-resolved.short-code-zip` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The preserved timing this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |

### `lib/controllers/users.js` -- 2 sites

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/users.js:1431` `assetUploadFromURL` -- serves `POST /api/users/assetFromURL` | stream site | binds a stream, creates a stream -- `writeStream = fs.createWriteStream(tmpPath);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. PRESERVED QUIRK -- a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. EVIDENCE: **route-level only**. `error-edge.asset-from-url.midstream-failure`, `error-edge.asset-from-url.query-bearing-url`, `error-edge.asset-from-url.transport-refused`, `route.post.api-users-assetFromURL.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [ ] | `lib/controllers/users.js:1552` `assetUploadFromURL` -- serves `POST /api/users/assetFromURL` | stream site | pipes a stream -- `body.pipe(writeStream);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.11. PRESERVED QUIRK -- a refused connection logs and leaves the route UNSETTLED; the conversion must not turn that into a rejection. Owned by `docs/preserved-quirks.md` §8.1; reproduce it, do not fix it. EVIDENCE: **route-level only**. `error-edge.asset-from-url.midstream-failure`, `error-edge.asset-from-url.query-bearing-url`, `error-edge.asset-from-url.transport-refused`, `route.post.api-users-assetFromURL.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |

### `lib/controllers/courses.js` -- 2 sites

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/courses.js:432` `returnZip` (module-scope function, reached via `download`) -- serves `GET /{userSlug}/courses/{courseSlug}/download.zip` | stream site | binds a stream, creates a stream -- `var stream = fs.createReadStream(zipFile);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. PRESERVED QUIRK -- the unauthorized branch evaluates an UNBOUND `Boom`, so it throws a `ReferenceError` before any response is constructed and the route answers 500 where the expression names 403. Owned by `docs/preserved-quirks.md` §9.7; reproduce it, do not fix it. EVIDENCE: **route-level only**. `quirk.reply-chain.header-resolved.course-download-zip`, `route.get.userSlug-courses-courseSlug-download-zip.html`, `route.get.userSlug-courses-courseSlug-download-zip.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [ ] | `lib/controllers/courses.js:462` `returnZip` (module-scope function, reached via `download`) -- serves `GET /{userSlug}/courses/{courseSlug}/download.zip` | stream site | hands a stream to the response -- `return h.response(stream)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.6. PRESERVED QUIRK -- the unauthorized branch evaluates an UNBOUND `Boom`, so it throws a `ReferenceError` before any response is constructed and the route answers 500 where the expression names 403. Owned by `docs/preserved-quirks.md` §9.7; reproduce it, do not fix it. EVIDENCE: `quirk.reply-chain.header-resolved.course-download-zip` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The preserved timing this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |

### `lib/controllers/files.js` -- 3 sites

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [ ] | `lib/controllers/files.js:527` `finish` (module-scope function) -- **no route resolved** | stream site | pipes a stream -- `source.pipe(replay);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.7. EVIDENCE: **no linked scenario**. Nothing in `test/parity/corpus.json` drives this site, so the preserved timing this row claims rests on nothing measurable. The row cannot close until a scenario covers it. |
| [ ] | `lib/controllers/files.js:690` `download` -- serves `GET /api/files/{fileId}/{fileName}` | stream site | binds a stream -- `var stream = FileUtil.downloadMaterialFile(remoteFile);` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.7. EVIDENCE: **route-level only**. `quirk.reply-chain.header-resolved.file-download-attachment`, `quirk.reply-chain.never-settles.image-download`, `route.get.api-files-fileId-fileName.json` in `test/parity/corpus.json` cover the route this site is reached from, but none of them is pinned to THIS site: route-level coverage cannot distinguish it from the other sites the same route reaches, and some of those sit on branches that are mutually exclusive with each other. So those scenarios are candidates, not proof of the preserved timing this row claims. The row closes when a scenario is pinned to this site -- by a reply chain located around it -- and that scenario carries a captured baseline and a confirming replay. |
| [ ] | `lib/controllers/files.js:761` `download` -- serves `GET /api/files/{fileId}/{fileName}` | stream site | hands a stream to the response -- `return h.response(stream)` | Preserve completion and error TIMING exactly: the same event ordering, the same point at which the response begins, and the same behaviour for an error raised after it has begun. Awaiting a stream that baseline did not await, or surfacing an error baseline swallowed, is a behaviour change. Error mapping (status, payload, side effects, timing): `docs/error-edge-inventory.md` §7.7. EVIDENCE: `quirk.reply-chain.header-resolved.file-download-attachment` in `test/parity/corpus.json` carries a captured baseline response, but **no replay result for the target** -- so what is recorded is what the PRE-MIGRATION code did, which is one half of a comparison. The preserved timing this row claims stays open until `test/parity/replay.js` records a confirming verdict for that scenario against this tree. |

A stream row closes on measured behaviour of ITS OWN BRANCH or not at all. Two
things have to hold. First a scenario must be pinned to this branch: the pin is
derived from the structurally located reply chain that CONTAINS the site, not from
the routes the carrier is reached from. That distinction is load-bearing --
`files.download` reaches three stream sites across two mutually exclusive branches,
so a result for the inline-image branch is silent about the attachment branch, and
spreading one route-level scenario across both would tick a row nothing exercised.
A site that cannot be placed on a branch -- one that builds the stream BEFORE the
branch, for instance -- reports its route-level scenarios as candidates and stays
open. Second, the pinned scenario must carry a captured baseline AND a confirming
replay verdict. Until both hold the row stays open with the reason stated in it,
which is the difference between an open checklist item and an unsupported claim.

## 8. EXCLUDED -- defined-but-unrouted controller exports (2)

**Not counted in the 154.** These are exported handlers
that no route references, so under blocking-only scope they are not hapi-facing
work. They convert only if another gate independently forces it, and that
determination is **recorded per function below rather than assumed**. Two gates can
force one: the zero-deprecation-warning bar, and the repaired test suite.

**The `Done` box means something different in this section and the next**: it is
ticked when the recorded determination is that no gate forces this function into
scope, and left open when one does. It is not a conversion claim -- an unrouted
function in its legacy shape is finished work under blocking-only scope.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/controllers/admin.js:115` `uploadForm` | unrouted export (excluded) | legacy `(request, reply)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws; no route declaration references `admin.uploadForm` | **DO NOT CONVERT -- no gate forces it under blocking-only scope.** deprecation bar: no deprecated Node-core API in the body; repaired suite: no reference from test/lib or test/helpers. Leave as it is; R-a confines the diff to conversion work that something requires. |
| [ ] | `lib/controllers/pages.js:73` `features` | unrouted export (excluded) | legacy `(request, reply)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws; no route declaration references `pages.features` | **CONVERT -- an independent gate forces it.** deprecation bar: no deprecated Node-core API in the body; repaired suite: referenced by test/lib/api/files.js, test/lib/api/trinket.js. Convert to `async function (request, h)` returning on every path. |

## 9. EXCLUDED -- unrouted named pre-handlers (3)

**Not counted in the 154.** Same conditional basis as the
section above: no route references them, so nothing forces their conversion unless
a gate does. Recorded, not assumed.

| Done | Site | Kind | Current shape | Target disposition |
| --- | --- | --- | --- | --- |
| [x] | `lib/util/helpers.js:306` `toLowerCaseURI` | unrouted pre-handler (excluded) | legacy `(request, reply)`; returns its response -- all 2 signalling calls delivered: 2 returned directly; every path through the body delivers a value or throws; still consumes the shim `reply`; no route declaration references `helpers.toLowerCaseURI` | **DO NOT CONVERT -- no gate forces it under blocking-only scope.** deprecation bar: no deprecated Node-core API in the body; repaired suite: no reference from test/lib or test/helpers. Leave as it is. |
| [x] | `lib/util/helpers.js:329` `logUnauth` | unrouted pre-handler (excluded) | legacy `(request, reply)`; returns its response -- all 1 signalling call delivered: 1 returned directly; every path through the body delivers a value or throws; still consumes the shim `reply`; no route declaration references `helpers.logUnauth` | **DO NOT CONVERT -- no gate forces it under blocking-only scope.** deprecation bar: no deprecated Node-core API in the body; repaired suite: no reference from test/lib or test/helpers. Leave as it is. |
| [x] | `lib/util/helpers.js:422` `trinketByOwnerAndSlug` | unrouted pre-handler (excluded) | legacy `(request, reply)`; RELIES ON THE INTERCEPTION -- 2 of 3 signalling calls produce a value that never leaves the function (0 unreturned in the body, 2 dropped inside a nested function); every path through the body delivers a value or throws; still consumes the shim `reply`; no route declaration references `helpers.trinketByOwnerAndSlug` | **DO NOT CONVERT -- no gate forces it under blocking-only scope.** deprecation bar: no deprecated Node-core API in the body; repaired suite: no reference from test/lib or test/helpers. Leave as it is. |

One of these -- `trinketByOwnerAndSlug` -- carries a `reply().redirect(...)
.permanent().takeover()` chain of the same shape as the two dead 301s in section 2,
and `toLowerCaseURI` carries a fourth `.permanent()` chain of a different shape
(`reply('')` settles the deferred immediately and there is no `.takeover()`).
Neither is in the conversion set, because neither is routed. They are named here so
that a file-wide search for `.permanent()` in `lib/util/helpers.js` -- which finds
four hits, not two -- does not read as unfinished work.

## 10. EXCLUDED -- routes whose named controller method does not exist (3)

**Not counted in the 154, and there is nothing to convert:
these are routes with no function.** They are recorded so that nobody hunts for a
handler that was never written.

All three answer entirely through the wrapper's missing-controller fallback at
`lib/util/routeParser.js:574-576`, which returns `request.success(request.params)`.
**That fallback must be PRESERVED.** It sits immediately below the interception
block being removed and is easy to delete by association -- which would turn all
three of these routes into failures.

| Done | Route | Named binding | Declared at | Behaviour to preserve |
| --- | --- | --- | --- | --- |
| [x] | `POST /api/interest` | `pages.interest` -- **not defined** | `config/api_routes.js:1090` | Resolves through the preserved fallback at `lib/util/routeParser.js:574-576`, returning `request.success(request.params)`. Same status, same payload, same template resolution as today. Baseline outcome owned by `docs/preserved-quirks.md` §1. |
| [x] | `GET /api/trinkets/popular` | `trinket.mostActive` -- **not defined** | `config/api_routes.js:714` | Resolves through the preserved fallback at `lib/util/routeParser.js:574-576`, returning `request.success(request.params)`. Same status, same payload, same template resolution as today. Baseline outcome owned by `docs/preserved-quirks.md` §1. |
| [x] | `GET /api/trinkets/active` | `trinket.risingActive` -- **not defined** | `config/api_routes.js:729` | Resolves through the preserved fallback at `lib/util/routeParser.js:574-576`, returning `request.success(request.params)`. Same status, same payload, same template resolution as today. Baseline outcome owned by `docs/preserved-quirks.md` §1. |

The `Done` box here means "the route declaration is present and the fallback is
still what serves it" -- these rows are a **preservation** check, not conversion
work.

## Retained by design -- never a removal row

R-a confines the diff to four things: the runtime bump, the hapi API migration, the
async conversion, and blocking-only dependency swaps. The following are explicitly
**retained**, and if any of them ever shows up in this document as work to be done,
the generator is wrong -- not the code.

| Retained | Why it must not be touched |
| --- | --- |
| Per-request debug logging at `lib/util/routeParser.js:311`, `:543-552`, `:556` | Explicitly retained. Performance is not a goal of this migration, so there is no licence to remove logging along the way. Recorded in `docs/preserved-quirks.md` §9.3. |
| The missing-controller fallback at `lib/util/routeParser.js:574-576` | Three registered routes answer through it (section 10). It is adjacent to the interception block being removed, which is the whole risk. |
| `request.success` (`:412-479`), `request.fail` (`:482-514`) and the hand-rolled validation block (`:516-541`) | Reshaped to *return* their response, but their projection, flash-and-redirect behaviour and validation outcomes are invariant. |
| The handler catch-all at `lib/util/routeParser.js:578-589`, including its `if (err)` guard | A falsy throw produces no return and the toolkit converts `undefined` to the same status with a different message. That is observable, so the guard is copied verbatim. |
| The route DSL, the `route.config` -> `route.options` rename, the forced `cors = false` and `delete route.options.validate` | The route surface is an invariant of this migration. |
| The cross-request `fail.redirect` state leak | A measured baseline defect with a documented blast radius. R-d preserves it; two consecutive corpus requests exist so it cannot be silently fixed. Recorded in `docs/preserved-quirks.md` §3. |
| The two validation message maps in `config/routes.js` | Measured inert on both joi 17.13.3 and 18.2.5. Inert is the baseline behaviour, so inert is the target. Recorded in `docs/preserved-quirks.md` §9.1. |

No row in this document proposes a rename, a cleanup, or a "while we're here".
Every target disposition is the **preserved** behaviour, with exactly one approved
exception among these rows, labelled as such in section 6 and argued in
`docs/preserved-quirks.md` §11.1. The migration approves
**two** deviations; the second (`docs/preserved-quirks.md` §11.2)
is an audit result and reaches no row in this document.
