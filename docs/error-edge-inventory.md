<!-- Generated output. Every value in this block is a function of the analysed commits, not of the machine or the moment: see --provenance-out for the volatile physical detail. -->
# Error-edge inventory

| | |
|---|---|
| Analysed tree | the repository this generator lives in (the target worktree) |
| Analysed tree HEAD | `ef246cf0e05030ff447fd0f4201f5dd90c4ebee1` - Identify the asset-fetch error edge per tree in the error-edge generator |
| Is the R-f baseline commit | no (baseline is `2f8712a112db46f923918c4507c75abc732d83d0`) |
| Compared against | a worktree at the R-f baseline commit `2f8712a` |
| Compared-against HEAD | `2f8712a112db46f923918c4507c75abc732d83d0` - chore: extend catalog tags (security, web-app) |
| Generator | `test/parity/error-edges.js` |
| Generator blob | `9fa6fdee8a18202c71a15fb286ac2ffabb2e4d44` - retrieve the exact source that ran with `git cat-file blob 9fa6fdee8a18` |
| Generator commit | `ef246cf0e05030ff447fd0f4201f5dd90c4ebee1` - verified: its tree holds `test/parity/error-edges.js` as the blob above (`git cat-file -e ef246cf0e050` resolves it) |
| Node major | `v22` |
| Token self-check | reported only (`--counts-check=auto`) |
| Closure comparison | run against the baseline worktree, reported |
| Scenario coverage | not joined - no corpus supplied |
| Rows emitted by this run | 373 |
| Reproduce with | `node test/parity/error-edges.js --baseline "$BASELINE" --app . --out docs/error-edge-inventory.md --counts-check auto` |

<!-- The same facts, machine-readable, so a verifier reads them without
     parsing the table above. Consumed by
     `node test/parity/manifest.js --verify-provenance <this file>`. -->
<!-- provenance-json: {"analysedTree":{"head":"ef246cf0e05030ff447fd0f4201f5dd90c4ebee1","headShort":"ef246cf","isBaselineCommit":false,"subject":"Identify the asset-fetch error edge per tree in the error-edge generator","worktreeState":"dirty"},"artifact":"error-edge-inventory.md","baselineCommit":"2f8712a112db46f923918c4507c75abc732d83d0","bodyDigest":{"algorithm":"sha256","canonicalization":"the document with its provenance-json line removed and trailing whitespace stripped per line, hashed as utf8","value":"680496ec36a59d484e97520a18fd7f5156a1cb6690ea36ef652acec153dd696c"},"delivered":{"head":"ef246cf0e05030ff447fd0f4201f5dd90c4ebee1","headShort":"ef246cf","isBaselineCommit":false,"subject":"Identify the asset-fetch error edge per tree in the error-edge generator","worktreeState":"dirty"},"detail":{"analysedSourcesDirty":false,"analysedTreeLabel":"the repository this generator lives in (the target worktree)","artifactLabel":"tool:docs/error-edge-inventory.md","closureMode":"run against the baseline worktree, reported","comparedAgainstHead":"2f8712a112db46f923918c4507c75abc732d83d0","countsCheck":{"allMatch":false,"applied":false,"mode":"auto"},"coverageMode":"not joined - no corpus supplied","filesAnalysed":12,"invocation":"node test/parity/error-edges.js --baseline \"$BASELINE\" --app . --out docs/error-edge-inventory.md --counts-check auto","rowsEmitted":373},"generator":{"blob":"9fa6fdee8a18202c71a15fb286ac2ffabb2e4d44","commit":"ef246cf0e05030ff447fd0f4201f5dd90c4ebee1","commitState":"contains-this-exact-source","deliveredHead":"ef246cf0e05030ff447fd0f4201f5dd90c4ebee1","deliveredHeadShort":"ef246cf","matchesDeliveredHead":true,"path":"test/parity/error-edges.js","verified":true},"provenanceSchema":2,"role":"analysis","runtime":{"node":"v22.23.2","platform":"linux"}} -->

<!-- /provenance -->

> **This file is generated output. Do not hand-edit it.** Every line below is
> written by `test/parity/error-edges.js` from the analysed tree named above;
> an edit made here is lost on the next run and, worse, is indistinguishable
> from a measurement while it survives. To change what this document says,
> change the generator or the tree and re-run the exact command in the
> provenance block.
>
> The HEAD above is the commit whose sources these rows describe, which is
> the commit BEFORE the one that carries this file: generating the document
> and committing it cannot happen in the same revision. Regenerating after
> that commit changes this one line and nothing else, because the annotation
> beside it is scoped to the analysed sources rather than to the whole
> checkout - so an uncommitted controller edit is reported here, and an
> uncommitted document is not.
>
> Everything in this file, including the block above, is a pure function of
> the analysed commits. There is no wall clock in it and no path from the
> machine that produced it, so two runs over the same commits - on any two
> machines - produce byte-identical output and `diff` reviews the tree
> rather than the run. The volatile physical detail of a run is written by
> `--provenance-out` to a sidecar, which is not this artifact.

## 1. What this is, and what its length does not mean

Rule R-e requires every converted path to preserve its error-to-response
mapping - same status codes, same error payload shapes. The migration
preserves three shared funnels verbatim, and that is **necessary but not
sufficient**: the funnels are reached from hundreds of local branches, each
branch decides *which* funnel its error reaches, and some reach none. This
document is one checkbox row per changed error edge, so the claim "the error
mappings survived" can be inspected rather than asserted.

**The row count is not a target and is not authoritative.** This run emitted 373 rows over 12 files. A different tree
yields a different number, and that is the intended behaviour: conversion
closes rows.

**293 of the 402 rows are closed** against this tree, **90** are open and **19** carry no mapping either tree can be held to.
Every figure in this document for that quantity is this one: section 8's verdict table partitions these same 90 open rows, and `--closure-gate` counts them with the same predicate.
Of the open rows 47 carry a baseline edge (of 342) and 67 carry a target edge (of 373); a row new in the target has no baseline edge, so the two
sides have their own denominators rather than sharing one.
A row is closed when the edge produces the same observable outcome on both
trees - same funnel, same served status, same answers-or-does-not - so a
ticked box is a comparison and not an opinion. Section 8 lists every open
row with both outcomes side by side, and section 9 says which rows a
measured request actually reached, because a closed row is a reading of two
trees and not a response anyone observed.

### What is in scope, and what deliberately is not

The analysed surface is the 12 files hapi itself invokes into: the ten
controllers, the named pre-handlers in `lib/util/helpers.js`, and the one
inline pre-handler in `config/api_routes.js`. Every one of them is read,
and a file with no row says so in its own section rather than being omitted.

Nothing else is reviewed. Rule R-a requires the diff to read as a runtime bump,
a hapi API migration, an async conversion and blocking-only dependency swaps -
so this is a record of the error edges **that conversion moves**, not a general
audit of the application's error handling. The internal callback modules keep
their callback interfaces under rule T-3, which puts the `await` boundary at the
lifecycle method rather than pushing it into the utility, so their internal
error edges are not conversion sites and carry no row here. Where a converted
handler awaits one of them, the edge that appears below is the handler's own.

### The sizing metric is not the row count

AAP 0.1.1.1 records **132 functions with an `err` parameter** in `lib/controllers/**`. That figure is a
**sizing metric and nothing else.** A crude grep over the baseline tree returns
133 rather than 132 depending on the pattern, which is itself the point:

- one callback can carry **zero** error dispositions - it never inspects `err` -
  or **several**: a guard, a log, and a late resolve;
- promise `.catch` handlers and synchronous `throw` statements add edges
  **entirely outside** that count;
- a `reply(` site is an error edge only when what it carries is an error, and at
  baseline 61 of the 202 sites carry success payloads.

This run counted 131 `err`-parameter functions in the ten controllers and
emitted 373 rows. Neither number derives from the other. Do not read the row
count as a coverage figure for the sizing metric, or the sizing metric as a
target for the row count.

## 2. The three shared funnels

Every row names the funnel its edge reaches. Line ranges below are read from
the analysed tree, not quoted from the plan, so they are correct for whichever
tree this document describes.

### Layer 1 - the handler catch-all, `lib/util/routeParser.js:1088-1099`

Logs `err.stack` when present, else `String(err)`, and returns
`Boom.badImplementation(err.message || String(err))` - so **500**. After the
async conversion this becomes structural: a rejecting `async` handler lands
here without anything having to call it.

Two properties of this block decide many rows below.

1. **The `if (err)` guard at `:1089` is preserved.** A falsy throw satisfies the `catch` but
   fails the guard, so the block produces **no return**, and hapi's toolkit
   converts that `undefined` into the same status with a different message.
2. **It does not test `isBoom`.** A `throw Boom.notFound()` inside a
   handler is therefore answered **500** with Boom's hidden 5xx body, not 404,
   while `return Boom.notFound()` is answered 404. Same value, different verb,
   different status. Rows distinguish the two.

### Layer 2 - `request.fail`, `lib/util/routeParser.js:614-683`

Logs at info level, then selects one of three responses:

- negotiated `html` **and** a configured `fail.redirect`: flashes `failure` (`:630`),
  **interpolates `fail.redirect` in place** (`:655`) - the cross-request state leak,
  because the object being mutated was captured once at parse time - flashes
  `payload` and `query`, and redirects;
- negotiated `html`, a configured `fail.html` and no `.json` extension on the
  path: renders that view after `addUserContext`;
- otherwise: defaults the payload to `{}`, attaches `json.flash`, and returns
  `h.response(json)`.

None of those three is a Boom, so a Layer 2 response takes Layer 3's non-Boom
branch and **does** receive the cache headers.

The in-place interpolation is the **cross-request state leak**: the `fail`
object was captured once at parse time and the handler closure holds it by
reference, so the assignment mutates the long-lived object and a later request
to the same route redirects to the earlier request's target. It is catalogued,
with its measurement and its blast radius, in `docs/preserved-quirks.md` section 3.
R-d requires it: rows on this funnel preserve the mutation rather than
repairing it.

### Layer 3 - `onPreResponse`, `app.js:202-260`

For a Boom response it computes `isApiRequest` at `:217` from three tests: a path
beginning `/api/`, an `Accept` containing `application/json`, or a path
beginning `/partials/`.

**Branch order matters, and the code's shape misleads on it.** For a browser
HTML request the extension **returns immediately** on 401 (`h.redirect('/login')`
with `.takeover()` at `:229`), on 404, on 403 and on any status >= 500 -
**before** the header assignments that begin at `:239`. So:

- `Cache-Control`, `Pragma`, `Expires` and the conditional `X-Frame-Options`
  reach **API/JSON error responses and non-Boom responses only** (the non-Boom branch re-applies them at `:250`);
- they do **not** reach the rendered HTML error pages;
- a Boom status outside that set - a **400**, for instance - falls through the
  early returns and **does** receive them.

This is a correction R-e forced on an earlier reading of the code's shape. It
is stated here so nobody re-derives it wrongly from the source order.

`X-Frame-Options: deny` applies only to the 5 paths at `config/default.yaml:353-358`: `/`, `/login`, `/signup`, `/contact`, `/educators`.

### Not a funnel, but decisive: how a pre-handler error is answered, `lib/util/routeParser.js:427-518`

Pre-handlers are not invoked by the handler wrapper, so **their errors never
reach Layer 1**. What answers them instead depends on whether the analysed
tree still carries the response emulation, and this tree **does not**.

`convertPreHandlers` passes a function-valued pre-handler to hapi as it is,
so hapi's own contract applies. Read from the installed framework rather
than remembered:

- a returned or thrown **Boom** keeps its own status. `Response.wrap` returns
  it unchanged, `isBoom` routes it to the route's pre `failAction`, and the
  default `'error'` throws it - so **Layer 3** answers with that status
  (`@hapi/hapi/lib/handler.js`, the `if (response.isBoom)` branch of the pre
  path).
- a returned **plain `Error`** is **not** resolved as the pre value. It is
  boomified - `Response.wrap` calls `Boom.boomify` on any `Error` - and then
  takes the same path, so the request is answered **500** through Layer 3.
  **This is where the two contracts differ, and it is the reason a
  pre-handler that used to hand an `Error` on as its assigned value must
  now return a non-`Error` value to keep the request continuing.**
- returning **`undefined`** - falling off the end of the function - becomes
  `Boom.badImplementation`, so a pre-handler with nothing to contribute must
  `return null`, which is a pre value of `null` and not an error.

The string form is not a lifecycle method at all: the dispatcher wraps it in
its own `async (request, h)` and returns `serverMethod.apply(null, args)`, so
a server method's throw rejects that wrapper and reaches Layer 3 the same
way. Rows on the `server method` surface say so.

## 3. Two dispositions a mechanical conversion changes silently

These two are called out because converting them correctly looks like
converting them wrongly. Both appear as row dispositions below.

### log-and-continue

The handler logs and **returns nothing**, so the surrounding flow continues and
this edge produces no response. Mechanically converting such a handler into a
rejection - or into a returned Boom - turns an edge that produces no response
into one that does, and changes what a client receives from whatever else
settles the request into a 500.

The clearest instance is the streaming asset fetch, whose `error` listener only
`console.log`s. Two measured details belong with it, because they are different
failure modes with different outcomes:

- a **refused connection** emits `error` and **not** `end`, so the upload
  continuation never runs, **nothing is uploaded at all**, and the request is
  left **unsettled**. An earlier reading of this site had it uploading partial
  bytes; that was wrong, and the distinction matters because the two failure
  modes need different fixtures and produce different observable results;
- a **mid-stream failure after `response`** may still reach `end`, so *there*
  the continuation does run, on a partial file.

Both are baseline behaviour and both are preserved.

### resolve-on-later-callback

The response is produced inside a continuation-passing callback, so it settles
only when that operation calls back, and what a client receives today is
whichever settlement wins. These must **not** be collapsed into an earlier
`await`: rule T-3 puts the await boundary at the call site, and moving the
settlement earlier can change the response itself. Where such a callback also
declares no error parameter, the operation's own failure is dropped, and the row
says so.

## 4. How to read a row

```
  ... - [ ] `<file>:<line>` - <Disposition> - <Funnel> - <Shape>
  ...       Carrier <reachable code path> - Routes <bound routes> - Source <the line>
  ...       Target: <the outcome to preserve, ending in Side effects and Timing>
  ...       Note: <a measured detail, when there is one>
  ...       See also: <the sibling document that catalogues this edge>
```

A real row is a line beginning `- [ ] ` or `- [x] ` at column 0, and its
continuation lines are indented six spaces and begin `Carrier `, `Target: `,
`Closure: `, `Driven: `, `Note: ` or `See also: `. Every line of the sketch
above carries a `... ` prefix that a real row does not, so counting lines
matching `^- \[[ x]\] ` returns the row count this document states and
nothing else - the sketch does not inflate it by one.

### The join contract

**Every row carries a stable id**, printed in the row as `` `id=...` `` and
shaped `<file>.<carrier>.<class>.<ordinal>`. It deliberately contains no
line number, no disposition and no funnel, because conversion changes all
three: a `reply(err)` site becomes `return errors.notFound()` and moves from
`calls reply(err)` to `returns or throws a Boom`, so an id
carrying either would never match its own target row - and matching a
baseline row to its target row is the one comparison the id exists to make.

That id is the join key in both directions:

| Consumer | How it joins |
|---|---|
| a reviewer | `grep 'id=<the id>'` in this file |
| `test/parity/capture.js` | put the id in a scenario's **`coversEdges`** array - NOT in `covers`; this generator then reports that row as **edge-level** driven |
| `test/parity/replay.js`, or any tool | read the machine-readable index this generator writes with `--edge-index <path>`: schema `trinket-oss/error-edge-index@3`, holding one record per TARGET row and one per BASELINE row, each with the id, file, carrier, surface, class, disposition, funnel, served status, the dimensions that differ, the closure verdict, the pairing tier that produced it, any prescribed mechanism transition, and the driver - plus an `unpaired` block naming every row missing from the target, new in the target, unpairable, or proven unreachable. Schema 2 carried a `renumberedOrdinal` field for the ordinal-based pairing this generator no longer performs |

**`coversEdges`, and not `covers`.** `test/parity/capture.js` validates every
`covers` entry against the route manifest and reports anything that is not a
declared route as `unknownRoutes`, which it treats as a defect in its own
tables. So a scenario naming an edge id in `covers` fails the producer, and
an earlier edition of this document told authors to put them there - a join
contract the producer cannot satisfy. `coversEdges` is a field capture.js
does not read, so a scenario carries it without tripping that validation,
and it is the field this generator names. The change that closes the join is
one line per scenario in the `error-edge.*` groups of
`test/parity/capture.js` - `coversEdges: ['<edge id>']` alongside the
existing route-keyed `covers` - and that file belongs to the
capture-scenario unit, not to this generator.

**Edge-level coverage does not wait for that.** This generator carries its
own binding table, resolved and VALIDATED at generation time against the
corpus, which is why section 9 reports 0 rows driven at edge level
today rather than 0. A binding names the edge by the same stable semantic
identity the pairing uses - file, routed carrier and guarded subject - and
the scenario by its corpus id, and generation FAILS if the pair does not
resolve to exactly one edge per tree or if the scenario does not exist or
does not drive a route the edge is reachable from. A binding that rots is
therefore fatal rather than silently downgraded, which is the property that
makes it evidence. `coversEdges` remains the route for the corpus unit to
add more without touching this file, and both joins are read.

The remaining rows report route-level coverage, which is weaker: a scenario
that reaches a route does not establish that it reached a particular error
branch within it.

So this document is no longer the only carrier of its own contents, and a
consumer never has to parse the prose. The previous edition of this file
said "Nothing machine-reads this document", and that was the problem rather
than a design note: with 341 rows and no key, the corpus could not state
which edge a scenario reached, and no tool could report that a changed edge
had never been driven.

**Disposition** is a closed vocabulary and every row carries exactly one
value from it. Six of the values are the ones AAP 0.6.3 enumerates:

- `calls request.fail locally`
- `calls reply(err)`
- `returns or throws a Boom`
- `logs and continues`
- `swallows silently`
- `resolves on a later callback`

The seventh is an addition this generator makes, and it is called out rather
than slipped in:

- `propagates to its caller`

It exists because none of the six describes a callback that hands its error
to an outer continuation - `reject(err)`, `resolve({err : err, ...})`,
`next(err)` - and R-e's deliverable is worth having only if its rows are
true. Filing such an edge under `swallows silently` states two
false things at once: that the error is absorbed, and that no response can
follow from it. Filing a callback that logs *and* rejects under
`logs and continues` states a third, that the normal path
continues, when the continuation is a rejection. The value takes precedence
over both, and each such row names the vehicle and the line so the hop can
be checked against the source.

**Shape** carries the sub-shape that six values cannot express - `reply(err)`
with no return, a bare `.catch(request.fail)` reference, a synchronous throw, a
CPS callback boundary. Shape is descriptive and open; Disposition is closed.

**Funnel** is `Layer 1`, `Layer 2`, `Layer 3` or `none`. `none` is a legitimate and
important value: an edge that produces no response at all is the edge most
likely to be given one by accident.

**Target** states the outcome to **preserve** - status, payload or redirect,
side effects, timing. **No row proposes a fix.** R-d prohibits improvements, so
where the baseline outcome is a defect the row requires the defect.

**Carrier** is the reachable code path - the exported handler or pre-handler
the edge sits in - and **Routes** are the literal route declarations that bind
it. Every row carries a carrier, so every row is drivable; see section 11.

**See also** appears on a row whose edge is also catalogued elsewhere, and is
the only form of duplication this document permits.

### What this document does not own

Three documents describe the same edges from different angles, and each claim
belongs to exactly one of them:

| Document | Owns |
|---|---|
| this document | the per-edge status, payload or redirect, side effects and timing, and the funnel each edge must still reach |
| `docs/preserved-quirks.md` | why a baseline defect is kept, its measurement, and the two approved deviations |
| `docs/conversion-inventory.md` | the per-site return/await disposition for every handler body the conversion touches |

So a row states the outcome to preserve and points at the quirk catalogue for
the reasoning; it does not restate it. Every row whose Target carries the
return-discipline sentence is also a conversion site, and the call sites where
a missing `return` is what makes the conversion decisive carry an explicit
`See also` to `docs/conversion-inventory.md`.

## 5. Token counts in the analysed tree

The scanner's counts against the figures measured at `2f8712a` with `grep -o | wc -l`,
which is an independent witness because it does not use the scanner.

| Token | Baseline | This tree | |
|---|---:|---:|---|
| reply( in lib/controllers/*.js | 172 | 0 | differs |
| reply( in lib/util/helpers.js | 29 | 6 | differs |
| reply( in config/api_routes.js | 1 | 0 | differs |
| reply( total | 202 | 6 | differs |
| .then( in lib/controllers/*.js | 183 | 195 | differs |
| .catch( in lib/controllers/*.js | 85 | 92 | differs |
| `function (request, reply)` declarations (fingerprint) | 145 | 2 | |
| `function (request, h)` declarations | 2 | 145 | |

**Those are CALL-SITE counts, and they are not error-edge counts.** The
distinction is not pedantry: a `reply(` site is an error edge only when what
it carries is an error, and the table above counts the token either way.
`config/api_routes.js` is the clearest case. It contributes exactly one
`reply(` site, which is why the row above reads 1 - and that site is
`return reply(true)`, an inline pre-handler answering with a boolean. It
carries no error, so it produces **no error-edge row**, and the file's
section below correctly holds none. A reconciliation that treated the site
count as evidence of edge coverage would have reported that file as covered
on the strength of a row that does not exist.

| | Sites (tokens) | Error-edge rows |
|---|---:|---:|
| `lib/controllers/*.js` | 0 `reply(` | 346 |
| `lib/util/helpers.js` | 6 `reply(` | 27 |
| `config/api_routes.js` | 0 `reply(` | 0 |

The counts were **reported, not asserted** because the analysed tree was not detected as the baseline: the legacy-handler fingerprint is 2 against a floor of 100, its HEAD is not the baseline commit, and the token counts already differ.
A differing count here is the conversion, not a scanner fault: the legacy
idiom is what these tokens measure and removing it is the work. The
scanner's own correctness is guaranteed independently, by 68 self-tests that
run on every invocation and include the regex-literal-with-quotes hazard.

## 6. Summary

| File | Rows | calls request.fail locally | calls reply(err) | returns or throws a Boom | logs and continues | propagates to its caller | swallows silently | resolves on a later callback |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| `config/api_routes.js` | 0 | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| `lib/controllers/admin.js` | 22 | 7 | 0 | 7 | 1 | 2 | 1 | 4 |
| `lib/controllers/auth.js` | 9 | 4 | 0 | 0 | 0 | 3 | 2 | 0 |
| `lib/controllers/classes.js` | 7 | 0 | 0 | 6 | 0 | 0 | 1 | 0 |
| `lib/controllers/course.js` | 105 | 10 | 0 | 88 | 0 | 2 | 1 | 4 |
| `lib/controllers/courses.js` | 6 | 2 | 0 | 2 | 0 | 0 | 2 | 0 |
| `lib/controllers/files.js` | 5 | 1 | 0 | 3 | 0 | 1 | 0 | 0 |
| `lib/controllers/folders.js` | 6 | 0 | 0 | 4 | 1 | 0 | 1 | 0 |
| `lib/controllers/pages.js` | 3 | 1 | 0 | 2 | 0 | 0 | 0 | 0 |
| `lib/controllers/trinket.js` | 67 | 4 | 0 | 35 | 2 | 11 | 14 | 1 |
| `lib/controllers/users.js` | 116 | 57 | 0 | 27 | 4 | 8 | 2 | 18 |
| `lib/util/helpers.js` | 27 | 0 | 2 | 23 | 0 | 1 | 0 | 1 |
| **total** | **373** | 86 | 2 | 197 | 8 | 28 | 24 | 28 |

| Funnel | Rows |
|---|---:|
| Layer 1 | 83 |
| Layer 2 | 106 |
| Layer 3 | 135 |
| none | 49 |

Rows reaching **no funnel**: 49. Each of those is an error that produces no
response today, and each must still produce none after conversion.

| File | Rows | Closed | Open |
|---|---:|---:|---:|
| `config/api_routes.js` | 0 | 0 | - |
| `lib/controllers/admin.js` | 22 | 19 | 3 |
| `lib/controllers/auth.js` | 9 | 7 | 2 |
| `lib/controllers/classes.js` | 7 | 4 | 3 |
| `lib/controllers/course.js` | 105 | 80 | 25 |
| `lib/controllers/courses.js` | 6 | 5 | 1 |
| `lib/controllers/files.js` | 5 | 3 | 2 |
| `lib/controllers/folders.js` | 6 | 4 | 2 |
| `lib/controllers/pages.js` | 3 | 3 | - |
| `lib/controllers/trinket.js` | 67 | 46 | 21 |
| `lib/controllers/users.js` | 116 | 99 | 17 |
| `lib/util/helpers.js` | 27 | 17 | 10 |

Counted over the rows THIS tree emits. Section 8 counts over the baseline's
342 rows as well, so it also carries the 23 baseline edge(s) that have no row
here at all - which no per-file count over this tree could show.

## 7. The inventory

Grouped by file, then by line. Re-running produces a reviewable diff, not a
reshuffle.

### 7.1 `config/api_routes.js` - 0 rows

No error edge detected in this file. The scanner counted 0 `reply(` site(s), 0 `.catch(` site(s) and 0 `err`-parameter function(s) here, and
classified every one of them as carrying a non-error value: the file was
analysed, not skipped.

### 7.2 `lib/controllers/admin.js` - 22 rows

- [x] `lib/controllers/admin.js:69-72` `id=admin.index.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `admin.index` (route handler) - Routes `GET /admin`, `GET /admin/{adminPage*}` - Source `.catch(function(err) {`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 43 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is assigned to `promise` at that statement rather than returned, so whether anything waits for the rejection is decided where `promise` is consumed - preserve that consumption point, and do not move the settlement to this one.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/admin.js:69-72` and `lib/controllers/admin.js:69-72`. Paired by semantic subject `shape:.catch() handler - absorbs`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler body runs work but produces no response and rethrows nothing, so the error is absorbed here.
- [ ] `lib/controllers/admin.js:107` `id=admin.ohnoes.response.1` - **returns or throws a Boom** - Layer 3 - return errors.unauthorized('Not logged in');
      Carrier `admin.ohnoes` (route handler) - Routes `POST /api/ohnoes` - Source `return errors.unauthorized('Not logged in');`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 401 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/admin.js:212-252` `id=admin.uploadUsers.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (parse)
      Carrier `admin.uploadUsers` (route handler) - Routes `POST /admin/upload` - Source `}, function(err, records) {`
      Target: The response is produced inside the callback passed to `parse` (`request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `parse` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/admin.js:119-156` and `lib/controllers/admin.js:212-252`. Paired by semantic subject `guards:parse`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/admin.js:213` `id=admin.uploadUsers.response.1` - **calls request.fail locally** - Layer 2 - request.fail(err) with no return
      Carrier `admin.uploadUsers` (route handler) - Routes `POST /admin/upload` - Source `if (err) return resolve(request.fail(err));`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `parse` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/admin.js:120` and `lib/controllers/admin.js:213`. Paired by semantic subject `produces:err`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/admin.js:264-279` `id=admin.updateUser.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (User.findById)
      Carrier `admin.updateUser` (route handler) - Routes `POST /api/admin/user/{userId}` - Source `User.findById(request.params.userId, function(err, user) {`
      Target: The response is produced inside the callback passed to `User.findById` (`request.fail`, `request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `User.findById` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/admin.js:159-174` and `lib/controllers/admin.js:264-279`. Paired by semantic subject `guards:User.findById`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/admin.js:265` `id=admin.updateUser.response.1` - **calls request.fail locally** - Layer 2 - request.fail(err) with no return
      Carrier `admin.updateUser` (route handler) - Routes `POST /api/admin/user/{userId}` - Source `if (err) return resolve(request.fail(err));`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/admin.js:160` and `lib/controllers/admin.js:265`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/admin.js:267` `id=admin.updateUser.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message : 'user not found' }) with no return
      Carrier `admin.updateUser` (route handler) - Routes `POST /api/admin/user/{userId}` - Source `if (!user) return resolve(request.fail({ message : 'user ...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/admin.js:162` and `lib/controllers/admin.js:267`. Paired by semantic subject `produces:{ message : 'user not found' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/admin.js:271-277` `id=admin.updateUser.cps.2` - **resolves on a later callback** - Layer 2 - CPS callback boundary (user.save)
      Carrier `admin.updateUser` (route handler) - Routes `POST /api/admin/user/{userId}` - Source `user.save(function(err, user) {`
      Target: The response is produced inside the callback passed to `user.save` (`request.fail`, `request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `user.save` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/admin.js:166-172` and `lib/controllers/admin.js:271-277`. Paired by semantic subject `guards:user.save`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/admin.js:272` `id=admin.updateUser.response.3` - **calls request.fail locally** - Layer 2 - request.fail(err) with no return
      Carrier `admin.updateUser` (route handler) - Routes `POST /api/admin/user/{userId}` - Source `if (err) return resolve(request.fail(err));`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `user.save` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/admin.js:167` and `lib/controllers/admin.js:272`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/admin.js:287-339` `id=admin.grantRole.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (User.findById)
      Carrier `admin.grantRole` (route handler) - Routes `POST /api/admin/user/{userId}/grant` - Source `User.findById(request.params.userId, function(err, user) {`
      Target: The response is produced inside the callback passed to `User.findById` (`request.fail`, `request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `User.findById` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/admin.js:177-212` and `lib/controllers/admin.js:287-339`. Paired by semantic subject `guards:User.findById`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/admin.js:288` `id=admin.grantRole.response.1` - **calls request.fail locally** - Layer 2 - request.fail(err) with no return
      Carrier `admin.grantRole` (route handler) - Routes `POST /api/admin/user/{userId}/grant` - Source `if (err) return resolve(request.fail(err));`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/admin.js:178` and `lib/controllers/admin.js:288`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/admin.js:290` `id=admin.grantRole.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message : 'user not found' }) with no return
      Carrier `admin.grantRole` (route handler) - Routes `POST /api/admin/user/{userId}/grant` - Source `if (!user) return resolve(request.fail({ message : 'user ...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/admin.js:180` and `lib/controllers/admin.js:290`. Paired by semantic subject `produces:{ message : 'user not found' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/admin.js:337` `id=admin.grantRole.response.3` - **calls request.fail locally** - Layer 2 - request.fail(err)
      Carrier `admin.grantRole` (route handler) - Routes `POST /api/admin/user/{userId}/grant` - Source `return request.fail(err);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 292, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/admin.js:210` and `lib/controllers/admin.js:337`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/admin.js:354` `id=admin.addFeaturedCourse.response.1` - **returns or throws a Boom** - Layer 3 - synchronous throw (ReferenceError: Boom is not defined) evaluating Boom.notFound()
      Carrier `admin.addFeaturedCourse` (route handler) - Routes `POST /api/admin/featured-course` - Source `throw Boom.notFound();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.notFound(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 404 - is therefore not the status served: hapi boomifies the ReferenceError and answers **500** through Layer 3. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: none here; the writes belong to the `.catch(...)` handler at line 378, whose own row states them. Timing: asynchronous, in the chain step at line 348, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/admin.js:221` and `lib/controllers/admin.js:354`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/admin.js:363` `id=admin.addFeaturedCourse.response.2` - **returns or throws a Boom** - Layer 3 - synchronous throw (ReferenceError: Boom is not defined) evaluating Boom.notFound()
      Carrier `admin.addFeaturedCourse` (route handler) - Routes `POST /api/admin/featured-course` - Source `throw Boom.notFound();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.notFound(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 404 - is therefore not the status served: hapi boomifies the ReferenceError and answers **500** through Layer 3. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: none here; the writes belong to the `.catch(...)` handler at line 378, whose own row states them. Timing: asynchronous, in the chain step at line 348, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/admin.js:230` and `lib/controllers/admin.js:363`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/admin.js:378-380` `id=admin.addFeaturedCourse.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via errorResponse
      Carrier `admin.addFeaturedCourse` (route handler) - Routes `POST /api/admin/featured-course` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 348 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/admin.js:246` and `lib/controllers/admin.js:378-380`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `errorResponse`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/admin.js:387-389` `id=admin.removeFeaturedCourse.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via errorResponse
      Carrier `admin.removeFeaturedCourse` (route handler) - Routes `DELETE /api/admin/featured-course/{courseId}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 383 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/admin.js:255` and `lib/controllers/admin.js:387-389`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `errorResponse`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/admin.js:396-398` `id=admin.moveFeaturedCourse.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via errorResponse
      Carrier `admin.moveFeaturedCourse` (route handler) - Routes `POST /api/admin/featured-course/move` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 392 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/admin.js:264` and `lib/controllers/admin.js:396-398`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `errorResponse`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [ ] `lib/controllers/admin.js:427` `id=admin.errorResponse.response.1` - **returns or throws a Boom** - Layer 3 - return errors.badImplementation(err.message);
      Carrier `admin.errorResponse (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `admin.addFeaturedCourse`, `admin.moveFeaturedCourse`, `admin.removeFeaturedCourse` - Source `return errors.badImplementation(err.message);`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 500 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/admin.js:437-462` `id=admin.userSearch.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'User.findByLogin' - handed onward
      Carrier `admin.userSearch (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `admin.index` - Source `User.findByLogin(q, function(err, user) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 439 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / internal callee, measured at `lib/controllers/admin.js:273-298` and `lib/controllers/admin.js:437-462`. Paired by semantic subject `guards:User.findByLogin`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 439, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/admin.js:455-457` `id=admin.userSearch.handler.1` - **propagates to its caller** - Layer 1 - .catch() handler - hands the error onward
      Carrier `admin.userSearch (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `admin.index` - Source `.catch(function(err) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 456 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: deferred. The handler is registered synchronously while the chain at line 446 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / internal callee, measured at `lib/controllers/admin.js:291-293` and `lib/controllers/admin.js:455-457`. Paired by semantic subject `shape:.catch() handler - hands the error onward`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler rejects the enclosing promise via `reject(err)` at line 456, so the error leaves this function and the awaiting caller decides the response.
- [ ] `lib/controllers/admin.js:558-560` `id=admin.sendOhnoesAlert.handler.1` - **logs and continues** - none - .catch() handler - logs only
      Carrier `admin.sendOhnoesAlert (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `admin.ohnoes` - Source `})).catch(function(err) {`
      Target: No response is produced here and nothing is thrown: the error is logged via `log.error` and control continues. Funnel: none - and that is the point. Preserve the continuation. Converting a log-and-continue branch into a rejection or a returned Boom turns an edge that produces no response into one that does, changes the status a client sees from whatever else settles the request to a 500, and is exactly the silent conversion R-e is written to catch. Side effects: the `log.error` call and nothing else - no flash, no session write, no response, no rethrow. A conversion that adds any of those changes this edge. Timing: deferred. The handler is registered synchronously while the chain at line 556 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.

### 7.3 `lib/controllers/auth.js` - 9 rows

- [ ] `lib/controllers/auth.js:196-203` `id=auth.legacyJsonRequest.errparam.1` - **swallows silently** - none - error parameter of a callback to a local function - undispositioned
      Carrier `auth.legacyJsonRequest (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `auth.googleCallback` - Source `function dispatch(err, response, body) {`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: `err` is read but never turned into a response, a log or a rethrow.
- [ ] `lib/controllers/auth.js:297-302` `id=auth.attempt.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `auth.attempt (module-local)` (internal callee) - Routes none declared - driven by **nothing - proven unreachable** - Source `.catch(function(unexpected) {`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 230 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler body runs work but produces no response and rethrows nothing, so the error is absorbed here.
- [x] `lib/controllers/auth.js:322` `id=auth.google.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'Google OAuth is not configured. Please set up...)
      Carrier `auth.google` (route handler) - Routes `GET /auth/google` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/auth.js:12` and `lib/controllers/auth.js:322`. Paired by semantic subject `produces:{ message: 'Google OAuth is not configured. Please set up...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/auth.js:347` `id=auth.googleCallback.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'Google OAuth is not configured.' })
      Carrier `auth.googleCallback` (route handler) - Routes `GET /auth/google/callback` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/auth.js:37` and `lib/controllers/auth.js:347`. Paired by semantic subject `produces:{ message: 'Google OAuth is not configured.' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 6 - Google OAuth's new-user path, which saves the user and then reports failure
- [x] `lib/controllers/auth.js:354` `id=auth.googleCallback.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'No authorization code received from Google.' })
      Carrier `auth.googleCallback` (route handler) - Routes `GET /auth/google/callback` - Source `return request.fail({ message: 'No authorization code rec...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/auth.js:44` and `lib/controllers/auth.js:354`. Paired by semantic subject `produces:{ message: 'No authorization code received from Google.' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 6 - Google OAuth's new-user path, which saves the user and then reports failure
- [x] `lib/controllers/auth.js:382-393` `id=auth.googleCallback.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'legacyJsonRequest' - handed onward
      Carrier `auth.googleCallback` (route handler) - Routes `GET /auth/google/callback` - Source `}, function(err, response, body) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 390 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/auth.js:59-64` and `lib/controllers/auth.js:382-393`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 390, so `err` leaves this function and the awaiting caller decides the response.
      See also: docs/preserved-quirks.md section 6 - Google OAuth's new-user path, which saves the user and then reports failure
- [x] `lib/controllers/auth.js:411-420` `id=auth.googleCallback.errparam.2` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'legacyJsonRequest' - handed onward
      Carrier `auth.googleCallback` (route handler) - Routes `GET /auth/google/callback` - Source `}, function(err, response, profile) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 416 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/auth.js:73-79` and `lib/controllers/auth.js:411-420`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 416, so `err` leaves this function and the awaiting caller decides the response.
      See also: docs/preserved-quirks.md section 6 - Google OAuth's new-user path, which saves the user and then reports failure
- [x] `lib/controllers/auth.js:430-433` `id=auth.googleCallback.errparam.3` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'User.findByMultiple' - handed onward
      Carrier `auth.googleCallback` (route handler) - Routes `GET /auth/google/callback` - Source `}, function(err, user) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 431 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/auth.js:89-92` and `lib/controllers/auth.js:430-433`. Paired by semantic subject `guards:User.findByMultiple`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 431, so `err` leaves this function and the awaiting caller decides the response.
      See also: docs/preserved-quirks.md section 6 - Google OAuth's new-user path, which saves the user and then reports failure
- [x] `lib/controllers/auth.js:538` `id=auth.googleCallback.response.3` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'Authentication failed. Please try again.' })
      Carrier `auth.googleCallback` (route handler) - Routes `GET /auth/google/callback` - Source `return request.fail({ message: 'Authentication failed. Pl...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 358, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/auth.js:187` and `lib/controllers/auth.js:538`. Paired by semantic subject `produces:{ message: 'Authentication failed. Please try again.' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 6 - Google OAuth's new-user path, which saves the user and then reports failure

### 7.4 `lib/controllers/classes.js` - 7 rows

- [ ] `lib/controllers/classes.js:212` `id=classes.getClass.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `classes.getClass` (route handler) - Routes `GET /api/classes/{userSlug}/{courseSlug}` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/classes.js:261` `id=classes.acceptInvitation.response.1` - **returns or throws a Boom** - Layer 3 - return errors.tooManyRequests("Too many invitation attempts. Please
      Carrier `classes.acceptInvitation` (route handler) - Routes `GET /courses/accept/{token}` - Source `return errors.tooManyRequests("Too many invitation attemp...`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 429 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/classes.js:337-339` `id=classes.acceptInvitation.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `classes.acceptInvitation` (route handler) - Routes `GET /courses/accept/{token}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none here; the writes belong to the `.catch(...)` handler at line 347, whose own row states them. Timing: deferred. The handler is registered synchronously while the chain at line 267 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/classes.js:177` and `lib/controllers/classes.js:337-339`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/classes.js:347-349` `id=classes.acceptInvitation.handler.2` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `classes.acceptInvitation` (route handler) - Routes `GET /courses/accept/{token}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 264 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/classes.js:187` and `lib/controllers/classes.js:347-349`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [ ] `lib/controllers/classes.js:370` `id=classes.joinFromLink.response.1` - **returns or throws a Boom** - Layer 3 - return errors.tooManyRequests("Too many access code attempts. Pleas
      Carrier `classes.joinFromLink` (route handler) - Routes `GET /courses/join/{accessCode}` - Source `return errors.tooManyRequests("Too many access code attem...`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 429 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/classes.js:385` `id=classes.joinFromLink.response.2` - **returns or throws a Boom** - Layer 3 - return err in a catch clause - hands the caught error back as the response
      Carrier `classes.joinFromLink` (route handler) - Routes `GET /courses/join/{accessCode}` - Source `return err;`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - the factory's status - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/classes.js:196` and `lib/controllers/classes.js:385`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/classes.js:412-415` `id=classes.joinFromLink.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `classes.joinFromLink` (route handler) - Routes `GET /courses/join/{accessCode}` - Source `.catch(function(err) {`
      Target: The error is discarded and the handler answers with `h.redirect`, so the failure is reported to the client as a success. Funnel: none. Preserve the status, the payload and the discarding - R-d prohibits turning this into a failure response, however much it looks like one is owed. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 407 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce none / no response / answers / route handler, measured at `lib/controllers/classes.js:216-219` and `lib/controllers/classes.js:412-415`. Paired by semantic subject `shape:.catch() handler - absorbs`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.

### 7.5 `lib/controllers/course.js` - 105 rows

- [ ] `lib/controllers/course.js:86` `id=course.isEmail.response.1` - **returns or throws a Boom** - none - throw new TypeError('This library (validator.js) validates stri...
      Carrier `course.isEmail (module-local)` (internal callee) - Routes none declared - driven by **nothing - proven unreachable** - Source `throw new TypeError('This library (validator.js) validate...`
      Target: No funnel is entered: this edge produces no response, and the request settles only if something else settles it. Preserve that. Giving the edge a response - by returning a Boom, by throwing where the throw would now be caught, or by adding a catch - converts an edge that answers nothing into one that answers 500, which is the silent conversion R-e exists to catch. Side effects: none, and nothing is logged either: this edge reaches no funnel, so no funnel writes anything in response to it. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: not compared - proven unreachable
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: PROVEN UNREACHABLE. This member is mentioned nowhere in the analysed corpus outside its own declaration - not called, not handed on as a value, not registered as a server method, not exported as a lifecycle method and named by no route - so nothing in this tree can reach this edge. It claims no funnel because it produces no response, it is excluded from the closure gate because there is no outcome to compare, and it is dead code rather than an open question.
- [ ] `lib/controllers/course.js:268` `id=course.createCourse.response.1` - **returns or throws a Boom** - Layer 3 - return errors.forbidden();
      Carrier `course.createCourse` (route handler) - Routes `POST /api/courses` - Source `return errors.forbidden();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 403 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - the outcome changed.** Baseline produced Layer 2 / no response / answers / route handler; the target produces Layer 3 / 403 / answers / route handler. What differs: funnel, status served, payload or redirect shape, side effects, settlement timing - the settlement moved. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:289` `id=course.createCourse.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ err : err , message : "You already have a course with t...)
      Carrier `course.createCourse` (route handler) - Routes `POST /api/courses` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:22` and `lib/controllers/course.js:289`. Paired by semantic subject `produces:{ err : err , message : "You already have a course with t...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:330` `id=course.getCourse.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.getCourse` (route handler) - Routes `GET /api/courses/{courseId}` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:426-445` `id=course.updateCourse.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (course.save)
      Carrier `course.updateCourse` (route handler) - Routes `PUT /api/courses/{courseId}/metadata` - Source `course.save(function(err, savedCourse) {`
      Target: The response is produced inside the callback passed to `course.save` (`request.fail`, `request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `course.save` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/course.js:120-137` and `lib/controllers/course.js:426-445`. Paired by semantic subject `guards:course.save`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:429` `id=course.updateCourse.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ err : err , message : "You already have a course with t...) with no return
      Carrier `course.updateCourse` (route handler) - Routes `PUT /api/courses/{courseId}/metadata` - Source `return resolve(request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `course.save` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:123` and `lib/controllers/course.js:429`. Paired by semantic subject `produces:{ err : err , message : "You already have a course with t...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/course.js:454` `id=course.updateCourse.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.updateCourse` (route handler) - Routes `PUT /api/courses/{courseId}/metadata` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:140` and `lib/controllers/course.js:454`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:464-470` `id=course.deleteCourse.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.deleteCourse` (route handler) - Routes `DELETE /api/courses/{courseId}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 462 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:151` and `lib/controllers/course.js:464-470`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:473` `id=course.deleteCourse.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.deleteCourse` (route handler) - Routes `DELETE /api/courses/{courseId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:155` and `lib/controllers/course.js:473`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:488` `id=course.archiveCourse.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `course.archiveCourse` (route handler) - Routes `PATCH /api/courses/{courseId}` - Source `savedCourse = await course.save().catch(function() { /* i...`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 488 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / route handler; the target produces none / no response / answers nothing / route handler. What differs: payload or redirect shape. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler body is empty.
- [x] `lib/controllers/course.js:493` `id=course.archiveCourse.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.archiveCourse` (route handler) - Routes `PATCH /api/courses/{courseId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:170` and `lib/controllers/course.js:493`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:514-546` `id=course.copyCourse.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (request.pre.course.copy)
      Carrier `course.copyCourse` (route handler) - Routes `POST /api/courses/{courseId}/copy` - Source `request.pre.course.copy(request.user, function(err, cours...`
      Target: The response is produced inside the callback passed to `request.pre.course.copy` (`request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `request.pre.course.copy` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/course.js:183-209` and `lib/controllers/course.js:514-546`. Paired by semantic subject `guards:request.pre.course.copy`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:517` `id=course.copyCourse.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ err : err , message : "You already have a course with t...) with no return
      Carrier `course.copyCourse` (route handler) - Routes `POST /api/courses/{courseId}/copy` - Source `return resolve(request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `request.pre.course.copy` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:186` and `lib/controllers/course.js:517`. Paired by semantic subject `produces:{ err : err , message : "You already have a course with t...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/course.js:550` `id=course.copyCourse.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.copyCourse` (route handler) - Routes `POST /api/courses/{courseId}/copy` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:212` and `lib/controllers/course.js:550`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:580-582` `id=course.addLesson.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.addLesson` (route handler) - Routes `POST /api/courses/{courseId}/lessons` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 568 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:240` and `lib/controllers/course.js:580-582`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:585` `id=course.addLesson.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.addLesson` (route handler) - Routes `POST /api/courses/{courseId}/lessons` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:244` and `lib/controllers/course.js:585`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:595` `id=course.getLesson.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.getLesson` (route handler) - Routes `GET /api/courses/{courseId}/lessons/{lessonId}` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:609` `id=course.getLesson.response.2` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.getLesson` (route handler) - Routes `GET /api/courses/{courseId}/lessons/{lessonId}` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:632-646` `id=course.updateLesson.cps.1` - **resolves on a later callback** - none - CPS callback boundary (lesson.save)
      Carrier `course.updateLesson` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/name`, `PUT /api/courses/{courseId}/lessons/{lessonId}/draft` - Source `lesson.save(function(err, savedLesson) {`
      Target: The response is produced inside the callback passed to `lesson.save` (`request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `lesson.save` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/course.js:258-266` and `lib/controllers/course.js:632-646`. Paired by semantic subject `guards:lesson.save`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback declares `err` and never inspects it, so the failure of `lesson.save` is dropped while the response is produced regardless.
      Note: `err` is declared and never read.
- [x] `lib/controllers/course.js:650` `id=course.updateLesson.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.updateLesson` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/name`, `PUT /api/courses/{courseId}/lessons/{lessonId}/draft` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:269` and `lib/controllers/course.js:650`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:670-677` `id=course.moveLesson.cps.1` - **resolves on a later callback** - none - CPS callback boundary (course.save)
      Carrier `course.moveLesson` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/move` - Source `course.save(function(err, savedCourse) {`
      Target: The response is produced inside the callback passed to `course.save` (`request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `course.save` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/course.js:282-289` and `lib/controllers/course.js:670-677`. Paired by semantic subject `guards:course.save`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback declares `err` and never inspects it, so the failure of `course.save` is dropped while the response is produced regardless.
      Note: `err` is declared and never read.
- [x] `lib/controllers/course.js:681` `id=course.moveLesson.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.moveLesson` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/move` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:292` and `lib/controllers/course.js:681`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:702` `id=course.deleteLesson.handler.1` - **calls request.fail locally** - Layer 2 - .catch(request.fail) bare reference
      Carrier `course.deleteLesson` (route handler) - Routes `DELETE /api/courses/{courseId}/lessons/{lessonId}` - Source `.catch(request.fail);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. The reference is bare, so the rejection value arrives as request.fail's FIRST parameter - the `json` payload, not `err` - and is what gets logged and flashed. Preserve that: the flash content is the rejection value itself, not a message derived from it. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: deferred. The handler is registered synchronously while the chain at line 694 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:309` and `lib/controllers/course.js:702`. Paired by semantic subject `shape:.catch(request.fail) bare reference`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:705` `id=course.deleteLesson.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.deleteLesson` (route handler) - Routes `DELETE /api/courses/{courseId}/lessons/{lessonId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:312` and `lib/controllers/course.js:705`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:760-762` `id=course.addMaterial.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.addMaterial` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 733 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:368` and `lib/controllers/course.js:760-762`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:765` `id=course.addMaterial.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.addMaterial` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:372` and `lib/controllers/course.js:765`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:777` `id=course.getMaterial.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.getMaterial` (route handler) - Routes `GET /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:803` `id=course.getMaterial.response.2` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.getMaterial` (route handler) - Routes `GET /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:895` `id=course.updateMaterial.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ data : _.extendOwn({ status : "error" , message : "This...) with no return
      Carrier `course.updateMaterial` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/name`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/draft`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/assignment`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/patchContent` - Source `request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 856, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:442` and `lib/controllers/course.js:895`. Paired by semantic subject `produces:{ data : _.extendOwn({ status : "error" , message : "This...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [ ] `lib/controllers/course.js:902` `id=course.updateMaterial.response.2` - **returns or throws a Boom** - Layer 3 - throw errors.internal()
      Carrier `course.updateMaterial` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/name`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/draft`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/assignment`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/patchContent` - Source `throw errors.internal();`
      Target: The rejection is taken by the `.catch(...)` at line 914, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 914, whose own row states them. Timing: asynchronous, in the chain step at line 856, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:914-916` `id=course.updateMaterial.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.updateMaterial` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/name`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/draft`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/assignment`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/patchContent` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 856 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:460` and `lib/controllers/course.js:914-916`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:919` `id=course.updateMaterial.response.3` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.updateMaterial` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/name`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/draft`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/assignment`, `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/patchContent` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:464` and `lib/controllers/course.js:919`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:950` `id=course.moveMaterial.handler.1` - **calls request.fail locally** - Layer 2 - .catch(request.fail) bare reference
      Carrier `course.moveMaterial` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/move` - Source `.catch(request.fail);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. The reference is bare, so the rejection value arrives as request.fail's FIRST parameter - the `json` payload, not `err` - and is what gets logged and flashed. Preserve that: the flash content is the rejection value itself, not a message derived from it. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: deferred. The handler is registered synchronously while the chain at line 935 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:495` and `lib/controllers/course.js:950`. Paired by semantic subject `shape:.catch(request.fail) bare reference`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:953` `id=course.moveMaterial.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.moveMaterial` (route handler) - Routes `PUT /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/move` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:498` and `lib/controllers/course.js:953`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:973` `id=course.deleteMaterial.handler.1` - **calls request.fail locally** - Layer 2 - .catch(request.fail) bare reference
      Carrier `course.deleteMaterial` (route handler) - Routes `DELETE /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}` - Source `.catch(request.fail);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. The reference is bare, so the rejection value arrives as request.fail's FIRST parameter - the `json` payload, not `err` - and is what gets logged and flashed. Preserve that: the flash content is the rejection value itself, not a message derived from it. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: deferred. The handler is registered synchronously while the chain at line 963 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:516` and `lib/controllers/course.js:973`. Paired by semantic subject `shape:.catch(request.fail) bare reference`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:976` `id=course.deleteMaterial.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.deleteMaterial` (route handler) - Routes `DELETE /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:519` and `lib/controllers/course.js:976`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1008` `id=course.listUsers.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.listUsers` (route handler) - Routes `GET /api/courses/{courseId}/users` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:551` and `lib/controllers/course.js:1008`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1022-1024` `id=course.listInvitations.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.listInvitations` (route handler) - Routes `GET /api/courses/{courseId}/invitations` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1016 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:566` and `lib/controllers/course.js:1022-1024`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1027` `id=course.listInvitations.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.listInvitations` (route handler) - Routes `GET /api/courses/{courseId}/invitations` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:570` and `lib/controllers/course.js:1027`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1041` `id=course.userLookup.response.1` - **returns or throws a Boom** - Layer 3 - synchronous throw (ReferenceError: Boom is not defined) evaluating Boom.notFound()
      Carrier `course.userLookup` (route handler) - Routes `POST /api/courses/{courseId}/userLookup` - Source `throw Boom.notFound();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.notFound(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 404 - is therefore not the status served: hapi boomifies the ReferenceError and answers **500** through Layer 3. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1064, whose own row states them. Timing: asynchronous, in the chain step at line 1035, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:584` and `lib/controllers/course.js:1041`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1064-1066` `id=course.userLookup.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.userLookup` (route handler) - Routes `POST /api/courses/{courseId}/userLookup` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1035 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:602` and `lib/controllers/course.js:1064-1066`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1069` `id=course.userLookup.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.userLookup` (route handler) - Routes `POST /api/courses/{courseId}/userLookup` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:606` and `lib/controllers/course.js:1069`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1093-1095` `id=course.removeUser.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.removeUser` (route handler) - Routes `DELETE /api/courses/{courseId}/users/{userId}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1079 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:631` and `lib/controllers/course.js:1093-1095`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1098` `id=course.removeUser.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.removeUser` (route handler) - Routes `DELETE /api/courses/{courseId}/users/{userId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:635` and `lib/controllers/course.js:1098`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1112` `id=course.addUser.response.1` - **returns or throws a Boom** - Layer 3 - synchronous throw (ReferenceError: Boom is not defined) evaluating Boom.notFound()
      Carrier `course.addUser` (route handler) - Routes `POST /api/courses/{courseId}/users` - Source `throw Boom.notFound();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.notFound(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 404 - is therefore not the status served: hapi boomifies the ReferenceError and answers **500** through Layer 3. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1120, whose own row states them. Timing: asynchronous, in the chain step at line 1106, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:649` and `lib/controllers/course.js:1112`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1120-1122` `id=course.addUser.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.addUser` (route handler) - Routes `POST /api/courses/{courseId}/users` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1106 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:658` and `lib/controllers/course.js:1120-1122`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1125` `id=course.addUser.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.addUser` (route handler) - Routes `POST /api/courses/{courseId}/users` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:662` and `lib/controllers/course.js:1125`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1140` `id=course.updateRoles.response.1` - **returns or throws a Boom** - Layer 3 - synchronous throw (ReferenceError: Boom is not defined) evaluating Boom.notFound()
      Carrier `course.updateRoles` (route handler) - Routes `POST /api/courses/{courseId}/roles` - Source `throw Boom.notFound();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.notFound(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 404 - is therefore not the status served: hapi boomifies the ReferenceError and answers **500** through Layer 3. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1148, whose own row states them. Timing: asynchronous, in the chain step at line 1134, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:677` and `lib/controllers/course.js:1140`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1148-1150` `id=course.updateRoles.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.updateRoles` (route handler) - Routes `POST /api/courses/{courseId}/roles` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1134 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:686` and `lib/controllers/course.js:1148-1150`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1153` `id=course.updateRoles.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.updateRoles` (route handler) - Routes `POST /api/courses/{courseId}/roles` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:690` and `lib/controllers/course.js:1153`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1170-1172` `id=course.updateViews.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.updateViews` (route handler) - Routes `POST /api/courses/{courseId}/views` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1164 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:708` and `lib/controllers/course.js:1170-1172`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1175` `id=course.updateViews.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.updateViews` (route handler) - Routes `POST /api/courses/{courseId}/views` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:712` and `lib/controllers/course.js:1175`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1183` `id=course.sendInvitations.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: "Email is not configured. Course invitations c...)
      Carrier `course.sendInvitations` (route handler) - Routes `POST /api/courses/{courseId}/invitations` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:720` and `lib/controllers/course.js:1183`. Paired by semantic subject `produces:{ message: "Email is not configured. Course invitations c...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1243-1245` `id=course.sendInvitations.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.sendInvitations` (route handler) - Routes `POST /api/courses/{courseId}/invitations` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1189 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:740` and `lib/controllers/course.js:1243-1245`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1248` `id=course.sendInvitations.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.sendInvitations` (route handler) - Routes `POST /api/courses/{courseId}/invitations` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:744` and `lib/controllers/course.js:1248`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:1275` `id=course.removeInvitation.response.1` - **returns or throws a Boom** - Layer 3 - throw errors.notFound()
      Carrier `course.removeInvitation` (route handler) - Routes `DELETE /api/courses/{courseId}/invitations/{invitationId}` - Source `throw errors.notFound();`
      Target: The rejection is taken by the `.catch(...)` at line 1284, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1284, whose own row states them. Timing: asynchronous, in the chain step at line 1255, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1284-1286` `id=course.removeInvitation.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.removeInvitation` (route handler) - Routes `DELETE /api/courses/{courseId}/invitations/{invitationId}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1255 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:760` and `lib/controllers/course.js:1284-1286`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1289` `id=course.removeInvitation.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.removeInvitation` (route handler) - Routes `DELETE /api/courses/{courseId}/invitations/{invitationId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:764` and `lib/controllers/course.js:1289`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1299` `id=course.updateInvitation.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: "Email is not configured. Course invitations c...)
      Carrier `course.updateInvitation` (route handler) - Routes `PUT /api/courses/{courseId}/invitations/{invitationId}/resend`, `PUT /api/courses/{courseId}/invitations/{invitationId}/email` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:774` and `lib/controllers/course.js:1299`. Paired by semantic subject `produces:{ message: "Email is not configured. Course invitations c...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:1316` `id=course.updateInvitation.response.2` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.updateInvitation` (route handler) - Routes `PUT /api/courses/{courseId}/invitations/{invitationId}/resend`, `PUT /api/courses/{courseId}/invitations/{invitationId}/email` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1370` `id=course.updateInvitation.response.3` - **calls request.fail locally** - Layer 2 - request.fail({ err : err , message : "An invitation with that email ad...)
      Carrier `course.updateInvitation` (route handler) - Routes `PUT /api/courses/{courseId}/invitations/{invitationId}/resend`, `PUT /api/courses/{courseId}/invitations/{invitationId}/email` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 1354, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/course.js:814` and `lib/controllers/course.js:1370`. Paired by semantic subject `produces:{ err : err , message : "An invitation with that email ad...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1395` `id=course.updateInvitation.response.4` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.updateInvitation` (route handler) - Routes `PUT /api/courses/{courseId}/invitations/{invitationId}/resend`, `PUT /api/courses/{courseId}/invitations/{invitationId}/email` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:835` and `lib/controllers/course.js:1395`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1410-1412` `id=course.generateAccessCode.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.generateAccessCode` (route handler) - Routes `POST /api/courses/{courseId}/accessCode` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1403 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:851` and `lib/controllers/course.js:1410-1412`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1415` `id=course.generateAccessCode.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.generateAccessCode` (route handler) - Routes `POST /api/courses/{courseId}/accessCode` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:855` and `lib/controllers/course.js:1415`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1428` `id=course.getAccessCode.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.getAccessCode` (route handler) - Routes `GET /api/courses/{courseId}/accessCode` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:868` and `lib/controllers/course.js:1428`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:1456` `id=course.join.response.1` - **returns or throws a Boom** - Layer 3 - return errors.tooManyRequests("Too many access code attempts. Pleas
      Carrier `course.join` (route handler) - Routes `POST /api/courses/join` - Source `return errors.tooManyRequests("Too many access code attem...`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 429 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:1464-1469` `id=course.join.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'Course.findByAccessCode' - handed onward
      Carrier `course.join` (route handler) - Routes `POST /api/courses/join` - Source `Course.findByAccessCode(request.payload.accessCode, funct...`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 1466 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / route handler; the target produces Layer 1 / no response / answers nothing / route handler. What differs: funnel, payload or redirect shape, side effects. R-e requires these to match, and no approved deviation names this edge. Paired by semantic subject `guards:Course.findByAccessCode`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 1466, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/course.js:1475` `id=course.join.response.2` - **returns or throws a Boom** - Layer 3 - return err in a catch clause - hands the caught error back as the response
      Carrier `course.join` (route handler) - Routes `POST /api/courses/join` - Source `return err;`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - the factory's status - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:874` and `lib/controllers/course.js:1475`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1512-1514` `id=course.join.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.join` (route handler) - Routes `POST /api/courses/join` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1491 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:899` and `lib/controllers/course.js:1512-1514`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [ ] `lib/controllers/course.js:1551` `id=course.startAssignment.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.startAssignment` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/startAssignment` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:1563` `id=course.startAssignment.response.2` - **returns or throws a Boom** - Layer 3 - throw errors.forbidden()
      Carrier `course.startAssignment` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/startAssignment` - Source `throw errors.forbidden();`
      Target: The rejection is taken by the `.catch(...)` at line 1596, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1596, whose own row states them. Timing: asynchronous, in the chain step at line 1554, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1596-1598` `id=course.startAssignment.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.startAssignment` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/startAssignment` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1554 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:939` and `lib/controllers/course.js:1596-1598`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [ ] `lib/controllers/course.js:1633` `id=course.submitAssignment.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.submitAssignment` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/submissions` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:1639` `id=course.submitAssignment.response.2` - **returns or throws a Boom** - Layer 3 - return errors.forbidden();
      Carrier `course.submitAssignment` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/submissions` - Source `return errors.forbidden();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 403 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1649` `id=course.submitAssignment.response.3` - **returns or throws a Boom** - Layer 1 - throw new Error("Assignment is not available.")
      Carrier `course.submitAssignment` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/submissions` - Source `throw new Error("Assignment is not available.");`
      Target: The rejection is taken by the `.catch(...)` at line 1698, which answers through Layer 1 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1698, whose own row states them. Timing: asynchronous, in the chain step at line 1643, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:953` and `lib/controllers/course.js:1649`. Paired by semantic subject `throws:new Error("Assignment is not available.")`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1653` `id=course.submitAssignment.response.4` - **returns or throws a Boom** - Layer 1 - throw new Error("Assignment is past due.")
      Carrier `course.submitAssignment` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/submissions` - Source `throw new Error("Assignment is past due.");`
      Target: The rejection is taken by the `.catch(...)` at line 1698, which answers through Layer 1 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1698, whose own row states them. Timing: asynchronous, in the chain step at line 1643, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:957` and `lib/controllers/course.js:1653`. Paired by semantic subject `throws:new Error("Assignment is past due.")`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1699` `id=course.submitAssignment.response.5` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden(err);
      Carrier `course.submitAssignment` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/submissions` - Source `return Boom.forbidden(err);`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: asynchronous, in the chain step at line 1643, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1003` and `lib/controllers/course.js:1699`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1715` `id=course.updateMySubmission.response.1` - **returns or throws a Boom** - Layer 1 - throw new Error("Assignment is not available.")
      Carrier `course.updateMySubmission` (route handler) - Routes `POST /api/submissions/{trinketId}` - Source `throw new Error("Assignment is not available.");`
      Target: The rejection is taken by the `.catch(...)` at line 1755, which answers through Layer 1 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1755, whose own row states them. Timing: asynchronous, in the chain step at line 1709, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1019` and `lib/controllers/course.js:1715`. Paired by semantic subject `throws:new Error("Assignment is not available.")`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1719` `id=course.updateMySubmission.response.2` - **returns or throws a Boom** - Layer 1 - throw new Error("Assignment is past due.")
      Carrier `course.updateMySubmission` (route handler) - Routes `POST /api/submissions/{trinketId}` - Source `throw new Error("Assignment is past due.");`
      Target: The rejection is taken by the `.catch(...)` at line 1755, which answers through Layer 1 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1755, whose own row states them. Timing: asynchronous, in the chain step at line 1709, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1023` and `lib/controllers/course.js:1719`. Paired by semantic subject `throws:new Error("Assignment is past due.")`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1756` `id=course.updateMySubmission.response.3` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden(err);
      Carrier `course.updateMySubmission` (route handler) - Routes `POST /api/submissions/{trinketId}` - Source `return Boom.forbidden(err);`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: asynchronous, in the chain step at line 1709, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1060` and `lib/controllers/course.js:1756`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1760` `id=course.updateMySubmission.response.4` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.updateMySubmission` (route handler) - Routes `POST /api/submissions/{trinketId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1064` and `lib/controllers/course.js:1760`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1780` `id=course.autosaveComments.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.autosaveComments` (route handler) - Routes `POST /api/comments/{trinketId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1084` and `lib/controllers/course.js:1780`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1792` `id=course.getUserSubmissionsForMaterial.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.getUserSubmissionsForMaterial` (route handler) - Routes `GET /api/submissions/{materialId}`, `GET /api/courses/{courseId}/users/{userId}/materials/{materialId}/submissions` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1096` and `lib/controllers/course.js:1792`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:1825` `id=course.getUserSubmissionsForMaterial.response.2` - **returns or throws a Boom** - Layer 3 - return errors.forbidden();
      Carrier `course.getUserSubmissionsForMaterial` (route handler) - Routes `GET /api/submissions/{materialId}`, `GET /api/courses/{courseId}/users/{userId}/materials/{materialId}/submissions` - Source `return errors.forbidden();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 403 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: asynchronous, in the chain step at line 1799, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:1845-1847` `id=course.getUserSubmissionsForMaterial.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.getUserSubmissionsForMaterial` (route handler) - Routes `GET /api/submissions/{materialId}`, `GET /api/courses/{courseId}/users/{userId}/materials/{materialId}/submissions` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1799 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:1123` and `lib/controllers/course.js:1845-1847`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1962-1964` `id=course.dashboardOverview.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.dashboardOverview` (route handler) - Routes `GET /api/courses/{courseId}/dashboard` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1886 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:1240` and `lib/controllers/course.js:1962-1964`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:1967` `id=course.dashboardOverview.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.dashboardOverview` (route handler) - Routes `GET /api/courses/{courseId}/dashboard` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1244` and `lib/controllers/course.js:1967`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:2019-2021` `id=course.materialDashboard.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.materialDashboard` (route handler) - Routes `GET /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/dashboard` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1992 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:1297` and `lib/controllers/course.js:2019-2021`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:2024` `id=course.materialDashboard.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.materialDashboard` (route handler) - Routes `GET /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/dashboard` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1301` and `lib/controllers/course.js:2024`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:2122-2124` `id=course.getMaterialSubmissionsForAllUsers.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.getMaterialSubmissionsForAllUsers` (route handler) - Routes `GET /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/submissions` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 2071 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:1380` and `lib/controllers/course.js:2122-2124`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:2127` `id=course.getMaterialSubmissionsForAllUsers.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.getMaterialSubmissionsForAllUsers` (route handler) - Routes `GET /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/submissions` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1384` and `lib/controllers/course.js:2127`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:2143` `id=course.getUserSubmissionsForCourse.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.notFound();
      Carrier `course.getUserSubmissionsForCourse` (route handler) - Routes `GET /api/courses/{courseId}/users/{userId}/submissions` - Source `return Boom.notFound();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.notFound(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 404 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1400` and `lib/controllers/course.js:2143`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:2195` `id=course.getUserSubmissionsForCourse.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.getUserSubmissionsForCourse` (route handler) - Routes `GET /api/courses/{courseId}/users/{userId}/submissions` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1452` and `lib/controllers/course.js:2195`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:2220` `id=course.autosaveFeedbackComments.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.autosaveFeedbackComments` (route handler) - Routes `POST /api/feedback-comments/{trinketId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1477` and `lib/controllers/course.js:2220`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:2232` `id=course.autosaveSubmissionOpt.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.autosaveSubmissionOpt` (route handler) - Routes `POST /api/submission-opt/{trinketId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1489` and `lib/controllers/course.js:2232`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:2270` `id=course.sendFeedback.response.1` - **returns or throws a Boom** - Layer 3 - throw errors.forbidden()
      Carrier `course.sendFeedback` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/feedback` - Source `throw errors.forbidden();`
      Target: The rejection is taken by the `.catch(...)` at line 2358, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 2358, whose own row states them. Timing: asynchronous, in the chain step at line 2259, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:2358-2360` `id=course.sendFeedback.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `course.sendFeedback` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/feedback` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 2259 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/course.js:1578` and `lib/controllers/course.js:2358-2360`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/course.js:2363` `id=course.sendFeedback.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.sendFeedback` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/feedback` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1582` and `lib/controllers/course.js:2363`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:2394` `id=course.acceptSubmission.response.1` - **returns or throws a Boom** - Layer 3 - return errors.forbidden();
      Carrier `course.acceptSubmission` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/acceptSubmission` - Source `return errors.forbidden();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 403 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/course.js:2407` `id=course.acceptSubmission.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `course.acceptSubmission` (route handler) - Routes `POST /api/courses/{courseId}/lessons/{lessonId}/materials/{materialId}/acceptSubmission` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/course.js:1600` and `lib/controllers/course.js:2407`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:2462` `id=course.createCourseCore.response.1` - **returns or throws a Boom** - Layer 1 - throw errors.forbidden()
      Carrier `course.createCourseCore` (route handler) - Routes none declared - driven by its traced caller(s) `course.createCourse` - Source `throw errors.forbidden();`
      Target: Layer 1. The handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message || String(err)), so the answer is 500 and Boom hides the 5xx message - the thrown value's own status, 403, is NOT preserved by the funnel and must not start being preserved. After conversion this becomes structural: a rejecting async handler lands in the same catch. Preserve the 500, the generic body and the error-level log. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:2471-2485` `id=course.createCourseCore.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'course.save' - handed onward
      Carrier `course.createCourseCore` (route handler) - Routes none declared - driven by its traced caller(s) `course.createCourse` - Source `course.save(function(err, savedCourse) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 2473 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 2473, so `err` leaves this function and the awaiting caller decides the response.
- [ ] `lib/controllers/course.js:2565` `id=course.resolveMaterialContext.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.resolveMaterialContext (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `course.acceptSubmission`, `course.getMaterialSubmissionsForAllUsers`, `course.sendFeedback`, `course.startAssignment`, `course.submitAssignment` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:2571` `id=course.resolveMaterialContext.response.2` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.resolveMaterialContext (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `course.acceptSubmission`, `course.getMaterialSubmissionsForAllUsers`, `course.sendFeedback`, `course.startAssignment`, `course.submitAssignment` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: asynchronous, in the chain step at line 2568, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/course.js:2577` `id=course.resolveMaterialContext.response.3` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `course.resolveMaterialContext (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `course.acceptSubmission`, `course.getMaterialSubmissionsForAllUsers`, `course.sendFeedback`, `course.startAssignment`, `course.submitAssignment` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: asynchronous, in the chain step at line 2574, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.

### 7.6 `lib/controllers/courses.js` - 6 rows

- [x] `lib/controllers/courses.js:53` `id=courses.create.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message : "Your account doesn't include private courses...)
      Carrier `courses.create` (route handler) - Routes `POST /courses` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/courses.js:45` and `lib/controllers/courses.js:53`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/courses.js:76` `id=courses.create.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ err : err , message : "You already have a course with t...)
      Carrier `courses.create` (route handler) - Routes `POST /courses` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/courses.js:52` and `lib/controllers/courses.js:76`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/courses.js:140-142` `id=courses.featuredCourses.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `courses.featuredCourses` (route handler) - Routes `GET /api/featured-courses` - Source `.catch(function(error) {`
      Target: The error is discarded and the handler answers with `request.success`, so the failure is reported to the client as a success. Funnel: none. Preserve the status, the payload and the discarding - R-d prohibits turning this into a failure response, however much it looks like one is owed. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 122 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce none / no response / answers / route handler, measured at `lib/controllers/courses.js:77-79` and `lib/controllers/courses.js:140-142`. Paired by semantic subject `shape:.catch() handler - absorbs`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/courses.js:164-183` `id=courses.copy.errparam.1` - **swallows silently** - none - error parameter of a callback to 'request.pre.course.copy' - undispositioned
      Carrier `courses.copy` (route handler) - Routes `POST /{userSlug}/courses/{courseSlug}/copy` - Source `request.pre.course.copy(request.user, function(err, cours...`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/courses.js:83-98` and `lib/controllers/courses.js:164-183`. Paired by semantic subject `guards:request.pre.course.copy`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: `err` is declared and never read.
- [ ] `lib/controllers/courses.js:429` `id=courses.download.response.1` - **returns or throws a Boom** - Layer 3 - return errors.badImplementation(err.message);
      Carrier `courses.download` (route handler) - Routes `GET /{userSlug}/courses/{courseSlug}/download.zip` - Source `return errors.badImplementation(err.message);`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 500 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: asynchronous, in the chain step at line 415, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - the outcome changed.** Baseline produced Layer 3 / 500 / answers / route handler; the target produces Layer 3 / 500 / answers / route handler. What differs: payload or redirect shape. R-e requires these to match, and no approved deviation names this edge. Paired by nearest outcome in the routed carrier. paired to the nearest outcome in `lib/controllers/courses.js` routed carrier `courses.download`, which differs, so this row is reported open on that difference rather than closed on the pairing.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/courses.js:444` `id=courses.download.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `courses.download` (route handler) - Routes `GET /{userSlug}/courses/{courseSlug}/download.zip` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/courses.js:289` and `lib/controllers/courses.js:444`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.

### 7.7 `lib/controllers/files.js` - 5 rows

- [ ] `lib/controllers/files.js:246-248` `id=files.settle.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'invoke' - handed onward
      Carrier `files.settle (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `files.setThumbnail`, `files.upload`, `files.uploadAvatar` - Source `invoke(function(err, value) {`
      Target: The error is handed to an outer continuation - resolves the enclosing promise WITH the error, via `resolve({ err : err, value : value })`, at line 247 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `resolve`, and its own row states them where it is in this file. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback resolves the enclosing promise WITH the error, via `resolve({ err : err, value : value })` at line 247, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/files.js:257` `id=files.uploadAvatar.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notImplemented('Avatar uploads are not enabled');
      Carrier `files.uploadAvatar` (route handler) - Routes `POST /file/avatar` - Source `return errors.notImplemented('Avatar uploads are not enab...`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 501 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 501 / answers / route handler, measured at `lib/controllers/files.js:12` and `lib/controllers/files.js:257`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/files.js:270` `id=files.uploadAvatar.response.2` - **returns or throws a Boom** - Layer 3 - return errors.badImplementation(upload.err.message);
      Carrier `files.uploadAvatar` (route handler) - Routes `POST /file/avatar` - Source `: errors.badImplementation(upload.err.message);`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 500 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - the outcome changed.** Baseline produced Layer 3 / 500 / answers / route handler; the target produces Layer 3 / 500 / answers / route handler. What differs: payload or redirect shape, settlement timing - the settlement moved. R-e requires these to match, and no approved deviation names this edge. Paired by nearest outcome in the routed carrier. paired to the nearest outcome in `lib/controllers/files.js` routed carrier `files.uploadAvatar`, which differs, so this row is reported open on that difference rather than closed on the pairing.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/files.js:278` `id=files.upload.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notImplemented('File uploads are not enabled');
      Carrier `files.upload` (route handler) - Routes `POST /file` - Source `return errors.notImplemented('File uploads are not enable...`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 501 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 501 / answers / route handler, measured at `lib/controllers/files.js:25` and `lib/controllers/files.js:278`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/files.js:410` `id=files.setThumbnail.response.1` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `files.setThumbnail` (route handler) - Routes `POST /api/files/{fileId}/thumbnail` - Source `return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/files.js:110` and `lib/controllers/files.js:410`. Paired by semantic subject `produces:`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.

### 7.8 `lib/controllers/folders.js` - 6 rows

- [ ] `lib/controllers/folders.js:51` `id=folders.legacyReply.response.1` - **returns or throws a Boom** - Layer 3 - return errors.badImplementation(value.message);
      Carrier `folders.legacyReply (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `folders.deleteFolder`, `folders.list`, `folders.trinkets`, `folders.update` - Source `return errors.badImplementation(value.message);`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 500 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/folders.js:151-153` `id=folders.list.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `folders.list` (route handler) - Routes `GET /api/folders` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 139 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/folders.js:32` and `lib/controllers/folders.js:151-153`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [ ] `lib/controllers/folders.js:218-271` `id=folders.create.errparam.1` - **swallows silently** - none - error parameter of a callback to 'folder.save' - undispositioned
      Carrier `folders.create` (route handler) - Routes `POST /api/folders` - Source `folder.save(function(err, savedFolder) {`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / route handler; the target produces none / no response / answers nothing / route handler. What differs: payload or redirect shape. R-e requires these to match, and no approved deviation names this edge. Paired by semantic subject `guards:folder.save`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: `err` is read but never turned into a response, a log or a rethrow.
- [x] `lib/controllers/folders.js:303-305` `id=folders.update.handler.1` - **logs and continues** - none - .catch() handler - logs only
      Carrier `folders.update` (route handler) - Routes `PUT /api/folders/{folderId}/name` - Source `.catch(function(err) {`
      Target: No response is produced here and nothing is thrown: the error is logged via `console.error` and control continues. Funnel: none - and that is the point. Preserve the continuation. Converting a log-and-continue branch into a rejection or a returned Boom turns an edge that produces no response into one that does, changes the status a client sees from whatever else settles the request to a 500, and is exactly the silent conversion R-e is written to catch. Side effects: the `console.error` call and nothing else - no flash, no session write, no response, no rethrow. A conversion that adds any of those changes this edge. Timing: deferred. The handler is registered synchronously while the chain at line 295 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/folders.js:115-117` and `lib/controllers/folders.js:303-305`. Paired by semantic subject `shape:.catch() handler - logs only`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/folders.js:314-330` `id=folders.update.handler.2` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `folders.update` (route handler) - Routes `PUT /api/folders/{folderId}/name` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 283 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/folders.js:135` and `lib/controllers/folders.js:314-330`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/folders.js:353-355` `id=folders.deleteFolder.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `folders.deleteFolder` (route handler) - Routes `DELETE /api/folders/{folderId}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 347 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/folders.js:153` and `lib/controllers/folders.js:353-355`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.

### 7.9 `lib/controllers/pages.js` - 3 rows

- [x] `lib/controllers/pages.js:21` `id=pages.login.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (TypeError: reply.redirect is not a function)
      Carrier `pages.login` (route handler) - Routes `GET /login` - Source `return reply.redirect('/home');`
      Target: A synchronous TypeError: `reply` is a bare function and only the object it RETURNS carries `.redirect`, so this line throws before any response is built. Layer 1 catches it, logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered 500 with Boom's generic 5xx body - not the redirect the line reads as. Preserve the 500 and the log. R-d requires this: it is a defect and it is baseline behaviour. Side effects: none from this line, and that is part of the contract: it throws before a response is built, so every statement that follows it in the same branch does not run. Layer 1 then logs `err.stack` at error level. Preserve both the absence and the log. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/pages.js:17` and `lib/controllers/pages.js:21`. Paired by semantic subject `reply-property:redirect`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 5 - the two `pages` handlers that answer 500 to authenticated visitors
- [x] `lib/controllers/pages.js:33` `id=pages.signup.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (TypeError: reply.redirect is not a function)
      Carrier `pages.signup` (route handler) - Routes `GET /signup` - Source `return reply.redirect('/welcome');`
      Target: A synchronous TypeError: `reply` is a bare function and only the object it RETURNS carries `.redirect`, so this line throws before any response is built. Layer 1 catches it, logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered 500 with Boom's generic 5xx body - not the redirect the line reads as. Preserve the 500 and the log. R-d requires this: it is a defect and it is baseline behaviour. Side effects: none from this line, and that is part of the contract: it throws before a response is built, so every statement that follows it in the same branch does not run. Layer 1 then logs `err.stack` at error level. Preserve both the absence and the log. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/pages.js:27` and `lib/controllers/pages.js:33`. Paired by semantic subject `reply-property:redirect`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 5 - the two `pages` handlers that answer 500 to authenticated visitors
- [x] `lib/controllers/pages.js:68` `id=pages.home.handler.1` - **calls request.fail locally** - Layer 2 - .catch(request.fail) bare reference
      Carrier `pages.home` (route handler) - Routes `GET /home` - Source `.catch(request.fail);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. The reference is bare, so the rejection value arrives as request.fail's FIRST parameter - the `json` payload, not `err` - and is what gets logged and flashed. Preserve that: the flash content is the rejection value itself, not a message derived from it. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: deferred. The handler is registered synchronously while the chain at line 62 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/pages.js:54` and `lib/controllers/pages.js:68`. Paired by semantic subject `shape:.catch(request.fail) bare reference`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.

### 7.10 `lib/controllers/trinket.js` - 67 rows

- [ ] `lib/controllers/trinket.js:58` `id=trinket.legacyReply.response.1` - **returns or throws a Boom** - Layer 3 - return errors.badImplementation(value.message);
      Carrier `trinket.legacyReply (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `trinket.addToFolder`, `trinket.autosave`, `trinket.create`, `trinket.createFork`, `trinket.downloadFile`, `trinket.downloadMain`, `trinket.embed`, `trinket.getByShortCode`, `trinket.interactions`, `trinket.list`, `trinket.remove`, `trinket.removeFromFolder`, `trinket.search`, `trinket.updateMetrics`, `trinket.updateSlug` - Source `return errors.badImplementation(value.message);`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 500 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/trinket.js:105` `id=trinket.assertZipCodeWithinBounds.response.1` - **returns or throws a Boom** - Layer 1 - throw new Error('zipCode archive declares ' + names.length + ' ...
      Carrier `trinket.assertZipCodeWithinBounds (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `trinket.autosave`, `trinket.draft` - Source `throw new Error('zipCode archive declares ' + names.length +`
      Target: Layer 1. The handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message || String(err)), so the answer is 500 and Boom hides the 5xx message. After conversion this becomes structural: a rejecting async handler lands in the same catch. Preserve the 500, the generic body and the error-level log. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/trinket.js:120` `id=trinket.assertZipCodeWithinBounds.response.2` - **returns or throws a Boom** - Layer 1 - throw new Error('zipCode archive declares more than ' + ZIP_MAX...
      Carrier `trinket.assertZipCodeWithinBounds (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `trinket.autosave`, `trinket.draft` - Source `throw new Error('zipCode archive declares more than ' +`
      Target: Layer 1. The handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message || String(err)), so the answer is 500 and Boom hides the 5xx message. After conversion this becomes structural: a rejecting async handler lands in the same catch. Preserve the 500, the generic body and the error-level log. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/trinket.js:129` `id=trinket.assertZipCodeWithinBounds.response.3` - **returns or throws a Boom** - Layer 1 - throw new Error('zipCode archive declares a ' + Math.round(tota...
      Carrier `trinket.assertZipCodeWithinBounds (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `trinket.autosave`, `trinket.draft` - Source `throw new Error('zipCode archive declares a ' + Math.roun...`
      Target: Layer 1. The handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message || String(err)), so the answer is 500 and Boom hides the 5xx message. After conversion this becomes structural: a rejecting async handler lands in the same catch. Preserve the 500, the generic body and the error-level log. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/trinket.js:162-169` `id=trinket.readZipCodeWithinBounds.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to a local function - handed onward
      Carrier `trinket.readZipCodeWithinBounds (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `trinket.autosave`, `trinket.draft` - Source `function fail(err) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 168 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 168, so `err` leaves this function and the awaiting caller decides the response.
- [ ] `lib/controllers/trinket.js:365` `id=trinket.lookupTrinket.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.lookupTrinket (module-local)` (internal callee) - Routes `GET /library/trinkets/{path*}` (bound on `trinket.library`, which encloses this one) - Source `return Trinket.findById(id).catch(function() { return nul...`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 365 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / route handler; the target produces none / no response / answers nothing / internal callee. What differs: payload or redirect shape. R-e requires these to match, and no approved deviation names this edge. Paired by nearest outcome in the routed carrier. The two trees differ in surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4), which the AAP prescribes and this comparison records rather than reads as an R-e failure. paired to the nearest outcome in `lib/controllers/trinket.js` routed carrier `trinket.library`, which differs, so this row is reported open on that difference rather than closed on the pairing.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler body runs work but produces no response and rethrows nothing, so the error is absorbed here.
- [x] `lib/controllers/trinket.js:447-449` `id=trinket.getById.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.getById` (route handler) - Routes `GET /api/trinkets/{trinketId}` - Source `.catch(function(err) {`
      Target: The error is discarded and the handler answers with `request.success`, so the failure is reported to the client as a success. Funnel: none. Preserve the status, the payload and the discarding - R-d prohibits turning this into a failure response, however much it looks like one is owed. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 439 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce none / no response / answers / route handler, measured at `lib/controllers/trinket.js:274-276` and `lib/controllers/trinket.js:447-449`. Paired by semantic subject `shape:.catch() handler - absorbs`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:458` `id=trinket.grant.handler.1` - **calls request.fail locally** - Layer 2 - .catch(request.fail) bare reference
      Carrier `trinket.grant` (route handler) - Routes `POST /api/trinkets/{trinketId}/grant` - Source `.catch(request.fail);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. The reference is bare, so the rejection value arrives as request.fail's FIRST parameter - the `json` payload, not `err` - and is what gets logged and flashed. Preserve that: the flash content is the rejection value itself, not a message derived from it. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: deferred. The handler is registered synchronously while the chain at line 456 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/trinket.js:285` and `lib/controllers/trinket.js:458`. Paired by semantic subject `shape:.catch(request.fail) bare reference`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:494-496` `id=trinket.update.handler.1` - **logs and continues** - none - .catch() handler - logs only
      Carrier `trinket.update` (route handler) - Routes `PUT /api/trinkets/{trinketId}/code`, `PUT /api/trinkets/{trinketId}/name`, `PUT /api/trinkets/{trinketId}/description`, `PUT /api/trinkets/{trinketId}/published` - Source `.catch(function(err) {`
      Target: No response is produced here and nothing is thrown: the error is logged via `console.error` and control continues. Funnel: none - and that is the point. Preserve the continuation. Converting a log-and-continue branch into a rejection or a returned Boom turns an edge that produces no response into one that does, changes the status a client sees from whatever else settles the request to a 500, and is exactly the silent conversion R-e is written to catch. Side effects: the `console.error` call and nothing else - no flash, no session write, no response, no rethrow. A conversion that adds any of those changes this edge. Timing: deferred. The handler is registered synchronously while the chain at line 484 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/trinket.js:321-323` and `lib/controllers/trinket.js:494-496`. Paired by semantic subject `shape:.catch() handler - logs only`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:506` `id=trinket.update.handler.2` - **calls request.fail locally** - Layer 2 - .catch(request.fail) bare reference
      Carrier `trinket.update` (route handler) - Routes `PUT /api/trinkets/{trinketId}/code`, `PUT /api/trinkets/{trinketId}/name`, `PUT /api/trinkets/{trinketId}/description`, `PUT /api/trinkets/{trinketId}/published` - Source `.catch(request.fail);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. The reference is bare, so the rejection value arrives as request.fail's FIRST parameter - the `json` payload, not `err` - and is what gets logged and flashed. Preserve that: the flash content is the rejection value itself, not a message derived from it. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: deferred. The handler is registered synchronously while the chain at line 480 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/trinket.js:333` and `lib/controllers/trinket.js:506`. Paired by semantic subject `shape:.catch(request.fail) bare reference`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:548-550` `id=trinket.create.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.create` (route handler) - Routes `POST /python`, `POST /api/trinkets` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 538 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:375` and `lib/controllers/trinket.js:548-550`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:601-603` `id=trinket.createFork.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.createFork` (route handler) - Routes `POST /api/trinkets/{trinketId}/forks` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 589 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:428` and `lib/controllers/trinket.js:601-603`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:620-622` `id=trinket.updateMetrics.cps.1` - **resolves on a later callback** - none - CPS callback boundary (Trinket.findById)
      Carrier `trinket.updateMetrics` (route handler) - Routes `PUT /api/trinkets/{trinketId}/metrics` - Source `return Trinket.findById(request.params.trinketId, functio...`
      Target: The response is produced inside the callback passed to `Trinket.findById` (`request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `Trinket.findById` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/trinket.js:441-443` and `lib/controllers/trinket.js:620-622`. Paired by semantic subject `guards:Trinket.findById`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback declares `err` and never inspects it, so the failure of `Trinket.findById` is dropped while the response is produced regardless.
      Note: `err` is declared and never read.
- [x] `lib/controllers/trinket.js:633-635` `id=trinket.updateMetrics.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.updateMetrics` (route handler) - Routes `PUT /api/trinkets/{trinketId}/metrics` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 629 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:454` and `lib/controllers/trinket.js:633-635`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:659-661` `id=trinket.remove.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.remove` (route handler) - Routes `DELETE /api/trinkets/{trinketId}` - Source `}).catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 653 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:478` and `lib/controllers/trinket.js:659-661`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:706` `id=trinket.getByShortCode.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notFound();
      Carrier `trinket.getByShortCode` (route handler) - Routes `GET /<lang>/{shortCode}` - Source `return errors.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / route handler, measured at `lib/controllers/trinket.js:518` and `lib/controllers/trinket.js:706`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:728-730` `id=trinket.getByShortCode.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.getByShortCode` (route handler) - Routes `GET /<lang>/{shortCode}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 710 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:540` and `lib/controllers/trinket.js:728-730`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:968-970` `id=trinket.embed.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.embed` (route handler) - Routes `GET /embed/{lang}/{trinketId}`, `GET /embed/{lang}`, `GET /tools/{version}/jekyll/embed/{lang}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 812 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:778` and `lib/controllers/trinket.js:968-970`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:1017` `id=trinket.email.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: "Email is not configured. Sharing via email is...)
      Carrier `trinket.email` (route handler) - Routes `POST /api/trinkets/{trinketId}/email` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/trinket.js:825` and `lib/controllers/trinket.js:1017`. Paired by semantic subject `produces:{ message: "Email is not configured. Sharing via email is...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1088` `id=trinket.email.response.2` - **returns or throws a Boom** - Layer 3 - return errors.forbidden();
      Carrier `trinket.email` (route handler) - Routes `POST /api/trinkets/{trinketId}/email` - Source `return errors.forbidden();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 403 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: asynchronous, in the chain step at line 1077, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 403 / answers / route handler, measured at `lib/controllers/trinket.js:881` and `lib/controllers/trinket.js:1088`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1120-1123` `id=trinket.snapshot.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'FileUtil.uploadSnapshotFromBuffer' - handed onward
      Carrier `trinket.snapshot` (route handler) - Routes `POST /api/trinkets/{trinketId}/snapshot` - Source `FileUtil.uploadSnapshotFromBuffer(imagename, buf, functio...`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 1121 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/trinket.js:907-910` and `lib/controllers/trinket.js:1120-1123`. Paired by semantic subject `guards:FileUtil.uploadSnapshotFromBuffer`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 1121, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/trinket.js:1129` `id=trinket.snapshot.handler.1` - **calls request.fail locally** - Layer 2 - .catch(request.fail) bare reference
      Carrier `trinket.snapshot` (route handler) - Routes `POST /api/trinkets/{trinketId}/snapshot` - Source `.catch(request.fail);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. The reference is bare, so the rejection value arrives as request.fail's FIRST parameter - the `json` payload, not `err` - and is what gets logged and flashed. Preserve that: the flash content is the rejection value itself, not a message derived from it. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: deferred. The handler is registered synchronously while the chain at line 1119 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/trinket.js:916` and `lib/controllers/trinket.js:1129`. Paired by semantic subject `shape:.catch(request.fail) bare reference`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1143-1145` `id=trinket.interactions.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.interactions` (route handler) - Routes `GET /api/trinkets/{trinketId}/interactions` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1139 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:927` and `lib/controllers/trinket.js:1143-1145`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [ ] `lib/controllers/trinket.js:1166` `id=trinket.logError.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.logError` (route handler) - Routes `POST /api/trinkets/codeerror` - Source `await error.save().catch(function() {});`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 1166 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / route handler; the target produces none / no response / answers nothing / route handler. What differs: payload or redirect shape. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler body is empty.
- [x] `lib/controllers/trinket.js:1204-1206` `id=trinket.logClientMetric.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.logClientMetric` (route handler) - Routes `POST /api/trinkets/clientmetric` - Source `.catch(function(err) {`
      Target: The error is discarded and the handler answers with `request.success`, so the failure is reported to the client as a success. Funnel: none. Preserve the status, the payload and the discarding - R-d prohibits turning this into a failure response, however much it looks like one is owed. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 1200 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce none / no response / answers / route handler, measured at `lib/controllers/trinket.js:982-984` and `lib/controllers/trinket.js:1204-1206`. Paired by semantic subject `shape:.catch() handler - absorbs`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1249-1255` `id=trinket.draft.errparam.1` - **swallows silently** - none - error parameter of a callback to '.then' - undispositioned
      Carrier `trinket.draft` (route handler) - Routes `POST /api/trinkets/{trinketId}/draft` - Source `}, function(err) {`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/trinket.js:1009-1011` and `lib/controllers/trinket.js:1249-1255`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: `err` is declared and never read.
- [ ] `lib/controllers/trinket.js:1264-1266` `id=trinket.draft.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.draft` (route handler) - Routes `POST /api/trinkets/{trinketId}/draft` - Source `.catch(function() {`
      Target: The error is discarded and the handler answers with `request.success`, so the failure is reported to the client as a success. Funnel: none. Preserve the status, the payload and the discarding - R-d prohibits turning this into a failure response, however much it looks like one is owed. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 1258 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1267-1269` `id=trinket.draft.errparam.2` - **swallows silently** - none - error parameter of a callback to '.then' - undispositioned
      Carrier `trinket.draft` (route handler) - Routes `POST /api/trinkets/{trinketId}/draft` - Source `}, function(err) {`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/trinket.js:1023-1025` and `lib/controllers/trinket.js:1267-1269`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: `err` is declared and never read.
- [x] `lib/controllers/trinket.js:1274-1276` `id=trinket.draft.handler.2` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.draft` (route handler) - Routes `POST /api/trinkets/{trinketId}/draft` - Source `.catch(function(err) {`
      Target: The error is discarded and the handler answers with `request.success`, so the failure is reported to the client as a success. Funnel: none. Preserve the status, the payload and the discarding - R-d prohibits turning this into a failure response, however much it looks like one is owed. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 1240 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: CLOSED. Baseline and target both produce none / no response / answers / route handler, measured at `lib/controllers/trinket.js:1020-1022` and `lib/controllers/trinket.js:1274-1276`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1286-1288` `id=trinket.draft.handler.3` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.draft` (route handler) - Routes `POST /api/trinkets/{trinketId}/draft` - Source `.catch(function() {`
      Target: The error is discarded and the handler answers with `request.success`, so the failure is reported to the client as a success. Funnel: none. Preserve the status, the payload and the discarding - R-d prohibits turning this into a failure response, however much it looks like one is owed. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 1280 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce none / no response / answers / route handler, measured at `lib/controllers/trinket.js:1034-1036` and `lib/controllers/trinket.js:1286-1288`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1302-1304` `id=trinket.discardDraft.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.discardDraft` (route handler) - Routes `DELETE /api/drafts/{trinketId}` - Source `.catch(function() {`
      Target: The error is discarded and the handler answers with `request.success`, so the failure is reported to the client as a success. Funnel: none. Preserve the status, the payload and the discarding - R-d prohibits turning this into a failure response, however much it looks like one is owed. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 1296 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce none / no response / answers / route handler, measured at `lib/controllers/trinket.js:1050-1052` and `lib/controllers/trinket.js:1302-1304`. Paired by semantic subject `shape:.catch() handler - absorbs`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/trinket.js:1344-1350` `id=trinket.autosave.errparam.1` - **swallows silently** - none - error parameter of a callback to '.then' - undispositioned
      Carrier `trinket.autosave` (route handler) - Routes `POST /api/trinkets/{trinketId}/autosave` - Source `}, function(err) {`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: `err` is read but never turned into a response, a log or a rethrow.
- [ ] `lib/controllers/trinket.js:1358-1360` `id=trinket.autosave.handler.1` - **propagates to its caller** - Layer 1 - .catch() handler - hands the error onward
      Carrier `trinket.autosave` (route handler) - Routes `POST /api/trinkets/{trinketId}/autosave` - Source `}).catch(function(err) {`
      Target: The error is handed to an outer continuation - resolves the enclosing promise WITH the error, via `resolve(legacyReply(err, h))`, at line 1359 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `resolve`, and its own row states them where it is in this file. Timing: deferred. The handler is registered synchronously while the chain at line 1353 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: **OPEN - the outcome changed.** Baseline produced Layer 3 / 500 / answers / route handler; the target produces Layer 1 / no response / answers / route handler. What differs: funnel, status served, payload or redirect shape, side effects, settlement timing - the target waits for LESS than the baseline did. R-e requires these to match, and no approved deviation names this edge. Paired by nearest outcome in the routed carrier. paired to the nearest outcome in `lib/controllers/trinket.js` routed carrier `trinket.autosave`, which differs, so this row is reported open on that difference rather than closed on the pairing.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler resolves the enclosing promise WITH the error, via `resolve(legacyReply(err, h))` at line 1359, so the error leaves this function and the awaiting caller decides the response.
- [ ] `lib/controllers/trinket.js:1361-1363` `id=trinket.autosave.errparam.2` - **propagates to its caller** - Layer 1 - error parameter of a callback to '.then' - handed onward
      Carrier `trinket.autosave` (route handler) - Routes `POST /api/trinkets/{trinketId}/autosave` - Source `}, function(err) {`
      Target: The error is handed to an outer continuation - resolves the enclosing promise WITH the error, via `resolve(legacyReply(err, h))`, at line 1362 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `resolve`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback resolves the enclosing promise WITH the error, via `resolve(legacyReply(err, h))` at line 1362, so `err` leaves this function and the awaiting caller decides the response.
- [ ] `lib/controllers/trinket.js:1368-1370` `id=trinket.autosave.handler.2` - **propagates to its caller** - Layer 1 - .catch() handler - hands the error onward
      Carrier `trinket.autosave` (route handler) - Routes `POST /api/trinkets/{trinketId}/autosave` - Source `.catch(function(err) {`
      Target: The error is handed to an outer continuation - resolves the enclosing promise WITH the error, via `resolve(legacyReply(err, h))`, at line 1369 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `resolve`, and its own row states them where it is in this file. Timing: deferred. The handler is registered synchronously while the chain at line 1335 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: **OPEN - the outcome changed.** Baseline produced Layer 3 / 500 / answers / route handler; the target produces Layer 1 / no response / answers / route handler. What differs: funnel, status served, payload or redirect shape, side effects. R-e requires these to match, and no approved deviation names this edge. Paired by nearest outcome in the routed carrier. paired to the nearest outcome in `lib/controllers/trinket.js` routed carrier `trinket.autosave`, which differs, so this row is reported open on that difference rather than closed on the pairing.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler resolves the enclosing promise WITH the error, via `resolve(legacyReply(err, h))` at line 1369, so the error leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/trinket.js:1379-1381` `id=trinket.autosave.handler.3` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.autosave` (route handler) - Routes `POST /api/trinkets/{trinketId}/autosave` - Source `}).catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1374 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1077` and `lib/controllers/trinket.js:1379-1381`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:1385` `id=trinket.autosave.response.1` - **returns or throws a Boom** - Layer 3 - return errors.forbidden();
      Carrier `trinket.autosave` (route handler) - Routes `POST /api/trinkets/{trinketId}/autosave` - Source `return errors.forbidden();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 403 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 403 / answers / route handler, measured at `lib/controllers/trinket.js:1105` and `lib/controllers/trinket.js:1385`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1425-1427` `id=trinket.addToFolder.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.addToFolder` (route handler) - Routes `POST /api/trinkets/{trinketId}/folder` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1409 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1134` and `lib/controllers/trinket.js:1425-1427`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:1430` `id=trinket.addToFolder.response.1` - **returns or throws a Boom** - Layer 3 - return errors.forbidden();
      Carrier `trinket.addToFolder` (route handler) - Routes `POST /api/trinkets/{trinketId}/folder` - Source `return errors.forbidden();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 403 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 403 / answers / route handler, measured at `lib/controllers/trinket.js:1138` and `lib/controllers/trinket.js:1430`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1469-1471` `id=trinket.removeFromFolder.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.removeFromFolder` (route handler) - Routes `DELETE /api/trinkets/{trinketId}/folder` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1460 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1156` and `lib/controllers/trinket.js:1469-1471`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:1474` `id=trinket.removeFromFolder.response.1` - **returns or throws a Boom** - Layer 3 - return errors.forbidden();
      Carrier `trinket.removeFromFolder` (route handler) - Routes `DELETE /api/trinkets/{trinketId}/folder` - Source `return errors.forbidden();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 403 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 403 / answers / route handler, measured at `lib/controllers/trinket.js:1160` and `lib/controllers/trinket.js:1474`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1491-1493` `id=trinket.search.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.search` (route handler) - Routes `GET /api/trinkets/search` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1485 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1171` and `lib/controllers/trinket.js:1491-1493`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:1502` `id=trinket.downloadMain.response.1` - **returns or throws a Boom** - Layer 3 - throw errors.notFound()
      Carrier `trinket.downloadMain` (route handler) - Routes `GET /<lang>/{shortCode}/` - Source `throw errors.notFound();`
      Target: The rejection is taken by the `.catch(...)` at line 1532, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1532, whose own row states them. Timing: asynchronous, in the chain step at line 1499, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / route handler, measured at `lib/controllers/trinket.js:1181` and `lib/controllers/trinket.js:1502`. Paired by semantic subject `throws:errors.notFound()`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1508` `id=trinket.downloadMain.response.2` - **returns or throws a Boom** - Layer 3 - throw new Error()
      Carrier `trinket.downloadMain` (route handler) - Routes `GET /<lang>/{shortCode}/` - Source `throw new Error();`
      Target: The rejection is taken by the `.catch(...)` at line 1532, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1532, whose own row states them. Timing: asynchronous, in the chain step at line 1499, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1187` and `lib/controllers/trinket.js:1508`. Paired by semantic subject `throws:new Error()`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1532-1534` `id=trinket.downloadMain.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.downloadMain` (route handler) - Routes `GET /<lang>/{shortCode}/` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1499 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1207` and `lib/controllers/trinket.js:1532-1534`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:1545` `id=trinket.downloadFile.response.1` - **returns or throws a Boom** - Layer 3 - throw errors.notFound()
      Carrier `trinket.downloadFile` (route handler) - Routes `GET /<lang>/{shortCode}/{path*}` - Source `throw errors.notFound();`
      Target: The rejection is taken by the `.catch(...)` at line 1602, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1602, whose own row states them. Timing: asynchronous, in the chain step at line 1542, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / route handler, measured at `lib/controllers/trinket.js:1219` and `lib/controllers/trinket.js:1545`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1551` `id=trinket.downloadFile.response.2` - **returns or throws a Boom** - Layer 3 - throw new Error()
      Carrier `trinket.downloadFile` (route handler) - Routes `GET /<lang>/{shortCode}/{path*}` - Source `throw new Error();`
      Target: The rejection is taken by the `.catch(...)` at line 1602, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1602, whose own row states them. Timing: asynchronous, in the chain step at line 1542, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1225` and `lib/controllers/trinket.js:1551`. Paired by semantic subject `throws:new Error()`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1599` `id=trinket.downloadFile.response.3` - **returns or throws a Boom** - Layer 3 - throw errors.notFound()
      Carrier `trinket.downloadFile` (route handler) - Routes `GET /<lang>/{shortCode}/{path*}` - Source `throw errors.notFound();`
      Target: The rejection is taken by the `.catch(...)` at line 1602, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1602, whose own row states them. Timing: asynchronous, in the chain step at line 1542, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / route handler, measured at `lib/controllers/trinket.js:1263` and `lib/controllers/trinket.js:1599`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1602-1604` `id=trinket.downloadFile.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.downloadFile` (route handler) - Routes `GET /<lang>/{shortCode}/{path*}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1542 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1267` and `lib/controllers/trinket.js:1602-1604`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/trinket.js:1613` `id=trinket.updateSlug.response.1` - **returns or throws a Boom** - Layer 3 - return errors.badRequest();
      Carrier `trinket.updateSlug` (route handler) - Routes `PUT /api/trinkets/{trinketId}/slug` - Source `return errors.badRequest();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 400 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 400 / answers / route handler, measured at `lib/controllers/trinket.js:1276` and `lib/controllers/trinket.js:1613`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1620` `id=trinket.updateSlug.response.2` - **returns or throws a Boom** - Layer 3 - return errors.conflict();
      Carrier `trinket.updateSlug` (route handler) - Routes `PUT /api/trinkets/{trinketId}/slug` - Source `: errors.conflict();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 409 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: asynchronous, in the chain step at line 1616, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 409 / answers / route handler, measured at `lib/controllers/trinket.js:1283` and `lib/controllers/trinket.js:1620`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1622-1624` `id=trinket.updateSlug.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `trinket.updateSlug` (route handler) - Routes `PUT /api/trinkets/{trinketId}/slug` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1616 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1286` and `lib/controllers/trinket.js:1622-1624`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [ ] `lib/controllers/trinket.js:1657` `id=trinket.removeZipDir.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.removeZipDir (module-local)` (internal callee) - Routes `POST /api/trinkets/download` (bound on `trinket.downloadPostedZip`, which encloses this one) - Source `fs.promises.rm(zipDir, { recursive : true, force : true }...`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 1657 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler body is empty.
- [x] `lib/controllers/trinket.js:1664-1666` `id=trinket.downloadPostedZip.handler.1` - **propagates to its caller** - Layer 1 - .on('error') handler - hands the error onward
      Carrier `trinket.downloadPostedZip` (route handler) - Routes `POST /api/trinkets/download` - Source `archive.on('error', function(err) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 1665 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: when the stream emits `error`, which is not awaited by anything: for a refused connection it fires INSTEAD of `end`, and for a mid-stream failure it fires after `response`. The handler body has already returned by then, so the settlement time of the request is decided by whichever listener runs - or, when none does, not at all. Preserve that, including the case where nothing settles.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/trinket.js:1309-1311` and `lib/controllers/trinket.js:1664-1666`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler rejects the enclosing promise via `reject(err)` at line 1665, so the error leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/trinket.js:1670-1674` `id=trinket.downloadPostedZip.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'archive.on' - handed onward
      Carrier `trinket.downloadPostedZip` (route handler) - Routes `POST /api/trinkets/download` - Source `archive.on('warning', function(err) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 1672 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/trinket.js:1313-1317` and `lib/controllers/trinket.js:1670-1674`. Paired by semantic subject `guards:archive.on`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 1672, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/trinket.js:1682-1684` `id=trinket.downloadPostedZip.handler.2` - **propagates to its caller** - Layer 1 - .on('error') handler - hands the error onward
      Carrier `trinket.downloadPostedZip` (route handler) - Routes `POST /api/trinkets/download` - Source `outputWriteStream.on('error', function(err) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 1683 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: when the stream emits `error`, which is not awaited by anything: for a refused connection it fires INSTEAD of `end`, and for a mid-stream failure it fires after `response`. The handler body has already returned by then, so the settlement time of the request is decided by whichever listener runs - or, when none does, not at all. Preserve that, including the case where nothing settles.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/trinket.js:1323-1325` and `lib/controllers/trinket.js:1682-1684`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler rejects the enclosing promise via `reject(err)` at line 1683, so the error leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/trinket.js:1744-1747` `id=trinket.downloadPostedZip.handler.3` - **logs and continues** - none - .catch() handler - logs only
      Carrier `trinket.downloadPostedZip` (route handler) - Routes `POST /api/trinkets/download` - Source `.catch(function(err) {`
      Target: No response is produced here and nothing is thrown: the error is logged via `console.log` and control continues. Funnel: none - and that is the point. Preserve the continuation. Converting a log-and-continue branch into a rejection or a returned Boom turns an edge that produces no response into one that does, changes the status a client sees from whatever else settles the request to a 500, and is exactly the silent conversion R-e is written to catch. Side effects: the `console.log` call and nothing else - no flash, no session write, no response, no rethrow. A conversion that adds any of those changes this edge. Timing: deferred. The handler is registered synchronously while the chain at line 1737 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/trinket.js:1357-1360` and `lib/controllers/trinket.js:1744-1747`. Paired by semantic subject `shape:.catch() handler - logs only`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1794` `id=trinket.downloadPostedZip.response.1` - **returns or throws a Boom** - Layer 3 - return errors.badImplementation(err.message);
      Carrier `trinket.downloadPostedZip` (route handler) - Routes `POST /api/trinkets/download` - Source `return errors.badImplementation(err.message);`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 500 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: asynchronous, in the chain step at line 1752, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/trinket.js:1390` and `lib/controllers/trinket.js:1794`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/trinket.js:1895-1901` `id=trinket.listCore.errparam.1` - **swallows silently** - none - error parameter of a callback to 'User.findById' - undispositioned
      Carrier `trinket.listCore (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `trinket.list` - its value is handed on at line 1802 - Source `User.findById(options.user, function(err, user) {`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / internal callee, measured at `lib/controllers/trinket.js:175-181` and `lib/controllers/trinket.js:1895-1901`. Paired by semantic subject `guards:User.findById`. The two trees differ in surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4), which the AAP prescribes and this comparison records rather than reads as an R-e failure.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: `err` is read but never turned into a response, a log or a rethrow.
- [ ] `lib/controllers/trinket.js:2003` `id=trinket.downloadJSON.response.1` - **returns or throws a Boom** - none - throw new Error()
      Carrier `trinket.downloadJSON (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s)  - its value is handed on at line 34 - Source `throw new Error();`
      Target: No funnel is entered: this edge produces no response, and the request settles only if something else settles it. Preserve that. Giving the edge a response - by returning a Boom, by throwing where the throw would now be caught, or by adding a catch - converts an edge that answers nothing into one that answers 500, which is the silent conversion R-e exists to catch. Side effects: none, and nothing is logged either: this edge reaches no funnel, so no funnel writes anything in response to it. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: not compared - proven unreachable
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: PROVEN UNREACHABLE. This member is mentioned nowhere in the analysed corpus outside its own declaration - not called, not handed on as a value, not registered as a server method, not exported as a lifecycle method and named by no route - so nothing in this tree can reach this edge. It claims no funnel because it produces no response, it is excluded from the closure gate because there is no outcome to compare, and it is dead code rather than an open question.
- [ ] `lib/controllers/trinket.js:2066` `id=trinket.removeZipDir.handler.2` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `trinket.removeZipDir (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `trinket.downloadPostedZip` - its value is handed on at line 1774, 1775, 2170, 2171 - Source `fs.promises.rm(zipDir, { recursive : true, force : true }...`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 2066 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is NEITHER returned, awaited nor assigned at that statement, so nothing waits for the rejection today: the request settles on whichever path settles it first, and adding an await here would change that.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler body is empty.
- [ ] `lib/controllers/trinket.js:2073-2075` `id=trinket.$module.errparam.1` - **propagates to its caller** - none - error parameter of a callback to 'archive.on' - handed onward
      Carrier `(module scope)` (module scope) - Routes none declared - driven by loading the module - Source `archive.on('err', function(err) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 2074 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: none, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 2074, so `err` leaves this function and the awaiting caller decides the response.
- [ ] `lib/controllers/trinket.js:2077-2084` `id=trinket.$module.errparam.2` - **propagates to its caller** - none - error parameter of a callback to 'archive.on' - handed onward
      Carrier `(module scope)` (module scope) - Routes none declared - driven by loading the module - Source `archive.on('warning', function(err) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 2082 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: none, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself and `console.log`: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 2082, so `err` leaves this function and the awaiting caller decides the response. It logs `err` as well, which is why this row is not `logs and continues`: the continuation is a rejection, not the normal path.
- [ ] `lib/controllers/trinket.js:2091-2093` `id=trinket.$module.handler.1` - **propagates to its caller** - none - .on('error') handler - hands the error onward
      Carrier `(module scope)` (module scope) - Routes none declared - driven by loading the module - Source `outputWriteStream.on('error', function(err) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 2092 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: none, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: when the stream emits `error`, which is not awaited by anything: for a refused connection it fires INSTEAD of `end`, and for a mid-stream failure it fires after `response`. The handler body has already returned by then, so the settlement time of the request is decided by whichever listener runs - or, when none does, not at all. Preserve that, including the case where nothing settles.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler rejects the enclosing promise via `reject(err)` at line 2092, so the error leaves this function and the awaiting caller decides the response.
- [ ] `lib/controllers/trinket.js:2107` `id=trinket.$module.response.1` - **returns or throws a Boom** - Layer 1 - throw new Error()
      Carrier `(module scope)` (module scope) - Routes none declared - driven by loading the module - Source `throw new Error();`
      Target: Layer 1. The handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message || String(err)), so the answer is 500 and Boom hides the 5xx message. After conversion this becomes structural: a rejecting async handler lands in the same catch. Preserve the 500, the generic body and the error-level log. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/trinket.js:2154` `id=trinket.$module.response.2` - **returns or throws a Boom** - Layer 3 - throw new Error(streams[i].reason.message)
      Carrier `(module scope)` (module scope) - Routes none declared - driven by loading the module - Source `throw new Error(streams[i].reason.message);`
      Target: The rejection is taken by the `.catch(...)` at line 2184, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 2184, whose own row states them. Timing: asynchronous, in the chain step at line 2144, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/trinket.js:2184-2191` `id=trinket.$module.handler.2` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via legacyReply
      Carrier `(module scope)` (module scope) - Routes none declared - driven by loading the module - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 2144 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `legacyReply`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.

### 7.11 `lib/controllers/users.js` - 116 rows

- [ ] `lib/controllers/users.js:127` `id=users.attempt.response.1` - **returns or throws a Boom** - none - throw new Error('Exceeded maxRedirects. Probably stuck in a red...
      Carrier `users.attempt (module-local)` (internal callee) - Routes none declared - driven by **nothing - proven unreachable** - Source `throw new Error('Exceeded maxRedirects. Probably stuck in...`
      Target: No funnel is entered: this edge produces no response, and the request settles only if something else settles it. Preserve that. Giving the edge a response - by returning a Boom, by throwing where the throw would now be caught, or by adding a catch - converts an edge that answers nothing into one that answers 500, which is the silent conversion R-e exists to catch. Side effects: none, and nothing is logged either: this edge reaches no funnel, so no funnel writes anything in response to it. Timing: asynchronous, in the chain step at line 101, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: not compared - proven unreachable
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: PROVEN UNREACHABLE. This member is mentioned nowhere in the analysed corpus outside its own declaration - not called, not handed on as a value, not registered as a server method, not exported as a lifecycle method and named by no route - so nothing in this tree can reach this edge. It claims no funnel because it produces no response, it is excluded from the closure gate because there is no outcome to compare, and it is dead code rather than an open question.
- [x] `lib/controllers/users.js:381` `id=users.create.response.1` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `users.create` (route handler) - Routes `POST /users`, `POST /api/users` - Source `return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:34` and `lib/controllers/users.js:381`. Paired by semantic subject `produces:`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 3 - the cross-request state leak in `fail.redirect`
- [x] `lib/controllers/users.js:405` `id=users.create.response.2` - **returns or throws a Boom** - Layer 1 - throw new Error("blocking signup from: " + request.payload.email)
      Carrier `users.create` (route handler) - Routes `POST /users`, `POST /api/users` - Source `throw new Error("blocking signup from: " + request.payloa...`
      Target: Layer 1. The handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message || String(err)), so the answer is 500 and Boom hides the 5xx message. After conversion this becomes structural: a rejecting async handler lands in the same catch. Preserve the 500, the generic body and the error-level log. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/users.js:58` and `lib/controllers/users.js:405`. Paired by semantic subject `throws:new Error("blocking signup from: " + request.payload.email)`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:410-413` `id=users.create.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'User.exists' - handed onward
      Carrier `users.create` (route handler) - Routes `POST /users`, `POST /api/users` - Source `User.exists(user, function(err, result) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 411 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/users.js:63-66` and `lib/controllers/users.js:410-413`. Paired by semantic subject `guards:User.exists`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 411, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/users.js:418` `id=users.create.response.3` - **calls request.fail locally** - Layer 2 - request.fail(json)
      Carrier `users.create` (route handler) - Routes `POST /users`, `POST /api/users` - Source `return request.fail(json);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:71` and `lib/controllers/users.js:418`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 3 - the cross-request state leak in `fail.redirect`
- [x] `lib/controllers/users.js:428-431` `id=users.create.errparam.2` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'request.yar._logIn' - handed onward
      Carrier `users.create` (route handler) - Routes `POST /users`, `POST /api/users` - Source `request.yar._logIn(savedUser, function(err) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 429 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/users.js:81-84` and `lib/controllers/users.js:428-431`. Paired by semantic subject `guards:request.yar._logIn`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 429, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/users.js:441` `id=users.create.response.4` - **calls request.fail locally** - Layer 2 - request.fail(json)
      Carrier `users.create` (route handler) - Routes `POST /users`, `POST /api/users` - Source `return request.fail(json);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:94` and `lib/controllers/users.js:441`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 3 - the cross-request state leak in `fail.redirect`
- [x] `lib/controllers/users.js:443` `id=users.create.response.5` - **calls request.fail locally** - Layer 2 - request.fail(json)
      Carrier `users.create` (route handler) - Routes `POST /users`, `POST /api/users` - Source `return request.fail(json, err);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:96` and `lib/controllers/users.js:443`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 3 - the cross-request state leak in `fail.redirect`
- [x] `lib/controllers/users.js:500` `id=users.login.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: LOGIN_FAILURE_MESSAGE })
      Carrier `users.login` (route handler) - Routes `POST /login`, `POST /api/users/login` - Source `return request.fail({ message: LOGIN_FAILURE_MESSAGE });`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:121` and `lib/controllers/users.js:500`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:506-510` `id=users.login.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'User.findByLogin' - handed onward
      Carrier `users.login` (route handler) - Routes `POST /login`, `POST /api/users/login` - Source `User.findByLogin(requested, function(err, user) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 508 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself and `console.log`: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/users.js:111-115` and `lib/controllers/users.js:506-510`. Paired by semantic subject `guards:User.findByLogin`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 508, so `err` leaves this function and the awaiting caller decides the response. It logs `err` as well, which is why this row is not `logs and continues`: the continuation is a rejection, not the normal path.
- [x] `lib/controllers/users.js:521` `id=users.login.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message: LOGIN_FAILURE_MESSAGE })
      Carrier `users.login` (route handler) - Routes `POST /login`, `POST /api/users/login` - Source `return request.fail({ message: LOGIN_FAILURE_MESSAGE });`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:125` and `lib/controllers/users.js:521`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:541` `id=users.login.response.3` - **calls request.fail locally** - Layer 2 - request.fail({ message: LOGIN_FAILURE_MESSAGE })
      Carrier `users.login` (route handler) - Routes `POST /login`, `POST /api/users/login` - Source `return request.fail({ message: LOGIN_FAILURE_MESSAGE });`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:129` and `lib/controllers/users.js:541`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:547` `id=users.login.response.4` - **calls request.fail locally** - Layer 2 - request.fail({ message: LOGIN_FAILURE_MESSAGE })
      Carrier `users.login` (route handler) - Routes `POST /login`, `POST /api/users/login` - Source `return request.fail({ message: LOGIN_FAILURE_MESSAGE });`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:144` and `lib/controllers/users.js:547`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/users.js:557` `id=users.login.response.5` - **calls request.fail locally** - Layer 2 - request.fail({ message: LOGIN_FAILURE_MESSAGE })
      Carrier `users.login` (route handler) - Routes `POST /login`, `POST /api/users/login` - Source `return request.fail({ message: LOGIN_FAILURE_MESSAGE });`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:563-567` `id=users.login.errparam.2` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'user.comparePassword' - handed onward
      Carrier `users.login` (route handler) - Routes `POST /login`, `POST /api/users/login` - Source `user.comparePassword(password, function(err, isMatch) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 565 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself and `console.log`: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/users.js:135-139` and `lib/controllers/users.js:563-567`. Paired by semantic subject `guards:user.comparePassword`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 565, so `err` leaves this function and the awaiting caller decides the response. It logs `err` as well, which is why this row is not `logs and continues`: the continuation is a rejection, not the normal path.
- [ ] `lib/controllers/users.js:574` `id=users.login.response.6` - **calls request.fail locally** - Layer 2 - request.fail({ message: LOGIN_FAILURE_MESSAGE })
      Carrier `users.login` (route handler) - Routes `POST /login`, `POST /api/users/login` - Source `return request.fail({ message: LOGIN_FAILURE_MESSAGE });`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:655` `id=users.login.response.7` - **calls request.fail locally** - Layer 2 - request.fail(err)
      Carrier `users.login` (route handler) - Routes `POST /login`, `POST /api/users/login` - Source `return request.fail(err);`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:199` and `lib/controllers/users.js:655`. Paired by semantic subject `produces:err`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:702-707` `id=users.remove.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `users.remove` (route handler) - Routes `DELETE /api/users` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 688 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:209` and `lib/controllers/users.js:702-707`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/users.js:715` `id=users.remove.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `users.remove` (route handler) - Routes `DELETE /api/users` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/users.js:213` and `lib/controllers/users.js:715`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:734` `id=users.sendPassReset.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: "Email is not configured. Password reset is no...)
      Carrier `users.sendPassReset` (route handler) - Routes `POST /send-pass-reset` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:230` and `lib/controllers/users.js:734`. Paired by semantic subject `produces:{ message: "Email is not configured. Password reset is no...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/users.js:765` `id=users.sendPassReset.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message: PASS_RESET_UNIFORM_MESSAGE })
      Carrier `users.sendPassReset` (route handler) - Routes `POST /send-pass-reset` - Source `return request.fail({ message: PASS_RESET_UNIFORM_MESSAGE...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:775-831` `id=users.sendPassReset.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (User.findByLogin)
      Carrier `users.sendPassReset` (route handler) - Routes `POST /send-pass-reset` - Source `User.findByLogin(request.payload.email, function(err, use...`
      Target: The response is produced inside the callback passed to `User.findByLogin` (`request.fail`, `request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `User.findByLogin` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:237-259` and `lib/controllers/users.js:775-831`. Paired by semantic subject `guards:User.findByLogin`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:776` `id=users.sendPassReset.response.3` - **calls request.fail locally** - Layer 2 - request.fail(err) with no return
      Carrier `users.sendPassReset` (route handler) - Routes `POST /send-pass-reset` - Source `if (err) return resolve(request.fail(err));`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findByLogin` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:238` and `lib/controllers/users.js:776`. Paired by semantic subject `produces:err`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:791` `id=users.sendPassReset.response.4` - **calls request.fail locally** - Layer 2 - request.fail({ message: PASS_RESET_UNIFORM_MESSAGE }) with no return
      Carrier `users.sendPassReset` (route handler) - Routes `POST /send-pass-reset` - Source `if (!user) return resolve(request.fail({ message: PASS_RE...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findByLogin` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:239` and `lib/controllers/users.js:791`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [ ] `lib/controllers/users.js:796-830` `id=users.sendPassReset.cps.2` - **resolves on a later callback** - Layer 2 - CPS callback boundary (.randomBytes)
      Carrier `users.sendPassReset` (route handler) - Routes `POST /send-pass-reset` - Source `require('crypto').randomBytes(48, async function(ex, buf) {`
      Target: The response is produced inside the callback passed to `.randomBytes` (`request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `.randomBytes` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / route handler; the target produces Layer 2 / no response / answers / route handler. What differs: funnel, whether a response is produced, payload or redirect shape, side effects. R-e requires these to match, and no approved deviation names this edge. Paired by semantic subject `guards:.randomBytes`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback declares `ex` and never inspects it, so the failure of `.randomBytes` is dropped while the response is produced regardless.
- [ ] `lib/controllers/users.js:816` `id=users.sendPassReset.response.5` - **calls request.fail locally** - Layer 2 - request.fail({ message: PASS_RESET_UNIFORM_MESSAGE }) with no return
      Carrier `users.sendPassReset` (route handler) - Routes `POST /send-pass-reset` - Source `resolve(request.fail({ message: PASS_RESET_UNIFORM_MESSAG...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `.randomBytes` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:865` `id=users.resetPasswordForm.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'reset password key not found' })
      Carrier `users.resetPasswordForm` (route handler) - Routes `GET /reset-pass` - Source `return request.fail({ message: 'reset password key not fo...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:272` and `lib/controllers/users.js:865`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/users.js:869` `id=users.resetPasswordForm.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'reset password key not found' })
      Carrier `users.resetPasswordForm` (route handler) - Routes `GET /reset-pass` - Source `if (!user_id) return request.fail({ message: 'reset passw...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:872-879` `id=users.resetPasswordForm.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (User.findById)
      Carrier `users.resetPasswordForm` (route handler) - Routes `GET /reset-pass` - Source `User.findById(user_id, function(err, user) {`
      Target: The response is produced inside the callback passed to `User.findById` (`request.fail`, `request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `User.findById` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:274-281` and `lib/controllers/users.js:872-879`. Paired by semantic subject `guards:User.findById`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:873` `id=users.resetPasswordForm.response.3` - **returns or throws a Boom** - Layer 3 - resolve(err) - hands the error back as the response, settling the promise the carrier awaits
      Carrier `users.resetPasswordForm` (route handler) - Routes `GET /reset-pass` - Source `if (err) return resolve(err);`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - the factory's status - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:275` and `lib/controllers/users.js:873`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:874` `id=users.resetPasswordForm.response.4` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'user not found' }) with no return
      Carrier `users.resetPasswordForm` (route handler) - Routes `GET /reset-pass` - Source `if (!user) return resolve(request.fail({ message: 'user n...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:276` and `lib/controllers/users.js:874`. Paired by semantic subject `produces:{ message: 'user not found' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:882` `id=users.resetPasswordForm.response.5` - **returns or throws a Boom** - Layer 3 - return err in a catch clause - hands the caught error back as the response
      Carrier `users.resetPasswordForm` (route handler) - Routes `GET /reset-pass` - Source `return err;`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - the factory's status - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:283` and `lib/controllers/users.js:882`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/users.js:912` `id=users.savePassword.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'user not found' })
      Carrier `users.savePassword` (route handler) - Routes `POST /save-pass` - Source `return request.fail({ message: 'user not found' });`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/controllers/users.js:939` `id=users.savePassword.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'user not found' })
      Carrier `users.savePassword` (route handler) - Routes `POST /save-pass` - Source `return request.fail({ message: 'user not found' });`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:943-953` `id=users.savePassword.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (User.findById)
      Carrier `users.savePassword` (route handler) - Routes `POST /save-pass` - Source `User.findById(user_id, function(err, user) {`
      Target: The response is produced inside the callback passed to `User.findById` (`request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `User.findById` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:296-307` and `lib/controllers/users.js:943-953`. Paired by semantic subject `guards:User.findById`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:944` `id=users.savePassword.response.3` - **returns or throws a Boom** - Layer 3 - resolve(err) - hands the error back as the response, settling the promise the carrier awaits
      Carrier `users.savePassword` (route handler) - Routes `POST /save-pass` - Source `if (err) return resolve(err);`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - the factory's status - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:297` and `lib/controllers/users.js:944`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:945` `id=users.savePassword.response.4` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'user not found' }) with no return
      Carrier `users.savePassword` (route handler) - Routes `POST /save-pass` - Source `if (!user) return resolve(request.fail({ message: 'user n...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:298` and `lib/controllers/users.js:945`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:948-952` `id=users.savePassword.cps.2` - **resolves on a later callback** - none - CPS callback boundary (user.save)
      Carrier `users.savePassword` (route handler) - Routes `POST /save-pass` - Source `user.save(async function(err) {`
      Target: The response is produced inside the callback passed to `user.save` (`request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `user.save` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/users.js:301-306` and `lib/controllers/users.js:948-952`. Paired by semantic subject `guards:user.save`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:949` `id=users.savePassword.response.5` - **returns or throws a Boom** - Layer 3 - resolve(err) - hands the error back as the response, settling the promise the carrier awaits
      Carrier `users.savePassword` (route handler) - Routes `POST /save-pass` - Source `if (err) return resolve(err);`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - the factory's status - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: inside the callback `user.save` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:302` and `lib/controllers/users.js:949`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:956` `id=users.savePassword.response.6` - **returns or throws a Boom** - Layer 3 - return err in a catch clause - hands the caught error back as the response
      Carrier `users.savePassword` (route handler) - Routes `POST /save-pass` - Source `return err;`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - the factory's status - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:309` and `lib/controllers/users.js:956`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:970-973` `id=users.account.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'Course.findForUser' - handed onward
      Carrier `users.account` (route handler) - Routes `GET /account`, `GET /account/{accountPage}` - Source `Course.findForUser(request.user.id, function(err, courses) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 971 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/users.js:323-326` and `lib/controllers/users.js:970-973`. Paired by semantic subject `guards:Course.findForUser`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 971, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/users.js:1008-1013` `id=users.account.handler.1` - **swallows silently** - none - .catch() handler - absorbs
      Carrier `users.account` (route handler) - Routes `GET /account`, `GET /account/{accountPage}` - Source `.catch(function(err) {`
      Target: The error is discarded and the handler answers with `request.success`, so the failure is reported to the client as a success. Funnel: none. Preserve the status, the payload and the discarding - R-d prohibits turning this into a failure response, however much it looks like one is owed. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: deferred. The handler is registered synchronously while the chain at line 989 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce none / no response / answers / route handler, measured at `lib/controllers/users.js:361-366` and `lib/controllers/users.js:1008-1013`. Paired by semantic subject `shape:.catch() handler - absorbs`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1027` `id=users.updateProfile.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `users.updateProfile` (route handler) - Routes `PUT /api/users/{userId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/users.js:377` and `lib/controllers/users.js:1027`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1036-1039` `id=users.updateProfile.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'User.exists' - handed onward
      Carrier `users.updateProfile` (route handler) - Routes `PUT /api/users/{userId}` - Source `User.exists(user, function(err, result) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 1037 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/users.js:386-389` and `lib/controllers/users.js:1036-1039`. Paired by semantic subject `guards:User.exists`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 1037, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/users.js:1054` `id=users.updateProfile.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message : "Sorry, that username is already taken. Pleas...)
      Carrier `users.updateProfile` (route handler) - Routes `PUT /api/users/{userId}` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 1052, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:404` and `lib/controllers/users.js:1054`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1062-1113` `id=users.updateProfile.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (user.save)
      Carrier `users.updateProfile` (route handler) - Routes `PUT /api/users/{userId}` - Source `user.save(function(err, user) {`
      Target: The response is produced inside the callback passed to `user.save` (`request.fail`, `request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `user.save` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:409-455` and `lib/controllers/users.js:1062-1113`. Paired by semantic subject `guards:user.save`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1065` `id=users.updateProfile.response.3` - **calls request.fail locally** - Layer 2 - request.fail({ message : "Sorry, that username is already taken. Pleas...) with no return
      Carrier `users.updateProfile` (route handler) - Routes `PUT /api/users/{userId}` - Source `return resolve(request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `user.save` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:412` and `lib/controllers/users.js:1065`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:1070` `id=users.updateProfile.response.4` - **calls request.fail locally** - Layer 2 - request.fail({ message : "Something went wrong when trying to update y...) with no return
      Carrier `users.updateProfile` (route handler) - Routes `PUT /api/users/{userId}` - Source `return resolve(request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `user.save` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:417` and `lib/controllers/users.js:1070`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:1083-1087` `id=users.updateProfile.handler.1` - **logs and continues** - none - .catch() handler - logs only
      Carrier `users.updateProfile` (route handler) - Routes `PUT /api/users/{userId}` - Source `.catch(function(err) {`
      Target: No response is produced here and nothing is thrown: the error is logged via `console.error` and control continues. Funnel: none - and that is the point. Preserve the continuation. Converting a log-and-continue branch into a rejection or a returned Boom turns an edge that produces no response into one that does, changes the status a client sees from whatever else settles the request to a 500, and is exactly the silent conversion R-e is written to catch. Side effects: the `console.error` call and nothing else - no flash, no session write, no response, no rethrow. A conversion that adds any of those changes this edge. Timing: deferred. The handler is registered synchronously while the chain at line 1077 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is assigned to `addFolderSlugJob` at that statement rather than returned, so whether anything waits for the rejection is decided where `addFolderSlugJob` is consumed - preserve that consumption point, and do not move the settlement to this one.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/users.js:430-434` and `lib/controllers/users.js:1083-1087`. Paired by semantic subject `shape:.catch() handler - logs only`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1117` `id=users.updateProfile.response.5` - **calls request.fail locally** - Layer 2 - request.fail({ message : "Something went wrong when trying to update y...)
      Carrier `users.updateProfile` (route handler) - Routes `PUT /api/users/{userId}` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 1052, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:458` and `lib/controllers/users.js:1117`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1130-1133` `id=users.assetList.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'File.findForUser' - handed onward
      Carrier `users.assetList` (route handler) - Routes `GET /api/users/assets` - Source `File.findForUser(request.user._id, function(err, files) {`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 1131 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/users.js:471-474` and `lib/controllers/users.js:1130-1133`. Paired by semantic subject `guards:File.findForUser`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 1131, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/users.js:1168-1170` `id=users.assetList.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `users.assetList` (route handler) - Routes `GET /api/users/assets` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1140 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:510` and `lib/controllers/users.js:1168-1170`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/users.js:1177` `id=users.assetUpload.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notImplemented('Asset uploads are not enabled');
      Carrier `users.assetUpload` (route handler) - Routes `POST /api/users/assets` - Source `return errors.notImplemented('Asset uploads are not enabl...`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 501 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 501 / answers / route handler, measured at `lib/controllers/users.js:516` and `lib/controllers/users.js:1177`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1182-1185` `id=users.assetUpload.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (FileUtil.uploadUserAsset)
      Carrier `users.assetUpload` (route handler) - Routes `POST /api/users/assets` - Source `FileUtil.uploadUserAsset(request.payload.file, request.us...`
      Target: The response is produced inside the callback passed to `FileUtil.uploadUserAsset` (`request.fail`, `request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `FileUtil.uploadUserAsset` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:518-521` and `lib/controllers/users.js:1182-1185`. Paired by semantic subject `guards:FileUtil.uploadUserAsset`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1183` `id=users.assetUpload.response.2` - **calls request.fail locally** - Layer 2 - request.fail(err) with no return
      Carrier `users.assetUpload` (route handler) - Routes `POST /api/users/assets` - Source `if (err) return resolve(request.fail(err));`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `FileUtil.uploadUserAsset` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:519` and `lib/controllers/users.js:1183`. Paired by semantic subject `produces:err`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:1191` `id=users.replaceAsset.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notImplemented('Asset uploads are not enabled');
      Carrier `users.replaceAsset` (route handler) - Routes `POST /api/users/assets/{fileId}` - Source `return errors.notImplemented('Asset uploads are not enabl...`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 501 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 501 / answers / route handler, measured at `lib/controllers/users.js:526` and `lib/controllers/users.js:1191`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1197-1200` `id=users.replaceAsset.errparam.1` - **propagates to its caller** - Layer 1 - error parameter of a callback to 'FileUtil.uploadUserAsset' - handed onward
      Carrier `users.replaceAsset` (route handler) - Routes `POST /api/users/assets/{fileId}` - Source `FileUtil.uploadUserAsset(request.payload.file, request.us...`
      Target: The error is handed to an outer continuation - rejects the enclosing promise via `reject(err)`, at line 1198 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: Layer 1, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `reject`, and its own row states them where it is in this file. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 1 / no response / answers nothing / route handler, measured at `lib/controllers/users.js:532-535` and `lib/controllers/users.js:1197-1200`. Paired by semantic subject `guards:FileUtil.uploadUserAsset`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback rejects the enclosing promise via `reject(err)` at line 1198, so `err` leaves this function and the awaiting caller decides the response.
- [x] `lib/controllers/users.js:1205-1207` `id=users.replaceAsset.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `users.replaceAsset` (route handler) - Routes `POST /api/users/assets/{fileId}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1196 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:541` and `lib/controllers/users.js:1205-1207`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/users.js:1212` `id=users.replaceAsset.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `users.replaceAsset` (route handler) - Routes `POST /api/users/assets/{fileId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/users.js:545` and `lib/controllers/users.js:1212`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1225-1227` `id=users.removeAsset.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `users.removeAsset` (route handler) - Routes `DELETE /api/users/assets/{fileId}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1221 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:558` and `lib/controllers/users.js:1225-1227`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/users.js:1232` `id=users.removeAsset.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `users.removeAsset` (route handler) - Routes `DELETE /api/users/assets/{fileId}` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/users.js:562` and `lib/controllers/users.js:1232`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1244-1246` `id=users.restoreAsset.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `users.restoreAsset` (route handler) - Routes `POST /api/users/assets/restore` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1240 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:575` and `lib/controllers/users.js:1244-1246`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/users.js:1251` `id=users.restoreAsset.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.forbidden();
      Carrier `users.restoreAsset` (route handler) - Routes `POST /api/users/assets/restore` - Source `return Boom.forbidden();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.forbidden(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 403 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/users.js:579` and `lib/controllers/users.js:1251`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1270` `id=users.assetUploadFromURL.response.1` - **returns or throws a Boom** - Layer 3 - return errors.notImplemented('Asset uploads are not enabled');
      Carrier `users.assetUploadFromURL` (route handler) - Routes `POST /api/users/assetFromURL` - Source `return errors.notImplemented('Asset uploads are not enabl...`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 501 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 501 / answers / route handler, measured at `lib/controllers/users.js:585` and `lib/controllers/users.js:1270`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1274` `id=users.assetUploadFromURL.response.2` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `users.assetUploadFromURL` (route handler) - Routes `POST /api/users/assetFromURL` - Source `if (!requestUrl.protocol) return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:589` and `lib/controllers/users.js:1274`. Paired by semantic subject `produces:`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1289` `id=users.assetUploadFromURL.response.3` - **returns or throws a Boom** - none - throw err
      Carrier `users.assetUploadFromURL` (route handler) - Routes `POST /api/users/assetFromURL` - Source `throw err;`
      Target: The throw happens inside a callback invoked by `tmp.tmpName`, not on the handler's own stack, so the handler catch-all never sees it: it escapes as an uncaught exception and this edge produces no response. Funnel: none. Preserve that - do not wrap the callback so that it starts producing one. Side effects: none, and nothing is logged either: the throw escapes the callback as an uncaught exception rather than reaching a funnel that would log it. Timing: inside the callback `tmp.tmpName` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/users.js:591-617` and `lib/controllers/users.js:1289`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1327-1330` `id=users.startUpload.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (FileUtil.uploadUserAsset)
      Carrier `users.startUpload (module-local)` (internal callee) - Routes `POST /api/users/assetFromURL` (bound on `users.assetUploadFromURL`, which encloses this one) - Source `FileUtil.uploadUserAsset(fileupload, request.user, functi...`
      Target: The response is produced inside the callback passed to `FileUtil.uploadUserAsset` (`request.fail`, `request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `FileUtil.uploadUserAsset` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / internal callee, measured at `lib/controllers/users.js:611-614` and `lib/controllers/users.js:1327-1330`. Paired by semantic subject `guards:FileUtil.uploadUserAsset`. The two trees differ in surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4), which the AAP prescribes and this comparison records rather than reads as an R-e failure.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1328` `id=users.startUpload.response.1` - **calls request.fail locally** - Layer 2 - request.fail(err) with no return
      Carrier `users.startUpload (module-local)` (internal callee) - Routes `POST /api/users/assetFromURL` (bound on `users.assetUploadFromURL`, which encloses this one) - Source `if (err) return resolve(request.fail(err));`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `FileUtil.uploadUserAsset` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / internal callee, measured at `lib/controllers/users.js:612` and `lib/controllers/users.js:1328`. Paired by semantic subject `produces:err`. The two trees differ in surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4), which the AAP prescribes and this comparison records rather than reads as an R-e failure.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [ ] `lib/controllers/users.js:1346-1352` `id=users.assetUploadFromURL.handler.1` - **logs and continues** - none - .on('error') handler - logs only
      Carrier `users.assetUploadFromURL` (route handler) - Routes `POST /api/users/assetFromURL` - Source `body.on('error', function(err) {`
      Target: The error is logged via `console.log` and the handler then answers with `startUpload`, so a client sees that response and not a failure. Funnel: none - no error funnel is entered. Preserve the response, the log and the fact that the error does not change what is served. Side effects: the `console.log` call and nothing else - no flash, no session write, no response, no rethrow. A conversion that adds any of those changes this edge. Timing: when the stream emits `error`, which is not awaited by anything: for a refused connection it fires INSTEAD of `end`, and for a mid-stream failure it fires after `response`. The handler body has already returned by then, so the settlement time of the request is decided by whichever listener runs - or, when none does, not at all. Preserve that, including the case where nothing settles.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / route handler; the target produces none / no response / answers / route handler. What differs: whether a response is produced, payload or redirect shape, settlement timing - the target waits for LESS than the baseline did. R-e requires these to match, and no approved deviation names this edge. Paired by semantic subject `shape:.on('error') handler - logs only`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 8.1 - the streaming asset fetch's two failure modes
- [ ] `lib/controllers/users.js:1353-1355` `id=users.assetUploadFromURL.cps.1` - **resolves on a later callback** - none - CPS callback boundary (body.on)
      Carrier `users.assetUploadFromURL` (route handler) - Routes `POST /api/users/assetFromURL` - Source `body.on('end', function() {`
      Target: The response is produced inside the callback passed to `body.on` (`startUpload`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `startUpload` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `body.on` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback declares no error parameter, so the failure of `body.on` is dropped entirely: no response, no log, no branch. That is baseline behaviour and it is preserved.
- [ ] `lib/controllers/users.js:1358-1363` `id=users.assetUploadFromURL.errparam.1` - **logs and continues** - none - error parameter of a callback to '.then' - logs only
      Carrier `users.assetUploadFromURL` (route handler) - Routes `POST /api/users/assetFromURL` - Source `}, function(err) {`
      Target: No response is produced here and nothing is thrown: the error is logged via `console.log` and control continues. Funnel: none - and that is the point. Preserve the continuation. Converting a log-and-continue branch into a rejection or a returned Boom turns an edge that produces no response into one that does, changes the status a client sees from whatever else settles the request to a 500, and is exactly the silent conversion R-e is written to catch. Side effects: the `console.log` call and nothing else - no flash, no session write, no response, no rethrow. A conversion that adds any of those changes this edge. Timing: inside the callback `Promise` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: NOT COMPARED - this target site has no baseline counterpart and produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to. Its disposition above is what it does; there is no baseline fact it could contradict.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1371-1397` `id=users.changePassword.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (request.user.comparePassword)
      Carrier `users.changePassword` (route handler) - Routes `POST /api/users/password` - Source `request.user.comparePassword(request.payload.currentPassw...`
      Target: The response is produced inside the callback passed to `request.user.comparePassword` (`request.fail`, `request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `request.user.comparePassword` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:621-647` and `lib/controllers/users.js:1371-1397`. Paired by semantic subject `guards:request.user.comparePassword`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1373` `id=users.changePassword.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message : "Something went wrong when trying to change y...) with no return
      Carrier `users.changePassword` (route handler) - Routes `POST /api/users/password` - Source `return resolve(request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `request.user.comparePassword` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:623` and `lib/controllers/users.js:1373`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:1380-1390` `id=users.changePassword.cps.2` - **resolves on a later callback** - Layer 2 - CPS callback boundary (request.user.save)
      Carrier `users.changePassword` (route handler) - Routes `POST /api/users/password` - Source `request.user.save(function(err, user) {`
      Target: The response is produced inside the callback passed to `request.user.save` (`request.fail`, `request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `request.user.save` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:630-640` and `lib/controllers/users.js:1380-1390`. Paired by semantic subject `guards:request.user.save`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1382` `id=users.changePassword.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message : "Something went wrong when trying to change y...) with no return
      Carrier `users.changePassword` (route handler) - Routes `POST /api/users/password` - Source `return resolve(request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `request.user.save` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:632` and `lib/controllers/users.js:1382`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:1393` `id=users.changePassword.response.3` - **calls request.fail locally** - Layer 2 - request.fail({ message : "The password you entered did not match what ...) with no return
      Carrier `users.changePassword` (route handler) - Routes `POST /api/users/password` - Source `return resolve(request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `request.user.comparePassword` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:643` and `lib/controllers/users.js:1393`. Paired by semantic subject `produces:{ message : "The password you entered did not match what ...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:1401` `id=users.changePassword.response.4` - **calls request.fail locally** - Layer 2 - request.fail({ message : "Your new password entries did not match. Ple...)
      Carrier `users.changePassword` (route handler) - Routes `POST /api/users/password` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:650` and `lib/controllers/users.js:1401`. Paired by semantic subject `produces:{ message : "Your new password entries did not match. Ple...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1420` `id=users.getAvatar.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.notFound();
      Carrier `users.getAvatar` (route handler) - Routes `GET /api/users/{userId}/avatar` - Source `return Boom.notFound();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.notFound(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 404 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/users.js:667` and `lib/controllers/users.js:1420`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1471` `id=users.getInfo.response.1` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.notFound();
      Carrier `users.getInfo` (route handler) - Routes `GET /api/users/{userId}/info` - Source `return Boom.notFound();`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.notFound(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 404 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/users.js:680` and `lib/controllers/users.js:1471`. Paired by semantic subject `unbound:Boom`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1481-1483` `id=users.updateSettings.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `users.updateSettings` (route handler) - Routes `POST /api/users/settings`, `POST /api/users/settings/lineWrapping`, `POST /api/users/settings/indentation` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: deferred. The handler is registered synchronously while the chain at line 1475 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:691` and `lib/controllers/users.js:1481-1483`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/controllers/users.js:1525` `id=users.sendEmailChange.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: "Email is not configured. Email changes are no...)
      Carrier `users.sendEmailChange` (route handler) - Routes `POST /api/users/email` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:696` and `lib/controllers/users.js:1525`. Paired by semantic subject `produces:{ message: "Email is not configured. Email changes are no...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1531-1612` `id=users.sendEmailChange.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (User.findByLogin)
      Carrier `users.sendEmailChange` (route handler) - Routes `POST /api/users/email` - Source `User.findByLogin(request.payload.email, function(err, use...`
      Target: The response is produced inside the callback passed to `User.findByLogin` (`request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `User.findByLogin` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:701-726` and `lib/controllers/users.js:1531-1612`. Paired by semantic subject `guards:User.findByLogin`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback declares `err` and never inspects it, so the failure of `User.findByLogin` is dropped while the response is produced regardless.
- [x] `lib/controllers/users.js:1539` `id=users.sendEmailChange.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'Another account with that email address alrea...) with no return
      Carrier `users.sendEmailChange` (route handler) - Routes `POST /api/users/email` - Source `return resolve(request.fail({ message: 'Another account w...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findByLogin` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:704` and `lib/controllers/users.js:1539`. Paired by semantic subject `produces:{ message: 'Another account with that email address alrea...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [ ] `lib/controllers/users.js:1543-1611` `id=users.sendEmailChange.cps.2` - **resolves on a later callback** - none - CPS callback boundary (.randomBytes)
      Carrier `users.sendEmailChange` (route handler) - Routes `POST /api/users/email` - Source `require('crypto').randomBytes(48, async function(ex, buf) {`
      Target: The response is produced inside the callback passed to `.randomBytes` (`request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `.randomBytes` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / route handler; the target produces none / no response / answers nothing / route handler. What differs: payload or redirect shape. R-e requires these to match, and no approved deviation names this edge. Paired by semantic subject `guards:.randomBytes`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback declares `ex` and never inspects it, so the failure of `.randomBytes` is dropped while the response is produced regardless.
      Note: `ex` is declared and never read.
- [x] `lib/controllers/users.js:1617` `id=users.resendEmailChange.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: "Email is not configured. Email changes are no...)
      Carrier `users.resendEmailChange` (route handler) - Routes `GET /api/users/resendEmailChange` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:730` and `lib/controllers/users.js:1617`. Paired by semantic subject `produces:{ message: "Email is not configured. Email changes are no...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1627` `id=users.resendEmailChange.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ message: 'change email key not found' })
      Carrier `users.resendEmailChange` (route handler) - Routes `GET /api/users/resendEmailChange` - Source `if (!changeVal) return request.fail({ message: 'change em...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:740` and `lib/controllers/users.js:1627`. Paired by semantic subject `produces:{ message: 'change email key not found' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1639` `id=users.resendEmailChange.response.3` - **returns or throws a Boom** - Layer 3 - return err in a catch clause - hands the caught error back as the response
      Carrier `users.resendEmailChange` (route handler) - Routes `GET /api/users/resendEmailChange` - Source `return err;`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - the factory's status - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / route handler, measured at `lib/controllers/users.js:749` and `lib/controllers/users.js:1639`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1656` `id=users.changeEmail.response.1` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `users.changeEmail` (route handler) - Routes `GET /change-email` - Source `return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:766` and `lib/controllers/users.js:1656`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1663` `id=users.changeEmail.response.2` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `users.changeEmail` (route handler) - Routes `GET /change-email` - Source `return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:773` and `lib/controllers/users.js:1663`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1684` `id=users.changeEmail.response.3` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `users.changeEmail` (route handler) - Routes `GET /change-email` - Source `return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:794` and `lib/controllers/users.js:1684`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1689` `id=users.sendEmailVerification.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ message: "Email is not configured. Email verification i...)
      Carrier `users.sendEmailVerification` (route handler) - Routes `POST /api/users/verify-email` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:799` and `lib/controllers/users.js:1689`. Paired by semantic subject `produces:{ message: "Email is not configured. Email verification i...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1703-1716` `id=users.sendEmailVerification.cps.1` - **resolves on a later callback** - none - CPS callback boundary (.randomBytes)
      Carrier `users.sendEmailVerification` (route handler) - Routes `POST /api/users/verify-email` - Source `require('crypto').randomBytes(48, async function(ex, buf) {`
      Target: The response is produced inside the callback passed to `.randomBytes` (`request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `.randomBytes` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/users.js:807-818` and `lib/controllers/users.js:1703-1716`. Paired by semantic subject `guards:.randomBytes`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback declares `ex` and never inspects it, so the failure of `.randomBytes` is dropped while the response is produced regardless.
      Note: `ex` is declared and never read.
- [ ] `lib/controllers/users.js:1720` `id=users.sendEmailVerification.response.2` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `users.sendEmailVerification` (route handler) - Routes `POST /api/users/verify-email` - Source `return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - the outcome changed.** Baseline produced Layer 2 / the route's request.fail response / answers / route handler; the target produces Layer 2 / the route's request.fail response / answers / route handler. What differs: settlement timing - the settlement moved. R-e requires these to match, and no approved deviation names this edge. Paired by semantic subject `produces:`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1737` `id=users.verifyEmail.response.1` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `users.verifyEmail` (route handler) - Routes `GET /verify-email` - Source `return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:839` and `lib/controllers/users.js:1737`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1742` `id=users.verifyEmail.response.2` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `users.verifyEmail` (route handler) - Routes `GET /verify-email` - Source `return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:844` and `lib/controllers/users.js:1742`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1753` `id=users.verifyEmail.response.3` - **calls request.fail locally** - Layer 2 - request.fail()
      Carrier `users.verifyEmail` (route handler) - Routes `GET /verify-email` - Source `return request.fail();`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:855` and `lib/controllers/users.js:1753`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1758` `id=users.activateAccountForm.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ redirectTo : 'home' })
      Carrier `users.activateAccountForm` (route handler) - Routes `GET /activate-account` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:860` and `lib/controllers/users.js:1758`. Paired by semantic subject `produces:{ redirectTo : 'home' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 3 - the cross-request state leak in `fail.redirect`
- [x] `lib/controllers/users.js:1786` `id=users.activateAccount.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ redirectTo : 'home' })
      Carrier `users.activateAccount` (route handler) - Routes `POST /activate-account` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:888` and `lib/controllers/users.js:1786`. Paired by semantic subject `produces:{ redirectTo : 'home' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 3 - the cross-request state leak in `fail.redirect`
- [x] `lib/controllers/users.js:1796` `id=users.activateAccount.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ redirectTo : 'activate-account' })
      Carrier `users.activateAccount` (route handler) - Routes `POST /activate-account` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:898` and `lib/controllers/users.js:1796`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 3 - the cross-request state leak in `fail.redirect`
- [x] `lib/controllers/users.js:1804-1822` `id=users.activateAccount.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (User.findById)
      Carrier `users.activateAccount` (route handler) - Routes `POST /activate-account` - Source `User.findById(activateVal.email, function(err, user) {`
      Target: The response is produced inside the callback passed to `User.findById` (`request.fail`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `User.findById` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:905-921` and `lib/controllers/users.js:1804-1822`. Paired by semantic subject `guards:User.findById`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1806` `id=users.activateAccount.response.3` - **calls request.fail locally** - Layer 2 - request.fail({ redirectTo : 'activate-account' }) with no return
      Carrier `users.activateAccount` (route handler) - Routes `POST /activate-account` - Source `return resolve(request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:907` and `lib/controllers/users.js:1806`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 3 - the cross-request state leak in `fail.redirect`
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:1814-1821` `id=users.activateAccount.errparam.1` - **swallows silently** - none - error parameter of a callback to 'user.save' - undispositioned
      Carrier `users.activateAccount` (route handler) - Routes `POST /activate-account` - Source `user.save(async function(err) {`
      Target: The error is absorbed: no response, no rethrow, no log. Funnel: none. Preserve the swallow exactly - R-d prohibits repairing it, and a caller that today proceeds on a failed operation must keep proceeding. Side effects: none at all - no log, no response, no rethrow, no flash. The error value is discarded where it is received, and R-d requires that it keep being discarded. Timing: inside the callback `User.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/users.js:913-920` and `lib/controllers/users.js:1814-1821`. Paired by semantic subject `guards:user.save`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: `err` is read but never turned into a response, a log or a rethrow.
- [x] `lib/controllers/users.js:1816-1820` `id=users.activateAccount.cps.2` - **resolves on a later callback** - none - CPS callback boundary (request.yar._logIn)
      Carrier `users.activateAccount` (route handler) - Routes `POST /activate-account` - Source `request.yar._logIn(user, async function(err) {`
      Target: The response is produced inside the callback passed to `request.yar._logIn` (`request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `request.yar._logIn` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / route handler, measured at `lib/controllers/users.js:915-919` and `lib/controllers/users.js:1816-1820`. Paired by semantic subject `guards:request.yar._logIn`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback declares `err` and never inspects it, so the failure of `request.yar._logIn` is dropped while the response is produced regardless.
      Note: `err` is declared and never read.
- [x] `lib/controllers/users.js:1825` `id=users.activateAccount.response.4` - **calls request.fail locally** - Layer 2 - request.fail({ redirectTo : 'activate-account' })
      Carrier `users.activateAccount` (route handler) - Routes `POST /activate-account` - Source `return request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:923` and `lib/controllers/users.js:1825`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/preserved-quirks.md section 3 - the cross-request state leak in `fail.redirect`
- [x] `lib/controllers/users.js:1846` `id=users.requestExport.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ error: 'Export already in progress', exportId: existing...) with no return
      Carrier `users.requestExport` (route handler) - Routes `POST /api/exports` - Source `failResponse = request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 1843, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:937` and `lib/controllers/users.js:1846`. Paired by semantic subject `produces:{ error: 'Export already in progress', exportId: existing...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:1858` `id=users.requestExport.response.2` - **calls request.fail locally** - Layer 2 - request.fail({ error: 'Please wait 1 hour between exports', lastExport...) with no return
      Carrier `users.requestExport` (route handler) - Routes `POST /api/exports` - Source `failResponse = request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 1843, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:949` and `lib/controllers/users.js:1858`. Paired by semantic subject `produces:{ error: 'Please wait 1 hour between exports', lastExport...`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [ ] `lib/controllers/users.js:1884` `id=users.requestExport.response.3` - **calls request.fail locally** - Layer 2 - request.fail({ error: 'Export already in progress', exportId: outcome....) with no return
      Carrier `users.requestExport` (route handler) - Routes `POST /api/exports` - Source `failResponse = request.fail({`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 1843, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [ ] `lib/controllers/users.js:1936` `id=users.requestExport.response.4` - **returns or throws a Boom** - Layer 2 - throw queueErr
      Carrier `users.requestExport` (route handler) - Routes `POST /api/exports` - Source `throw queueErr;`
      Target: The rejection is taken by the `.catch(...)` at line 1948, which answers through Layer 2 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 1948, whose own row states them. Timing: asynchronous, in the chain step at line 1843, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - new in the target.** No baseline row matched this edge, so there is no baseline fact to preserve and nothing has been verified about it. Conversion may legitimately introduce a site; this row is where that introduction is reviewed.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1951` `id=users.requestExport.response.5` - **calls request.fail locally** - Layer 2 - request.fail({ error: err.message || 'Failed to start export' })
      Carrier `users.requestExport` (route handler) - Routes `POST /api/exports` - Source `return request.fail({ error: err.message \|\| 'Failed to st...`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 1843, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:986` and `lib/controllers/users.js:1951`. Paired by semantic subject `produces:{ error: err.message || 'Failed to start export' }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1976` `id=users.listExports.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ error: err.message })
      Carrier `users.listExports` (route handler) - Routes `GET /api/exports` - Source `return request.fail({ error: err.message });`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: asynchronous, in the chain step at line 1958, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:1011` and `lib/controllers/users.js:1976`. Paired by semantic subject `produces:{ error: err.message }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:1997-2041` `id=users.getExportStatus.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (Export.findById)
      Carrier `users.getExportStatus` (route handler) - Routes `GET /api/exports/{exportId}` - Source `Export.findById(exportId, function(err, exportRecord) {`
      Target: The response is produced inside the callback passed to `Export.findById` (`request.fail`, `request.success`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `request.success` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `Export.findById` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:1020-1061` and `lib/controllers/users.js:1997-2041`. Paired by semantic subject `guards:Export.findById`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:2000` `id=users.getExportStatus.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ error: err.message }) with no return
      Carrier `users.getExportStatus` (route handler) - Routes `GET /api/exports/{exportId}` - Source `return resolve(request.fail({ error: err.message }));`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `Export.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:1023` and `lib/controllers/users.js:2000`. Paired by semantic subject `produces:{ error: err.message }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [x] `lib/controllers/users.js:2047` `id=users.getExportStatus.response.2` - **returns or throws a Boom** - Layer 1 - synchronous throw (ReferenceError: Boom is not defined) evaluating return Boom.internal('Export status error');
      Carrier `users.getExportStatus` (route handler) - Routes `GET /api/exports/{exportId}` - Source `return Boom.internal('Export status error');`
      Target: A synchronous `ReferenceError: Boom is not defined`. This file does not bind `Boom` at this offset - it imports @hapi/boom under another name - so evaluating `Boom.internal(...)` throws before any value exists and no Boom is ever constructed. The status the line READS as - 500 - is therefore not the status served: the handler catch-all logs err.stack at error level and returns Boom.badImplementation(err.message), so the client is answered **500** with Boom's generic 5xx body. Preserve the 500 and the log. R-d requires this: it is a defect, it is baseline behaviour, and repairing the binding would change an observable status. Side effects: Layer 1 logs `err.stack` at error level, or `String(err)` when there is no stack. Nothing else is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 1 / 500 / answers / route handler, measured at `lib/controllers/users.js:1064` and `lib/controllers/users.js:2047`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:2056-2095` `id=users.downloadExport.cps.1` - **resolves on a later callback** - Layer 2 - CPS callback boundary (Export.findById)
      Carrier `users.downloadExport` (route handler) - Routes `GET /api/exports/{exportId}/download` - Source `Export.findById(exportId, function(err, exportRecord) {`
      Target: The response is produced inside the callback passed to `Export.findById` (`request.fail`, `h.redirect`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `request.fail`, `h.redirect` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `Export.findById` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce Layer 2 / no response / answers / route handler, measured at `lib/controllers/users.js:1072-1102` and `lib/controllers/users.js:2056-2095`. Paired by semantic subject `guards:Export.findById`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/controllers/users.js:2058` `id=users.downloadExport.response.1` - **calls request.fail locally** - Layer 2 - request.fail({ error: err.message }) with no return
      Carrier `users.downloadExport` (route handler) - Routes `GET /api/exports/{exportId}/download` - Source `return resolve(request.fail({ error: err.message }));`
      Target: Layer 2. request.fail logs its argument at info level, then selects one of three responses: with a negotiated `html` type and a configured `fail.redirect` it flashes `failure`, interpolates `fail.redirect` IN PLACE on the parse-time route object - the cross-request leak - flashes `payload` and `query`, and redirects; with `html`, a configured `fail.html` and no `.json` extension on the path it renders that view through addUserContext; otherwise it returns h.response(json) with `json.flash` attached. Preserve the branch selection, both flash writes, the status and the body. The response is not a Boom, so the onPreResponse extension takes its `else if (response.header)` branch and applies Cache-Control, Pragma and Expires. The emulation is gone from this tree, so the returned value IS the response: this path must return it exactly once, because a path that returns nothing is converted by hapi into Boom.badImplementation - a 500 in place of whatever this edge answers - and a returned-but-unawaited chain answers with a pending promise instead of its value. This call site has no `return`, which is immaterial under the shim and decisive after conversion. Side effects: request.fail logs its argument at info level when one is present; on the `html` + `fail.redirect` branch it writes the `failure` flash (only when a payload was passed), mutates `fail.redirect` IN PLACE on the object captured at parse time, and writes the `payload` and `query` flashes; on the other two branches it calls `request.yar.flash()` with no arguments, which READS AND CLEARS the accumulated flash into the response body. Preserve exactly those writes - no more, and none dropped. The in-place mutation is the cross-request leak catalogued in docs/preserved-quirks.md section 3 and is required, not repaired. Timing: inside the callback `Export.findById` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: CLOSED. Baseline and target both produce Layer 2 / the route's request.fail response / answers / route handler, measured at `lib/controllers/users.js:1074` and `lib/controllers/users.js:2058`. Paired by semantic subject `produces:{ error: err.message }`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      See also: docs/conversion-inventory.md - this call site has no `return`, so the per-site return/await disposition is tracked there as well as here
- [ ] `lib/controllers/users.js:2140-2142` `id=users.send_mail_detached.errparam.1` - **logs and continues** - none - error parameter of a callback to 'sent.then' - logs only
      Carrier `users.send_mail_detached (module-local)` (internal callee) - Routes none declared - driven by its traced caller(s) `users.sendPassReset`, `users.send_email_confirmation (module-local)`, `users.send_email_verification (module-local)` - Source `sent.then(null, function(err) {`
      Target: No response is produced here and nothing is thrown: the error is logged via `console.log` and control continues. Funnel: none - and that is the point. Preserve the continuation. Converting a log-and-continue branch into a rejection or a returned Boom turns an edge that produces no response into one that does, changes the status a client sees from whatever else settles the request to a 500, and is exactly the silent conversion R-e is written to catch. Side effects: the `console.log` call and nothing else - no flash, no session write, no response, no rethrow. A conversion that adds any of those changes this edge. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / route handler; the target produces none / no response / answers nothing / internal callee. What differs: payload or redirect shape, log calls. R-e requires these to match, and no approved deviation names this edge. Paired by nearest outcome in the routed carrier. The two trees differ in surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4), which the AAP prescribes and this comparison records rather than reads as an R-e failure. paired to the nearest outcome in `lib/controllers/users.js` routed carrier `users.sendPassReset`, which differs, so this row is reported open on that difference rather than closed on the pairing.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.

### 7.12 `lib/util/helpers.js` - 27 rows

- [x] `lib/util/helpers.js:249` `id=helpers.isAdmin.response.1` - **returns or throws a Boom** - Layer 3 - throw Boom.forbidden()
      Carrier `helpers.isAdmin (internal)` (server method) - Routes none declared - driven by a route naming its `server.method` registration - Source `throw Boom.forbidden();`
      Target: This is a server method, reached through routeParser's string-form pre-handler dispatcher, which returns `serverMethod.apply(null, args)` from its own `async (request, h)` wrapper. A throw here rejects that wrapper. hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 403 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 403 / answers / server method, measured at `lib/util/helpers.js:27` and `lib/util/helpers.js:249`. Paired by semantic subject `throws:Boom.forbidden()`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:297` `id=helpers.findById.response.1` - **returns or throws a Boom** - Layer 3 - throw err
      Carrier `helpers.findById (internal)` (server method) - Routes none declared - driven by a route naming its `server.method` registration - Source `throw err;`
      Target: This is a server method, reached through routeParser's string-form pre-handler dispatcher, which returns `serverMethod.apply(null, args)` from its own `async (request, h)` wrapper. A throw here rejects that wrapper. hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - the thrown value's status - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: asynchronous, in the chain step at line 289, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / server method, measured at `lib/util/helpers.js:56` and `lib/util/helpers.js:297`. Paired by semantic subject `throws:err`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:303-306` `id=helpers.userByLogin.errparam.1` - **propagates to its caller** - none - error parameter of a callback to 'User.findByLogin' - handed onward
      Carrier `helpers.userByLogin (internal)` (internal callee) - Routes none declared - driven by **nothing - proven unreachable** - Source `return User.findByLogin(userSlug, function(err, doc) {`
      Target: The error is handed to an outer continuation - invokes its caller's continuation `next(err)`, at line 304 - so this edge produces no response of its own and the response comes from whatever awaits that continuation. Funnel: none, reached through the awaiting caller rather than from here. Preserve the HANDING ON as much as the eventual response: a conversion that answers here instead produces a response one frame earlier than baseline and skips whatever the caller does with the error, and one that drops the propagation turns an error the caller acts on into one it never sees. Side effects: none here beyond the handing on itself: no response is built and nothing is written on this line. The effects belong to whatever receives the error through `next`, and its own row states them where it is in this file. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: not compared - proven unreachable
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The callback invokes its caller's continuation `next(err)` at line 304, so `err` leaves this function and the awaiting caller decides the response.
      Note: PROVEN UNREACHABLE. This member is mentioned nowhere in the analysed corpus outside its own declaration - not called, not handed on as a value, not registered as a server method, not exported as a lifecycle method and named by no route - so nothing in this tree can reach this edge. It claims no funnel because it produces no response, it is excluded from the closure gate because there is no outcome to compare, and it is dead code rather than an open question.
- [x] `lib/util/helpers.js:337` `id=helpers.contains.response.1` - **returns or throws a Boom** - Layer 3 - throw Boom.badRequest()
      Carrier `helpers.contains (internal)` (server method) - Routes none declared - driven by a route naming its `server.method` registration - Source `throw Boom.badRequest();`
      Target: This is a server method, reached through routeParser's string-form pre-handler dispatcher, which returns `serverMethod.apply(null, args)` from its own `async (request, h)` wrapper. A throw here rejects that wrapper. hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 400 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 400 / answers / server method, measured at `lib/util/helpers.js:96` and `lib/util/helpers.js:337`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:342` `id=helpers.contains.response.2` - **returns or throws a Boom** - Layer 3 - throw Boom.badRequest()
      Carrier `helpers.contains (internal)` (server method) - Routes none declared - driven by a route naming its `server.method` registration - Source `throw Boom.badRequest();`
      Target: This is a server method, reached through routeParser's string-form pre-handler dispatcher, which returns `serverMethod.apply(null, args)` from its own `async (request, h)` wrapper. A throw here rejects that wrapper. hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 400 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 400 / answers / server method, measured at `lib/util/helpers.js:101` and `lib/util/helpers.js:342`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:347` `id=helpers.contains.response.3` - **returns or throws a Boom** - Layer 3 - throw result
      Carrier `helpers.contains (internal)` (server method) - Routes none declared - driven by a route naming its `server.method` registration - Source `if (result instanceof Error) throw result;`
      Target: This is a server method, reached through routeParser's string-form pre-handler dispatcher, which returns `serverMethod.apply(null, args)` from its own `async (request, h)` wrapper. A throw here rejects that wrapper. hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - the thrown value's status - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / server method, measured at `lib/util/helpers.js:106` and `lib/util/helpers.js:347`. Paired by semantic subject `throws:result`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:380` `id=helpers.populate.response.1` - **returns or throws a Boom** - Layer 3 - throw err
      Carrier `helpers.populate (internal)` (server method) - Routes none declared - driven by a route naming its `server.method` registration - Source `throw err;`
      Target: This is a server method, reached through routeParser's string-form pre-handler dispatcher, which returns `serverMethod.apply(null, args)` from its own `async (request, h)` wrapper. A throw here rejects that wrapper. hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - the thrown value's status - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the response: hapi answers from the value without logging it here, and Layer 3 post-processes the result. Timing: asynchronous, in the chain step at line 373, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 500 / answers / server method, measured at `lib/util/helpers.js:139` and `lib/util/helpers.js:380`. Paired by semantic subject `throws:err`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:404` `id=helpers.findTrinket.response.1` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound()
      Carrier `helpers.findTrinket` (pre-handler) - Routes `GET /embed/{lang}/{trinketId}`, `GET /assignment-embed/{lang}/{trinketId}`, `GET /assignment-embed-feedback/{lang}/{trinketId}`, `GET /assignment-embed-viewonly/{lang}/{trinketId}`, `GET /<lang>/{shortCode}`, `POST /api/trinkets/{trinketId}/autosave`, `POST /api/trinkets/{shortCode}/list`, `DELETE /api/trinkets/{lang}/list/{shortCode}` - Source `throw Boom.notFound();`
      Target: The rejection is taken by the `.catch(...)` at line 433, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 433, whose own row states them. Timing: asynchronous, in the chain step at line 399, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / pre-handler, measured at `lib/util/helpers.js:163` and `lib/util/helpers.js:404`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:430` `id=helpers.findTrinket.response.2` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound()
      Carrier `helpers.findTrinket` (pre-handler) - Routes `GET /embed/{lang}/{trinketId}`, `GET /assignment-embed/{lang}/{trinketId}`, `GET /assignment-embed-feedback/{lang}/{trinketId}`, `GET /assignment-embed-viewonly/{lang}/{trinketId}`, `GET /<lang>/{shortCode}`, `POST /api/trinkets/{trinketId}/autosave`, `POST /api/trinkets/{shortCode}/list`, `DELETE /api/trinkets/{lang}/list/{shortCode}` - Source `throw Boom.notFound();`
      Target: The rejection is taken by the `.catch(...)` at line 433, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 433, whose own row states them. Timing: asynchronous, in the chain step at line 399, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / pre-handler, measured at `lib/util/helpers.js:186` and `lib/util/helpers.js:430`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:433-438` `id=helpers.findTrinket.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `helpers.findTrinket` (pre-handler) - Routes `GET /embed/{lang}/{trinketId}`, `GET /assignment-embed/{lang}/{trinketId}`, `GET /assignment-embed-feedback/{lang}/{trinketId}`, `GET /assignment-embed-viewonly/{lang}/{trinketId}`, `GET /<lang>/{shortCode}`, `POST /api/trinkets/{trinketId}/autosave`, `POST /api/trinkets/{shortCode}/list`, `DELETE /api/trinkets/{lang}/list/{shortCode}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: deferred. The handler is registered synchronously while the chain at line 399 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / pre-handler; the target produces Layer 3 / 500 / answers / pre-handler. What differs: funnel, status served, whether a response is produced, payload or redirect shape, side effects. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/util/helpers.js:453` `id=helpers.validLang.response.1` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound()
      Carrier `helpers.validLang` (pre-handler) - Routes `GET /embed/{lang}/{trinketId}`, `GET /assignment-embed/{lang}/{trinketId}`, `GET /assignment-embed-feedback/{lang}/{trinketId}`, `GET /assignment-embed-viewonly/{lang}/{trinketId}`, `GET /embed/{lang}`, `GET /tools/{version}/jekyll/embed/{lang}`, `GET /<lang>/`, `GET /api/trinkets/popular`, `GET /api/trinkets/active`, `POST /api/trinkets/codeerror` - Source `throw Boom.notFound();`
      Target: hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 404 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / pre-handler, measured at `lib/util/helpers.js:203` and `lib/util/helpers.js:453`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:484` `id=helpers.trinketTypeEnabled.response.1` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound('This trinket type is not available')
      Carrier `helpers.trinketTypeEnabled` (pre-handler) - Routes `GET /embed/{lang}/{trinketId}`, `GET /assignment-embed/{lang}/{trinketId}`, `GET /assignment-embed-feedback/{lang}/{trinketId}`, `GET /assignment-embed-viewonly/{lang}/{trinketId}`, `GET /embed/{lang}`, `GET /tools/{version}/jekyll/embed/{lang}`, `GET /library/trinkets/{path*}`, `GET /<lang>`, `GET /<lang>/`, `GET /<lang>/{shortCode}`, `GET /<lang>/{shortCode}/`, `GET /<lang>/{shortCode}/{path*}` - Source `throw Boom.notFound('This trinket type is not available');`
      Target: hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 404 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / pre-handler, measured at `lib/util/helpers.js:234` and `lib/util/helpers.js:484`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:498` `id=helpers.coursesEnabled.response.1` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound('Courses are not available')
      Carrier `helpers.coursesEnabled` (pre-handler) - Routes `GET /courses/new`, `POST /courses`, `POST /{userSlug}/courses/{courseSlug}/copy`, `GET /{userSlug}/courses/{courseSlug}/download.zip`, `GET /{userSlug}/courses/{courseSlug}`, `GET /api/classes/{userSlug}/{courseSlug}`, `GET /courses/accept/{token}`, `GET /courses/join/{accessCode}`, `GET /u/{username}/classes`, `GET /u/{username}/classes/{courseSlug}` - Source `throw Boom.notFound('Courses are not available');`
      Target: hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 404 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / pre-handler, measured at `lib/util/helpers.js:248` and `lib/util/helpers.js:498`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:595` `id=helpers.verifyEmailToken.response.1` - **returns or throws a Boom** - Layer 3 - throw Boom.forbidden()
      Carrier `helpers.verifyEmailToken` (pre-handler) - Routes `POST /api/trinkets/{trinketId}/email` - Source `throw Boom.forbidden();`
      Target: hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 403 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 403 / answers / pre-handler, measured at `lib/util/helpers.js:269` and `lib/util/helpers.js:595`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:599` `id=helpers.verifyEmailToken.response.2` - **returns or throws a Boom** - Layer 3 - throw Boom.badRequest()
      Carrier `helpers.verifyEmailToken` (pre-handler) - Routes `POST /api/trinkets/{trinketId}/email` - Source `throw Boom.badRequest();`
      Target: hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 400 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 400 / answers / pre-handler, measured at `lib/util/helpers.js:273` and `lib/util/helpers.js:599`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:672-677` `id=helpers.getDefaultTrinket.handler.1` - **returns or throws a Boom** - Layer 3 - .catch() handler - returns the error as the response via return err
      Carrier `helpers.getDefaultTrinket` (pre-handler) - Routes `GET /embed/{lang}` - Source `.catch(function(err) {`
      Target: Layer 3. The handler RETURNS the error value rather than throwing it, so there is no throw for the handler catch-all to take and no rejection for a later `.catch` to receive - returning from a catch handler RESOLVES the chain. hapi wraps the returned value - `Response.wrap` calls `Boom.boomify` on an Error - and answers **500** from it, then post-processes the result. Preserve the RETURN as much as the status: converting this into a throw would route the same 500 through the catch-all instead and add its error-level `err.stack` log, which is a side-effect change even though the status a client sees is identical. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: deferred. The handler is registered synchronously while the chain at line 670 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / pre-handler; the target produces Layer 3 / 500 / answers / pre-handler. What differs: funnel, status served, whether a response is produced, payload or redirect shape, side effects. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
      Note: The handler returns the error as the response, through `return err`, so the rejection is answered with a response derived from `err` rather than absorbed. A Boom keeps its own status; any other Error is boomified and answered 500 with the generic 5xx body; a value that is neither is served 200 as the body. Preserve the selection, not just the common case.
- [x] `lib/util/helpers.js:691` `id=helpers.userByUsername.response.1` - **returns or throws a Boom** - Layer 3 - return Boom.notFound();
      Carrier `helpers.userByUsername` (pre-handler) - Routes `GET /u/{username}/classes`, `GET /u/{username}/classes/{courseSlug}` - Source `return Boom.notFound();`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - 404 - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / pre-handler, measured at `lib/util/helpers.js:360` and `lib/util/helpers.js:691`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:694` `id=helpers.userByUsername.response.2` - **returns or throws a Boom** - Layer 3 - return err in a catch clause - hands the caught error back as the response
      Carrier `helpers.userByUsername` (pre-handler) - Routes `GET /u/{username}/classes`, `GET /u/{username}/classes/{courseSlug}` - Source `return err;`
      Target: Layer 3. The value is RETURNED, not thrown, so the wrapper hands it to hapi and the answer carries the Boom's own status - the factory's status - which Layer 3 then post-processes. Note the contrast with `throw` on this same surface, which the handler catch-all converts to 500. Preserve the status and the payload shape. Side effects: none beyond the response itself - the value is returned, not thrown, so nothing is logged and nothing is written. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / pre-handler; the target produces Layer 3 / 500 / answers / pre-handler. What differs: funnel, status served, whether a response is produced, payload or redirect shape, side effects. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:708` `id=helpers.courseBySlug.response.1` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound()
      Carrier `helpers.courseBySlug` (pre-handler) - Routes `POST /{userSlug}/courses/{courseSlug}/copy`, `GET /{userSlug}/courses/{courseSlug}/download.zip`, `GET /{userSlug}/courses/{courseSlug}`, `GET /api/classes/{userSlug}/{courseSlug}`, `GET /u/{username}/classes/{courseSlug}` - Source `if (!id) throw Boom.notFound();`
      Target: hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 404 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / pre-handler, measured at `lib/util/helpers.js:377` and `lib/util/helpers.js:708`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:725` `id=helpers.courseBySlug.response.2` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound()
      Carrier `helpers.courseBySlug` (pre-handler) - Routes `POST /{userSlug}/courses/{courseSlug}/copy`, `GET /{userSlug}/courses/{courseSlug}/download.zip`, `GET /{userSlug}/courses/{courseSlug}`, `GET /api/classes/{userSlug}/{courseSlug}`, `GET /u/{username}/classes/{courseSlug}` - Source `throw Boom.notFound();`
      Target: hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - 404 - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: CLOSED. Baseline and target both produce Layer 3 / 404 / answers / pre-handler, measured at `lib/util/helpers.js:391` and `lib/util/helpers.js:725`. Paired by identical outcome within the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:729` `id=helpers.courseBySlug.response.3` - **returns or throws a Boom** - Layer 3 - throw err
      Carrier `helpers.courseBySlug` (pre-handler) - Routes `POST /{userSlug}/courses/{courseSlug}/copy`, `GET /{userSlug}/courses/{courseSlug}/download.zip`, `GET /{userSlug}/courses/{courseSlug}`, `GET /api/classes/{userSlug}/{courseSlug}`, `GET /u/{username}/classes/{courseSlug}` - Source `throw err;`
      Target: hapi takes the rejection through its own lifecycle error handling, so a thrown Boom keeps its own status - the thrown value's status - and a non-Boom becomes hapi's own 500. Layer 3 post-processes the result. Preserve the status and the payload shape; nothing on this surface reaches the handler catch-all. Side effects: none beyond the rejection: the pre-handler assigns nothing when it rejects, so no `request.pre` value is written on this path. Timing: synchronous, on the handler body's own stack, before it returns.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / pre-handler; the target produces Layer 3 / 500 / answers / pre-handler. What differs: funnel, status served, whether a response is produced, payload or redirect shape, side effects. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [x] `lib/util/helpers.js:746-773` `id=helpers.trinketByOwnerAndSlug.cps.1` - **resolves on a later callback** - none - CPS callback boundary (Trinket.findByOwnerAndSlug)
      Carrier `helpers.trinketByOwnerAndSlug` (pre-handler) - Routes none declared - driven by invoking it as the lifecycle method it is exported as - Source `return Trinket.findByOwnerAndSlug(user._id, slug, functio...`
      Target: The response is produced inside the callback passed to `Trinket.findByOwnerAndSlug` (`reply`, `reply`), so it settles only when that operation calls back, and what a client receives today is whichever settlement wins. Preserve the ordering: rule T-3 puts the await boundary at this call site, and the response must still be produced after the callback would have run. Collapsing this into an earlier await changes which settle wins and can change the response itself. Status and payload unchanged. Side effects: whatever the callback body performs before it settles, plus the effects of the settling calls `reply`, `reply` itself - for `request.fail` that includes its flash writes. The deferred promise is resolved at that point and not before. Timing: deferred. The response is built only when `Trinket.findByOwnerAndSlug` invokes its callback, so the request settles then and not when the handler body returns. Rule T-3 puts the `await` boundary at this call site; moving the settlement earlier changes which settle wins.
      Closure: CLOSED. Baseline and target both produce none / no response / answers nothing / pre-handler, measured at `lib/util/helpers.js:410-437` and `lib/util/helpers.js:746-773`. Paired by semantic subject `guards:Trinket.findByOwnerAndSlug`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:747` `id=helpers.trinketByOwnerAndSlug.response.1` - **calls reply(err)** - Layer 3 - reply(err)
      Carrier `helpers.trinketByOwnerAndSlug` (pre-handler) - Routes none declared - driven by invoking it as the lifecycle method it is exported as - Source `if (err) return reply(err);`
      Target: This tree has no pre-handler emulation, so hapi's own contract applies and it does NOT resolve a non-Boom Error the way the shim did: `Response.wrap` calls `Boom.boomify` on any Error, and the default pre `failAction` then throws it. So a Boom keeps its own status and a plain Error becomes **500**, both answered through Layer 3, and the request does NOT continue with the error as `request.pre.<assign>`. This is the sharpest difference between the two contracts on this surface: under the shim the same value produced no response at all. Where baseline resolved and continued, the converted pre-handler must reproduce THAT - return a non-Error value, so the request still continues - rather than returning the Error and letting hapi answer. Side effects: on the resolving path the pre-handler's assigned value becomes the value passed here, so `request.pre.<assign>` holds it for every later stage that reads it; on the rejecting path nothing is assigned. No session or database write happens at this edge. Timing: inside the callback `Trinket.findByOwnerAndSlug` invokes, off the handler's own stack, so it runs after the handler body has already returned.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / pre-handler; the target produces Layer 3 / 500 / answers / pre-handler. What differs: funnel, status served, whether a response is produced, payload or redirect shape, side effects. R-e requires these to match, and no approved deviation names this edge. Paired by semantic subject `produces:err`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:752` `id=helpers.trinketByOwnerAndSlug.response.2` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound()
      Carrier `helpers.trinketByOwnerAndSlug` (pre-handler) - Routes none declared - driven by invoking it as the lifecycle method it is exported as - Source `if (!id) throw Boom.notFound();`
      Target: The rejection is taken by the `.catch(...)` at line 772, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 772, whose own row states them. Timing: asynchronous, in the chain step at line 750, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / pre-handler; the target produces Layer 3 / 404 / answers / pre-handler. What differs: funnel, status served, whether a response is produced. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:760` `id=helpers.trinketByOwnerAndSlug.response.3` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound()
      Carrier `helpers.trinketByOwnerAndSlug` (pre-handler) - Routes none declared - driven by invoking it as the lifecycle method it is exported as - Source `throw Boom.notFound();`
      Target: The rejection is taken by the `.catch(...)` at line 772, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 772, whose own row states them. Timing: asynchronous, in the chain step at line 750, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / pre-handler; the target produces Layer 3 / 404 / answers / pre-handler. What differs: funnel, status served, whether a response is produced. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:770` `id=helpers.trinketByOwnerAndSlug.response.4` - **returns or throws a Boom** - Layer 3 - throw Boom.notFound()
      Carrier `helpers.trinketByOwnerAndSlug` (pre-handler) - Routes none declared - driven by invoking it as the lifecycle method it is exported as - Source `throw Boom.notFound();`
      Target: The rejection is taken by the `.catch(...)` at line 772, which answers through Layer 3 - see its row for the status and the payload, which are that handler's and not this line's. Preserve the ROUTING as much as the response: after conversion the chain must still be what catches this, not a try/catch added around the handler body, or the edge silently changes funnel. Side effects: none here; the writes belong to the `.catch(...)` handler at line 772, whose own row states them. Timing: asynchronous, in the chain step at line 750, so it settles a microtask after the handler body has run. The chain is returned or awaited, so the handler waits for it and must keep waiting.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / pre-handler; the target produces Layer 3 / 404 / answers / pre-handler. What differs: funnel, status served, whether a response is produced. R-e requires these to match, and no approved deviation names this edge. Paired by uniform residual pool of equal size in the routed carrier.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.
- [ ] `lib/util/helpers.js:772` `id=helpers.trinketByOwnerAndSlug.handler.1` - **calls reply(err)** - Layer 3 - .catch(reply) bare reference
      Carrier `helpers.trinketByOwnerAndSlug` (pre-handler) - Routes none declared - driven by invoking it as the lifecycle method it is exported as - Source `.catch(reply);`
      Target: This tree has no pre-handler emulation, so hapi's own contract applies and it does NOT resolve a non-Boom Error the way the shim did: `Response.wrap` calls `Boom.boomify` on any Error, and the default pre `failAction` then throws it. So a Boom keeps its own status and a plain Error becomes **500**, both answered through Layer 3, and the request does NOT continue with the error as `request.pre.<assign>`. This is the sharpest difference between the two contracts on this surface: under the shim the same value produced no response at all. Where baseline resolved and continued, the converted pre-handler must reproduce THAT - return a non-Error value, so the request still continues - rather than returning the Error and letting hapi answer. Side effects: on the resolving path the pre-handler's assigned value becomes the value passed here, so `request.pre.<assign>` holds it for every later stage that reads it; on the rejecting path nothing is assigned. No session or database write happens at this edge. Timing: deferred. The handler is registered synchronously while the chain at line 750 is built, and runs only when that chain rejects - a microtask at the earliest, and after the handler body has already returned. The chain is returned or awaited, so the enclosing function waits for the rejection and must keep waiting for it.
      Closure: **OPEN - the outcome changed.** Baseline produced none / no response / answers nothing / pre-handler; the target produces Layer 3 / 500 / answers / pre-handler. What differs: funnel, status served, whether a response is produced, payload or redirect shape, side effects. R-e requires these to match, and no approved deviation names this edge. Paired by semantic subject `shape:.catch(reply) bare reference`.
      Driven: not joined - pass `--scenarios <corpus.json>` to join this row to the capture corpus, or `--coverage-gate` to make an undriven changed edge fatal.

## 8. Closure against the target tree

Each row above is joined to the row measuring the same edge on the other
tree and closed only when the two agree on every dimension R-e names.
All eight are recorded on every row; the six below are COMPARED:

- funnel
- status served
- whether a response is produced
- payload or redirect shape
- side effects
- log calls

Comparing the status alone is not closure, and it was what an earlier
edition of this document compared: 16 rows closed on a matching status
while their payload, their side effects or their settlement had moved, and
R-e exists to catch exactly those.

The MECHANISM is deliberately not compared - a `reply(Boom.notFound())` and
a `return Boom.notFound()` are one outcome by two means, and changing the
means IS the migration. Keeping the mechanism out took three calibrations,
each forced by a measurement rather than assumed:

- **Timing** compares whether the edge settles before the carrier returns,
  later with the carrier waiting, or later with nothing waiting. Naming the
  vehicle instead - a callback against an awaited chain - reported 54 rows
  as retimed for having been converted exactly as rule T-3 requires, and
  reading a registered `.catch` handler's declaration site as its
  settlement moved another 38 from deferred to synchronous.
- **Produced responses** compare their semantic KIND - a failure response, a
  success response, a redirect, a rendered view - and not the callee that
  produced them. `reply` was the shim's one universal producer, so its own
  kind is not knowable from the name; a row whose only producer was `reply`
  says so rather than claiming a kind.
- **A bare `.catch(reply)`** carries the rejection value itself, so its
  payload is an error value and not an unclassifiable one.
- **The shim's builder chain names the response where the source does.**
  `return reply().redirect("/home")` produces a redirect, and reading only
  the `reply` token recorded it as unknowable - so against a target that
  produces `h.redirect(...)` the comparison reported a payload difference on
  6 rows where an unknown was being compared against a known. The baseline
  parser's own builder resolves on `.redirect`, `.code`, `.header` and
  `.view` and NOT on `.type` or `.bytes`, so the last resolving member is
  read as the kind and a chain with no resolving member is still recorded as
  unknowable - which is the never-settling and builder-returned shapes.

Two dimensions are RECORDED ON EVERY ROW AND NOT READ AS R-e FAILURES,
because the AAP prescribes a change to both and reading a prescribed change
as a failure sends a reviewer to preserve something the plan required to
change. Neither is dropped: every row carries both values, and the 5 rows
that closed while carrying one are listed in full below.

- **Surface** is where the code lives. AAP 0.6.4 mandates extraction, so a
  branch that sat inline in a routed handler now sits in an internal callee
  it calls, and the client receives the same response either way.
- **Settlement timing is DIRECTIONAL, and only one direction is
  prescribed.** Under the shim a handler signalled its response out of band
  and fell off the end, so a chain's own value was routinely discarded;
  rules T-1 and T-3 exist to make that chain return it. So a baseline that
  waited for nothing becoming a target that waits is the migration doing
  what it was told. A target that waits for LESS than the baseline did may
  drop a response the baseline delivered, and that is still a failure - the
  rule is one-sided, and a self-test asserts both of its sides.

| Verdict | Rows | What it means |
|---|---:|---|
| closed | 287 | baseline and target agree on every compared dimension |
| closed by approved deviation | 0 | the outcome changed, and this exact edge and this exact change are on the approved list |
| closed - callback boundary removed by rule T-3 | 6 | the baseline site is a callback boundary the conversion turns into an `await`, and every response it produced is still produced in the same routed carrier - the row names where |
| **open - outcome changed** | 24 | R-e requires these to match and they do not; each row names the dimensions |
| **open - no target row** | 23 | the baseline edge could not be located in the target |
| **open - new in the target** | 43 | no baseline fact to preserve, so nothing is verified |
| **open - pairing not established** | 0 | no semantic pairing could be established in that carrier, so neither verdict may be borrowed |
| **open - compared equal on a provisional fact** | 0 | the outcomes agree, but the edge's own reachability or caller is unresolved, so the agreement is provisional |
| not compared - proven unreachable | 4 | nothing in the corpus reaches the edge, so there is no outcome on either tree to compare |
| not compared - new mechanism, produces no response | 15 | a target site with no baseline counterpart that produces no response, so it introduces no error-to-response mapping for R-e to hold either tree to |
| **closed, total** | **293** | |
| **open, total** | **90** | the authoritative figure. Section 1 quotes this number and `--closure-gate` counts it with the same predicate |
| **not compared, total** | **19** | |
| **all rows** | **402** | |

One predicate, `isOpenRow`, partitions the rows, and every figure in this
document for that quantity is derived from it: the three totals cover the
row count exactly and the open buckets sum to the open total, both asserted
before anything is written. An earlier edition let four call sites decide
independently and two of them disagreed by 23 rows - the preamble said 96
and the buckets said 119 - which makes the closure claim unreadable
whichever figure was right.

| Reconciliation | Rows |
|---|---:|
| baseline rows, all accounted for | 342 of 342 |
| target rows, all accounted for | 373 of 373 |
| **T1** paired by the guarded subject - *may close* | 161 |
| **T2** paired by an identical observable outcome - *may close* | 136 |
| **T3-anchored** paired by a uniform residual pool of equal size - *order-independent; every difference reported* | 10 |
| **T3** paired by the nearest outcome - *every difference reported* | 6 |
| baseline callback boundaries rule T-3 removed | 6 |
| target rows carrying no mapping | 15 |
| rows carrying a prescribed mechanism transition, any verdict | 5 |
| of those, closed | 3 |

### Why the pairing is not the printed identity, and not source order

The identity a row prints is `<file>.<carrier>.<class>.<ordinal>`, and two
of its four components are not invariants of this migration. **The class
flips**: a `.catch` whose body called `reply(err)` is a response-class edge
at the reply site, and the converted `.catch` that returns the error has no
terminal, so the edge is the handler itself. Both classes then renumber and
an id match becomes an artefact of the renumbering - measured on
`course.deleteCourse`, where taking the match paired line 151 with line 192
and reported two unchanged rows changed. **The carrier moves**: AAP 0.6.4
mandates extraction, so `createCourseCore`, `listCore`, `lookupTrinket`,
`startUpload`, `abandon`, `settle`, `logFailure`, `removeTempFile`,
`redactText` and the stream handlers all hold code that used to sit inline
in a routed handler, and grouping by the lexical carrier read every one of
those edges as deleted from one carrier and created in another - measured,
41 rows "missing" and 28 "new" on a tree where almost none of either had
happened.

An earlier edition papered over both by FILLING GAPS POSITIONALLY, pairing
the k-th unpaired baseline row in a gap with the k-th unpaired target row.
78 pairs rested on source order and 24 of those overrode an
identically-named row elsewhere, so rows were closed on the order two files
happen to list their statements in. **No row closes on source order here.**
Edges are grouped by the ROUTED carrier - resolved through each
module-local function's traced callers, which makes extraction transparent
because the route is what a client reaches and the route surface is an
invariant this migration gates - and paired within that group on the tiers
above. A tie between two equally near candidates is reported ambiguous
rather than resolved by any rule at all.

**No tier decides closure.** The tier decides only WHICH target row a
baseline row is compared against; the verdict then comes from the same
comparison for every tier - every R-e dimension must agree, and the only
movement permitted in a closed row is a mechanism transition the AAP
prescribes, listed by row above. So a T3 or T3-anchored pair closes exactly
when its only difference is a surface move, and is reported open with the
differing dimensions named otherwise. In this run 10 rows paired at
T3-anchored and 6 at T3.

Two editions of this table got that wrong in opposite directions, and both
are recorded because the second is the one a reader would not otherwise
suspect. The first labelled these tiers "never closes", which was simply
untrue of them. The second made it true of the outcome dimensions but
ranked the three settlements and let a move up the rank close a row, so a
pairing this tier had not established could still close on a settlement
change - two soft steps compounding into a tick. The settlement
prescription is withdrawn, so the only movement any tier may close over is
a surface move, which AAP 0.6.4 mandates outright. What makes these tiers
safe is not the tier: it is that no tier can close a row whose observable
outcome moved, which a scanner self-test asserts by driving all four tiers
against outcomes that differ on funnel, on status and on settlement.

One tier does use position, and the distinction is worth stating plainly
rather than eliding. **T3-anchored** pairs a residual pool by position, but
only when that pool is uniform on BOTH sides - every member byte-identical
in outcome to every other member of its own side - and equal in size. Under
those two conditions every possible bijection yields the same verdict on the
same rows, so the order the two files list their statements in cannot change
the answer. That is the property the old gap-filling lacked; using position
was never the defect in itself. Where either condition fails the tier
declines and the rows are reported missing and added, which is the honest
reading: one tree has an edge the other does not. Both conditions are pinned
by a scanner self-test, including the two negative cases.

### The 90 open rows

Listed in full, because a closure count with the open rows left out is the
same claim the previous edition of this document made by ticking nothing.

| Row | Edge | Baseline outcome | Target outcome | Verdict | What differs |
|---|---|---|---|---|---|
| `course.archiveCourse.cps.1` | `lib/controllers/course.js:165-167` | none / no response / answers nothing / route handler | none / no response / answers nothing / route handler | changed | payload or redirect shape |
| `course.createCourse.cps.1` | `lib/controllers/course.js:19-40` | Layer 2 / no response / answers / route handler | Layer 3 / 403 / answers / route handler | changed | funnel, status served, payload or redirect shape, side effects, settlement timing - the settlement moved |
| `course.join.cps.1` | `lib/controllers/course.js:872-901` | none / no response / answers nothing / route handler | Layer 1 / no response / answers nothing / route handler | changed | funnel, payload or redirect shape, side effects |
| `courses.returnZip.cps.1` | `lib/controllers/courses.js:268-273` | none / no response / answers nothing / internal callee | n/a - not in the target | missing from the target | n/a |
| `courses.download.response.1` | `lib/controllers/courses.js:285` | Layer 3 / 500 / answers / route handler | Layer 3 / 500 / answers / route handler | changed | payload or redirect shape |
| `files.setThumbnail.cps.1` | `lib/controllers/files.js:123-125` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `files.upload.cps.1` | `lib/controllers/files.js:58-76` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `files.uploadAvatar.cps.1` | `lib/controllers/files.js:14-20` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `files.uploadAvatar.response.2` | `lib/controllers/files.js:16` | Layer 3 / 500 / answers / route handler | Layer 3 / 500 / answers / route handler | changed | payload or redirect shape, settlement timing - the settlement moved |
| `folders.create.cps.1` | `lib/controllers/folders.js:68-92` | none / no response / answers nothing / route handler | none / no response / answers nothing / route handler | changed | payload or redirect shape |
| `folders.deleteFolder.response.2` | `lib/controllers/folders.js:157` | Layer 1 / 500 / answers / route handler | n/a - not in the target | missing from the target | n/a |
| `folders.trinkets.response.1` | `lib/controllers/folders.js:60` | Layer 3 / 500 / answers / route handler | n/a - not in the target | missing from the target | n/a |
| `folders.update.response.2` | `lib/controllers/folders.js:139` | Layer 1 / 500 / answers / route handler | n/a - not in the target | missing from the target | n/a |
| `trinket.autosave.response.2` | `lib/controllers/trinket.js:1087` | Layer 3 / 500 / answers / route handler | Layer 1 / no response / answers / route handler | changed | funnel, status served, payload or redirect shape, side effects |
| `trinket.autosave.response.3` | `lib/controllers/trinket.js:1090` | Layer 3 / 500 / answers / route handler | Layer 1 / no response / answers / route handler | changed | funnel, status served, payload or redirect shape, side effects, settlement timing - the target waits for LESS than the baseline did |
| `trinket.autosave.response.4` | `lib/controllers/trinket.js:1100` | Layer 3 / 500 / answers / route handler | n/a - not in the target | missing from the target | n/a |
| `trinket.downloadZip.handler.1` | `lib/controllers/trinket.js:1486-1488` | none / no response / answers nothing / internal callee | n/a - not in the target | missing from the target | n/a |
| `trinket.downloadZip.response.1` | `lib/controllers/trinket.js:1502` | none / no response / answers nothing / internal callee | n/a - not in the target | missing from the target | n/a |
| `trinket.downloadZip.response.2` | `lib/controllers/trinket.js:1534` | Layer 3 / 500 / answers / internal callee | n/a - not in the target | missing from the target | n/a |
| `trinket.downloadZip.response.3` | `lib/controllers/trinket.js:1554` | Layer 3 / 500 / answers / internal callee | n/a - not in the target | missing from the target | n/a |
| `trinket.email.cps.1` | `lib/controllers/trinket.js:830-887` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `trinket.library.cps.1` | `lib/controllers/trinket.js:80-90` | none / no response / answers nothing / route handler | none / no response / answers nothing / internal callee | changed | payload or redirect shape |
| `trinket.library.cps.2` | `lib/controllers/trinket.js:97-103` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `trinket.list.response.1` | `lib/controllers/trinket.js:250` | Layer 3 / 500 / answers / route handler | n/a - not in the target | missing from the target | n/a |
| `trinket.logError.cps.1` | `lib/controllers/trinket.js:944-946` | none / no response / answers nothing / route handler | none / no response / answers nothing / route handler | changed | payload or redirect shape |
| `users.assetUploadFromURL.handler.1` | `lib/controllers/users.js:596-598` | none / no response / answers nothing / route handler | none / no response / answers / route handler | changed | whether a response is produced, payload or redirect shape, settlement timing - the target waits for LESS than the baseline did |
| `users.downloadExport.response.2` | `lib/controllers/users.js:1078` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `users.downloadExport.response.3` | `lib/controllers/users.js:1082` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `users.downloadExport.response.4` | `lib/controllers/users.js:1086` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `users.downloadExport.response.5` | `lib/controllers/users.js:1090` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `users.getExportStatus.response.2` | `lib/controllers/users.js:1027` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `users.getExportStatus.response.3` | `lib/controllers/users.js:1031` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `users.getExportStatus.response.4` | `lib/controllers/users.js:1059` | none / no response / answers nothing / route handler | n/a - not in the target | missing from the target | n/a |
| `users.sendEmailChange.errparam.1` | `lib/controllers/users.js:708-725` | none / no response / answers nothing / route handler | none / no response / answers nothing / route handler | changed | payload or redirect shape |
| `users.sendEmailVerification.cps.1` | `lib/controllers/users.js:804-823` | Layer 2 / no response / answers / route handler | n/a - not in the target | missing from the target | n/a |
| `users.sendEmailVerification.response.2` | `lib/controllers/users.js:821` | Layer 2 / the route's request.fail response / answers / route handler | Layer 2 / the route's request.fail response / answers / route handler | changed | settlement timing - the settlement moved |
| `users.sendPassReset.cps.1` | `lib/controllers/users.js:235-264` | none / no response / answers nothing / route handler | none / no response / answers nothing / internal callee | changed | payload or redirect shape, log calls |
| `users.sendPassReset.cps.3` | `lib/controllers/users.js:241-258` | none / no response / answers nothing / route handler | Layer 2 / no response / answers / route handler | changed | funnel, whether a response is produced, payload or redirect shape, side effects |
| `helpers.courseBySlug.response.3` | `lib/util/helpers.js:393` | none / no response / answers nothing / pre-handler | Layer 3 / 500 / answers / pre-handler | changed | funnel, status served, whether a response is produced, payload or redirect shape, side effects |
| `helpers.findTrinket.response.3` | `lib/util/helpers.js:190` | none / no response / answers nothing / pre-handler | Layer 3 / 500 / answers / pre-handler | changed | funnel, status served, whether a response is produced, payload or redirect shape, side effects |
| `helpers.getDefaultTrinket.response.1` | `lib/util/helpers.js:347` | none / no response / answers nothing / pre-handler | Layer 3 / 500 / answers / pre-handler | changed | funnel, status served, whether a response is produced, payload or redirect shape, side effects |
| `helpers.trinketByOwnerAndSlug.response.1` | `lib/util/helpers.js:411` | none / no response / answers nothing / pre-handler | Layer 3 / 500 / answers / pre-handler | changed | funnel, status served, whether a response is produced, payload or redirect shape, side effects |
| `helpers.trinketByOwnerAndSlug.response.2` | `lib/util/helpers.js:416` | none / no response / answers nothing / pre-handler | Layer 3 / 404 / answers / pre-handler | changed | funnel, status served, whether a response is produced |
| `helpers.trinketByOwnerAndSlug.response.3` | `lib/util/helpers.js:424` | none / no response / answers nothing / pre-handler | Layer 3 / 404 / answers / pre-handler | changed | funnel, status served, whether a response is produced |
| `helpers.trinketByOwnerAndSlug.response.4` | `lib/util/helpers.js:434` | none / no response / answers nothing / pre-handler | Layer 3 / 404 / answers / pre-handler | changed | funnel, status served, whether a response is produced |
| `helpers.trinketByOwnerAndSlug.handler.1` | `lib/util/helpers.js:436` | none / no response / answers nothing / pre-handler | Layer 3 / 500 / answers / pre-handler | changed | funnel, status served, whether a response is produced, payload or redirect shape, side effects |
| `helpers.userByUsername.response.2` | `lib/util/helpers.js:363` | none / no response / answers nothing / pre-handler | Layer 3 / 500 / answers / pre-handler | changed | funnel, status served, whether a response is produced, payload or redirect shape, side effects |
| `admin.ohnoes.response.1` | `lib/controllers/admin.js:107` | n/a - not in the baseline | Layer 3 / 401 / answers / route handler | new in the target | n/a |
| `admin.errorResponse.response.1` | `lib/controllers/admin.js:427` | n/a - not in the baseline | Layer 3 / 500 / answers / internal callee | new in the target | n/a |
| `classes.getClass.response.1` | `lib/controllers/classes.js:212` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `classes.acceptInvitation.response.1` | `lib/controllers/classes.js:261` | n/a - not in the baseline | Layer 3 / 429 / answers / route handler | new in the target | n/a |
| `classes.joinFromLink.response.1` | `lib/controllers/classes.js:370` | n/a - not in the baseline | Layer 3 / 429 / answers / route handler | new in the target | n/a |
| `course.getCourse.response.1` | `lib/controllers/course.js:330` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `course.getLesson.response.1` | `lib/controllers/course.js:595` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `course.getLesson.response.2` | `lib/controllers/course.js:609` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `course.getMaterial.response.1` | `lib/controllers/course.js:777` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `course.getMaterial.response.2` | `lib/controllers/course.js:803` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `course.updateMaterial.response.2` | `lib/controllers/course.js:902` | n/a - not in the baseline | Layer 3 / 500 / answers / route handler | new in the target | n/a |
| `course.removeInvitation.response.1` | `lib/controllers/course.js:1275` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `course.updateInvitation.response.2` | `lib/controllers/course.js:1316` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `course.join.response.1` | `lib/controllers/course.js:1456` | n/a - not in the baseline | Layer 3 / 429 / answers / route handler | new in the target | n/a |
| `course.startAssignment.response.1` | `lib/controllers/course.js:1551` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `course.startAssignment.response.2` | `lib/controllers/course.js:1563` | n/a - not in the baseline | Layer 3 / 403 / answers / route handler | new in the target | n/a |
| `course.submitAssignment.response.1` | `lib/controllers/course.js:1633` | n/a - not in the baseline | Layer 3 / 404 / answers / route handler | new in the target | n/a |
| `course.submitAssignment.response.2` | `lib/controllers/course.js:1639` | n/a - not in the baseline | Layer 3 / 403 / answers / route handler | new in the target | n/a |
| `course.getUserSubmissionsForMaterial.response.2` | `lib/controllers/course.js:1825` | n/a - not in the baseline | Layer 3 / 403 / answers / route handler | new in the target | n/a |
| `course.sendFeedback.response.1` | `lib/controllers/course.js:2270` | n/a - not in the baseline | Layer 3 / 403 / answers / route handler | new in the target | n/a |
| `course.acceptSubmission.response.1` | `lib/controllers/course.js:2394` | n/a - not in the baseline | Layer 3 / 403 / answers / route handler | new in the target | n/a |
| `course.createCourseCore.response.1` | `lib/controllers/course.js:2462` | n/a - not in the baseline | Layer 1 / 500 / answers / route handler | new in the target | n/a |
| `course.resolveMaterialContext.response.1` | `lib/controllers/course.js:2565` | n/a - not in the baseline | Layer 3 / 404 / answers / internal callee | new in the target | n/a |
| `course.resolveMaterialContext.response.2` | `lib/controllers/course.js:2571` | n/a - not in the baseline | Layer 3 / 404 / answers / internal callee | new in the target | n/a |
| `course.resolveMaterialContext.response.3` | `lib/controllers/course.js:2577` | n/a - not in the baseline | Layer 3 / 404 / answers / internal callee | new in the target | n/a |
| `folders.legacyReply.response.1` | `lib/controllers/folders.js:51` | n/a - not in the baseline | Layer 3 / 500 / answers / internal callee | new in the target | n/a |
| `trinket.legacyReply.response.1` | `lib/controllers/trinket.js:58` | n/a - not in the baseline | Layer 3 / 500 / answers / internal callee | new in the target | n/a |
| `trinket.assertZipCodeWithinBounds.response.1` | `lib/controllers/trinket.js:105` | n/a - not in the baseline | Layer 1 / 500 / answers / internal callee | new in the target | n/a |
| `trinket.assertZipCodeWithinBounds.response.2` | `lib/controllers/trinket.js:120` | n/a - not in the baseline | Layer 1 / 500 / answers / internal callee | new in the target | n/a |
| `trinket.assertZipCodeWithinBounds.response.3` | `lib/controllers/trinket.js:129` | n/a - not in the baseline | Layer 1 / 500 / answers / internal callee | new in the target | n/a |
| `trinket.draft.handler.1` | `lib/controllers/trinket.js:1264-1266` | n/a - not in the baseline | none / no response / answers / route handler | new in the target | n/a |
| `trinket.$module.response.1` | `lib/controllers/trinket.js:2107` | n/a - not in the baseline | Layer 1 / 500 / answers / module scope | new in the target | n/a |
| `trinket.$module.response.2` | `lib/controllers/trinket.js:2154` | n/a - not in the baseline | Layer 3 / 500 / answers / module scope | new in the target | n/a |
| `trinket.$module.handler.2` | `lib/controllers/trinket.js:2184-2191` | n/a - not in the baseline | Layer 3 / 500 / answers / module scope | new in the target | n/a |
| `users.login.response.5` | `lib/controllers/users.js:557` | n/a - not in the baseline | Layer 2 / the route's request.fail response / answers / route handler | new in the target | n/a |
| `users.login.response.6` | `lib/controllers/users.js:574` | n/a - not in the baseline | Layer 2 / the route's request.fail response / answers / route handler | new in the target | n/a |
| `users.sendPassReset.response.2` | `lib/controllers/users.js:765` | n/a - not in the baseline | Layer 2 / the route's request.fail response / answers / route handler | new in the target | n/a |
| `users.sendPassReset.response.5` | `lib/controllers/users.js:816` | n/a - not in the baseline | Layer 2 / the route's request.fail response / answers / route handler | new in the target | n/a |
| `users.resetPasswordForm.response.2` | `lib/controllers/users.js:869` | n/a - not in the baseline | Layer 2 / the route's request.fail response / answers / route handler | new in the target | n/a |
| `users.savePassword.response.1` | `lib/controllers/users.js:912` | n/a - not in the baseline | Layer 2 / the route's request.fail response / answers / route handler | new in the target | n/a |
| `users.savePassword.response.2` | `lib/controllers/users.js:939` | n/a - not in the baseline | Layer 2 / the route's request.fail response / answers / route handler | new in the target | n/a |
| `users.requestExport.response.3` | `lib/controllers/users.js:1884` | n/a - not in the baseline | Layer 2 / the route's request.fail response / answers / route handler | new in the target | n/a |
| `users.requestExport.response.4` | `lib/controllers/users.js:1936` | n/a - not in the baseline | Layer 2 / no response / answers / route handler | new in the target | n/a |

### Baseline callback boundaries closed by rule T-3

These 6 rows close without a target row to compare against, so they
are stated in full here rather than counted. Rule T-3 puts the promise
boundary at the lifecycle method, which means a baseline callback whose
own error parameter carried a disposition can disappear as a SITE while
every response it produced is still produced by the routed carrier that
replaced it. The closure is that claim, and it is only as good as the
surviving carrier named in the last column - which is why the column is
here.

| Row | Baseline edge | Baseline outcome | Why the boundary closes |
|---|---|---|---|
| `classes.joinFromLink.cps.1` | `lib/controllers/classes.js:194-231` | none / no response / answers nothing / route handler | a cps-class callback boundary rule T-3 converts into an `await` at the call site, so the boundary is not a site in the target tree. Every response it produced is still produced in the same routed carrier: a redirect (still produced at `lib/controllers/classes.js:412-415`). |
| `courses.returnZip.errparam.1` | `lib/controllers/courses.js:266-274` | none / no response / answers nothing / internal callee | a errparam-class callback boundary that produced no response of its own. Rule T-3 converts the callback into an `await` at the call site, so the boundary is not a site in the target tree; it carried no error-to-response mapping for R-e to preserve. |
| `files.upload.errparam.1` | `lib/controllers/files.js:27-77` | none / no response / answers nothing / route handler | a errparam-class callback boundary that produced no response of its own. Rule T-3 converts the callback into an `await` at the call site, so the boundary is not a site in the target tree; it carried no error-to-response mapping for R-e to preserve. |
| `trinket.downloadZip.errparam.1` | `lib/controllers/trinket.js:1468-1470` | none / no response / answers nothing / internal callee | a errparam-class callback boundary that produced no response of its own. Rule T-3 converts the callback into an `await` at the call site, so the boundary is not a site in the target tree; it carried no error-to-response mapping for R-e to preserve. |
| `trinket.downloadZip.errparam.2` | `lib/controllers/trinket.js:1472-1479` | none / no response / answers nothing / internal callee | a errparam-class callback boundary that produced no response of its own. Rule T-3 converts the callback into an `await` at the call site, so the boundary is not a site in the target tree; it carried no error-to-response mapping for R-e to preserve. |
| `users.sendEmailChange.cps.2` | `lib/controllers/users.js:718-724` | none / no response / answers nothing / route handler | a cps-class callback boundary rule T-3 converts into an `await` at the call site, so the boundary is not a site in the target tree. Every response it produced is still produced in the same routed carrier: a success response (still produced at `lib/controllers/users.js:1543-1611`). |

### Rows carrying a prescribed mechanism transition

Every row whose two trees differ in a dimension the AAP prescribed a
change to. They are listed because a tick that absorbed a difference
silently is the defect this whole section exists to avoid: a reviewer who
disagrees with a transition can find it here rather than by re-deriving
it. A row that ALSO differs on a compared dimension is open, and appears
above as well.

| Row | Edge | Verdict | Prescribed transition |
|---|---|---|---|
| `trinket.library.cps.1` | `lib/controllers/trinket.js:80-90` | changed | surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4) |
| `trinket.list.errparam.1` | `lib/controllers/trinket.js:175-181` | closed | surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4) |
| `users.assetUploadFromURL.cps.1` | `lib/controllers/users.js:611-614` | closed | surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4) |
| `users.assetUploadFromURL.response.3` | `lib/controllers/users.js:612` | closed | surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4) |
| `users.sendPassReset.cps.1` | `lib/controllers/users.js:235-264` | changed | surface: `route handler` -> `internal callee` (extraction, AAP 0.6.4) |

### The approved-deviation list

**Empty, by measurement.** AAP 0.7 records exactly one approved behaviour
deviation on this surface - the never-settling image branch of the file
download - and its `reply(stream)` calls carry a stream rather than an
error, so no error edge exists for it and there is nothing to exempt here.
An entry would have to name the exact row id AND the exact from/to
outcome; a difference that merely claimed to be approved would still be
reported open, because approving a deviation approves one reviewed change
and not any change under the same name.

The gate is reported only on this run. Pass `--closure-gate` to exit non-zero while any row is open.

## 9. Driven coverage

**Not joined.** No corpus was supplied, so no row above carries a measured
response and every `Driven:` line says so. Pass
`--scenarios test/parity/corpus.json` to join, and `--coverage-gate` to
exit non-zero while a changed edge has no scenario naming it.

## 10. Locator reconciliation

Locations in this document are measured in the analysed tree. Where a locator
cited in the plan differs, both are given, so following either finds the same
edge.

| Cited | Measured | Subject |
|---|---|---|
| `lib/util/routeParser.js:578-589` | lib/util/routeParser.js:1088-1099 | Layer 1, the handler catch-all |
| `lib/util/routeParser.js:482-514` | lib/util/routeParser.js:614-683 | Layer 2, request.fail |
| `app.js:152-200` | app.js:202-260 | Layer 3, onPreResponse |
| `app.js:183-190` | app.js:239-244 (Boom branch), app.js:250 (non-Boom branch) | Layer 3 Boom-branch header assignments |
| `lib/util/routeParser.js:79-126, :137-178` | lib/util/routeParser.js:427-518 | the pre-handler shim |
| `config/default.yaml:353-358` | config/default.yaml:353-358 (5 paths) | the paths X-Frame-Options is limited to |
| `lib/controllers/pages.js:52` | lib/controllers/pages.js:68 | the `.catch(request.fail)` chain |
| `lib/controllers/pages.js:17, :27` | lib/controllers/pages.js:21, :33 | the `reply.redirect is not a function` synchronous throws |
| `lib/controllers/users.js:596-598` | lib/controllers/users.js:1346-1352 | the streaming asset fetch error listener (log-and-continue) |
| `lib/controllers/trinket.js:375` | no matching row in this tree - either the shape has closed here, or the cited site carries no error value | `reply(err)` with no return on an error path |
| `lib/controllers/courses.js:268` | no matching row in this tree - either the shape has closed here, or the cited site carries no error value | resolve-on-later-callback inside rimraf |
| `lib/util/helpers.js:190, :393` | lib/util/helpers.js:747 | `reply(err)` inside a pre-handler |
| `config/api_routes.js:1104` | no `reply(` site in this file, and no error-edge row | the inline pre-handler |

A cited locator that matches is listed too: a reader checking the plan against
this document needs to see the agreement as much as the disagreement.

## 11. Drivability

`test/parity/capture.js`'s failure-path scenarios are derived from these rows -
by hand, in its `error-edge.*` groups, not by parsing this file - and
`test/parity/replay.js` cites this checklist as what supplies the failure paths
the success sweep cannot reach. A row nothing can drive is not a testable
edge, so every row names the MECHANISM that reaches it - and where nothing
does, says so with the search that proves it.

Driveability is a FIELD on each row - `driveVia` - resolved from
reachability rather than inferred from the row carrying metadata. Two
earlier versions of this check were dead in the same way, and the second is
worth stating because it looked like a fix: it tested
`carrierMember && surface !== module`, which every non-module row satisfies
by construction, so a function that nothing anywhere calls came back
drivable. Each value below therefore names something the analysis RESOLVED:
a route declaration, a `server.method` registration, a lifecycle export, a
traced caller, module load - or a corpus-wide search returning nothing but
the declaration itself, which is a proof of unreachability rather than an
absence of evidence. `unresolved` is fatal in the generator, and that
assertion is live; so is the check that this table accounts for every row.

| How the row is driven | Rows |
|---|---:|
| a bound route declaration | 316 |
| a lifecycle export bound by at least one route | 14 |
| a lifecycle export hapi can invoke, named by no route here | 6 |
| a `server.method` registration, through the string-form dispatcher | 6 |
| a traced caller - a call, or its value handed on | 21 |
| module load, which every require of the file runs | 6 |
| **proven unreachable** - the corpus mentions it only where it is declared | 4 |
| **nothing resolved** - fatal | 0 |

The **proven unreachable** rows are listed rather than counted. Each was
searched for across the analysis targets, both route modules, the route
parser and the bootstrap, and found only at its own declaration. Nothing
can drive them, so they carry no funnel claim, they are not ticked, and the
closure gate excludes them - an unreachable edge has no observable outcome,
so calling it closed would be asserting parity of nothing against nothing:

- `lib/controllers/auth.js:297-302` `id=auth.attempt.handler.1` - .catch() handler - absorbs. Corpus mentions outside its declaration: 0.
- `lib/controllers/course.js:86` `id=course.isEmail.response.1` - throw new TypeError('This library (validator.js) validates stri.... Corpus mentions outside its declaration: 0.
- `lib/controllers/users.js:127` `id=users.attempt.response.1` - throw new Error('Exceeded maxRedirects. Probably stuck in a red.... Corpus mentions outside its declaration: 0.
- `lib/util/helpers.js:303-306` `id=helpers.userByLogin.errparam.1` - error parameter of a callback to `User.findByLogin` - handed onward. Corpus mentions outside its declaration: 0.

No row is unresolved: every row either names a driver or carries the
search that proves it has none. That is the property that makes this
checklist convertible into failure-path scenarios - for the 369 rows that
have a driver - and it is what keeps the 4 that do not out of the gate
rather than silently inside it.

Route bindings are read from the `route :` declarations in `config/routes.js` and `config/api_routes.js`.
`config/routes.js` expands a subset of those per language at parse time, so the
registered surface is larger than the declarations named here; the expanded
route manifest belongs to `test/parity/manifest.js`. A declaration built by
concatenation is read through the concatenation and rendered with its
non-literal operands as `<name>`, so `GET /<lang>/{shortCode}` is a template
standing for one registered route per configured language - not a path with a
`lang` parameter.

A row whose carrier names no declaration is reached **through a traced
caller**, and tracing covers indirect dispatch as well as call syntax: a
function value passed as an argument, stored in an array or object, assigned
to a `pre` entry, re-exported, or registered with `server.method` is invoked
somewhere its own name is never followed by a `(`. Recognising only call
syntax reported such a function as having no caller, which understates its
reachability in exactly the direction that matters.

| Carrier surface | Rows |
|---|---:|
| route handler | 314 |
| pre-handler | 20 |
| server method | 6 |
| module scope | 6 |
